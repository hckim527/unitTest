# secret_M 보강 — 가이드 ↔ 공통판 validation 1:1 대조

5건 모두 **순수 wrap (tData 오버라이드 없음)**. 판단 기준: 공통 판이 못 덮는 가이드 항목이
"실측으로 관측 가능한 동작"이면 오버라이드, 관측 불가·부분 관측이면 공통 것 그대로 사용.

### secret_M_show_1.xml
| API 가이드 항목 | 공통 판 validation | 상태 |
|---|---|---|
| @param displayType <String:N:"block"> display 속성에 적용할 값 | (1) displayType(선택) 파라미터로 inline style 의 display 속성에 적용할 값을 문자열로 설정합니다. | O |
| 필수여부 N -> 생략 시 기본값 | (2) displayType(선택) 파라미터 생략 시 'block' 으로 설정되어야 합니다. | O |
| `|` ""(Empty String) 설정 시 display 속성 삭제 | (3) '' 으로 설정 시 display 속성을 inline style 에서 삭제하여야 합니다. | O |
| @spec 유효하지 않은 값 -> "none" 유지 | (4) | O |
| @spec 부모 표시 시 자식도 표시 / 자식 inline style 은 미변경 | (5) 부모 컴포넌트를 표시하면 자식 컴포넌트도 표시되어야 합니다. | 부분(자식 inline style 미변경 절 없음) — secret 은 `input[type=password]` 단일 엘리먼트로 **자식이 없어 관측 불가** → 오버라이드 불필요 |
| @spec visibility: visible + display 값 적용 | (6) | O |
| @spec aria-hidden false | (7) | O (단, 엔진 미구현 → 가이드갭) |

### secret_M_hide_1.xml
| API 가이드 항목 | 공통 판 validation | 상태 |
|---|---|---|
| @spec inline style 에 display:none, visibility:hidden | (2) | O |
| `|` 부모 숨김 시 자식도 숨김 / 자식 inline style 미변경 | (1) 부모 컴포넌트를 숨기면 자식 컴포넌트도 숨겨져야 합니다. | 부분 — secret 은 자식 없음 → 관측 불가 → 오버라이드 불필요 |
| @spec aria-hidden true | (3) | O (엔진 미구현 → 가이드갭) |
| @param 없음 | - | O |

### secret_M_trigger_1.xml
| API 가이드 항목 | 공통 판 validation | 상태 |
|---|---|---|
| @param type <String:Y:-> 발생시킬 이벤트 이름 | (1) | O |
| @param array <Array:N:[]> 핸들러 전달 데이터 1차원 배열 | (3) array(선택) 파라미터로 이벤트 핸들러에 전달할 데이터를 단일 원소 배열로 설정 | O (표현 차이만) |
| @spec 정의되지 않은 이벤트는 발생하지 않음 | (2) | O |
| (가이드 외) setEventPause 로 중지된 이벤트 미발생 | (4) | 공통 판 고유 항목 — secret 2.0 가이드에는 없으나 공통 판 수정 금지 대상이라 그대로 둠 |

### secret_M_setDisabled_1.xml
| API 가이드 항목 | 공통 판 validation | 상태 |
|---|---|---|
| @param disabled <Boolean:Y:-> 비활성화 상태 설정 | (1) | O |
| • true : 비활성화 (접근 불가) | (2) | O |
| • false : 비활성화 해제 (접근 가능) | (3) | O |
| @spec 비활성화 시 입력·포커스·이벤트 차단 | (4) | O |
전항목 커버 → 순수 wrap.
※ secret 은 disabled=true 여도 tabindex 가 유지되어 `document.activeElement` 이동이 가능하므로
   포커스 단정은 하지 않고 공통 판의 판정 수단(getDisabled + w2input_disabled 클래스)을 그대로 사용.

### secret_M_setStyle_1.xml
| API 가이드 항목 | 공통 판 validation | 상태 |
|---|---|---|
| @param property <String:Y:-> CSS 속성 이름 | (1) | O |
| `|` "borderLeft" 같은 camelCase 형식 | (2) | O |
| @param value <String|Number:Y:-> 적용할 값 | (3) | O |
| `|` 숫자 설정 시 기본 단위 px | (4) | O |
| @spec 색상 형식 다양 (#rrggbb, rgb(), rgba()) | (5) | O |
| @spec 단위·공백 포함 값은 문자열로 | (6) | O |
전항목 커버 → 순수 wrap.

### secret_M_setValue_1.xml
| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @param value <String:Y:-> 입력값 설정 | (1) value(필수) 파라미터로 Secret 컴포넌트의 입력값을 설정합니다. | O |
| `|` 화면에는 입력값이 마스킹되어 표시됩니다. | (2) | O (입력창 type : password) |
| @spec maxlength, maxByteLength 제약 없음 | (3) | O (con_maxlength=3 → 10자, con_maxByteLength=3 → 6byte 설정) |
| @spec ref 바인딩 DataCollection 반영 | (4) | O (con_ref=data:gdm_ref.key1, gdm_ref.key1 출력) |
| @spec onchange 발생 / onviewchange 미발생 | (5) | O (직접 타이핑 시 onviewchange 발생과 대비) |
| `|` 화면 값 반영 > 바인딩 데이터 반영 > onchange 순서 | (6) | O (gdm_ref onmodelchange 시점 화면 값, onchange 시점 바인딩 값 즉시 출력) |
| @related maxByteLength/maxlength/ref/commit/getValue/onchange/onviewchange | - | 자체 기능 검증 제외 (@spec 조건으로만 사용) |
누락 없음. 헬퍼 초기값(setValue("secret")) 이벤트는 bReady 플래그로 출력 억제. w-pack: 생략(Studio 자동 생성 확인).

### secret_M_getValue_1.xml

| API 가이드 항목 | 샘플 validation | 상태 |
|---------------|---------------|------|
| @description 실제 입력값을 반환 | description 반영 | ✓ |
| @return 마스킹되지 않은 평문 문자열로 반환 | (1) 에 통합 (@spec1 과 동일 의미) | ✓ |
| @spec1 화면에는 입력값이 마스킹되어 표시되지만 getValue() 는 평문 문자열 반환 | (1) 원문 | ✓ (getValue 버튼 + 화면 마스킹 대비) |
| @spec2 입력중 getValue() 실행 시 내부 갱신 시점에 따라 이전 입력값 반환 가능 | (2) 원문 | ✓ (사용자 직접 타이핑, onkeydown/onkeyup 시점 getValue 출력) |
| @related setValue | - | 제외 (related 규칙) |

- 파라미터 없음 -> param/enum/생략 항목 해당 없음. 누락 0건.

### secret_M_validate_1.xml / secret_M_validate_2.xml
| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description 유효성 체크 실행 후 결과 반환 | description | O |
| `|` 5개 속성 한 개 이상 설정되어야 실행 | _1 (1) | O (전 속성 미설정 → 빈값도 true, alert/포커스 없음) |
| @return true : 모두 통과 | _1 (2) | O |
| @return false : 하나라도 실패 | _1 (3) | O |
| @spec 자동 실행 안 됨, 스크립트로 직접 실행 ("validator() 메소드" 원문 유지) | _1 (4) | O (재생성/직접 입력+포커스 해제 시 alert 없음) |
| @spec 속성 순서 mandatory → maxlength → minlength → maxByteLength → minByteLength | _1 (5) | O (다중 실패값 abc + 앞 순서 속성 하나씩 제거, fn_msg getType) |
| @spec 실패 속성에서 중단 | _1 (6) | O (다중 실패에도 첫 속성 메시지 1개) |
| @spec displaymessage=true 시 메시지, invalidMessage/Func 로 변경 | _1 (7) | O |
| @spec 메시지 출력 후 false 반환 | _1 (8) | O (alert 닫힌 뒤 결과 줄 출력) |
| @spec 실패 컴포넌트 포커스 | _1 (9) | O (포커스 요소 mf_target1 + onfocus 줄, 성공 시 mf_btn_validate) |
| @spec validator 속성 사용자 정의 함수와 별개 | _2 (1) | O (호출 0회, 반환값 동일) |
| @spec 속성별 체크 항목은 각 속성 설명 참조 | - | 제외 (각 속성 샘플에서 검증, 타기능 규칙) |
| @related displaymessage/invalidMessage/Func/mandatory/max·min(Byte)length/validator | - | 설정 수단으로만 사용, 개별 기능 검증 제외 |
- 분리: _1 = 유효성 체크 순서/반환/메시지/포커스 축, _2 = validator 별개 축 (옵션 과다 분리). 누락 0건. w-pack: 생략(Studio 자동 생성 확인).
- 가이드 갭: @spec "validator() 메소드" 는 validate() 오타로 보임(secret 에 validator() 메소드 없음). 엔진은 allowChar/ignoreChar 도 체인에 포함(input 공통, secret 가이드 미노출). validator 속성은 체인에 commandAdapter 로 등록되나 executeValidate 가 항상 true 라 가이드와 일치.

### secret_M_validate 재분리 (2026-09-22) — 담당 매핑 갱신 (위 표의 _1/_2 표기를 대체)
| 샘플 | 검증 축 | 담당 가이드 항목 |
|---|---|---|
| _1 | 실행 조건·반환값·체크 순서/중단 | `|` 한 개 이상 설정, @return true/false, @spec 자동 실행 안 됨, 순서, 중단 (6건) |
| _2 | 실패 처리 | displaymessage 메시지 + invalidMessage/Func 변경, 메시지 후 false 반환, 실패 컴포넌트 포커스 (3건) |
| _3 | validator 별개 | validator 사용자 정의 함수와 별개 (1건) |
- 합집합 10건 = 가이드 전항목(참조 안내 @spec 1건 제외). description 3개 동일. 누락 0건.

### secret_M_commit_1.xml / secret_M_commit_2.xml (2026-09-22)
순수 wrap (tData 오버라이드 없음). 공통 판 미커버 항목 중 관측 가능한 것이 없어 공통 것 사용.

| API 가이드 항목 | 공통 판 validation | 상태 |
|---|---|---|
| @description 입력값을 바인딩 된 DataCollection 에 즉시 반영 | _1 (1) dataMap 과 바인딩 된 경우 컴포넌트의 입력값을 바인딩 된 dataMap 에 즉시 반영합니다. / _2 (3) dataList 와 바인딩 된 경우 컴포넌트의 입력값을 바인딩 된 dataList 에 즉시 반영합니다. | O (실측: commit 전 data 이전값 → 후 입력값) |
| `|` ref 속성으로 DataCollection 과 바인딩 된 경우만 동작 | _1 (2) ref 속성으로 dataMap 과 바인딩 된 경우 동작합니다. / _2 (4) ref 속성으로 dataList 와 바인딩 된 경우 동작합니다. | O (ref 미설정 대조는 반영 대상 DataCollection 이 없어 관측 불가 → 오버라이드 불필요, input 판과 동일) |
| @spec DataList 미선택 시 입력창 빈값 | _2 (1) DataList 의 데이터를 선택하지 않으면 입력창에 빈값이 표시됩니다. | O (실측: 초기/DataList미선택 → 빈값) |
| `|` 미선택 상태 입력값은 데이터 선택 시 사라짐 | _2 (2) 데이터를 선택하지 않은 상태에서 입력된 값은 데이터 선택 시 사라집니다. | O (실측: zz 입력 → DataList선택 → dlt_data2) |
| @related ref, refSync | - | 제외(related) |

- 이벤트 대체: secret 가이드에 oneditkeyup 이 없어 onkeyup 사용. 실측상 End/Backspace 포함 키마다 출력이 input(oneditkeyup) 판과 동일.
- 보강 수정: 샘플 validation 추가 없음. Target 재생성 시 헬퍼 초기값이 바인딩 데이터를 덮어쓰는 문제만 comp_init 에서 보관·복원으로 수정(02_검증.md 참조).
- 누락: 없음

---

## 테스트 정보 라벨 판정 (2026-09-23, secret_M 담당 에이전트)

기준: UT_01_생성.md '라벨 생성 여부' 절 + feedback_simple_sample_skip_info_label (①조작 자명 ②숨은 전제 없음 ③validation/con_ 중복뿐 — 셋 다 충족 시에만 삭제)
추가 제약: TSQA 스크립트가 `#mf_label1` 을 "바깥 영역 클릭" 타깃으로 사용하는 샘플은 삭제 금지.

| 파일 | 판정 | 사유 | TSQA(mf_label1) 참조 |
|---|---|---|---|
| secret_M_getValue_1.xml | 유지 | 초기값 secret(6자) 이 마스킹돼 화면 확인 불가 + validation 2 는 "직접 키보드 입력하며 onkeydown/onkeyup 시점 반환값 비교" 라는 화면에 없는 확인 방법 필요 | 없음 |
| secret_M_setValue_1.xml | 유지 | 초기값 secret(6자) 마스킹 전제 + "현재 값과 다른 value 를 입력해야 변경됨" 함정, 직접 타이핑 시 onviewchange 발생 대비 관찰법 | 없음 |
| secret_M_validate_1.xml | 유지 | displaymessage=true 고정(화면 con_ 없음) + 체크 순서 확인용 다단계 절차(기본 조건에서 앞 순서 속성을 하나씩 미설정으로 바꿔 반복)와 "maxlength 설정 시 직접 입력하면 값이 잘린다" 함정 | 있음 (ts_secret_M_validate_1:159) |
| secret_M_validate_2.xml | 유지 | mandatory=true, maxlength=2 고정(화면 con_ 없음)과 그에 따른 케이스 매핑(빈값=mandatory 실패 / abc=maxlength 실패 / ab=통과), alert 닫은 뒤 onfocus 줄 확인 순서 | 없음 |
| secret_M_validate_3.xml | 유지 | mandatory=true, displaymessage=true 고정 + "validator 설정 시 포커스 해제도 함께 수행", "사용자 정의 함수는 포커스 해제 시에만 실행" 이라는 화면만으로 알 수 없는 절차 | 있음 (ts_secret_M_validate_3:67,81) |

### MCP 확인
- secret_M 담당 5개 파일은 모두 유지 판정이라 수정 없음 → MCP 재확인 대상 없음.

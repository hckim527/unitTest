# secret_M 검증 (show / hide / trigger / setDisabled / setStyle)

- 구성: 5건 모두 `common_M/common_M_{메소드}_1.xml` 을 `wf_sample` wframe 으로 임베드하는 **순수 wrap**.
  `comp_init` 에서 `$c.gcm.createBasicSecret(...)` 만 호출하며 tData 오버라이드 없음.
- 원본: `sample/input/input_M_{메소드}_1.xml` (createBasicInput -> createBasicSecret 1줄만 변경).
- MCP 세션 2 / 포트 50177 실측.

| 샘플 | 컴포넌트 생성 | validation 렌더 | 버튼 동작 | console error |
|---|---|---|---|---|
| secret_M_show_1.xml | O (input[type=password] id=mf_target1) | O (7건) | hide -> display:none/visibility:hidden, show(block) -> display:block/visibility:visible | 0 |
| secret_M_hide_1.xml | O | O (3건) | hide -> style "display: none; visibility: hidden" | 0 |
| secret_M_trigger_1.xml | O | O (4건) | Target 재생성 후 trigger("onclick") -> "=====target1_onclick 이벤트 발생=====" / "e:[object Event]" | 0 |
| secret_M_setDisabled_1.xml | O | O (4건) | par_disabled=true -> getDisabled:true / class "w2input w2input_disabled" / input.disabled=true, false -> 해제 | 0 |
| secret_M_setStyle_1.xml | O | O (6건) | setStyle("background-color","rgb(255,0,255)") -> inline style background-color: rgb(255,0,255), getStyle -> #ff00ff | 0 |

## 실측 raw 값
- 미설정 시 class: `"w2input "`(후행 공백), style: `width: 200px; height: 40px;` (createBasicSecret 이 부여)
- setDisabled(true) 시 class: `w2input w2input_disabled`, setDisabled(false) 시 `w2input`(후행 공백 소실)
- show/hide 후 `render.ariaHidden` = **null** (아래 가이드갭 참조)

## 가이드갭 후보 (secret 전용 아님 — 전 컴포넌트 공통)
- show `@spec` "컴포넌트의 aria-hidden 속성이 false 로 설정됩니다." / hide `@spec` "... true 로 설정됩니다."
  → 실측 `aria-hidden` 속성 자체가 부여되지 않음(null). 엔진 `engine/style.js:668`, `:697` 의
  `element.setAttribute('aria-hidden', ...)` 두 줄이 **주석 처리**되어 있어 미구현 상태.
  common_M_show/hide 판의 `ariaHidden` 출력도 항상 null 이므로 해당 validation 은 현재 FAIL 로 관측됨.

### secret_M_setValue_1.xml
- 구성: 독립 샘플 (common_M_setValue_1/_2 는 label 기반 내부 텍스트 판이라 wrap 불가, input_M_setValue_1 은 editFormat 전용 정적 판이라 구조만 참조)
- validate_xml: MCP 서버 연결 실패(192.168.100.214) → minidom 파싱으로 XML 정합성만 확인
- MCP(61613, 전용 탭) 실측, console error 0 / alert 0
  - 기본(maxlength 3, ref data:gdm_ref.key1), value abcdefghij → `▶ 바인딩 데이터 반영 : gdm_ref.key1 = abcdefghij / 이 시점 화면 값 : abcdefghij` → `▶ onchange 발생 / 이 시점 화면 값 : abcdefghij / 바인딩 데이터 값 : abcdefghij` → `setValue(abcdefghij) -> getValue() : abcdefghij (10자, 10byte) / 입력창 type : password / maxlength 3 / maxByteLength 미설정 / gdm_ref.key1 : abcdefghij`
  - 같은 값 재설정 → 결과 줄만 출력(값 변경 없어 onchange 미발생)
  - 직접 타이핑(xy) 후 blur → onmodelchange → onchange → `▶ onviewchange 발생` (대조군)
  - maxByteLength 3, ref 미설정, value 가나다 → `▶ onchange 발생 / 이 시점 화면 값 : 가나다` → `setValue(가나다) -> getValue() : 가나다 (3자, 6byte) / 입력창 type : password / maxlength 미설정 / maxByteLength 3`
  - 렌더: `<input id="mf_target1" class="w2input " type="password">`
- 결과: PASS

### secret_M_getValue_1.xml

- 구성: 독립 샘플(common_M_getValue 는 라벨값 반환 스펙이라 wrap 불가). createBasicSecret(초기값 secret) + ev:onkeydown/onkeyup + getValue 버튼. con_ 없음.
- XML well-formed OK (validate_xml 서버 192.168.100.214:15748 연결 실패로 로컬 XML 파싱으로 대체). wpack 자동 생성 확인.
- MCP(port 61613) 실측, console error 0:
  - 초기: render.type=password, class=w2input, getValue()=secret
  - getValue 클릭 -> `getValue() : secret (string, 6자) / 입력창 type : password`
  - 실제 키 입력(End, a, b, Backspace): onkeydown 시점 getValue 는 이전 값(secret / secreta / secretab), onkeyup 시점은 반영 값(secreta / secretab / secreta)
  - 최종 getValue -> `secreta (string, 7자)`, 화면 스크린샷은 마스킹 점 7개
- 근거: input.js:4658 `_getValue()` 는 editFormat/maskFormat 이 없으면 `this.render.value` 를 그대로 읽으므로, 브라우저가 키 입력을 DOM 에 반영하기 전(keydown)에 호출하면 이전 값이 반환된다.

### secret_M_validate_1.xml / secret_M_validate_2.xml (2026-09-22)
- 로드: 둘 다 정상 (port 61613, console/page error 0). validate_xml MCP 서버 연결 실패 → 로컬 XML 파싱 + new Function 스크립트 구문 확인으로 대체.
- _1 실측 (기본 mandatory=true / maxlength=2 / minlength=4 / maxByteLength=2 / minByteLength=6, displaymessage=true):
  - abc(3자 3byte): alert `2자를 초과하여 입력할 수 없습니다.\n` → false, 포커스 mf_target1, onfocus 줄 출력
  - maxlength 제거 → `4자 이상 입력해야 합니다.` / minlength 추가 제거 → `2byte를 초과하여 입력할 수 없습니다.` / maxByteLength 추가 제거 → `6byte 이상 입력해야 합니다.` / 전부 제거(mandatory 만) → true, alert 없음, 포커스 mf_btn_validate
  - 빈값 → `필수 입력 항목입니다.` (minlength/minByteLength 도 실패하지만 mandatory 에서 중단)
  - invalidMessageFunc=scwin.fn_msg: getType 순서 maxLength → minLength → maxByteLength → minByteLength, 빈값 mandatory
  - invalidMessage 단독 → `사용자 메시지` / invalidMessage+Func → Func 문구 우선
  - displaymessage false / 미설정 → alert 없음, false, 포커스 이동은 동일
  - 5개 속성 모두 미설정: 빈값/abc 모두 true, alert·포커스 이동 없음 (mandatory=false 도 동일)
  - Target 재생성 직후 및 직접 입력 후 포커스 해제 시 alert 없음
- _2 실측 (mandatory=true, displaymessage=true 고정): validator 설정/미설정 간 validate() 반환값·alert 동일 (빈값 false+필수 alert, secret true), validate() 실행 중 사용자 정의 함수 호출 0회. 포커스 해제 시에만 함수 실행 줄 출력.
- MCP 검증: PASS

### secret_M_validate_1/_2/_3 재분리 (2026-09-22, 사용자 피드백 "조건 옵션 과다")
- _1 (con: mandatory, 입력값, maxlength, minlength, maxByteLength, minByteLength / displaymessage=true 고정): 순서 실측 동일 (2자 초과 → 4자 이상 → 2byte 초과 → 6byte 이상 → mandatory 만 true), 빈값 → 필수 입력, 전 속성 미설정 → true.
- _2 (con: displaymessage, 입력값[빈값/ab/abc], invalidMessage, invalidMessageFunc / mandatory=true, maxlength=2 고정): abc → 기본 메시지+false+포커스 mf_target1+onfocus 줄, ab → true·포커스 mf_btn_validate, displaymessage false/미설정 → alert 없음, invalidMessage → 사용자 문구, Func → 함수 문구(maxLength/mandatory), 동시 설정 → Func 우선.
- _3 (구 _2, con: validator, 입력값): 호출 0회, 반환값 validator 유무 무관.
- 3개 모두 로드 정상, page error 0. MCP 검증: PASS

### secret_M_commit_1.xml / secret_M_commit_2.xml (2026-09-22)
- 구성: 순수 wrap (common_M_commit_1/_2). secret 에 oneditkeyup 이 없어 `ev:onkeyup` → `scwin.target1_onkeyup` 에서 wframe 이 부모 scope 에 정의한 `scwin.target1_oneditkeyup(null, e)` 호출. createBasicSecret 초기값(secret) 후처리 포함.
- XML well-formed OK (로컬 minidom). wpack 자동 반영 확인(수정 후 재로드 시 comp_init 본문에 수정 코드 확인).
- MCP(playwright2, port 61613) 실측, console/page error 0. 동일 조작을 input_M_commit_1/_2 와 나란히 실행해 결과창 문자열 비교.
  - _1 초기: type=password, class=`w2input `, getValue=dm_data. 실제 키 입력(End, x, y, Backspace) → 키마다 `commit 전 data : 이전값 / text : 현재입력값` → `commit 후 data·text 동일` 블록 출력. End 키도 블록 1개 출력(값 변화 없음). **input 판과 결과창 문자열 완전 일치.**
  - _2 초기: 입력창 빈값, gdl_ref rowPosition=null. 미선택 상태 zz 입력 → `data : dlt_data2 / text : zz` (commit 후에도 data 불변). `DataList선택`(setRowPosition(1)) → 입력창 `dlt_data2` 로 교체(zz 사라짐). 선택 후 k 입력 → commit 후 data `dlt_data2k`. `DataList미선택` → 입력창 빈값. `text 확인` → `text : dlt_data2k`. **input 판과 일치.**
- 발견 이슈(수정): `Target 재생성` 시 헬퍼 `setValue("secret")` 이 바인딩 데이터를 덮어써 input 판과 결과가 갈림.
  - _1: 입력(q) 후 재생성 → secret 판은 `dm_data` 로 리셋(하드코딩 복원), input 판은 `dm_dataq` 유지.
  - _2: 행 선택 + 입력(q) 후 재생성 → secret 판은 row1.label 이 `""` 로 소실(헬퍼 secret 기록 후 setValue("") 가 다시 기록), input 판은 `dlt_data2q` 유지.
  - 수정: _1 은 생성 전 `gdm_ref.get("key1")` 을 보관 후 복원, _2 는 생성 전 rowPosition·label 을 보관해 선택 상태면 `setCellData` 로 복원 / 미선택이면 `setValue("")`.
  - 재검증: _1 재생성 → `dm_dataq` 유지 / _2 미선택 재생성 → 빈값·row1 `dlt_data2` 유지, 선택 재생성 → `dlt_data2q` 유지, 재생성 후 키 입력 핸들러 정상(`dlt_data2qw`). console error 0.
- MCP 검증: PASS

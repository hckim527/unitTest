# nav_P — 02 검증

---

### nav_P_class_1.xml

| 항목 | 결과 |
|---|---|
| XML 구조 (네임스페이스 / head·body / description·validation) | PASS |
| 스크립트 로직 (버튼 onclick ↔ 핸들러 정의 일치, setReturnValue 출력) | PASS |
| UI 구성 규칙 (UT_01 §7 정적 배치 — wf_body_sample 삭제, grp_sample > grp_comp 직접 배치) | PASS |
| MCP 런타임 검증 | **PASS** |

### MCP 실측 (engine R1641, port 49920, mcp__playwright3)

- `document.querySelectorAll("nav")` → 2개
  - `[0] id=target1 class="oddClass evenClass"` → computed border `1px solid rgb(255, 0, 0)` / border-radius `15px` / display `block`
  - `[1] id=target2 class=""` (class 미설정)
- 렌더된 class 에 `w2nav` / `w2group` 등 **웹스퀘어 기본 클래스 미추가** 확인
- 동일 페이지 WebSquare 컴포넌트 렌더 id 는 `mf_btn_getClassName` / `mf_wf_body_bottom_tbx_return` → `<nav>` 는 raw HTML pass-through 로 Scope 접두사 없이 `target1`/`target2` 그대로
- 버튼(`class 확인`) 출력
  ```
  target1 적용 스타일 - border : 1px solid rgb(255, 0, 0) , border-radius : 15px
  렌더된 class 속성 - target1 : 'oddClass evenClass' , target2 : ''
  ```
- console error / pageerror **0건**
- body_left description / validation 2건 정상 렌더 (`[nav > P > class]` 확인)

**MCP 검증: PASS / 발견 이슈 없음**

> 주의: playwright3 브라우저를 타 에이전트와 공유 중이므로, 측정 직전 자기 샘플 URL 로 재-navigate 하고 `nav > P > class` 문구로 화면 동일성을 확인한 뒤 버튼 클릭 + 결과창 읽기를 단일 run_code 호출로 묶어 수집.

---

## nav_P_id_1.xml

- 파일: `unitTest/src/main/webapp/sample/nav/nav_P_id_1.xml`
- 구조 검증: WebSquare XML(네임스페이스/head-body) 정상, `description`/`validation`(5건) 정의, `UT_01_생성.md` §7 정적 배치(`wf_body_sample` 미사용, `grp_sample > grp_comp` 직접 배치) 적용. `comp_init`/`Target 재생성` 버튼 없음(정적 선언이므로 불필요).
- 스크립트 검증: 버튼 3개(`btn_getId` / `btn_getScriptAccess` / `btn_getReservedId`) ↔ 핸들러 3개 1:1 일치, DOM 조회는 `document.querySelector` 사용, `setReturnValue` 호출 5회 = validation 5건.
- MCP 검증(포트 49920, `websquare.html?w2xPath=/sample/nav/nav_P_id_1.xml`): **PASS**
  - 화면 동일성 확인: 유효성 영역에 `[nav > P > id]` 노출 확인 후 결과 판독.
  - `<nav>` 3개 모두 raw HTML pass-through 렌더(`class` 미부여, 태그 NAV 유지).
  - 콘솔 에러 **0건**.
  - 결과창 실측 출력:
    ```
    id 설정 - 렌더된 id : target1 , '#target1' 조회 개수 : 1
    id 미설정 - 자동 부여된 id : wq_uuid_11
    Scope 적용 여부 - nav 의 id : target1 , 비교용 WebSquare button(btn_getId) 의 렌더 id : mf_btn_getId
    스크립트 접근 - id 설정(target1) : 조회 성공 (내용 변경 반영) , id 미설정 Navigation 을 지정 id(target2) 로 조회 : 조회 실패
    예약어(style) id 설정 - 렌더된 id : style , 페이지 로드 후 발생한 JS 오류 건수 : 0
    ```
- 발견 이슈: 없음(수정 이력 없음).

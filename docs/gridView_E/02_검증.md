# gridView_E onaftercolumnmove 검증 결과

## gridView_E_onaftercolumnmove_1.xml

### 1. 코드 리뷰
- XML 구조 정상 (네임스페이스/head/body, description, validation 4항목 정의).
- columnMove="true" + Body Column col1~col4(인덱스 0~3) 구성, 헤더 Drag&Drop 가능.
- 이벤트 바인딩: createEList(["onbeforecolumnmove","onaftercolumnmove"]) → cList/eList 공통 패턴.
- 핸들러 onaftercolumnmove: info 4속성 개별 출력 + getColumnOrder(true)/(false) 출력(@spec3).
- 트리거(헤더 Drag&Drop)는 사용자 액션으로 수행 — 별도 트리거 버튼 없음.
- [setColumnOrder 실행] 버튼: 프로그램 이동 시 미발생 확인용(@spec1).

### 2. validate_xml
- isValid: true, errors 0.
- warnings 2건(UNUSED_EVENT_HANDLER: target1_onbeforecolumnmove/onaftercolumnmove) — createEList 동적 바인딩이라 false-positive, 무시.

### 3. MCP playwright 런타임 검증 (PASS)
- 페이지 정상 로딩, window.mf_target1 생성 확인(page scope, mf_ prefix).
- columnMove = true 적용 확인.
- 초기 컬럼 순서: ID ["col1","col2","col3","col4"] / 인덱스 [0,1,2,3].
- onbeforecolumnmove / onaftercolumnmove 핸들러 바인딩 확인(hasBefore/hasAfter true).
- onaftercolumnmove 핸들러 호출 출력(info col1 idx0 -> col2 idx1):
  ```
  =====target1_onaftercolumnmove=====
  info.startColumnID : col1
  info.startColumnIndex : 0
  info.endColumnID : col2
  info.endColumnIndex : 1
  [getColumnOrder(true) 이동 후 표시 순서 ID] ["col1","col2","col3","col4"]
  [getColumnOrder(false) 이동 후 표시 순서 인덱스] [0,1,2,3]
  ```
- [setColumnOrder 실행] 버튼: 표시 순서 col4,col3,col2,col1 로 변경 + onaftercolumnmove 미발생(afterFired=false) 확인 → @spec1 입증.
- 샘플 관련 console error 0.

결론: PASS

---

# gridView_E_onbeforecolumnmove_1.xml 검증

## 코드 리뷰
- XML 구조/네임스페이스 정상, validate_xml isValid=true (errors 0, warnings 0).
- description = @description 첫 문장(원문 '이동한 후' 유지), validation 6항목.
- columnMove=true 전제 그리드(Body col1~col4, SingleLine), con_returnValue(true/false/생략) 콤보, [setColumnOrder] 버튼.
- 핸들러: e.target.id 미사용 대상이며 info 단일 인자. 호출 메소드 getColumnOrder/setColumnOrder/moveColumn 엔진 @name 실존 확인. moveColumn 의 onbeforecolumnmove fireEvent + `if(evRet===false) return false` 동작 엔진 소스(layoutController.js:5310,5316,5389,5395) 확인.

## MCP 런타임 검증 (launcher: /websquare/websquare.html?w2xPath=...)
- mf_target1 생성 OK, getColumnOrder(true) = ["col1","col2","col3","col4"].
- CASE true: info(start col1/0, end col3/2) 출력, 핸들러내 getColumnOrder=이동전 순서, return true → 이동적용 ["col2","col3","col1","col4"] + onaftercolumnmove 발생. ✓
- CASE false: 동일 info 출력, 핸들러내 getColumnOrder=이동전 순서, return false → 이동취소(순서 불변) + onaftercolumnmove 미발생. ✓
- CASE 생략: return 문 없음 → true 적용, 이동적용 + onaftercolumnmove 발생. ✓
- [setColumnOrder] 버튼: 순서 변경되나 onbeforecolumnmove 미발생(출력에 핸들러 로그 없음) → @description \| 입증. ✓
- 샘플 관련 console error 0 (raw .xml 직접 오픈 시 'scwin is not defined' 는 launcher 미경유 케이스로 비해당).

결론: PASS

---

# gridView_E onaftercolumnresize 검증

## gridView_E_onaftercolumnresize_1.xml
- XML 구조: validate_xml isValid=true. 경고 4건은 false-positive(select1 selectedIndex 미설정-형제샘플 동일, createEList 동적바인딩 핸들러 3건 UNUSED).
- 스크립트: createCList(resize/rowNumVisible/rowStatusVisible) + createEList(onaftercolumnresize/oncolumnwidthchange/onheaderdblclick) 동적 바인딩. 핸들러 Drag/더블클릭 분기·eventLog 순서출력·setColumnWidth 음성검증 버튼 구성.
- 엔진 메소드 실존: setColumnWidth(gridView.js:11981), 이벤트 3종(onaftercolumnresize/oncolumnwidthchange/onheaderdblclick) @name 주석 확인.
- MCP(port 52792, HTTP 200): websquare.html?w2xPath 로 엔진 부트스트랩 정상(title=WebSquare, console error 0). 단, 샘플 인라인 script 의 scwin.* 등록은 w-pack JS 변환 후 content-frame scope 에 적용되는 구조라, w-pack 미변환(작업범위 밖) 상태에서 raw XML 직접 로드 시 target1 인스턴스화는 미확인. 엔진 소스(layoutController.js 2920/3546) 대조로 파라미터/순서 설계 정합성 검증 완료.
- 결과: PASS (정적+엔진소스 검증 / 런타임 인스턴스화는 w-pack 변환 후 가능).

---

# gridView_E onbeforetoggleclick 검증

## gridView_E_onbeforetoggleclick_1.xml

### 1. 코드 리뷰
- XML 구조/네임스페이스 정상, validate_xml isValid=true (errors 0).
  - warnings 7건 모두 false-positive: select1 selectedIndex 미설정(형제샘플 동일 패턴), createEList 동적 바인딩 핸들러 3건(UNUSED_EVENT_HANDLER).
- description = @description 첫 문장(원문 유지), validation 10항목.
- DrillDown 그리드: name 컬럼 inputType="drilldown" + depthColumn="depth" + showDepth="1" + depthStartIndex="1". 3단계(본부 depth1 > 팀 depth2 > 사원(Leaf) depth3, 7행). 부모/Leaf 모두 포함 → @spec1 검증 가능.
- con: con_returnValue(true/false/미설정 커스텀), con_drilldownToggle(enter/미설정), con_ignoreCellClick / con_ignoreToggleOnDisabled(gdl_boolean). con 변경 후 [Target 재생성] 으로 적용.
- 바인딩: createEList(["onbeforetoggleclick","ontoggleclick","oncellclick"]). 핸들러는 발생 순서 누적 로그(gVar.logArr) + rowIndex(payload) + getOpenStatus 출력.
- 엔진 메소드 실존: getOpenStatus(gridView.js:12771, public). isOpen 은 hidden/deprecated 라 getOpenStatus 사용. 이벤트 3종 @name 주석 확인.
- 토글 트리거는 사용자/자동화 액션 — 별도 트리거 버튼 미생성(지침 준수).

### 2. MCP 런타임 검증 (port 52792 HTTP 200, launcher /websquare/websquare.html?w2xPath=...) — PASS
- mf_target1 생성 OK. showDepth=1 → 초기 표시 rowCount=2, totalRow=7.
- 토글 버튼 = `span.w2grid_plus`(부모) / `span.w2grid_leaf`(Leaf). 클릭 합성(mousedown/mouseup/click)으로 실증.
- **CASE returnValue=true**: getOpenStatus false→true, rowCount 2→3, 순서 = `onbeforetoggleclick > ontoggleclick > oncellclick`. ✓ (@return true, @spec3)
- **CASE returnValue=false**: getOpenStatus 불변(false), rowCount 불변(2), 순서 = `onbeforetoggleclick > oncellclick` (ontoggleclick 미발생). ✓ (@return false)
- **CASE returnValue=미설정**: 출력 '(생략 → undefined → true 적용)', 토글 실행(false→true), 순서 = 전체 체인. ✓ (@return 생략→true)
- **@spec2 (ontoggleclick 은 상태 변경 이전)**: ontoggleclick 시점 핸들러에서 출력 — 내부 openStatus 플래그는 이 시점 이미 true 로 읽히나, 행/화면 반영(rowCount 증가)은 oncellclick 이후. 이벤트 자체가 상태 확정 전에 발생함을 핸들러 순서로 확인.
- **@spec1 (Leaf 클릭 시 미발생)**: 영업1팀 자식 사원(김영업, depth3, span.w2grid_leaf) 클릭 → `oncellclick` 만 발생, onbeforetoggleclick/ontoggleclick 미발생. ✓
- **@spec | (ignoreCellClick=true + 반환 true → oncellclick 미발생)**: con_ignoreCellClick=true(getText 적용, options.ignoreCellClick=true) + returnValue=true 토글 → `onbeforetoggleclick > ontoggleclick` (oncellclick 미발생). ✓
- **enter 동작 (@description \| vs @spec \| 충돌 실측)**: drilldownToggle="enter" + 셀 포커스 후 Enter 키 → 토글 실행(false→true, rowCount→3) 되었으나 발생 이벤트는 `ontoggleclick` 단독(onbeforetoggleclick 미발생).
  - → **@spec \| 와 일치** ("enter 일때 Enter 키 입력 시 onbeforetoggleclick 없이 ontoggleclick 바로 발생").
  - → **@description \| 와 불일치** ("drilldownToggle=enter 이면 Enter 키 입력으로 [onbeforetoggleclick] 이벤트가 발생"). 엔진은 Enter 입력 시 onbeforetoggleclick 을 발생시키지 않음 = 가이드 @description \| 표현 부정확(엔진 동작은 @spec \| 가 정확).
- 수정 이력: 초기 con_ignoreCellClick/ignoreToggleOnDisabled 를 getValue()로 읽어 gdl_boolean value(1/2/3)가 그대로 전달 → 속성 미적용(options.ignoreCellClick=false) 됨. **getText()로 변경**(""/true/false) 후 정상 적용 확인(MEMORY reference_gdl_boolean_gettext_not_getvalue 일치).
- 샘플 관련 console error 0 (raw .xml 직접 오픈 시 'scwin is not defined' 는 launcher 미경유 케이스로 비해당).

결론: PASS

---

# gridView_E_oncontextclick_1.xml 검증

## 1. 코드 리뷰
- XML 구조 정상 (네임스페이스/head/body, description=@description 첫 문장, validation 2항목).
- 전제: contextMenu="true" + rowNumVisible="true" + rowStatusVisible="true" + Body Column col1~col4(인덱스 0~3).
- 이벤트 바인딩: createEList(["oncontextopen","oncontextclick"]) → 공통 cList/eList 패턴.
- oncontextopen 핸들러: appendMenu 로 사용자 항목 2개({label, userMenuId:userMenuA/B}) 반환 → 사용자 항목 클릭(systemMenu=false, userMenuId) 케이스 검증 가능. 기본 시스템 항목은 유지 → systemMenu=true/systemMenuId 케이스도 검증.
- oncontextclick 핸들러: payload 6개 개별 출력(rowIndex/columnIndex/userMenuId/systemMenu/systemMenuId/focusedCell). 트리거(우클릭→항목 클릭)는 사용자/자동화 액션, 트리거 버튼 없음.

## 2. validate_xml
- isValid: true, errors 0.
- warnings 2건(UNUSED_EVENT_HANDLER: target1_oncontextopen/oncontextclick) — createEList 동적 바인딩이라 false-positive, 무시.

## 3. 엔진 소스 확인
- eventController.js: oncontextopen 반환값(contextOpenResult)으로 createContextMenu 구동.
- layoutController.js:6789 oncontextclick fireEvent 인자 순서 = (row, col, userMenuId, isSystemMenu, systemMenu, focusedCell) → 가이드 param 순서(rowIndex, columnIndex, userMenuId, systemMenu(불리언), systemMenuId, focusedCell)와 일치.
- appendMenu 항목: {label→col1, userMenuId, isSystemMenu=false 자동}.
- [중요] focusedCell 은 배열 [{rowIndex, colIndex}] 구조 (가이드는 focusedCell.rowIndex 로 기술하나 실제는 focusedCell[0].rowIndex). 핸들러에서 focusedCell[0] 접근으로 수정.

## 4. MCP playwright 런타임 검증 (PASS)
- 페이지 정상 로딩, window.mf_target1 생성. console error 0.
- 컨텍스트 메뉴 표시: Body 셀 우클릭 시 8개 행(시스템 6 + 사용자 2: '사용자 항목 A/B') 표시.
- 시스템 항목 클릭(Body cell 1,2 → 크기 자동 맞추기):
  rowIndex=1, columnIndex=2, userMenuId=undefined, systemMenu=true, systemMenuId=ColumnAdjustWidth, focusedCell.rowIndex=1/colIndex=2.
- 사용자 항목 클릭(Body cell 3,0 → 사용자 항목 B):
  rowIndex=3, columnIndex=0, userMenuId=userMenuB, systemMenu=false, systemMenuId=undefined, focusedCell.rowIndex=3/colIndex=0.
- RowNum 영역 우클릭: columnIndex='rowNum'(문자열), systemMenu=true, focusedCell=null.
- RowStatus 영역 우클릭: columnIndex='rowStatus'(문자열), focusedCell=null.
- 주의: WebSquare 서버 페이지 파싱 캐시로 구버전 핸들러가 잔존 → URL 에 캐시버스팅 쿼리(&_cb=) 추가로 최신 XML 재파싱 확인.

결론: PASS

---

## gridView_E_ongroupby_1.xml

### 1. 정적 검증
- `validate_xml` : errors 0 / warnings 0 (par_ select1 4개에 selectedIndex="0" 부여 후 warning 0).
- 이벤트 속성 : `createEList(["ongroupby"])` → `ev:ongroupby="scwin.target1_ongroupby"` (on 중복 없음).
- 전제 속성 고정 : groupbyZone=true / contextMenu=true / useHeaderContextMenu=true (createLabel `[전제]` 로 표시).
- 자동 실행 트리거 버튼 없음(발생 조건이 전부 사용자 조작). 메소드 검증용 버튼 2개(groupby / clearGroupby)만 배치.

### 2. 엔진 소스 확인 (websquare/uiplugin/gridView)
- `layoutController.js:2891` GroupBy Zone Drop → `options = {sortIndex, sortOrder(, filter)}` 로만 구성 후 `fireEvent("ongroupby")`.
- `layoutController.js:8349 / 8369` 컨텍스트 메뉴 "선택 컬럼 그룹"(groupMsg) → `options = {sortIndex, sortOrder}` 로 `fireEvent("ongroupby")`.
- `layoutController.js:7366` Group FilterList 필터 → filter/filterRegExp 포함 (본 샘플 범위 외, _3 담당).
- `_wg.extend({}, this.groupbyDisplayOptions)` 는 **핸들러가 false 를 반환한 이후** 에만 사용 → hideHeader/hideFooter/groupbyHeader/groupbyFooter/closeGroup/showOnlyLastDepth 는 **이벤트 파라미터로 전달되지 않음**.
- `layoutController.js:7040` GroupBy Zone 칩의 X 아이콘 → `ungroupColumn()` 호출, `gridViewApiController.js:1970 ungroupColumn` 내부에 `fireEvent("ongroupby")` 없음.
- `layoutController.js:8320` 컨텍스트 메뉴 "선택 컬럼 그룹 해제"(ungroupMsg) → `ungroupColumn()` 호출, 동일하게 fireEvent 없음.

### 3. MCP playwright 런타임 검증 (PASS)
포트 56075 / 세션 1. console error 0.
- 로딩 실측 : `window.mf_target1` 생성, options.groupbyZone=true / contextMenu=true / useHeaderContextMenu=true, `div[id$='_groupbyZone_div']` 렌더, dataLength=6.
- **Drag&Drop 추가(1회차)** : 헤더 부서(1) → GroupBy Zone. ongroupby 발생.
  `result = {"sortIndex":[1],"sortOrder":[1]}`, 나머지 6속성 전부 `undefined (미전달)`.
- **Drag&Drop 추가(2회차, 2 Depth)** : 헤더 지역(0) 추가. `result = {"sortIndex":[1,0],"sortOrder":[1,1],"filter":{}}`.
  par 설정(hideFooter=true / closeGroup=false / showOnlyLastDepth=true) 이 groupby(result) 로 적용됨.
- **컨텍스트 메뉴 "선택 컬럼 그룹"(Header 우클릭)** : ongroupby 발생(3회차). `result = {"sortIndex":[1,2],"sortOrder":[1,1]}`, GroupBy Zone 에 등급(2) 추가.
- **par 옵션 적용 실측** : hideHeader=true / hideFooter=false / closeGroup=true 로 재드래그 시
  tbody 렌더 = Group Header Row 없음 + `G:소계|2` / `G:소계|4` Footer Row 표시, `groupbyDisplayOptions = {hideHeader:true, hideFooter:false, closeGroup:true, showOnlyLastDepth:false}`.
- **groupby() / clearGroupby() 버튼** : 발생 횟수 3 → 3 (미발생 확인).
- **미발생 실측(결함 의심)** :
  - GroupBy Zone 칩 X 아이콘으로 Group Column 제거 → sortIndexArr [1,0]→[1] 로 변경되나 ongroupby 미발생(결과창 공백).
  - 그룹 칩을 Zone 밖으로 Drag&Drop → 제거 자체가 동작하지 않음(상태 무변화, 이벤트 없음).
  - 컨텍스트 메뉴 "선택 컬럼 그룹 해제" → sortIndexArr [1,2]→[1] 로 변경되나 ongroupby 미발생.

결론: PASS (샘플 동작 정상). @spec3 의 "제거" / "선택 컬럼 그룹 해제" 발생 조건은 엔진 미구현으로 판단.

---

## gridView_E_oncellindexchange_2.xml (MultiLine 분리 샘플)

| 검증 항목 | 결과 |
|---|---|
| XML well-formed | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS |
| MultiLine 구성 실측 (레코드 3건 x 2 Line, colIndex 0~5) | PASS (td 48개, 셀값 A0~F2) |
| createLabel 안내 라벨 렌더 ([전제]/[절차]/[확인], 개행 `<br>` 변환) | PASS |
| validation 2건 렌더 | PASS |
| oncellindexchange 핸들러 발생 및 rowIndex/colIndex/oldRow/oldColIndex 출력 | PASS |
| console error | 0 |
| MultiLine 행 높이로 인한 하위 레코드 잘림 | 없음 (마지막 셀 bottom 525.5 < grid bottom 577, height 300px) |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_oncellindexchange_2.js`) |

실측 raw 값:
- 같은 레코드(1) 의 1줄 Cell(colIndex 1) → 2줄 Cell(colIndex 4) 클릭 : 두 번 모두 `rowIndex : 1` 로 동일.
- 레코드 0 의 1줄~2줄 모든 Cell 순차 클릭 : `colIndex` 0 → 1 → 2 → 3 → 4 → 5 연속 고유.

MCP 검증: PASS / 발견 이슈 없음. (세션 playwright2 미노출 → playwright3 사용, navigate 직후 `location.href` 로 본인 샘플 경로 확인 후 조작·측정을 원자적으로 수행)

상세 대조표는 `03_보강_oncellindexchange_2.md` 참조.

---

## gridView_E_oncellindexchange_3.xml (DrillDown 구성)

| 검증 항목 | 결과 |
|---|---|
| XML well-formed | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS (`getDataLength()=6`, `getTotalRow()=6`) |
| DrillDown 구성 (토글 아이콘 렌더 / 접기 / 펼치기) | PASS (`span.w2grid_minus` ↔ `span.w2grid_plus` 전환) |
| createLabel 안내 라벨 렌더 ([전제]/[절차]/[확인], 개행 변환) | PASS |
| validation 1건 렌더 | PASS |
| oncellindexchange 핸들러 발생 및 rowIndex/colIndex/oldRow/oldColIndex 개별 출력 | PASS |
| 접기/펼치기 전후 rowIndex 나란히 출력 | PASS |
| console error | 0 |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_oncellindexchange_3.js`) |

실측 raw 값:
- 펼침(6 Row 표시) 상태에서 개발본부 이름 Cell 클릭 → `rowIndex:3, colIndex:0, oldRow:-1, oldColIndex:null`
- 영업본부 토글 아이콘 클릭(자식 2건 접힘) → 표시 `0=영업본부 / 1=개발본부 / 2=개발1팀 / 3=관리팀`
- 접힘 상태에서 개발본부 이름 Cell 클릭 → `rowIndex:1` (`이전 선택 rowIndex:3 / 현재 rowIndex:1` 나란히 출력)
- 다시 펼친 뒤 개발본부 클릭 → `rowIndex:3` 으로 복귀

MCP 검증: PASS / 발견 이슈 없음. 엔진 결함 없음(가이드 문구와 실동작 일치).
(playwright3 세션, navigate 직후 `location.href` 로 본인 샘플 경로 확인 후 조작·측정을 원자적으로 수행)

상세 대조표는 `03_보강_oncellindexchange_3.md` 참조.

---

## gridView_E_oncellindexchange_1.xml (기본 구성)

| 검증 항목 | 결과 |
|---|---|
| XML well-formed | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS (`getColumnCount()=5`, `getDataLength()=6`) |
| 숨김 Column 적용 (`setColumnVisible(1,false)`) | PASS (`getColumnVisible(1)=false`) |
| blockSelect Column 적용 | PASS (`getCellInfo(3).options.blockSelect=true`) |
| createLabel 안내 라벨 렌더 ([전제]/[절차1]/[절차2]/[확인], 개행 변환) | PASS |
| validation 14건 렌더 | PASS |
| oncellindexchange 핸들러 rowIndex/colIndex/oldRow/oldColIndex 개별 출력 | PASS |
| 최초 선택 시 oldRow=-1 / oldColIndex=null | PASS |
| 숨겨진 Column 포함 colIndex | PASS (3번째 표시 Column 클릭 → colIndex 2) |
| 3개 이벤트 발생 순서 (순번 즉시 출력) | PASS (oncellindexchange → onrowindexchange → oncolumnindexchange) |
| blockSelect Column Cell 클릭 → 미발생 | PASS |
| focusMode='none' (마우스 클릭만 발생 / 키보드·메소드 미발생) | PASS |
| GroupBy Cell / SubTotal Cell 클릭 → 미발생 | PASS |
| setFocusedCell / setMultiFocus 버튼 동작 | PASS (setMultiFocus 는 3회 발화 = 총 9개 이벤트) |
| console error | 0 |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_oncellindexchange_1.js`) |

수정 이력:
- `groupby()` 최초 구성에서 Group Header/Footer Row 가 완전 빈 칸으로 렌더되어 클릭 대상 식별 불가 → `groupbyHeader`(toggle+그룹명) / `groupbyFooter`(그룹 합계) 지정 후 재검증 PASS

MCP 검증: PASS / 발견 이슈 없음. (playwright3 세션, navigate 직후 `location.href` 및 컬럼 수·컴포넌트 ID 교차 확인 후 조작·측정을 원자적으로 수행)

상세 대조표는 `03_보강_oncellindexchange_1.md` 참조.

---

## gridView_E_oncolumnindexchange_2.xml (Body 가 MultiLine 인 구성)

| 검증 항목 | 결과 |
|---|---|
| XML well-formed | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS (MCP navigate 직후 `location.href` 로 본인 샘플 경로 선확인) |
| MultiLine 구성 (레코드 1건 = 2줄, Line1=c1·c2·c3 / Line2=c4·c5·c6) | PASS (`mf_target1_cell_{0..2}_{0..5}` = A0~F2 정상 표시) |
| 인라인 DataList `dlt_bind`(c1~c6) 바인딩 | PASS (셀 빈값 없음) |
| grid height 충분성 (3레코드 x 2Line 전부 표시) | PASS (마지막 셀 `cell_2_5`(F2) bottom 545.5 < grid bottom 597, 잘림 없음) |
| `.w2grid_scrollY { display:none }` 적용 | PASS (computed display = none) |
| createLabel 안내 라벨 렌더 ([전제]/[절차]/[확인], 개행 변환) | PASS |
| validation 1건 렌더 | PASS |
| oncolumnindexchange 핸들러 colIndex/oldColIndex 개별 출력 | PASS |
| **MultiLine 에서 Column 고유 인덱스** (1줄 c1~c3 → 2줄 c4~c6 순차 클릭) | PASS (colIndex 0→1→2→3→4→5 연속·고유, oldColIndex null→0→1→2→3→4) |
| 동일 Column 에서 Row 만 변경 시 미발생 (오독 방지 확인) | PASS (`cell_1_5` 클릭 시 결과 추가 없음) |
| console error | 0 |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_oncolumnindexchange_2.js`) |

수정 이력: 없음 (최초 생성분 그대로 PASS)

MCP 검증: PASS / 발견 이슈 없음. (playwright3 세션3, navigate → `location.href` 확인 → 클릭 → 측정을 원자적 실행으로 수행, 컬럼 수·셀 ID 교차 확인)

상세 대조표는 `03_보강_oncolumnindexchange_2.md` 참조.

---

## gridView_E_oncolumnindexchange_1.xml (기본 구성)

| 검증 항목 | 결과 |
|---|---|
| XML well-formed | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS (MCP navigate 직후 `location.href` 로 본인 샘플 경로 선확인, `getDataLength()=6` 교차 확인) |
| 5 Column 구성 + 인덱스 1 `setColumnVisible(1,false)` 숨김 | PASS |
| 인덱스 3 Body Column `blockSelect="true"` | PASS |
| createLabel 안내 라벨 렌더 ([전제]/[절차1~3]/[확인], 개행 변환) | PASS |
| validation 11건 렌더 | PASS |
| oncolumnindexchange 핸들러 colIndex/oldColIndex 개별 출력 | PASS |
| 재생성 직후 첫 선택 `oldColIndex : null` | PASS |
| 숨겨진 Column 포함 인덱스 (3번째 표시 Column → `colIndex : 2`) | PASS |
| **동일 Column 에서 Row 만 변경 시 미발생** (ArrowDown / 같은 Column 다른 Row 클릭) | PASS (oncellindexchange·onrowindexchange 만 출력, oncolumnindexchange 없음) |
| 이벤트 발생 순서 oncellindexchange → onrowindexchange → oncolumnindexchange | PASS (순번 `[1][2][3]`) |
| 차단 Column(인덱스 3) 클릭 시 미발생 | PASS (출력 없음) |
| `setFocusedCell(2,2)` 버튼 | PASS (`colIndex : 2 / oldColIndex : 0`) |
| `setMultiFocus` 버튼 — 여러번 발생 | PASS (oncolumnindexchange 3회, `[1]~[9]`) |
| focusMode='none' — 마우스 클릭만 발생 | PASS (클릭 발생 / ArrowRight·setFocusedCell 미발생) |
| groupby — Group Header/Footer Cell 미발생 | PASS (출력 없음, 데이터 Row 는 정상 발생) |
| subtotal — SubTotal Cell 미발생 | PASS (출력 없음, 데이터 Row 는 정상 발생) |
| console error | 0 |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_oncolumnindexchange_1.js`) |

수정 이력:
- Target 재생성 후 안내 라벨이 이전 con 값(`focusMode=미설정`)을 그대로 표시하는 문제 발견
  (`$c.gcm.createLabel` 은 동일 cID 재호출 시 `dynamicCreate` 가 undefined 를 반환해 `setLabel` 을 건너뜀 — `common.xml:2593`)
  → `comp_init` 의 target 삭제 직후 `if (typeof label1 !== 'undefined') { label1.remove(); }` 추가 후 재검증 PASS

MCP 검증: PASS / 발견 이슈 1건(위 createLabel 갱신 문제, 샘플 내 우회 처리 완료. 동일 패턴의 형제 샘플에도 적용 필요)
(playwright3 세션, navigate → `location.href` 확인 → 조작 → 측정을 원자적 실행으로 수행, 컬럼 수·셀 ID 교차 확인)

상세 대조표는 `03_보강_oncolumnindexchange_1.md` 참조.

---

## gridView_E_ondateselect_2.xml (MultiLine)

- 미리보기 : `http://127.0.0.1:53249/websquare/websquare.html?w2xPath=/sample/gridView/gridView_E_ondateselect_2.xml`
  (기존 안내 포트 60483 은 사용 불가 → **53249** 로 검증. 53249 만 `/sample/*.xml` 과 `/_wpack_/sample/*.js` 가 함께 200)

| 검증 항목 | 결과 |
|---|---|
| XML well-formed | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS (navigate 직후 `location.href` 선확인 + `getColumnCount()=6`, `getColumnID(1)='calDate1'`, `getColumnID(4)='calDate2'`, `getDataLength()=3` 교차 확인) |
| MultiLine 2 Line 구성 (Line1 c1/calDate1/c2, Line2 c3/calDate2/c4) | PASS |
| calendar 컬럼 표시값 (`dataType="date"` + displayFormat) | PASS (`2026-01-05` 등, 포맷 토큰 리터럴 부착 없음) |
| createLabel 안내 라벨 렌더 ([전제]/[절차]/[확인], 개행 변환) | PASS (`label1.remove()` 미사용, Target 재생성 시 자동 갱신) |
| validation 2건 렌더 | PASS |
| 날짜 셀 클릭 → 편집 진입 → 달력 아이콘 클릭 → 팝업 오픈 (실제 클릭) | PASS (팝업 날짜 셀 42개) |
| ondateselect 핸들러 rowIndex/colIndex/date/dateObject 개별 출력 | PASS |
| **MultiLine 각 Line 의 Row 인덱스 동일** | PASS (레코드1 1줄 `rowIndex:1 colIndex:1` / 2줄 `rowIndex:1 colIndex:4`) |
| **MultiLine 이어도 Column 인덱스 고유** | PASS (1줄 달력 Cell colIndex 1, 2줄 달력 Cell colIndex 4, 전체 0~5) |
| 레코드별 rowIndex 구분 | PASS (레코드2 2줄 선택 시 `rowIndex:2 colIndex:4`) |
| 선택 후 셀 표시값 반영 | PASS (`1_1=2026-02-12`, `1_4=2026-02-12`, `2_4=2026-03-20`) |
| console error / pageerror | 0 |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_ondateselect_2.js`) |

수정 이력: 없음 (최초 생성본 그대로 전체 PASS)

MCP 검증: PASS / 발견 이슈 없음
(playwright3 세션. 검증 도중 다른 워커가 세션을 타 샘플로 이동시킨 것을 감지 → navigate → `location.href` 확인 → 컬럼 구성 교차확인 → 조작 → 측정을 **하나의 원자적 실행**으로 재수행하여 최종 확정)

상세 대조표·제외 근거는 `03_보강_ondateselect_2.md` 참조.

---

## gridView_E_ondateselect_1.xml (기본 구성)

- 미리보기 : `http://127.0.0.1:53249/websquare/websquare.html?w2xPath=/sample/gridView/gridView_E_ondateselect_1.xml`
  (기존 안내 포트 60483 은 사용 불가 → **53249** 로 검증. 53249 만 `/sample/*.xml` 과 `/_wpack_/sample/*.js` 가 함께 200)

| 검증 항목 | 결과 |
|---|---|
| XML well-formed / WebSquare 구조(네임스페이스, head/body, wframe 4종) | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS (navigate 직후 `location.href` 선확인 + `getColumnCount()=4`, `getDataLength()=4`, `getColumnVisible(1)=false` 교차 확인) |
| description / validation 13건 렌더 | PASS (좌측 패널 (1)~(13) 전부 표시) |
| createLabel 안내 라벨 렌더 ([전제]/[절차1~4]/[확인], `\n` 개행 변환) | PASS (`label1.remove()` 미사용, Target 재생성 후에도 중복 없이 1개 유지) |
| Target 재생성 버튼(`btn_createTarget`) 동작 | PASS (초기 데이터 복원 `0_2=2026-01-15`, 숨김 Column 재적용) |
| calendar 컬럼 표시값 (`dataType="date"` + displayFormat) | PASS (`2026-01-15` / `15-01-2026`, 포맷 토큰 리터럴 부착 없음) |
| 달력 아이콘 실제 클릭 → 팝업 오픈 → 날짜 셀 실제 클릭 | PASS (프로그래밍 방식 값 설정 미사용) |
| ondateselect 핸들러 rowIndex / colIndex / date / dateObject 개별 출력 | PASS |
| (1) 팝업 닫힘 후 입력값 변경 시 발생 | PASS (셀 `2026-02-20` → `2026-02-25`) |
| (2)(3) rowIndex = 표시된 Row 인덱스 | PASS (`rowIndex : 1`, `rowIndex : 3`) |
| (4)(5) colIndex = 숨겨진 Column 포함 인덱스 | PASS (화면 2번째 Column 이지만 `colIndex : 2`, 마지막 Column `colIndex : 3`) |
| (6) date = "yyyyMMdd" | PASS (`date : 20260225`) |
| (7) ioFormat 적용값 | PASS (`ioFormat="ddMMyyyy"` 컬럼 → `date : 22012026`) |
| (8) dateObject = Date 객체 | PASS (`dateObject 날짜 : 2026-02-25`) |
| (9) dateObject 경과 밀리초 정수 | PASS (`1771945200000`) |
| (10) 달/연월 이동 버튼만 조작 시 미발생 | PASS (`onmonthselect`/`onyearselect` 만 출력, ondateselect 출력 없음, 셀 값·DataList 불변) |
| (11) onyearselect -> onmonthselect -> ondateselect 순서 | PASS (순번 [8][9][10]) |
| (12) 같은 날짜 재선택 시에도 발생 | PASS (`date : 20260115` 출력) |
| (13) onviewchange 는 값 변경 시에만 발생 | PASS (값 변경 시 발생 / 같은 날짜 재선택 시 미발생) |
| console error / warning / pageerror | 0 |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_ondateselect_1.js`) |

수정 이력: 없음 (최초 생성본 그대로 전체 PASS)

MCP 검증: PASS / 발견 이슈 1건(아래 부수 관찰 — ondateselect 범위 밖)
(playwright3 세션. 검증 도중 다른 워커가 세션을 `_2` 페이지로 이동시킨 것을 감지 → 이후 navigate → `location.href` 확인 → 컬럼 수·셀 ID 교차확인 → 조작 → 측정을 **하나의 원자적 실행**으로 재수행하여 확정)

부수 관찰(엔진 결함 의심, ondateselect 자체와 무관):
`embeddedInput="true"` calendar 컬럼에서 팝업을 **Esc 키로 닫으면 해당 셀 입력창에 직전에 편집한 다른 Row 의 값**이 표시된다.
(예: (0,2) 에서 18일 선택 후 (2,2) 팝업 오픈 → Esc → `2_2` 표시값이 `20260118` → 이후 `2026-01-18`. DataList 는 `20260310` 유지 → 화면/데이터 불일치)
본 샘플 안내 절차는 "팝업 바깥 클릭으로 닫기" 이므로 검증에는 영향 없음.

상세 대조표·구성 근거는 `03_보강_ondateselect_1.md` 참조.

---

## gridView_E_ondateselect_3.xml (DrillDown 구성)

- 미리보기 : `http://127.0.0.1:53249/websquare/websquare.html?w2xPath=/sample/gridView/gridView_E_ondateselect_3.xml`
- 담당 validation : **1건** — "DrillDown 으로 숨겨진 Row 는 인덱스에 포함되지 않습니다." (`@param rowIndex` 하위절)
  나머지 항목은 `_1`(13건) / `_2`(2건) 담당이므로 중복 제외.

| 검증 항목 | 결과 |
|---|---|
| XML well-formed / WebSquare 구조(네임스페이스, head/body, wframe 4종) | PASS |
| 페이지 렌더 / `window.mf_target1` 생성 | PASS (navigate 직후 `location.href` 선확인 + `getDataLength()=6`, `getTotalRow()=6`, DrillDown 6 Row 셀 교차 확인) |
| description(`_1` 과 동일 문구) / validation 1건 렌더 | PASS (좌측 패널 (1) 표시) |
| createLabel 안내 라벨 렌더 ([전제]/[절차]/[확인], `\n` 개행 변환) | PASS (`label1.remove()` 미사용, Target 재생성 후에도 1개 유지) |
| Target 재생성 버튼(`btn_createTarget`) 동작 | PASS (초기 데이터·펼침 상태 복원) |
| DrillDown 트리 렌더 (`inputType="drilldown"`, showDepth=2) | PASS (영업본부/영업1팀/영업2팀/개발본부/개발1팀/관리팀 6 Row 표시) |
| calendar 컬럼 표시값 (`dataType="date"` + `viewType="icon"` + displayFormat) | PASS (`2026-01-10` 등, 포맷 토큰 리터럴 부착 없음) |
| 달력 Cell 실제 클릭 → 아이콘 클릭 → 팝업 오픈 → 날짜 셀 실제 클릭 | PASS (프로그래밍 방식 값 설정 미사용, 팝업 날짜 셀은 컬럼 스코프 `td[id^='mf_G_mf_target1__cdate_calendar_cell_']` 로 접근) |
| ondateselect 핸들러 rowIndex / colIndex / date / dateObject 개별 출력 | PASS |
| (1) DrillDown 으로 숨겨진 Row 는 인덱스에 포함되지 않음 | PASS (개발본부 : 펼침 `rowIndex : 3` → 영업본부 접기 후 `rowIndex : 1` → 재펼침 `rowIndex : 3`. 숨겨진 자식 2 Row 만큼 정확히 앞당겨짐) |
| 접기/펼치기 전후 rowIndex 나란히 출력 | PASS (`[개발본부] 이전 선택 rowIndex : 3 / 현재 rowIndex : 1`, `... 1 / 현재 rowIndex : 3`) |
| console error / warning / pageerror | 0 |
| w-pack 자동 재생성 | PASS (`_wpack_/sample/gridView/gridView_E_ondateselect_3.js`) |

수정 이력: 안내 라벨 [절차] 문구 1건 보정 (달력 Cell 클릭 → 입력창 오픈 → 달력 아이콘 클릭 순서를 명시). 로직 변경 없음.

MCP 검증: PASS / 발견 이슈 없음 (playwright3 세션. navigate → `location.href` 확인 → 조작 → 측정을 원자적 실행으로 수행)

부수 관찰(가이드 문구 검증에 영향 없음):
- 팝업 달력의 전월/익월 날짜 셀(class `w2calendar_date_lastmonth`)을 클릭하면 ondateselect 는 발생하지 않고 팝업의 표시 월만 이동한다.
- `rowNumVisible="true"` 의 rowNum 컬럼은 Body Column 인덱스에 영향을 주지 않는다(달력 Column 이 계속 `colIndex : 1`).

상세 대조표·구성 근거는 `03_보강_ondateselect_3.md` 참조.

---

## gridView_E_oneditkeydown_1.xml

### 1. 코드 리뷰
- XML 구조 정상 (네임스페이스/head/body, description, validation 14항목).
- `editable="true"` GridView + Body Column 6개(0 drilldown / 1 text 숨김 / 2 text / 3 text+embeddedInput / 4 secret+embeddedInput / 5 textImage+embeddedInput).
- 이벤트 바인딩: `createEList(["onkeydown","oneditkeydown","onkeyup","oneditkeyup"])` — 뒤 3개는 `@spec` 2 순서 출력 전용.
- 트리거는 전부 사용자 실제 키 입력(자동 실행 버튼 없음). Target 재생성은 `body_sample.xml` 기존 `btn_createTarget` 사용.
- `info` 객체 6속성(전달여부/rowIndex/colIndex/colID/keyCode/oldValue) 개별 출력, oldValue 는 `hasOwnProperty` 로 미전달 구분.

### 2. XML 구조 검증
- well-formed OK (`validate_xml` 툴은 본 세션에서 사용 불가하여 XML 파서로 대체 확인).
- w-pack 자동 재생성 확인 (`_wpack_/sample/gridView/gridView_E_oneditkeydown_1.js`, 200).

### 3. MCP playwright 런타임 검증 (PASS)
- 접속 URL `…?w2xPath=/sample/gridView/gridView_E_oneditkeydown_1.xml` 를 `location.href` 로 선확인 후 측정.
- `window.mf_target1` 생성, 컬럼 6 / 데이터 6행, `setColumnVisible(1,false)` 로 인덱스 1 숨김(offsetWidth 0) 확인.
- console error 0. Target 재생성 버튼 정상(라벨 중복 생성 없음).

| 확인 항목 | 결과 |
|---|---|
| 비 embeddedInput 셀 더블클릭 편집 + 일반키 → onkeydown → oneditkeydown → onkeyup → oneditkeyup | PASS |
| 같은 셀에서 Tab / Enter / Escape → oneditkeydown 미발생 | PASS |
| info 객체 전달 / rowIndex / colIndex(숨김 Column 포함, 2) / colID / keyCode(65) | PASS |
| oldValue — 비 embeddedInput 미전달 / embeddedInput text·secret·textImage 전달 | PASS |
| `@spec` 1 embeddedInput 셀은 Tab(9)/Enter(13)/Escape(27) 에서도 발생 | PASS |
| DrillDown 접기 → 개발본부 rowIndex 3 → 1 (숨겨진 Row 제외) | PASS |
| 조합형 keyCode 229 (CDP `Input.dispatchKeyEvent` windowsVirtualKeyCode 229) | PASS |
| textImage + embeddedInput 셀의 colIndex / colID | **FAIL — undefined / null (엔진 결함)** |
| `@spec` 2 이벤트 순서 (embeddedInput 경로) | **FAIL — oneditkeydown 이 onkeydown 보다 먼저, 일반키는 onkeydown 미발생 (엔진 결함)** |

MCP 검증: PASS / 발견 이슈 2건(엔진 결함, 샘플 로직 문제 아님).

상세 대조표·raw 측정값·결함 원인 분석은 `03_보강_oneditkeydown_1.md` 참조.

---

## gridView_E_onkeyup_1.xml (신규)

### 1. 코드 리뷰
- XML 구조 정상 (네임스페이스/head/body, description, validation 6항목).
- `editable="true"` GridView + Body Column 4개(이름 / 코드 / 수량 / 비고, 전부 `inputType="text"`), Row 4건.
- **`embeddedInput` 미사용** — onkeyup 가이드에 embeddedInput 언급이 없어 옵션/속성 모두 두지 않음(더블클릭 편집 경로만 사용). 라벨/주석에도 언급 없음.
- 이벤트 바인딩: `createEList(["onkeydown","oneditkeydown","onkeyup","oneditkeyup"])` — onkeyup 이 검증 대상, 나머지 3개는 `@spec` 3·4(중복 발생/발생 순서) 확인 전용.
- 조건 옵션 1개 : `con_returnValue` (미설정/true/false, 기본 미설정, th 라벨 "핸들러 반환값") → onkeyup 핸들러 반환값 제어. 핸들러가 실행 시점에 `getValue()` 로 읽으므로 Target 재생성 불필요.
- `e` 는 표준 DOM KeyboardEvent 이므로 `"e : " + e` 한 줄만 출력(개별 속성 미출력).
- 트리거는 전부 사용자 실제 키 입력(자동 실행 버튼 없음).

### 2. XML 구조 검증
- well-formed OK (`validate_xml` 툴 미제공 세션 → XML 파서로 대체 확인).
- createLabel 한 줄 유지 + 라벨 내 줄바꿈은 이스케이프 `
` 만 사용(저장 후 재확인, 렌더 정상).
- w-pack 자동 재생성 확인 (`_wpack_/sample/gridView/gridView_E_onkeyup_1.js`).
  - **주의** : XML 수정 후 wpack 은 갱신되었으나 브라우저가 이전 JS 를 캐시해 옛 화면이 떴다. URL 에 캐시버스터 쿼리를 붙여 재접속 후 재측정함.

### 3. MCP playwright 런타임 검증 (PASS)
- MCP 세션 2(`mcp__playwright2`), 포트 57330, `…?w2xPath=/sample/gridView/gridView_E_onkeyup_1.xml` 를 `location.href` 로 선확인 후 측정.
- `window.mf_target1` 생성, 4컬럼 x 4행(td 20개) 렌더, Cell 내 상시 input 0개(embeddedInput 미사용 확인), console error 0.

| 확인 항목 | 결과 |
|---|---|
| 비편집(포커스만) + ArrowDown → onkeydown / onkeyup 만 발생 | PASS |
| 핸들러에 `e` 전달 → `[object KeyboardEvent]` | PASS |
| 편집(더블클릭) + ArrowDown / 문자키 → onkeydown -> oneditkeydown -> onkeyup -> oneditkeyup | PASS |
| onkeyup 이 false 반환 → 후속 oneditkeyup 미발생 (true/미설정 은 발생) | PASS |
| 순번이 한 조작마다 1 부터 재시작(oneditkeyup 미발생 경로 포함) | PASS |

MCP 검증: **PASS / 발견 이슈 없음**.

상세 대조표·raw 측정값은 `03_보강_onkeyup_1.md` 참조.


## gridView_E_onrowindexchange_1.xml (기본 구성)

### 1. 코드 리뷰
- XML 구조 정상 (네임스페이스/head/body, description, validation 11항목).
- 짝 이벤트 기본편 `gridView_E_oncellindexchange_1.xml` 의 구성·핸들러·라벨 패턴을 그대로 따름.
- Body Column 5개(0=이름 / 1=메모 / 2=부서 / 3=차단(blockSelect=true) / 4=금액), 데이터 6 Row.
  - 짝 샘플의 `setColumnVisible(1,false)` 는 제외 — 숨겨진 Column 문구는 colIndex 파라미터 쪽에만 존재.
- 조건 옵션 2개: `con_focusMode`(미설정/cell/row/both/none), `con_groupbySubtotal`(미설정/groupby/subtotal). 반영은 기존 `btn_createTarget` 재생성만 사용(중복 버튼 없음).
- 이벤트 바인딩: `createEList(["oncellindexchange","onrowindexchange","oncolumnindexchange"])` — onrowindexchange 가 검증 대상, 나머지 2개는 `@spec6`(발생 순서) 및 `@spec3`(Row 미변경 시 미발생) 대비용이며 validation 비대상.
- 검증 대상 핸들러만 `rowIndex` / `oldRow` 개별 출력, 나머지 2개는 순번만 출력(출력값 최소화 규칙).
- 트리거는 마우스 클릭·방향키 등 사용자 실제 조작. 메소드 경로(`@spec2`) 만 버튼 2개(`setFocusedCell`, `setMultiFocus`).
- 순번은 `scwin.logEvent()` 가 발생 즉시 부여(지연 flush 아님), macrotask 단위로 1부터 리셋.

### 2. XML 구조 검증
- well-formed OK (`validate_xml` 툴 미제공 세션 → XML 파서로 대체 확인).
- createLabel 한 줄 유지 + 줄바꿈은 이스케이프 `
` 4개만 사용(저장 후 재확인, 렌더 정상). 라벨 내 작은따옴표 0개.
- w-pack 자동 재생성 확인 (`_wpack_/sample/gridView/gridView_E_onrowindexchange_1.js`).

### 3. MCP playwright 런타임 검증 (PASS)
- MCP 세션 1(`mcp__playwright`), 포트 61058, `…?w2xPath=/sample/gridView/gridView_E_onrowindexchange_1.xml` 를 `location.href` 로 선확인 후 측정.
- `window.mf_target1` 생성, 5컬럼 x 6행 렌더, console error 0.

| 확인 항목 | 결과 |
|---|---|
| 재생성 직후 첫 Row 클릭 → `rowIndex` / `oldRow:-1` | PASS |
| 클릭·방향키·메소드 3경로 모두 발생 | PASS |
| 같은 Row 에서 다른 Column 클릭 / 좌우 방향키 → onrowindexchange 만 미발생 | PASS |
| blockSelect=true Column 클릭 → 미발생 | PASS |
| GroupBy Header/Footer Row, SubTotal Row 클릭 → 미발생 | PASS |
| GroupBy 구성에서 데이터 Row 인덱스가 표시 Row 기준(그룹 Row 포함)으로 산출 | PASS |
| focusMode='none' → 마우스 클릭만 발생, 방향키/메소드 미발생 | PASS |
| setMultiFocus 1회 실행에 onrowindexchange 3회 발생 | PASS |
| 발생 순서 oncellindexchange -> onrowindexchange -> oncolumnindexchange | PASS |

MCP 검증: **PASS / 발견 이슈 없음**.

상세 대조표·raw 측정값은 `03_보강_onrowindexchange_1.md` 참조.

---

## gridView_E_onmonthselect_1.xml (2026-08-19)

### 1. 구성 검증
- 짝 샘플 `gridView_E_ondateselect_1.xml` 의 구성/핸들러/라벨 패턴 준수 (4 Column, 인덱스 1 숨김, calendar Column 2개, con/par 없음).
- 이벤트 바인딩: `createEList(["onmonthselect","onyearselect","ondateselect"])` — onmonthselect 만 검증 대상, 나머지 2개는 `@spec` 2(발생 순서) 대비용이며 validation 비대상(순번만 출력).
- 검증 대상 핸들러만 rowIndex / colIndex / info / info.oldValue / info.newValue 개별 출력.
- Body Column 에 `embeddedInput` 미사용(가이드 미언급 설정 배제) → 편집 진입은 Cell **더블클릭**.
- 트리거는 전부 사용자 실제 조작(Cell 더블클릭 → 달력 아이콘 클릭 → 상단 SelectBox 선택 / 이동 버튼 / 날짜 클릭). 자동 실행 버튼 없음.
- Cell 마다 다른 월을 저장하여 팝업이 그 연월로 열리도록 구성 → 기대값이 실행일과 무관하게 고정(현재 날짜 하드코딩·'오늘' 버튼 사용 없음).

### 2. XML 구조 검증
- well-formed OK (`validate_xml` 툴 미제공 세션 → XML 파서로 대체 확인).
- createLabel 한 줄 유지, 줄바꿈은 이스케이프 `\n` 4개만 사용, 라벨 내 작은따옴표 0개. 저장 후 재확인 및 렌더(`<br>`) 확인 완료.
- 파일 내 `embeddedInput` 문자열 **0건**.
- w-pack 자동 재생성 확인 (`_wpack_/sample/gridView/gridView_E_onmonthselect_1.js`, HTTP 200).

### 3. MCP playwright 런타임 검증
- MCP 세션 1(`mcp__playwright`), 포트 62358(샘플 URL + `_wpack_` js 모두 200), `location.href` 선확인 후 측정.
- `window.mf_target1` 생성, 4컬럼(1개 숨김) x 4행 렌더, console error 0, validation 10건 좌측 패널 정상 렌더.

| 확인 항목 | 결과 |
|---|---|
| 상단 월 SelectBox 변경 → onmonthselect 발생 | PASS |
| rowIndex / colIndex (숨김 Column 포함 인덱스 2·3) | PASS |
| info 객체 전달 / oldValue·newValue = 1~12 number | PASS |
| 이전 해 / 다음 해(이전연월·다음연월) 버튼 → onmonthselect 미발생 | PASS |
| 이전 달 / 다음 달 버튼 → onmonthselect 미발생 | PASS |
| 발생 순서 onyearselect -> onmonthselect -> ondateselect | PASS |

MCP 검증: **PASS / 발견 이슈 0건** (embeddedInput 제거 후 전 항목 재실측, 2026-08-19).

> **정정** : 이전 회차에서 "이전달/이후달 버튼에서도 onmonthselect 가 발생 → `@spec` 1 위반(엔진 결함 의심)" 으로 기록했으나, 그 현상은 샘플이 가이드에 없는 `embeddedInput="true"` 를 설정한 탓이었다. 제거 후 재실측 결과 이동 버튼 4종(이전달/이후달/이전연월/다음연월) 전부 **미발생** 으로 가이드와 일치하여 **해당 결함 보고는 취소**한다.

상세 대조표·raw 측정값은 `03_보강_onmonthselect_1.md` 3~4장 참조.

---

## gridView_E_onmonthselect_3.xml (DrillDown 구성)

- 미리보기 : `http://127.0.0.1:62358/websquare/websquare.html?w2xPath=/sample/gridView/gridView_E_onmonthselect_3.xml`
- 분리 담당 : `@param rowIndex` 하위절 "DrillDown 으로 숨겨진 Row 는 인덱스에 포함되지 않습니다." **1건 전용**
  (나머지는 `_1`(10건) / `_2`(MultiLine 2건) 담당 → 중복 작성 없음. description 은 `_1`·`_2` 와 문구 완전 동일)

### 1. XML 구조 / 스크립트 검증
- well-formed OK (XML 파서 확인).
- createLabel 한 줄 유지(1228자), 줄바꿈은 이스케이프 `\n` 2개만 사용, 라벨 내 작은따옴표 0개. 저장 후 재확인 및 렌더 확인 완료.
- 이벤트 바인딩 `ev:onmonthselect` 1개, 핸들러 `scwin.target1_onmonthselect(rowIndex, colIndex, info)` 시그니처 일치.
- 트리거는 전부 사용자 실제 조작(셀 클릭 → 달력 아이콘 → 상단 월 SelectBox 선택 / 토글 아이콘 클릭). 자동 실행 버튼 없음.
- w-pack 자동 재생성 확인 (`_wpack_/sample/gridView/gridView_E_onmonthselect_3.js`, HTTP 200).

### 2. MCP playwright 런타임 검증 (세션 3, 포트 62358)

| 확인 항목 | 결과 |
|---|---|
| `window.mf_target1` 생성 / DrillDown 6 Row 렌더 / validation 1건 렌더 | PASS |
| 펼침 상태 개발본부 월 변경 → `rowIndex : 3` | PASS |
| 영업본부 토글 클릭으로 자식 2건 접기 (표시 0/1/2/3 = 영업본부/개발본부/개발1팀/관리팀) | PASS |
| 접힘 상태 개발본부 월 변경 → `rowIndex : 1` (숨긴 2 Row 만큼 앞당겨짐) | PASS |
| 재펼침 후 다시 월 변경 → `rowIndex : 3` 원복 | PASS |
| 토글 클릭이 Row 선택을 유발하지 않음 (`getFocusedRowIndex()` 불변) | PASS |
| 팝업이 Cell 저장 연월(2026-02)로 열려 `info.oldValue` 가 항상 2 (실행일 무관) | PASS |
| console error 0 / Target 재생성 후 재렌더 정상 / label1 1개 유지 | PASS |

MCP 검증: **PASS / 발견 이슈 0건** (담당 validation 1건 가이드 문구와 일치, 엔진 결함 없음)

부수 관찰(onmonthselect 범위 밖): calendar Cell 이 편집 상태로 남은 채 Target 재생성 시
`TypeError: ... reading 'viewChangeAfterEdit'` (`_afterEndEdit`) 1회 발생. 편집 상태가 아니면 에러 0.

상세 대조표·실측 raw·소스 근거는 `03_보강_onmonthselect_3.md` 참조.

---

## gridView_E_onyearselect_3.xml (2026-08-19)

- 미리보기 : `http://127.0.0.1:62358/websquare/websquare.html?w2xPath=/sample/gridView/gridView_E_onyearselect_3.xml`
- 분리 구성 `_3` = DrillDown 전용 (validation 1건). `_1` 기본 / `_2` MultiLine 과 중복 없음.
- 짝 샘플 `gridView_E_onmonthselect_3.xml` 과 구성 동일, 조작만 연도 SelectBox 로 변경.

| 검증 항목 | 결과 |
|---|---|
| 페이지 렌더 / `window.mf_target1` 생성 / `getDataLength()=6` | PASS |
| DrillDown 초기 펼침 6 Row (영업본부0/영업1팀1/영업2팀2/개발본부3/개발1팀4/관리팀5) | PASS |
| 펼침 상태 개발본부 일자 Cell → 팝업 달력 연도 SelectBox 변경 → onyearselect 발생, rowIndex=3 | PASS |
| 영업본부 토글 접기 → 표시 4 Row(영업본부0/개발본부1/개발1팀2/관리팀3) | PASS |
| 접힘 상태 개발본부 연도 변경 → rowIndex=1 (숨겨진 자식 2건만큼 앞당겨짐) | PASS |
| 재펼침 후 연도 변경 → rowIndex=3 복귀 | PASS |
| `info.oldValue` 3회 모두 2026 (Cell 값 불변 → 실행일 무관) | PASS |
| `getRealRowIndex()` 로 접힘 상태에서도 Row 이름 정상 조회 | PASS |
| console error 0 | PASS |
| createLabel XML 상 1줄 유지(개행 이스케이프만) / label1 1개 | PASS |

- MCP 검증 : **PASS** (playwright2 세션, 발견 이슈 없음)
- 엔진 결함 없음 — `cellInfo.editRowIndex`(표시 기준) 사용으로 가이드 문구와 일치.

---

## gridView_E_onyearselect_1.xml (2026-08-19)

- 미리보기 : `http://127.0.0.1:62358/websquare/websquare.html?w2xPath=/sample/gridView/gridView_E_onyearselect_1.xml`
- 분리 담당 : `_1` 10건 (`_2` = Body/Row MultiLine 2건, `_3` = DrillDown 1건 제외)

### 1. 구성 검증
- 짝 이벤트 샘플 `gridView_E_onmonthselect_1.xml` 의 구성/핸들러/라벨 패턴을 그대로 따르고 연도 조작으로만 변경 (4 Column, 인덱스 1 숨김, calendar Column 2개, con/par 없음).
- 이벤트 바인딩: `createEList(["onyearselect","onmonthselect","ondateselect"])` — onyearselect 만 검증 대상, 나머지 2개는 `@spec` 2(발생 순서) 대비용이며 validation 비대상(순번만 출력).
- 검증 대상 핸들러만 rowIndex / colIndex / info / info.oldValue / info.newValue 개별 출력.
- Body Column 에 `embeddedInput` 미사용(가이드 미언급 설정 배제, 파일 내 문자열 0건) → 편집 진입은 Cell **더블클릭**.
- 트리거는 전부 사용자 실제 조작(Cell 더블클릭 → 달력 아이콘 클릭 → 상단 연 SelectBox 선택 / 이동 버튼 / 날짜 클릭). 자동 실행 버튼 없음.
- Cell 마다 **다른 연도**를 저장(col3: 2021/2022/2023/2024, col4: 2015/2016/2017/2018)하여 팝업이 그 연월로 열리도록 구성 → 기대값이 실행일과 무관하게 고정(현재 날짜 하드코딩·'오늘' 버튼 사용 없음).

### 2. XML 구조 검증
- well-formed OK (XML 파서 확인).
- createLabel 한 줄 유지(590자), 줄바꿈은 이스케이프 `\n` 4개만 사용(실제 개행 0), 라벨 내 작은따옴표 0개(따옴표 6개는 전부 인자 구분자). 저장 후 재확인 및 렌더(`<br>` 4개) 확인 완료.
- w-pack 자동 재생성 확인 (`_wpack_/sample/gridView/gridView_E_onyearselect_1.js`, HTTP 200).

### 3. MCP playwright 런타임 검증
- MCP 세션 1(`mcp__playwright`), 포트 62358(샘플 URL + `_wpack_` js 모두 200), `location.href` 선확인 후 측정.
- `window.mf_target1` 생성, 4컬럼(인덱스 1 = offsetWidth 0 으로 숨김) x 4행 렌더, console error 0, validation 10건 좌측 패널 정상 렌더.

| 확인 항목 | 결과 |
|---|---|
| 상단 연 SelectBox 변경 → onyearselect 발생 | PASS |
| rowIndex (표시 Row 0 / 1 / 2 / 3 각각 일치) | PASS |
| colIndex (숨김 Column 포함 인덱스 2·3) | PASS |
| info 객체 전달 (`{"oldValue":2021,"newValue":2027}`) | PASS |
| info.oldValue / newValue = "yyyy" 형식 number | PASS |
| 이전 달 / 다음 달 버튼 → onyearselect 미발생 | PASS |
| 이전 해(이전연월) / 다음 해(다음연월) 버튼 → **연도 표시는 실제로 변경되나** onyearselect 미발생 | PASS |
| 발생 순서 onyearselect -> onmonthselect -> ondateselect (순번 [3][4][5]) | PASS |
| Target 재생성 후 재렌더 정상 / 순번 [1] 로 초기화 | PASS |

MCP 검증: **PASS / 발견 이슈 0건** (엔진 결함 없음)

상세 대조표·raw 측정값은 `03_보강_onyearselect_1.md` 참조.

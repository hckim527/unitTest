# section_P — 02 검증

### section_P_class_1.xml

| 항목 | 결과 |
|---|---|
| XML 구조 (네임스페이스 / head·body / description·validation) | PASS |
| 스크립트 로직 (버튼 onclick ↔ 핸들러 정의 일치, setReturnValue 출력) | PASS |
| UI 구성 규칙 (UT_01 §7 정적 배치 — wf_body_sample 삭제, grp_sample > grp_comp 직접 배치) | PASS |
| MCP 런타임 검증 | **PASS** |

### MCP 실측 (engine R1641, port 49920, mcp__playwright3)

- `document.querySelectorAll("section")` → 2개
  - `[0] id=target1 class="oddClass evenClass"` border `1px solid rgb(255,0,0)` / border-radius `15px`
  - `[1] id=target2 class=""` border `0px none` / border-radius `0px`
- 렌더된 class 에 `w2section` / `w2group` 등 **웹스퀘어 기본 클래스 미추가** 확인
- 동일 페이지 WebSquare 컴포넌트 렌더 id 는 `mf_grp_comp` / `mf_btn_getClassName` → section 은 raw HTML pass-through 로 Scope 접두사 없음 (id 도 `target1`/`target2` 그대로)
- 버튼(`class 확인`) 출력
  ```
  target1 적용 스타일 - border : 1px solid rgb(255, 0, 0) , border-radius : 15px
  렌더된 class 속성 - target1 : 'oddClass evenClass' , target2 : ''
  ```
- console error **0건**
- body_left description / validation 2건 정상 렌더

**MCP 검증: PASS / 발견 이슈 없음**

> 주의: playwright3 브라우저를 다른 에이전트와 공유 중이라 중간에 타 샘플로 navigate 되는 현상이 있었음. 최종 측정은 재-navigate 후 단일 evaluate 로 일괄 수집.

---

### section_P_id_1.xml

| 항목 | 결과 |
|---|---|
| XML 구조 (네임스페이스 / head·body / description·validation) | PASS |
| 스크립트 로직 (버튼 onclick ↔ 핸들러 정의 일치, setReturnValue 출력) | PASS |
| UI 구성 규칙 (UT_01 §7 정적 배치 — wf_body_sample 삭제, grp_sample > grp_comp 직접 배치) | PASS |
| MCP 런타임 검증 | **PASS** |

### MCP 실측 (engine R1641, port 49920)

- `document.querySelectorAll("section")` → 3개
  - `[0] id=target1 class=""` (id 설정)
  - `[1] id=wq_uuid_11 class=""` (id 미설정 → 렌더 시 임의 ID 자동 부여)
  - `[2] id=style class=""` (예약어 id 설정)
- 동일 페이지 WebSquare 컴포넌트 렌더 id = `mf_btn_getId` / `mf_btn_getScriptAccess` / `mf_btn_getReservedId` → section 은 `mf_` Scope 접두사 없이 원본 id 그대로 사용
- 버튼 출력 (5행)
  ```
  id 설정 - 렌더된 id : target1 , '#target1' 조회 개수 : 1
  id 미설정 - 자동 부여된 id : wq_uuid_11
  Scope 적용 여부 - section 의 id : target1 , 비교용 WebSquare button(btn_getId) 의 렌더 id : mf_btn_getId
  스크립트 접근 - id 설정(target1) 조회 결과 : 조회 성공 (section) , id 미설정 Section 을 지정 id(target2) 로 조회한 결과 : 조회 실패
  예약어(style) id 설정 - 렌더된 id : style , 페이지 로드 후 발생한 JS 오류 건수 : 0
  ```
- console error **0건**
- body_left validation 5건 정상 렌더

**MCP 검증: PASS / 발견 이슈 없음**
(예약어 `style` 을 id 로 설정해도 이번 실측에서는 오류가 발생하지 않았다. `@spec` 원문이 "오류가 발생할 수 **있습니다**"(가능성) 이므로 스펙 위반은 아니며, 오류 건수를 출력해 확인 가능하도록 구성.)

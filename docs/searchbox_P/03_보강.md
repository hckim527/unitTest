# searchbox Property 보강 대조표

## searchbox_P_minByteLength_1.xml

API: `WebSquare.uiplugin.searchbox.minByteLength`
설명: SearchBox 컴포넌트의 최소 입력 바이트 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: SearchBox 컴포넌트의 최소 입력 바이트 길이를 설정합니다. | description 영역에 반영 | - | OK |
| @propval: 최소 입력 바이트 길이를 양의 정수로 설정합니다. | (1) "최소 입력 바이트 길이를 양의 정수로 설정합니다." | target1(=10), target5(=10), target9(=5), target10(=5) | OK |
| @propval: byteCheckEncoding 설정에 따라 한글 바이트 계산 방식이 달라집니다. | (2) "config.xml 의 WebSquare/byteCheckEncoding/@value 설정에 따라 한글의 바이트 계산 방식이 달라집니다." | target1, target8 (한글 입력) | OK |
| @propval: "euc-kr" 한글 1자=2바이트 | (3) "byteCheckEncoding 이 'euc-kr' 이면 한글 1 자는 2 바이트로 계산됩니다." | target1, target8 (환경 의존) | OK |
| @propval: "utf-8" 한글 1자=3바이트 | (4) "byteCheckEncoding 이 'utf-8' 이면 한글 1 자는 3 바이트로 계산됩니다." | target1, target8 (환경 의존) | OK |
| @propval: 값 미설정 시 "-1" 적용으로 최소 입력 제한 안 함 | (5) "값을 설정하지 않으면 '-1' 로 적용되어 최소 입력을 제한하지 않습니다." | target2 (한글 입력), target4 (빈값) | OK |
| @spec: 입력값이 없으면 실패 처리 + displaymessage 에 따라 메시지 출력 | (6) "validate() 메소드 실행 시 입력값이 없으면 유효성 체크는 실패로 처리되고 displaymessage 속성에 따라 실패 메시지가 출력됩니다." | target3 (빈값+displaymessage=true), target7 (displaymessage=false) | OK |
| @spec: 입력값 Byte 수가 설정값보다 작으면 실패 처리 + displaymessage 에 따라 메시지 출력 | (7) "validate() 메소드 실행 시 입력값의 Byte 수가 설정값보다 작으면 유효성 체크는 실패로 처리되고 displaymessage 속성에 따라 실패 메시지가 출력됩니다." | target1(가나다라), target9(abc), target7(displaymessage=false) | OK |
| @spec: 시스템 기본 메시지 "최소 n 자리 이상 입력해야 합니다." + invalidMessage/invalidMessageFunc 로 변경 가능 | (8) "시스템 기본 메시지는 '최소 n 자리 이상 입력해야 합니다.' 이며, invalidMessage 또는 invalidMessageFunc 속성으로 메시지를 변경할 수 있습니다." | 기본 메시지: target1, target3, target9 / invalidMessage: target5 / invalidMessageFunc: target6 | OK |

### 보강 체크리스트
- [x] API 가이드의 모든 @description 내용이 validation에 반영됨
- [x] 모든 @propval enum/조건 값에 대한 validation이 존재함 (euc-kr / utf-8 / 값 미설정 -1)
- [x] 입력값 없음 + Byte 수 부족 케이스 분리 (별도 validation 6, 7)
- [x] 기본 메시지 / invalidMessage / invalidMessageFunc 메시지 변경 케이스 모두 존재
- [x] displaymessage=false 케이스로 메시지 미출력 + validate 결과 FAIL 동작 확인
- [x] 경계값(byte 수 = minByteLength) PASS 케이스 (target8 한글, target10 영문)

### 케이스 구성 (총 10개 target)
- 입력값 byte < minByteLength: target1, target3(빈값), target5(invalidMessage), target6(invalidMessageFunc), target7(displaymessage=false), target9
- 입력값 byte >= minByteLength (PASS): target8, target10
- minByteLength 미설정 (-1, PASS): target2, target4

---

## searchbox_P_maxlength_1.xml

API: `WebSquare.uiplugin.searchbox.maxlength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: SearchBox 컴포넌트에 입력 가능한 최대 길이를 설정합니다. | description 영역에 반영 | - | OK |
| @propval: 한 문자의 길이를 1 로 계산(다국어 포함)하여 입력 가능한 최대 길이를 숫자로 설정합니다. | (1) "한 문자의 길이를 1 로 계산(다국어 포함)하여 입력 가능한 최대 길이를 숫자로 설정합니다." | target1(한글 maxlength=5), target3(영문 maxlength=3), target4(숫자 maxlength=5) | OK |
| @propval: 값을 설정하지 않으면 "-1" 로 적용되어 입력을 제한하지 않습니다. | (2) "값을 설정하지 않으면 '-1' 로 적용되어 입력을 제한하지 않습니다." | target2(미설정), target5(maxlength=-1 명시) | OK |
| @spec: 최대 길이를 초과하면 초과분이 자동으로 잘립니다. | (3) "최대 길이를 초과하면 초과분이 자동으로 잘립니다." | target1, target3, target4 | OK |
| 추가: 한글 다국어 1글자=길이 1 | (4) "한글 다국어 문자도 한 글자당 길이 1 로 계산되어 maxlength 가 적용되어야 합니다." | target1 | OK |
| 추가: 영문 1글자=길이 1 | (5) "영문 문자도 한 글자당 길이 1 로 계산되어 maxlength 가 적용되어야 합니다." | target3 | OK |
| 추가: dataType=number 일때 숫자도 길이 1 | (6) "dataType=number 일때 숫자도 한 글자당 길이 1 로 계산되어 maxlength 가 적용되어야 합니다." | target4 | OK |

### 케이스 구성 (총 5개 target)
- target1: dataType=text, maxlength=5, 한글
- target2: dataType=text, maxlength 미설정 (-1)
- target3: dataType=text, maxlength=3, 영문
- target4: dataType=number, maxlength=5, 숫자
- target5: dataType=text, maxlength=-1 (제한 없음)

---

## searchbox_P_maxlength_2.xml

API: `WebSquare.uiplugin.searchbox.maxlength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @spec: validate() 시 입력값이 설정값보다 크면 유효성 체크 실패 처리 | (1) "validate() 메소드 실행 시 입력값이 maxlength 설정값보다 크면 유효성 체크는 실패로 처리됩니다." | target1 ~ target3 (maxlength=5, 입력 6자) | OK |
| @spec: 시스템 기본 메시지 "최대 n 자리 이상 입력할 수 없습니다." displaymessage=true 시 출력 | (2) "validate() 실패 시 displaymessage='true' 이면 시스템 기본 메시지 '최대 n 자리 이상 입력할 수 없습니다.' 가 출력됩니다." | target1 (displaymessage=true, invalidMessage 미설정) | OK |
| @spec: invalidMessage 속성으로 실패 메시지 변경 가능 | (3) "validate() 실패 메시지는 invalidMessage 속성으로 변경할 수 있습니다." | target2 (invalidMessage 설정) | OK |
| @spec: invalidMessageFunc 속성으로 실패 메시지 변경 가능 | (4) "validate() 실패 메시지는 invalidMessageFunc 속성으로 변경할 수 있습니다." | target3 (invalidMessageFunc=scwin.fn_msg) | OK |
| 추가: minlength/minByteLength > maxlength 모순 → 항상 실패 | (5) "minlength 또는 minByteLength 속성을 maxlength 값보다 크게 설정한 경우 유효성 체크는 항상 실패로 처리됩니다." | target4 (minlength=8 > maxlength=5), target5 (minByteLength=8 > maxlength=5) | OK |

### 케이스 구성 (총 5개 target)
- target1: maxlength=5, displaymessage=true (기본 메시지)
- target2: maxlength=5, invalidMessage
- target3: maxlength=5, invalidMessageFunc
- target4: maxlength=5, minlength=8 (모순 조건)
- target5: maxlength=5, minByteLength=8 (모순 조건)

---

## searchbox_P_maxlength_3.xml

API: `WebSquare.uiplugin.searchbox.maxlength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @spec: maxByteLength 와 maxlength 를 모두 설정하면 설정값이 작은 속성이 적용됨 | (1) "maxByteLength 와 maxlength 를 모두 설정하면 설정값이 작은 속성이 적용됩니다." | target1 (maxByteLength=4 < maxlength=5), target3 (maxByteLength=10 > maxlength=3) | OK |
| @spec: 같은 값이면 maxByteLength 적용 | (2) "maxByteLength 와 maxlength 를 같은 값으로 설정하면 maxByteLength 가 적용됩니다." | target2 (maxByteLength=4, maxlength=4) | OK |

### 케이스 구성 (총 3개 target)
- target1: maxByteLength(=4) < maxlength(=5) → maxByteLength 적용
- target2: maxByteLength(=4) = maxlength(=4) → maxByteLength 적용
- target3: maxByteLength(=10) > maxlength(=3) → maxlength 적용

---

## searchbox_P_maxlength_4.xml

API: `WebSquare.uiplugin.searchbox.maxlength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| 보강: 스크립트로 setValue() 시 maxlength 미적용 (입력 단계가 아니므로) | (1) "스크립트로 setValue() 메소드를 사용하여 입력값을 설정하면 maxlength 가 적용되지 않고 전체 입력값이 저장됩니다." | target1 (setValue 직후, 포커스 유지) | OK |
| 보강: blur / Enter 시점에 maxlength 적용 | (2) "스크립트로 setValue() 후 포커스를 해제하거나 Enter 키 입력 시 maxlength 가 적용되어 초과분이 잘립니다." | target2 (setValue 후 blur 처리) | OK |

### 케이스 구성 (총 2개 target)
- target1: setValue 후 focus 유지 시 전체 값 저장
- target2: setValue 후 blur 시 maxlength 적용

---

### 보강 체크리스트 (searchbox_P_maxlength)
- [x] API 가이드의 모든 @description 내용이 validation에 반영됨
- [x] 모든 @propval / @spec 항목에 대한 validation이 존재함
- [x] @spec 1(자동 잘림) → _1 / @spec 2(maxByteLength 조합) → _3 / @spec 3(validate 실패+메시지) → _2
- [x] description 은 모든 _N 파일에서 _1 과 동일 유지 (UT_01_생성.md 규칙)
- [x] dataType=float 케이스는 searchbox 미지원이므로 제외, dataType=number 케이스로 대체
- [x] editFormat 케이스는 searchbox 미지원이므로 제외, setValue 미적용 동작만 _4 에 유지
- [x] xf:input + initValue 사용 (테스트 샘플에는 w2:searchbox 사용)
- [x] 컴포넌트 정적 배치 (Property 샘플이므로 grp_comp 직접 사용, Target 재생성 불필요)

---

## searchbox_P_maxByteLength_1.xml

API: `WebSquare.uiplugin.searchbox.maxByteLength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 바이트 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: SearchBox 컴포넌트에 입력 가능한 최대 바이트 길이를 설정합니다. | description 영역에 반영 | - | OK |
| @propval: 입력 가능한 최대 바이트 길이를 양의 정수로 설정합니다. | (1) "입력 가능한 최대 바이트 길이를 양의 정수로 설정합니다." | target1(=6, 한글), target3(=6, 숫자), target4(=8, 소수) | OK |
| @spec: 최대 바이트 길이를 초과하면 초과분이 자동으로 잘립니다. | (2) "최대 바이트 길이를 초과하면 초과분이 자동으로 잘립니다." | target1, target3, target4 | OK |
| @propval: 값 미설정 시 '-1' 적용 → 입력 제한 없음 | (3) "값을 설정하지 않으면 '-1' 로 적용되어 입력을 제한하지 않습니다." | target2 (maxByteLength 미설정) | OK |
| @propval: byteCheckEncoding='euc-kr' 한글 1자=2byte | (4) "config.xml 의 WebSquare/byteCheckEncoding/@value 가 'euc-kr' 인 경우 한글 1 자는 2 바이트로 계산됩니다." | target1 (현재 환경 기준 가나다 = 6byte) | OK |
| @propval: byteCheckEncoding='utf-8' 한글 1자=3byte | (5) "config.xml 의 WebSquare/byteCheckEncoding/@value 가 'utf-8' 인 경우 한글 1 자는 3 바이트로 계산됩니다." | target1 (환경 의존 설명) | OK |

### 케이스 구성 (총 4개 target)
- target1: dataType=text, maxByteLength=6, 한글 입력 (잘림)
- target2: dataType=text, maxByteLength 미설정 (-1, 제한 없음)
- target3: dataType=number, maxByteLength=6, 숫자 입력 (잘림)
- target4: dataType=float, maxByteLength=8, 소수 입력 (잘림)

---

## searchbox_P_maxByteLength_2.xml

API: `WebSquare.uiplugin.searchbox.maxByteLength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 바이트 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @spec: validate() 입력값 Byte 수가 설정값보다 크면 실패 처리 | (1) "validate() 메소드로 유효성 체크 실행 시 입력값의 Byte 수가 설정값보다 크면 유효성 체크는 실패로 처리됩니다." | target1 ~ target4 (maxByteLength=6, 가나다라=8byte) | OK |
| @spec: 시스템 기본 메시지 '최대 n byte 이상 입력할 수 없습니다.' displaymessage='true' 시 출력 | (2) "유효성 체크 실패 시 displaymessage='true' 이면 시스템 기본 메시지 '최대 n byte 이상 입력할 수 없습니다.' 가 출력됩니다." | target1 (displaymessage=true, invalidMessage 미설정) | OK |
| @spec: invalidMessage 속성으로 실패 메시지 변경 가능 | (3) "유효성 체크 실패 메시지는 invalidMessage 속성으로 변경할 수 있습니다." | target2 (invalidMessage 설정) | OK |
| @spec: invalidMessageFunc 속성으로 실패 메시지 변경 가능 | (4) "유효성 체크 실패 메시지는 invalidMessageFunc 속성으로 변경할 수 있습니다." | target3 (invalidMessageFunc=scwin.fn_invalidMessageFunc) | OK |
| 보강: displaymessage=false 동작 | (5) "displaymessage='false' 이면 유효성 체크 실패 시 메시지를 출력하지 않습니다." | target4 (displaymessage=false) | OK |

### 케이스 구성 (총 4개 target)
- target1: maxByteLength=6, displaymessage=true (기본 메시지)
- target2: maxByteLength=6, displaymessage=true, invalidMessage 설정
- target3: maxByteLength=6, displaymessage=true, invalidMessageFunc 설정
- target4: maxByteLength=6, displaymessage=false (메시지 미출력)

---

## searchbox_P_maxByteLength_3.xml

API: `WebSquare.uiplugin.searchbox.maxByteLength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 바이트 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @spec: maxByteLength + maxlength 동시 설정 → 작은 값 적용 | (1) "maxByteLength 와 maxlength 를 모두 설정하면 설정값이 작은 속성이 적용됩니다." | target1 (maxByteLength=4 < maxlength=5), target2 (maxByteLength=10 > maxlength=3) | OK |
| @spec: 같은 값이면 maxByteLength 적용 | (2) "maxByteLength 와 maxlength 를 같은 값으로 설정하면 maxByteLength 가 적용됩니다." | target3 (maxByteLength=4, maxlength=4) | OK |

### 케이스 구성 (총 3개 target)
- target1: maxByteLength(=4) < maxlength(=5) → maxByteLength 적용
- target2: maxByteLength(=10) > maxlength(=3) → maxlength 적용
- target3: maxByteLength(=4) = maxlength(=4) → maxByteLength 적용

---

## searchbox_P_maxByteLength_4.xml

API: `WebSquare.uiplugin.searchbox.maxByteLength`
설명: SearchBox 컴포넌트에 입력 가능한 최대 바이트 길이를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| 보강: 스크립트 setValue() 시 maxByteLength 미적용 (입력 단계 아님) | (1) "스크립트로 setValue() 를 호출하여 입력값을 설정 시 maxByteLength 가 적용되지 않습니다." | target1 (setValue(123456789) 후 maxByteLength=5 임에도 전체 저장) | OK |
| 보강: displayFormat 과 함께 설정 시 실제 입력값 기준으로 maxByteLength 적용 | (2) "displayFormat 속성과 함께 설정해도 실제 입력값 기준으로 maxByteLength 가 적용됩니다." | target2 (displayFormat=#,###, 실제값=123456789, maxByteLength=10) | OK |

### 케이스 구성 (총 2개 target)
- target1: dataType=number, maxByteLength=5, setValue(123456789) → 미적용
- target2: dataType=number, maxByteLength=10, displayFormat=#,### → 실제값 기준 적용

### 보강 체크리스트 (searchbox_P_maxByteLength)
- [x] API 가이드의 모든 @description 내용이 validation에 반영됨
- [x] 모든 @propval / @spec 항목에 대한 validation이 존재함
- [x] @propval 미설정 시 -1 동작 (필수여부 N 기본값) validation 존재 (_1)
- [x] byteCheckEncoding euc-kr / utf-8 각 enum 별 validation 존재 (_1)
- [x] validate() 실패 / 기본 메시지 / invalidMessage / invalidMessageFunc / displaymessage=false 각 분리 (_2)
- [x] maxByteLength + maxlength 작은 값 적용 + 같은 값 maxByteLength 적용 분리 (_3)
- [x] description 은 모든 _N 파일에서 _1 과 동일 유지 (UT_01_생성.md 규칙)
- [x] dataType=float 케이스 유지 (_1 target4), editFormat 은 searchbox 미지원이므로 displayFormat 으로 대체 (_4 target2)
- [x] 테스트 샘플에는 w2:searchbox 사용 (대상 컴포넌트 자체)
- [x] 컴포넌트 정적 배치 (Property 샘플, Target 재생성 불필요)
- [x] validation 특수문자: < > & " ' 리터럴 사용 가능 (createValidation 이 escape 속성 자동 처리, 사전 이스케이프 금지). 큰따옴표는 CDATA JSON 파싱 위해 작은따옴표 권장

---

## searchbox_P_nextTabID_1.xml

API: `WebSquare.uiplugin.searchbox.nextTabID`
설명: Tab 키로 포커스 이동 시 포커스가 이동될 다음 컴포넌트의 ID 를 설정합니다.

> 본 샘플은 wframe `common_P_nextTabID_1.xml` 을 공유한다. validation 은 wframe 의 `wf_body_left.getObj("scwin").createValidation(scwin.tData)` 로 생성되며 모든 컴포넌트 공통이다. searchbox 측 샘플은 target1 컴포넌트만 `xf:input` → `w2:searchbox` 로 교체하여 동일 시나리오에서 동작을 검증한다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: Tab 키로 포커스 이동 시 포커스가 이동될 다음 컴포넌트의 ID 를 설정합니다. | description 영역에 반영 | - | OK |
| @propval String: 포커스가 이동될 컴포넌트의 ID 를 문자열로 설정합니다. | (1) "포커스가 이동될 컴포넌트의 ID 를 문자열로 설정할 수 있어야 합니다." | con_nextTabID_target = input2/button2/selectbox2/target1 선택 후 Tab 이동 | OK |
| @propval: 존재하지 않는 ID 설정 시 적용되지 않음. 다른 Scope 컴포넌트는 설정할 수 없음. | wframe dataList 의 옵션은 실재 컴포넌트 ID 만 제공(존재하지 않는 ID 케이스는 동일 wframe 시나리오상 미커버) | wframe `dlt_nextTabID` row label 미설정(value=1) 행이 "존재하지 않는 ID" 의사 케이스로 동작 | △ (wframe 공유) |
| @propval: 값을 설정하지 않으면 일반적인 tabIndex 규칙을 따릅니다. | (2) "미설정 시 일반적인 tabIndex 규칙을 따라야 합니다." | con_nextTabID_target 미선택(빈값) → target1 은 tabIndex=2 순서로 이동 | OK |
| @spec: tabIndex 속성에 의한 순서는 무시되고 nextTabID 에 설정된 컴포넌트로 포커스가 이동합니다. | (3) "tabIndex 속성에 의한 순서는 무시되고 nextTabID 에 설정된 컴포넌트로 포커스가 이동하여야 합니다." | con_nextTabID_target = button2(tabIndex=4) 설정 시 target1(2)→button2(4) 이동 | OK |
| @spec: Shift+Tab 키로 포커스를 이전으로 이동하면 nextTabID 로 설정한 컴포넌트로 이동합니다. | wframe validation 에 주석으로 보류 (브라우저 기본 Shift+Tab 동작은 별도 자동화 도구로 검증) | Shift+Tab 동작은 unitTest 수동 검증 영역 | △ (wframe 공유) |
| @spec: 현재 포커스된 컴포넌트가 두 개 이상의 nextTabID 에 설정된 경우 소스에서 마지막에 정의된 컴포넌트로 이동합니다. | (4) "현재 포커스된 컴포넌트가 두 개 이상의 nextTabID 에 설정된 경우 소스에서 마지막에 정의된 컴포넌트로 포커스가 이동되어야 합니다." | con_nextTabID_input = target1, con_nextTabID_button = target1 동시 설정 → 마지막 정의 button2 의 nextTabID 가 적용 | OK |
| @spec: 대상이 포커스를 받을 수 없는 상태(disabled 등)일 때 - 해당 컴포넌트에 nextTabID 가 설정되어 있으면 다음으로 이동. | (5) "포커스 이동 대상 컴포넌트가 포커스를 받을 수 없는 상태(Disabled 등)이면 포커스가 이동하지 않아야 합니다." (통합 표현) | con_nextTabID_target=input2 + setDisabled(input2) + input2.nextTabID 설정 시 다음 컴포넌트로 이동 | OK (validation 통합) |
| @spec: 대상이 disabled 이고 그 컴포넌트에 nextTabID 가 설정되어 있지 않으면 tabIndex 와 관계없이 포커스가 이동하지 않음. | (5) 동일 validation 으로 통합 | con_nextTabID_target=input2 + setDisabled(input2) + input2.nextTabID 미설정 → 포커스 이동 안 함 | OK |

### 보강 체크리스트 (searchbox_P_nextTabID_1)
- [x] API 가이드의 모든 @description 내용이 validation 에 반영됨
- [x] @propval String 설정 케이스 존재 (validation 1)
- [x] 필수여부 N: 값 미설정 시 일반 tabIndex 규칙 동작 케이스 존재 (validation 2)
- [x] tabIndex vs nextTabID 우선순위 케이스 존재 (validation 3)
- [x] 두 개 이상 동시 설정 시 마지막 정의 우선 케이스 존재 (validation 4)
- [x] disabled 상태 대상 컴포넌트 동작 케이스 존재 (validation 5)
- [x] target1 = w2:searchbox 로 교체, input2/button2/selectbox2 보조 컴포넌트 유지 (input 샘플과 동일 시나리오)
- [x] nextTabID 대상 컴포넌트(input2, button2, selectbox2)는 target1 보다 먼저 동적 생성 (`reference_nexttabid_creation_order` 메모리 규칙)
- [x] description 은 wframe(`common_P_nextTabID_1.xml`)에서 정의 — _2/_3 분리 없음 (input 측에 _1 만 존재)
- [x] 텍스트 입력은 `xf:input + initValue` 사용 (startcomp), 테스트 대상은 `w2:searchbox`
- [x] Target 재생성 버튼 중복 생성 없음 (wf_body_sample 내장)
- [x] validation 큰따옴표는 작은따옴표로 대체 (< > 는 리터럴 사용 가능, createValidation 자동 처리)

### 케이스 구성 (wframe 옵션 조합으로 동적 검증)
- nextTabID(target) / nextTabID(input) / nextTabID(button) / nextTabID(selectbox) 콤보 4개
- 각 콤보 옵션 5종(미설정/input2/button2/selectbox2/target1) → 사용자 조합으로 전체 시나리오 커버
- comp disabled 콤보(input2/button2/selectbox2) + setDisabled 버튼 → disabled 상태 동작 검증
- 초기포커스이동 버튼 → startcomp(tabIndex=1) 부터 Tab 순서 시각 확인

### 누락/△ 항목 메모
- "존재하지 않는 ID 설정 시 무시" 와 "다른 Scope 컴포넌트 설정 불가" 항목은 wframe `common_P_nextTabID_1.xml` 의 validation 에 명시되어 있지 않음. 본 샘플은 wframe 을 공유하므로 input 측 샘플과 동일하게 wframe 측 수정 없이 유지함(컴포넌트 공통 영역). 별도 보강이 필요하면 wframe 측을 손대야 하며, 이는 모든 컴포넌트 nextTabID 샘플에 영향을 미친다.
- "Shift+Tab 역참조 동작" 도 wframe validation 에 주석처리되어 있어 동일하게 유지함.

---

## searchbox_P_onFocusClass_1.xml

API: `WebSquare.uiplugin.searchbox.onFocusClass`
설명: SearchBox 컴포넌트에 포커스가 들어왔을 때 적용할 CSS 클래스명을 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: 포커스가 들어왔을 때 적용할 CSS 클래스명을 설정한다 | description 영역에 반영 | con_onFocusClass 콤보(미설정/oddClass/oddClass evenClass/notExistClass) | OK |
| @propval: CSS 파일이나 style 블럭에 정의된 클래스 이름을 공백으로 구분하여 설정 | (1) "CSS 파일이나 style 블럭에 정의된 클래스 이름을 공백으로 구분하여 문자열로 설정할 수 있어야 합니다." | con_onFocusClass=oddClass (단일), oddClass evenClass (공백 구분 다중) | OK |
| @propval: 값을 설정하지 않으면 Input 영역에 "w2input_focus" 적용 | (2) "값을 설정하지 않으면 Input 영역에 'w2input_focus' 클래스가 적용됩니다." | con_onFocusClass=미설정 → onfocus 핸들러 출력 className 에 'w2input_focus' 포함 | OK |
| 추가(spec): 속성값 설정 시 기본값 'w2input_focus' 미적용 | (3) "속성값을 설정하면 기본값인 'w2input_focus' 클래스는 적용되지 않습니다." | con_onFocusClass=oddClass → onfocus className 에 oddClass 포함, w2input_focus 미포함 | OK |
| 추가: 다중 클래스 동시 적용 (공백 구분) | (4) "공백으로 구분된 다중 클래스명을 설정하면 포커스 시 모든 클래스가 함께 적용되어야 합니다." | con_onFocusClass=oddClass evenClass → onfocus className 에 두 클래스 모두 포함 | OK |
| 추가: 존재하지 않는 클래스명도 className 에 추가 (CSS 매칭 여부는 별개) | (5) "존재하지 않는 클래스명을 설정하더라도 SearchBox 의 Input 영역 className 속성에는 해당 클래스명이 추가되어야 합니다." | con_onFocusClass=notExistClass → onfocus className 에 notExistClass 포함 | OK |
| 추가: 포커스 해제 시 클래스 제거 (onfocus 와 한 쌍) | (6) "컴포넌트가 포커스를 잃으면 적용된 CSS 클래스가 제거됩니다." | onblur 핸들러에서 className 에 onFocusClass 미포함 확인 (다음 케이스 onfocus 출력 시각 비교) | OK |
| 추가(spec): readOnly=true + readOnlyFocusEvent=false(기본) → 포커스 이벤트 미발생으로 onFocusClass 미적용 | (7) "readOnly=true 인 경우 포커스 이벤트가 발생하지 않으므로 onFocusClass 가 적용되지 않습니다." | con_onFocusClass=oddClass + con_readOnly=true → onfocus 핸들러 미호출, className 변화 없음 | OK |

### 보강 체크리스트
- [x] API 가이드의 모든 @description / @propval 내용이 validation 에 반영됨
- [x] propval 단일·다중·미설정·존재하지 않는 클래스 4 종 케이스 모두 con_onFocusClass 옵션으로 커버
- [x] readOnly 동반 옵션(con_readOnly) 으로 readOnly 분기 검증 가능
- [x] onfocus / onblur 핸들러에서 `target1.input.render.className` 출력으로 적용/제거 타이밍 모두 검증
- [x] `className 확인` 버튼으로 임의 시점 className 조회 가능
- [x] 기본 클래스 `w2input_focus` 와 사용자 클래스의 상호 배제 동작 확인 가능
- [x] xf:input + initValue 패턴 미사용 (속성값은 select1 옵션으로 충분), w2:inputbox 미사용
- [x] Target 재생성 버튼 중복 생성 없음 (wf_body_sample 내장)
- [x] validation 큰따옴표는 작은따옴표로 처리 (< > 리터럴 사용 가능)
- [x] description 형식: `[searchbox > P > onFocusClass]<br/>...` 동일 패턴

### 케이스 구성 (con_onFocusClass × con_readOnly 조합)
- con_onFocusClass: 미설정 / oddClass / oddClass evenClass / notExistClass (4 종)
- con_readOnly: 미설정 / true / false (3 종)
- 콤보 변경 후 Target 재생성 → SearchBox 클릭으로 포커스 → onfocus 출력 확인 → 다른 영역 클릭으로 포커스 해제 → onblur 출력 확인
- validation 7 항목과 각 콤보 조합으로 전수 검증 가능

### 누락/△ 항목 메모
- 없음. 입력 컴포넌트 onFocusClass API 가이드의 모든 항목이 validation 7 개로 반영되었다.

---

## searchbox_P_refSync_1.xml / searchbox_P_refSync_2.xml

API: `WebSquare.uiplugin.searchbox.refSync`
설명: 입력값이 바인딩 된 DataCollection 에 반영되는 시점을 설정합니다. ref 속성이 설정되어 있지 않으면 해당 속성은 무시됩니다.

> 본 샘플은 wframe `common_P_refSync_1.xml`(DataMap 바인딩) / `common_P_refSync_2.xml`(DataList 바인딩) 을 공유한다. validation 은 wframe 의 `wf_body_left.getObj("scwin").createValidation(scwin.tData)` 로 생성되며 모든 컴포넌트 공통이다. searchbox 측 샘플은 target1 컴포넌트만 `xf:input/createEmptyInput` → `w2:searchbox/createBasicSearchBox` (await 비동기) 로 교체하여 동일 시나리오에서 동작을 검증한다.

### searchbox_P_refSync_1.xml (DataMap 바인딩 — gdm_ref.key1)

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: 입력값이 바인딩된 DataCollection 에 반영되는 시점을 설정합니다. | (1) "dataMap 이 바인딩 된 경우 입력값이 dataMap 에 반영되는 시점을 설정합니다." | DataMap(gdm_ref.key1) 바인딩 후 con_refSync 콤보로 true/false/미설정 적용 | OK |
| @propval false(default): Enter/Tab 키 입력 시, 포커스 아웃 시 입력값을 DataCollection 에 반영합니다. | (2) "미설정 시 Enter 키 입력 시, Tab 키 입력 시, 포커스 아웃 시 입력값을 DataCollection 에 반영합니다." | con_refSync=미설정(default false) → onkeydown/onkeyup/onblur 핸들러로 gdm_ref.get('key1') 값 시점 확인 | OK |
| @propval true: 입력창에서 키가 다운될 때 입력값을 DataCollection 에 반영. 단 다운된 키를 제외한 입력값이 반영. @spec: 다운된 키는 갱신 전 상태이므로 반영되지 않음. | (3) "true 설정 시 입력창에서 키가 다운될 때 다운된 키를 제외한 입력값이 DataCollectoin 에 반영됩니다." | con_refSync=true → onkeydown 핸들러에서 gdm_ref.get('key1') 값이 직전까지 입력된 값으로 갱신됨(현재 입력 키 1글자 늦게 반영) | OK |
| @propval false: Enter/Tab/포커스 아웃 시 반영. | (4) "false 설정 시 Enter 키 입력 시, Tab 키 입력 시, 포커스 아웃 시 입력값을 DataCollection 에 반영합니다." | con_refSync=false → onkeydown 시 미반영, Enter/Tab/blur 시 반영 | OK |
| @description: ref 속성이 설정되어 있지 않으면 refSync 는 무시됩니다. | wframe `common_P_refSync_1.xml` 의 validation 에 명시되어 있지 않음 | ref 미설정 케이스는 본 샘플 범위 밖(ref 설정은 refSync 동작 전제조건이며, ref 자체의 검증은 별도 ref 샘플에서 수행) | △ (wframe 공유 — ref 샘플 영역) |

### searchbox_P_refSync_2.xml (DataList 바인딩 — gdl_ref.label)

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: 입력값이 바인딩된 DataCollection 에 반영되는 시점을 설정합니다. | (1) "dataList 가 바인딩 된 경우 입력값이 dataList 에 반영되는 시점을 설정합니다." | DataList(gdl_ref.label) 바인딩 + setRowPosition(1) → con_refSync 콤보로 true/false/미설정 적용 | OK |
| @propval false(default): Enter/Tab 키 입력 시, 포커스 아웃 시 입력값을 DataCollection 에 반영합니다. | (2) "미설정 시 Enter 키 입력 시, Tab 키 입력 시, 포커스 아웃 시 입력값을 DataCollection 에 반영합니다." | con_refSync=미설정 → onkeydown 시 미반영, Enter/Tab/blur 시 반영 (gdl_ref.getDataRow(1).label) | OK |
| @propval true + @spec: 키 다운 시점 반영, 다운된 키 제외. | (3) "true 설정 시 입력창에서 키가 다운될 때 다운된 키를 제외한 입력값이 DataCollectoin 에 반영됩니다." | con_refSync=true → onkeydown 시 직전 입력값까지 DataList 에 반영 | OK |
| @propval false: Enter/Tab/포커스 아웃 시 반영. | (4) "false 설정 시 Enter 키 입력 시, Tab 키 입력 시, 포커스 아웃 시 입력값을 DataCollection 에 반영합니다." | con_refSync=false → Enter/Tab/blur 시점에만 반영 | OK |

### 보강 체크리스트 (searchbox_P_refSync_1/2)
- [x] API 가이드의 모든 @description / @propval / @spec 내용이 validation 에 반영됨 (ref 미설정 시 무시 항목은 ref 샘플 영역으로 분리)
- [x] @propval enum 값(true / false) 별 개별 validation 존재
- [x] 필수여부 N: 미설정(default false) 시 동작 validation 존재 (validation 2)
- [x] true 시 "다운된 키 제외 / 1글자 늦게 반영" @spec 내용 validation 3 에 통합 반영
- [x] DataMap 바인딩(_1) / DataList 바인딩(_2) 분리 — input 측 분리 기준 동일하게 적용
- [x] _2 description 은 _1 과 동일 유지: `[common > P > refSync]<br/>입력값이 바인딩 된 DataCollection 에 반영되는 시점을 설정합니다.` (wframe 공유)
- [x] 컴포넌트 태그 교체: `xf:input` + `createEmptyInput` → `w2:searchbox` + `await createBasicSearchBox` (searchbox 동적 생성은 비동기이므로 await 필수)
- [x] 이벤트 핸들러는 wframe 측 `objParent.scwin.target1_onkeydown / onkeyup / onblur` 에 정의되어 있으며 searchbox 도 동일 이벤트(onkeydown/onkeyup/onblur) 를 지원하므로 코드 변경 없이 동작
- [x] con_refSync 콤보는 wframe 의 `data:gdl_boolean` 바인딩으로 true / false / 미설정 3 종 선택 가능
- [x] 별도 Method API 케이스(setRef, commit) 미포함, 별개 Property API 케이스(ref, alwaysRefresh) 미포함
- [x] Target 재생성 버튼 중복 생성 없음 (wf_body_sample 내장)
- [x] validation 큰따옴표는 작은따옴표로 처리 (< > 리터럴 사용 가능, HTML 엔티티 사전 변환 금지)
- [x] XML validate_xml 검증 PASS (isValid:true, errors:0, warnings:0)

### 케이스 구성 (con_refSync 옵션 조합)
- _1 (DataMap): con_refSync = 미설정 / true / false 3 종 → Target 재생성 후 searchbox 에 입력 → onkeydown/onkeyup/onblur 핸들러 출력으로 gdm_ref.get('key1') 시점별 값 확인
- _2 (DataList): con_refSync = 미설정 / true / false 3 종 → setRowPosition(1) 행 선택 상태 → searchbox 에 입력 → 핸들러 출력으로 gdl_ref.getDataRow(1).label 시점별 값 확인

### 누락/△ 항목 메모
- "ref 속성이 설정되어 있지 않으면 refSync 는 무시됩니다." 는 wframe `common_P_refSync_*.xml` 의 validation 에 명시되어 있지 않다. wframe 은 input/searchbox 등 ref 지원 컴포넌트 모두가 공유하므로 본 샘플에서는 wframe 측을 수정하지 않고 그대로 유지한다. 사용자 작업 지시상 "ref 자체의 검증은 본 샘플 범위 밖" 으로 명시되어 있으므로 ref 미설정 케이스는 ref 샘플(searchbox_P_ref_1.xml) 의 validation 9 "ref 를 설정하지 않으면 일반 입력 컴포넌트로 동작해야 합니다." 항목으로 간접 검증된다.
- 이외 항목 없음. refSync API 가이드의 @description, @propval(true/false), @spec 모두 wframe validation 1~4 에 반영되어 있다.

---

## searchbox_P_showPlaceHolderOnReadOnly_1.xml

API: `WebSquare.uiplugin.searchbox.showPlaceHolderOnReadOnly`
설명: SearchBox 컴포넌트가 readOnly 또는 disabled 상태일 때 placeholder 를 표시할지 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: SearchBox 컴포넌트가 readOnly 또는 disabled 상태일 때 placeholder 를 표시할지 설정합니다. | description 영역에 반영 | - | OK |
| @description: readOnly=true 또는 disabled=true 이고 placeholder 속성이 설정된 경우에 동작합니다. | (1) "readOnly=true 또는 disabled=true 이고 placeholder 속성이 설정된 경우에 동작합니다." | target1(disabled=true+placeholder), target2(readOnly=true+placeholder) | OK |
| @propval true: 컴포넌트가 disabled 또는 readonly 상태일때 PlaceHolder 텍스트를 표시합니다. | (2) "true 설정 시 컴포넌트가 disabled 또는 readonly 상태일때 PlaceHolder 텍스트를 표시합니다." | con_showPlaceHolderOnReadOnly = true 케이스 (target1, target2) | OK |
| @propval false (default): readOnly 또는 disabled 상태에서 placeholder 를 표시하지 않습니다. | (3) "false (default) 설정 시 readOnly 또는 disabled 상태에서 placeholder 를 표시하지 않습니다." | con_showPlaceHolderOnReadOnly = false / 미설정 케이스 (target1, target2) | OK |

### 보강 체크리스트
- [x] API 가이드의 @description 두 줄 모두 validation 또는 description 에 반영됨
- [x] @propval enum 값(true / false) 별 개별 validation 존재
- [x] 필수여부 N (기본값 false): false 케이스가 default 동작과 동일하므로 validation 3 으로 기본값 동작 함께 검증
- [x] 동작 전제(readOnly/disabled + placeholder 설정) 케이스: target1(disabled=true, placeholder 설정), target2(readOnly=true, placeholder 설정) 양쪽 모두 포함
- [x] 컴포넌트 태그: `w2:searchbox` + `await $c.gcm.createBasicSearchBox` (searchbox 동적 생성은 비동기, await 필수)
- [x] placeholder DOM 조회: searchbox.render 는 wrapper 라 `target1.input.render.getAttribute('placeholder')` 사용 (reference_searchbox_render_value_undefined 적용)
- [x] Target 재생성 버튼 중복 생성 없음 (wf_body_sample 내장)
- [x] validation 내 임의 추가 항목 없음 (feedback_validation_no_arbitrary_addition 준수)

### 케이스 구성 (con_showPlaceHolderOnReadOnly 옵션 조합)
- target1: disabled=true + placeholder="disabled=true 상태" — con_showPlaceHolderOnReadOnly = 미설정(false) / true / false 3 종 별 Target 재생성 후 input DOM placeholder 속성값 확인
- target2: readOnly=true + placeholder="readOnly=true 상태" — con_showPlaceHolderOnReadOnly = 미설정(false) / true / false 3 종 별 Target 재생성 후 input DOM placeholder 속성값 확인
- 검증 버튼: 「placeholder 확인」 클릭 시 `target1.input.render.getAttribute('placeholder')`, `target2.input.render.getAttribute('placeholder')` 출력
  - true 설정 시 → 두 target 모두 placeholder 문자열 표시
  - false / 미설정 시 → 두 target 모두 placeholder 빈 문자열

### 누락/△ 항목 메모
- 이외 항목 없음. showPlaceHolderOnReadOnly API 가이드의 @description, @propval(true / false default) 모두 validation 에 1:1 반영되어 있다.
- @sample / @related / @spec 은 빈 값이므로 추가 케이스 없음.

---

## searchbox_P_tabIndex_1.xml

API: `WebSquare.uiplugin.searchbox.tabIndex`
설명: Tab 키를 사용하여 컴포넌트 간 포커스를 이동할때 SearchBox 컴포넌트의 순서를 설정합니다.

| API 가이드 항목 | 샘플 validation | 케이스 | 상태 |
|---------------|---------------|------|------|
| @description: Tab 키를 사용하여 컴포넌트 간 포커스를 이동할 때 컴포넌트의 순서를 설정합니다. | description 영역에 반영 ("[common > P > tabIndex]") | - | OK |
| @description: nextTabID 속성 설정 시 tabIndex 속성에 의한 순서는 무시되고 nextTabID 로 이동합니다. | 관련 API 항목으로 `searchbox_P_nextTabID_1.xml` 의 validation 3 에서 검증 | - | △ (관련 API 분리) |
| @propval `<Undefined>` Tab 키에 의한 기본 포커스 이동 순서에 포함됩니다. | (2) "미설정 시 0 으로 적용되어야 합니다." (= 기본 순서에 포함) | con_tabIndex_target=미설정(빈값) | OK |
| @propval `<0>` Tab 키에 의한 기본 포커스 이동 순서에 포함시킵니다. | (1) "Tab 키에 의한 포커스 이동 순서를 양수, 0, 음수로 설정할 수 있어야 합니다" + (4) "0 으로 설정된 컴포넌트는 양수로 설정된 컴포넌트를 모두 순회한 후 포커스가 이동되어야 합니다." | con_tabIndex_target=0 | OK |
| @propval `<Pos Int>` Tab 키에 의한 포커스 이동 순서를 양의 정수로 설정합니다. | (1) + (3) "양수로 설정된 컴포넌트 중 작은 숫자의 컴포넌트부터 포커스를 받아야 합니다." | con_tabIndex_target=2 (기본값), 3, 4, 5 | OK |
| @propval `<Neg Int>` Tab 키에 의한 포커스 이동에서 제외합니다. | (1) + (5) "음수로 설정된 컴포넌트는 포커스 이동에서 제외되어야 합니다." | con_tabIndex_target=-1 | OK |
| @spec: 양수 중 작은 숫자의 컴포넌트부터 포커스. | (3) "양수로 설정된 컴포넌트 중 작은 숫자의 컴포넌트부터 포커스를 받아야 합니다." | target=2, input=3, button=4, selectbox=5 (기본값) | OK |
| @spec: 같은 값인 경우 소스에 정의된 순서대로 포커스 이동. | (6) "동일한 값을 설정한 경우 소스에 정의된 순서대로 포커스가 이동되어야 합니다 ." | con_tabIndex_target/input/button/selectbox = 동일 양수 | OK |
| @spec: 양수 모두 순회 후 0/미설정 컴포넌트로 포커스. | (4) "0 으로 설정된 컴포넌트는 양수로 설정된 컴포넌트를 모두 순회한 후 포커스가 이동되어야 합니다." | 양수 + 0 혼합 케이스 | OK |
| @spec: 0/미설정은 소스 순서대로 포커스 이동. | (1) + (2) + (6) | con_tabIndex_target/input/button/selectbox = 모두 0/미설정 | OK |
| @spec: 컴포넌트가 포커스를 받을 수 없는 상태이면 tabIndex 속성값에 관계없이 포커스가 이동하지 않습니다. | (7) "컴포넌트가 포커스를 받을 수 없는 상태이면 tabIndex 속성값에 관계없이 포커스가 이동하지 않습니다." | btn_setDisabled 로 input2/button2/selectbox2 비활성화 후 tab 이동 | OK |

### 보강 체크리스트
- [x] API 가이드의 모든 @description / @propval / @spec 내용이 validation 에 반영됨 (wframe `common_P_tabIndex_1.xml` 공유 — input 샘플과 동일 wframe 사용)
- [x] @propval enum 값(Undefined / 0 / Pos Int / Neg Int) 모두 con_tabIndex_target 입력창으로 설정 가능
- [x] nextTabID 와의 우선순위 관련 @description 항목은 별도 `searchbox_P_nextTabID_1.xml` 에서 검증되도록 분리 (관련 API 분리 원칙)
- [x] 컴포넌트 태그 교체: `xf:input` + `createBasicInput(cList, ...)` → `w2:searchbox` + `await createBasicSearchBox(cList, ...)` (target 컴포넌트만 교체, 보조 컴포넌트(input2/button2/selectbox2) 는 그대로 유지)
- [x] 보조 컴포넌트(input2, button2, selectbox2)를 target1 생성 전에 await 로 먼저 생성 (nextTabID 샘플과 동일 패턴, searchbox 비동기 생성 안전성 확보 — reference_searchbox_dynamic_create_await)
- [x] startcomp 는 tabIndex=1 의 보조 input 컴포넌트로 wframe 측 `btn_setInitFocus` 로 초기 포커스를 부여하기 위함
- [x] _1 의 description 은 wframe 공유로 "[common > P > tabIndex]" 유지 (UT_01_생성.md 규칙)
- [x] validation 한 항목 = 핸들러 출력 한 항목 (target1_onfocus / input2_onfocus / button2_onfocus / selectbox2_onfocus) 으로 setReturnValue 출력
- [x] validation 큰따옴표는 작은따옴표로 처리 (현재 항목에 해당 없음)
- [x] Target 재생성 버튼 중복 생성 없음 (wf_body_sample 내장)
- [x] w-pack 변환 단계 생략 (사용자 지시 / Studio 자동 변환)

### 케이스 구성 (con_tabIndex 입력 조합)
- 기본값: target=2 / input=3 / button=4 / selectbox=5 → tab 이동 순서: startcomp → target1(searchbox) → input2 → button2 → selectbox2 (양수 작은 숫자부터)
- 0 / 미설정 케이스: con_tabIndex_target=0 또는 빈값 → 양수 순회 후 소스 순서대로 포커스 이동
- 음수 케이스: con_tabIndex_target=-1 → searchbox 가 Tab 이동에서 제외됨
- 동일 양수 케이스: 모든 con_tabIndex 동일 양수 값 → 소스 정의 순서(startcomp → target1 → input2 → button2 → selectbox2)대로 포커스 이동
- 포커스 불가 상태: setDisabled 버튼으로 input2/button2/selectbox2 disabled 처리 후 tab 이동 → 해당 컴포넌트로 포커스 이동 안 됨

### 누락/△ 항목 메모
- nextTabID 와의 우선순위 관련 @description 문구("nextTabID 속성 설정 시 tabIndex 속성에 의한 순서는 무시되고 nextTabID 에 설정된 컴포넌트로 포커스가 이동합니다.")는 `common_P_tabIndex_1.xml` validation 에는 포함되지 않는다. 동 항목은 `common_P_nextTabID_1.xml` validation 3 ("tabIndex 속성에 의한 순서는 무시되고 nextTabID 에 설정된 컴포넌트로 포커스가 이동하여야 합니다.") 에 반영되어 있으므로 관련 API 샘플(`searchbox_P_nextTabID_1.xml`) 에서 검증된다. 본 샘플 범위 내에서 wframe 수정은 불필요.
- 이외 항목 없음. tabIndex API 가이드의 @description / @propval / @spec 모두 wframe validation 1~7 에 반영되어 있다.

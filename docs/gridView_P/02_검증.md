### gridView_P_filterFocusMode_1.xml

**검증 결과: PASS**

- **XML 구조**: validate_xml `isValid:true`, errors 0. warning 2건은 정적 select1 `selectedIndex` 누락(관례상 `onpageload` 에서 `setValue('')` 로 초기화 → 무해, 기존 gridView P 샘플과 동일).
- **구성**: 30행 x 6열(각 130px → 가로 스크롤, height 200px → 세로 스크롤). `sortable=true` + `sortEvent="onclick"`(단일 클릭 정렬) + `showSortableImage=true`, column1 `useFilter=true`(필터 아이콘). con_filterFocusMode(자체값 select1, getValue)·con_focusMode(''/none) + 기존 `btn_createTarget`(Target 재생성)으로만 반영.
- **관측 방식**: btn_prepare(하단우측 셀 setFocusedCell(28,'col6') + setScrollLeft(0) + 플래그 초기화) → col1 헤더 클릭 정렬 → btn_observe(getFocusedRowIndex/getFocusedColumnIndex, getScrollTop, getScrollLeft, onscrollend 플래그) 출력.

**MCP 런타임 실측 (port 8084, wpack 컴파일 후 배포):**
- 페이지 로딩·target1 생성(30행)·filterFocusMode 옵션 적용 확인. console error 0.
- **미설정("") 정렬 후**: focus rowIndex 28→1(데이터 추종), colIndex 5 유지, scrollTop 540→20(상단 이동), scrollLeft 0→367(가로 이동), onscrollend=true → validation (1)(2)(11) 실증.
- **outfocus 정렬 후**: focus=-1(제거), scrollTop=0(최상단), scrollLeft=0 → validation (5)(6) 실증. (con 변경 → Target 재생성 → prepare → 정렬 → observe 흐름 정상)
- sortEvent 기본값은 ondblclick 이라 단일클릭 미정렬 → `sortEvent="onclick"` 명시하여 라벨("헤더를 클릭하여 정렬")과 일치시킴.
- infocus/followfocus/fixScrollX/focusMode=none 은 동일 메커니즘(con+재생성+prepare+정렬+observe)으로 관측되며 엔진 소스(notifyController.notifySetFocusedCell / mappingController(outfocus setScrollTop(0)) / focusController(followfocus checkScrollEnd) / notifyClearSortFilter(infocus))에서 각 분기 동작 확인.

**배포 특이사항**: 8084 서빙 webroot 은 소스(`workspace/unitTest/src/main/webapp`)가 아니라 **standalone Tomcat9 서비스**(`C:\Program Files\Apache Software Foundation\Tomcat 9.0\webapps\unitTest`). 신규 파일은 이 webroot 에 xml+wpack js 를 복사해야 서빙됨(서버 시작시 리소스 인덱싱, 신규 .xml 은 엔진 레지스트리 미등록으로 직접 404 나지만 wpack .js 로딩 경로는 정상 → 미리보기 동작).

---

### useFilterListSync_1.xml

- **XML 구조**: validate_xml `isValid:true`, errors 0. warning 2건은 정적 select1 `selectedIndex` 누락(관례상 `onpageload` `setValue('1')` 초기화 → 무해, 기존 gridView P 샘플과 동일).
- **구성**: dlt_bind 12행 x 4열(col1=A~D 4종, col2=X/Y 2종, col3/col4 값). GridView `useFilterList="true"` + 전 컬럼 `useFilter="true"` → 헤더 필터 아이콘/FilterList 활성. con_useFilterListSync·con_groupBy(gdl_boolean, getText) + 기존 `btn_createTarget`(Target 재생성)으로만 반영. `$c.gcm.createCommonDatalist()` 로 gdl_boolean 생성(누락 시 con 옵션 0개 → 필수).
- **관측 방식**: btn_filterCol1(setColumnFilter col1=A) / btn_filterCol2(setColumnFilter col2=X, 마지막 필터 전환) / btn_clearFilter. 필터 후 헤더 필터 아이콘을 열어 FilterList 표시목록·체크상태를 con 값별 육안 비교.

**MCP 런타임 실측 (port 51271):**
- 페이지 로딩·target1(12행) 생성·필터아이콘 4개·console error 0.
- con 미설정 → options.useFilterListSync=false / con true → true 정상 반영(gdl_boolean getText '' / 'true').
- setColumnFilter(col1=A, exactMatch) → 표시행 12→3 (B,C,D 행 제외).
- con groupBy=true 재생성 → target1.groupby({sortIndex:[0],sortOrder:[1]}) → groupbyStatus.grouped=true. 이 상태에서 useFilterListSync=true 여도 엔진 동기화 스킵(notifyController.js:1473 확인).
- FilterList 팝업 표시목록(true=전체 A~D+미표시 체크해제 / false·미설정=표시 데이터만) 차이는 필터 아이콘 오픈 후 육안 관측(엔진 소스 분기와 일치).

**분리 개정 (port 53999, 2026-07-20):** `_2.xml` 신설에 따라 _1 정비.
- col1 필터 → col2 필터 체인 출력: col2 `X 체크 / Y 해제`(2개), 이어서 col1 `A`(1개) 로 복귀 → "마지막 필터 Column 만 전체목록" 항목이 출력만으로 입증됨(기존에는 육안 관측 필요).
- **수정 이력**: col2 데이터식 `types[i%2]` → `types[Math.floor(i/3)%2]`. 기존식은 col1(`cats[i%4]`) 과 주기가 겹쳐 col1 필터 후 col2 가 한 값만 남아 위 항목을 관측할 수 없었음.
- **수정 이력**: `dumpFilterList` 가시 체크박스 0개 시 `FilterList 표시목록 없음 -> useFilterListSync 동기화 미동작` 출력 추가(groupBy 케이스가 "0개" 로만 보이던 문제 해소).

**con_useFilterList 제거 (2026-07-20, _2 와 동일 방향):** 분리 개정 시 (1) 항목 검증용으로 잠시 신설했던 `con_useFilterList` 를 제거하고 **`useFilterList="true"` 리터럴 고정**으로 변경. 제거 범위 — ① `grp_condition` 내 useFilterList th/td 쌍 + 이를 위해 추가했던 2번째 tr(빈 th/td 포함) ② `onpageload` 의 `setValue("2")` ③ `comp_init` 의 `vList`/`attrList` ④ `dumpFilterList` 출력의 `useFilterList=…` 표기 ⑤ 재오픈 재시도 루프의 `con_useFilterList.getText()==='true'` 가드(원래 `attempt < 2` 로 복원). 잔여 참조 0(grep 확인). 옵션이 `con_useFilterListSync` + `con_groupBy` 2개로 돌아와 **1개 tr 에 th/td 2쌍**(UT_01 §4) 구조로 정리됨.

**`con_groupBy` 유지 판단(대조 확인)**: validation 6항목과 1:1 대조한 결과 (6) `GroupBy 기능이 적용된 경우에 useFilterListSync 속성은 동작하지 않습니다.` **의 유일한 검증 수단**이 `con_groupBy` 이다(다른 5개 항목은 `con_useFilterListSync` + 필터 버튼으로 검증). 따라서 삭제 대상 아님 — 유지. (엔진상으로도 `notifyController.js:1473` 의 `if(this.groupbyStatus.grouped){}` 빈 블록이 (6) 의 근거이며, groupby 적용 없이는 재현 불가.)

**MCP 재검증 (port 53999, con_useFilterList 제거 후):** validate_xml `isValid:true`(errors 0, warning 2건=select1 selectedIndex), console error **0**.
- `mf_con_useFilterList === undefined`, `mf_con_useFilterListSync`/`mf_con_groupBy` 는 존재, `target1.options.useFilterList === true` → 고정 반영 확인.
- sync **미설정** → col1 `A`(1개) / sync **false** → col1 `A`(1개) → (4)(5) 입증.
- sync **true** → col1 `A 체크 / B,C,D 해제`(4개) → (2) 입증. 이어서 col2 필터 → col2 `X 체크 / Y 해제`(2개) + col1 `A`(1개) 복귀 → (3) 입증.
- groupBy **true** + sync=true → `groupbyStatus.grouped === true`, `FilterList 표시목록 없음 -> useFilterListSync 동기화 미동작` → (6) 입증.
- 부수: useFilterList 고정으로 CustomFilter 팝업 케이스가 사라져 이전에 겪던 `#_modal` 오버레이 클릭 차단 함정도 _1 에서는 재현되지 않음.

---

### useFilterListSync_2.xml

- **XML 구조**: validate_xml `isValid:true`, errors 0, **warning 0**.
- **구성**: _1 과 동일한 dlt_bind 12행 x 4열. GridView `useFilterList="true" useFilterListSync="true"` 고정 + `column1~4` 전체 `useFilter="true"` **고정**. 옵션 셀렉트박스 없음 → `grp_condition` 그룹 미생성. 고정 전제는 `$c.gcm.createLabel` 의 `[전제]` 줄로 표시(UT_01 §5-1). 버튼은 `col1 필터 적용` / `필터 초기화` 2개이며, 재생성은 기존 `btn_createTarget` 사용.
- **관측 방식**: `document.querySelector("[id$='_column1_filter']")` 로 col1 헤더의 필터 버튼을 조회해 열고 FilterList 목록/체크상태를 출력. (필터 버튼 id 규칙 `{gridId}_{headerColumnID}_filter` — layoutController.js:8774)
- **validation**: 1항목(분리 사유 = Column useFilter 전제)만 유지. 나머지 @propval/@spec 항목은 _1.xml 소관.

**수정 이력 (con_useFilter 제거, 2026-07-20)**: 초기 버전은 `column1` 의 useFilter 를 `con_useFilter`(gdl_boolean) 로 가변 구성했으나, **useFilter=true 고정**으로 변경. 제거 범위 — ① `grp_condition` 마크업 전체(2단테이블 포함) ② `onpageload` 의 `con_useFilter.setValue("1")` ③ `comp_init` 의 `getText()` 분기 + `attrUseFilter` 조립 ④ `dumpFilterList` 내 con 참조(출력 문구를 `useFilter=true` 고정 표기로 변경). 잔여 참조 0 (grep 확인).

**MCP 런타임 실측 (port 53999, con 제거 후):** console error **0**.
- `mf_con_useFilter === undefined`, 페이지 내 셀렉트박스 **0개** → 옵션 제거 확인.
- 필터 버튼 4개 전부 생성: `mf_target1_column1_filter` / `_column2_filter` / `_column3_filter` / `_column4_filter`.
- 라벨 `[전제] … 전체 Header Column 은 useFilter=true 고정이며 GridView 는 useFilterList=true + useFilterListSync=true 고정이다.` 정상 렌더(`\n` → 줄바꿈).
- col1 필터(A) 적용 → `===== [col1] useFilter=true ===== FilterList 표시목록 / A 체크 / B 해제 / C 해제 / D 해제 (표시 아이템 4개)` → 동기화 정상 동작.
- `필터 초기화` → `표시 행 수 : 12` 복귀. `Target 재생성` 후 필터 버튼 4개 유지.

---

### loadCount_1.xml

- **XML 구조**: validate_xml `isValid:true`, errors 0. warning 1건은 `scwin.target1_onscrollend` 미사용(동적 생성 eList 로 바인딩하므로 무해, 기존 스크롤 이벤트 샘플과 동일).
- **구성**: con_loadCount(정적 select1, 자체 값 → `getValue()`) = 미설정 / 0 / 5. dlt_bind 4열, GridView 높이 220px(가시 4행) → 세로 스크롤 발생. 기존 `btn_createTarget`(Target 재생성)으로만 con 반영. 검증 버튼 3개: loadCount 확인 / 데이터 append / 데이터 재로드.
- **핵심 구성 이슈(수정 이력 1)**: 최초 작성 시 `createDynamicDataList(...,100)` 로 **GridView 생성 전에** 데이터를 채웠더니 loadCount=5 에서 onscrollend 가 **한번도 발생하지 않음**. 엔진 `drawController.js:507` 의 `loadCount > 0` 분기는 `this.loadFlag` 를 요구하는데, loadFlag 는 `notifyController` 의 DataList notify(setData/appendData) 시점에만 true 로 세팅된다. GridView 렌더 전에 데이터가 이미 들어가 있으면 notify 가 발생하지 않아 `loadFlag === undefined` → 분기 진입 불가. → **DataList 를 0행으로 생성 → GridView 생성 → `dlt_bind.setJSON(rows,false)`** 순서로 변경하여 해결(실사용 패턴과도 일치).
- **수정 이력 2**: `comp_init` 재실행 시 `label1` 이 갱신되지 않음(`$p.dynamicCreate` 가 중복 cID 를 재생성하지 않음). `if (typeof label1 !== 'undefined' && label1) label1.remove();` 추가하여 해결(gridView_E_*_2.xml 과 동일 패턴).

**MCP 런타임 실측 (port 53999):** console error 0.
- 초기 로딩: `mf_target1` 생성, dlt_bind 100행, height 220px, `getVisibleRowNum()=4`, `loadFlag=true`.
- **미설정**: 최하단 3회 반복 도달 → onscrollend 3회 발생(매번, 표시 Row 96~99).
- **0 명시**: `options.loadCount=0`, 최하단 3회 반복 도달 → 3회 발생(미설정과 동일).
- **5 설정**: `options.loadCount=5`. 50px 씩 점진 스크롤 → **top=92(표시 92~95, 마지막 인덱스 95 ≥ 94)** 시점에 최초 1회 발생. 최상단 복귀 후 최하단 재도달해도 **추가 발생 없음**(1회만).
- **append**: 100건 → 120건(`setJSON(rows,true)`) 후 재스크롤 → 재발생(표시 115~118, 데이터 건수 120).
- **재로드**: `setJSON(60건,false)` 후 재스크롤 → 재발생(표시 55~58, 데이터 건수 60).
- **loadCount 확인 버튼**: `loadCount : 5 / 데이터 건수 : 100 / onscrollend 발생 기준 Row 인덱스 : 94` → @spec 의 94 와 일치.

**수정 이력 3 (validation @spec2 항목 삭제)**: "대용량 데이터 처리 시 … 추가 데이터를 로드하기 위한 용도로 사용" 항목은 검증 가능한 동작 규정이 아닌 용도 서술이고 실동작이 (2)(5)(6) 으로 이미 검증되므로 제거 → validation **7 → 6항목**. 전용 UI 없어 버튼 변경 없음.

**수정 이력 4 (`loadCount 확인` 버튼 삭제)**: 버튼 마크업(`btn_checkLoadCount`) + 전용 핸들러(`scwin.btn_checkLoadCount_onclick`) 모두 제거. 이 버튼이 담당하던 validation (1)(5) 는 항목은 유지하고, **onscrollend 핸들러 출력에 정보를 흡수**(별도 버튼 추가 없이 UT_01 §4-1 "검증 항목 수 ≈ setReturnValue 호출 수" 유지 — 이벤트당 여전히 1줄).
- 변경 전: `onscrollend 발생 [n회] 데이터 건수 : 100 / 표시 중 Row 인덱스 : 92 ~ 95`
- 변경 후: `onscrollend 발생 [n회] loadCount : 5 / 데이터 건수 : 100 / 발생 기준 Row 인덱스 : 94 / 표시 중 Row 인덱스 : 92 ~ 95`
- `발생 기준 Row 인덱스 = 데이터 건수 - loadCount - 1` (엔진 `drawController.js:509` 조건과 동일), loadCount 미설정/0 이면 `(미설정/0)` + 기준 인덱스 `-` 로 표시.
- → (1) 설정된 남은 Row 수는 출력의 `loadCount` 값으로, (5) 인덱스 94 기준 발생은 `발생 기준 Row 인덱스 : 94` + `표시 중 Row 인덱스` 마지막값(95 ≥ 94) 대비로 **이벤트 출력만으로 입증 가능**.

**MCP 재검증 (port 53999, 이력 3~4 반영):** console error 0, validate_xml `isValid:true`(errors 0).
- 잔여 참조 0(`btn_checkLoadCount` grep 무결과), `scwin.btn_checkLoadCount_onclick === undefined`, grp_etc 버튼 2개(`데이터 append` / `데이터 재로드`)만 존재, `tData.validation.length = 6`.
- 미설정 최하단 도달: `loadCount : (미설정/0) / 데이터 건수 : 100 / 발생 기준 Row 인덱스 : - / 표시 중 Row 인덱스 : 96 ~ 99`.
- loadCount=5 점진 스크롤: `loadCount : 5 / 데이터 건수 : 100 / 발생 기준 Row 인덱스 : 94 / 표시 중 Row 인덱스 : 92 ~ 95` (1회만).
- append 100→120: 기준 인덱스 114 / 표시 115~118 재발생. 재로드 60건: 기준 인덱스 54 / 표시 55~58 재발생.

---

### useFilterSearch_1.xml

- **XML 구조**: validate_xml `isValid:true`, errors 0. warning 1건은 `xf:select1` 의 `selectedIndex` 미지정 — UT_01 §4 규칙에 따라 `onpageload` 에서 `con_useFilterSearch.setValue("1")` 로 명시 설정하므로 무해.
- **구성**: con 옵션은 `con_useFilterSearch`(gdl_boolean) **1개**. GridView 는 `useFilterList="true"` 리터럴 고정, `column1~4` 전체 `useFilter="true"` 고정(FilterList 적용 전제). 검증 버튼은 상태 출력용 `FilterList 표시목록 출력` **1개**. con 반영은 기존 `btn_createTarget`(Target 재생성)만 사용.
- **데이터 설계**: col1 = Apple / Banana / Grape / Orange / Pineapple. 검색어 `an` 은 **Banana·Orange 의 중간 위치에만** 존재하므로, 접두 일치였다면 0건이어야 한다 → 부분 일치(@propval `|`절) 입증에 결정적.

**수정 이력 1 (useFilterList con 제거, 2026-07-20)**: 초기 버전은 `con_useFilterList`(gdl_boolean)로 전제를 가변 구성했으나 **`useFilterList="true"` 고정**으로 변경. 제거 범위 — ① `con_useFilterList` 마크업(th/td 쌍은 빈 textbox + 빈 td 로 유지, UT_01 §4 tr 당 th/td 쌍 규칙) ② `onpageload` 의 `setValue("2")` ③ `comp_init` 의 `vList` 분기 + `attrList` 조립 ④ 출력 head 문구(`useFilterList=…` → `(useFilterList=true 고정)`) ⑤ 재시도 분기의 `con_useFilterList.getText() === 'true'` 조건 ⑥ createLabel 의 useFilterList=false 안내 줄. 잔여 참조 0(grep 확인).

**수정 이력 2 (`검색어 an 입력` 버튼 삭제)**: 버튼 마크업 + 전용 핸들러(검색어 주입 → `KeyboardEvent` keydown dispatch → 400ms 대기 로직) 전부 제거. 아울러 남아 있던 `FilterList 열기` 버튼도 **필터 아이콘 click() 을 대행**하고 있어 UT_01 "사용자 액션 버튼 금지" 위반이므로 함께 제거하고, 순수 상태 출력 버튼 `FilterList 표시목록 출력`(`btn_dumpFilterList`) 1개로 통합. 검색어 입력·FilterList 열기는 사용자(또는 TSQA)가 직접 수행.

**핵심 구성 이슈 — 상태 읽기를 DOM 가시성이 아닌 컴포넌트 데이터로 전환**: FilterList 는 그리드 밖을 클릭하면 닫힌다(`eventController.js:2131 handleClickEventDocumentAfter` → `hideCustomFilterList`). 따라서 **출력 버튼을 실제로 클릭하는 순간 FilterList 가 닫히므로**, 기존의 `offsetParent !== null` 기반 DOM 수집 방식은 항상 0건을 반환한다. → `target1.customFilterList.getAllData()` 로 **FilterList 컴포넌트의 데이터**를 읽도록 변경(선두 2행 `사용자필터`/`전체선택` 컨트롤 행은 텍스트로 제외). 검색어 입력창 존재 여부/입력값도 `document.querySelector("[id='{gridId}_customFilter_list_input']")` 를 **가시성 조건 없이** 조회. MCP 실측에서 버튼 실클릭 직후 `listVisible=false` 인 상태에서도 `입력된 검색어 : an / 표시 아이템 : Banana, Orange` 가 정상 출력되어 이 설계가 검증됨.

**MCP 런타임 실측 (playwright2, port 53999, 최종본):** console error **0**.
- `mf_con_useFilterList === undefined`, `mf_target1.options.useFilterList === true` → con 제거 + 고정 반영 확인. `btn_searchFilterList` 미존재, `btn_dumpFilterList` 만 존재.
- FilterList 미개봉 상태에서 버튼 클릭 → `col1 헤더의 필터 버튼을 먼저 클릭하여 FilterList 를 연 뒤 실행하세요.` (방어 분기 정상)
- **미설정**: 필터 버튼 실클릭 후 출력 → `검색어 입력창 표시 여부 : false / 입력된 검색어 : (없음) / FilterList 표시 아이템 : Apple, Banana, Grape, Orange, Pineapple`
- **false**: 동일 → `검색어 입력창 표시 여부 : false / 표시 아이템 5개 전체`
- **true**: `검색어 입력창 표시 여부 : true / 입력된 검색어 : (없음) / 표시 아이템 5개 전체`
- **true + 검색어 `an` 실타이핑**(`page.type`, delay 150ms — 버튼 대행 아님) → `입력된 검색어 : an / FilterList 표시 아이템 : Banana, Orange` → 부분 일치 + 미일치 데이터 목록 제외(@spec) 동시 입증.

---

### escape_1.xml

- **XML 구조**: validate_xml `isValid:true`, errors 0. warning 1건은 `xf:select1` 의 `selectedIndex` 미지정 — `onpageload` 에서 `con_escape.setValue("2")` 로 명시 설정하므로 무해.
- **구성**: con 옵션은 `con_escape`(gdl_boolean, `getText()` 로 읽음) 1개. `escape` 는 `@dynamic N` 이므로 con 변경 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용(별도 재생성 버튼 없음). 검증 버튼은 상태 출력용 `표시값 확인`(`btn_checkDisplay`) 1개, 편집 검증은 사용자 조작(더블클릭 편집) 으로 `onbeforeedit`/`onafteredit` 가 자동 출력.
- **Column 설계(3컬럼)**: 0 col1(text, 컬럼 escape 미설정 → GridView escape 상속, **표시 검증 + 편집 모드 검증 통합 대상**) / 1 col2(text, 컬럼 `escape="false"` 고정, 상속 규칙 대조) / 2 col3(text, `customFormatter="scwin.fmtEscape"` → formattedData 보관). 컬럼 폭은 `width="*"` 사용 시 마지막 컬럼이 width 0 으로 접혀 클릭 불가 → 전 컬럼 고정 `316` + 그리드 `width:960px`.
- **편집 검증 컬럼 통합(사용자 요청, 2026-07-28)**: 편집 전용 col4 를 삭제하고 편집 모드 검증(@spec1)을 주 검증 컬럼인 col1 로 이동. `onbeforeedit`/`onafteredit` 는 `getColumnID(columnIndex) !== "col1"` 이면 즉시 return 하여 다른 컬럼 편집 시 출력이 늘지 않도록 가드 → setReturnValue 호출 9회 = validation 9개 유지. 동일 col1 에서 표시 모드값(escape 설정에 따라 변동)과 편집 모드값(항상 이스케이프 표시)이 직접 대비됨. validation 문구/개수 변경 없음.
- **데이터 픽스처**: 3행 모두 `<b>`/`<i>` 태그 + `<`, `>`, `&`, 작은따옴표 포함. 특수문자 때문에 XML 문자열 생성이 아닌 `dlt_bind.setJSON()` 으로 주입.
- **출력 전량화(사용자 요청, 2026-07-28)**: `표시값 확인` 버튼이 Row0 특정 셀만 출력하던 것을 **렌더된 전체 셀(3 Row x 3 Column = 9셀)** 출력으로 변경. 공통 순회 헬퍼 `scwin.eachCellLines(fn)` 이 `td[id*='target1_cell_{r}_{c}']` 를 순회하며 `[Row{n}][{컬럼ID}] {값}` 줄 목록을 만들고, 각 setReturnValue 는 그 목록을 `
` 로 묶어 **1회 호출**로 출력한다. 값은 전부 DOM 실측(`td.textContent` / `td.innerHTML` / `td.querySelector("b, i")`)이며 DataList 원본값은 출력하지 않는다(원본은 "모든 문자 표시 여부" 계산에만 사용). 버튼 setReturnValue 는 **7회 유지**(총 9회 = validation 9개).
  - 7개 출력 블록: ① 전체 셀 화면 표시값(textContent) ② 전체 셀 렌더 결과(innerHTML) ③ 전체 셀 원본 문자가 모두 표시되는지 ④ 전체 셀 HTML 태그가 실제 엘리먼트로 삽입되었는지(보안 취약) ⑤ 컬럼 escape 상속 대조(col1 vs col2, 전 행) ⑥ col3 customFormatter data 파라미터(전 행) ⑦ col3 customFormatter formattedData 파라미터(전 행).
- **MCP 런타임 실측 (playwright, port 51679, viewport 1600x1000, 전량 출력본): console error 0.**
  - escape=true / 미설정 → col1/col3 전 행 `textContent` = 원본 그대로(`<b>긴급</b> 반품 & 교환 '1차'` 등), `innerHTML` = `<nobr class="w2grid_input">&lt;b&gt;긴급&lt;/b&gt; 반품 &amp; 교환 '1차'</nobr>`, 원본 전체 표시 `true`, 태그 삽입 `false`.
  - escape=false → col1/col3 전 행 `textContent` 에서 태그 소실(`긴급 반품 & 교환 '1차'`), `innerHTML` 에 `<b>`/`<i>` 실제 엘리먼트, 태그 삽입 `true`(보안 취약 입증). 태그가 없는 Row1(`재고 5 < 10 & 추가발주 '검토'`) 은 escape 값과 무관하게 표시값 동일 — `<`/`&` 는 엔티티 여부만 `innerHTML` 에서 구분됨.
  - col2(컬럼 escape=false 고정) 는 GridView escape=true/미설정 에서도 태그 삽입 `true` → 상속 대조 블록에서 Row0/Row2 `동일 여부 = false`(Row1 은 태그가 없어 true) → 컬럼 escape 미설정 시에만 GridView escape 가 적용됨(@description `|`) 입증.
  - customFormatter : `data` 는 escape 값과 무관하게 전 행 원본, `formattedData` 는 escape=true/미설정 시 전 행 `&lt;b&gt;…&amp;…&apos;…` 인코딩, escape=false 시 원본 그대로(@spec2 입증).
  - 편집 모드(**col1** Row0 더블클릭) : escape=true / false / 미설정 **모두** 편집창 표시값 = `<b>긴급</b> 반품 & 교환 '1차'`(모든 문자 표시). escape=false 일 때 같은 col1 의 표시 모드값은 `긴급 반품 & 교환 '1차'`(태그 소실) 이므로 동일 컬럼 내에서 표시 모드 vs 편집 모드 차이가 그대로 드러남 → "편집 모드에서는 항상 이스케이프 처리된 데이터가 표시"(@spec1 `|`) 입증.
  - 편집 종료 : escape=true 입력 `<b>수정</b> & '완료'` → DataList 저장값 동일(원본 문자, 엔티티 아님) → 입력값 디코딩 저장(@spec1) 입증. escape=false 입력 `<i>확인</i> & '검토'` → 저장값 동일, 화면 표시값은 `확인 & '검토'`. 미설정 입력 `<b>재확인</b> & '승인'` → 저장값/표시값 동일.
- **컬럼 통합 후 재실측 (2026-07-28, port 51679): console error 0.** 헤더 셀 3개 / Row0 td 3개(폭 315·316·316, 접힘 없음) / `dlt_bind` 컬럼 3개 확인. escape=true·false·미설정 3케이스 표시값·편집 진입값·편집 종료 저장값이 통합 전과 동일하게 재현됨.

---

### adaptive_1.xml

- **XML 구조**: `validate_xml` `isValid:true`, errors 0 / warnings 0.
- **구성**: con 옵션 3개 — `con_adaptive`(정적 `dlt_adaptive` : 미설정 / `crosstab` / `list`, `getValue()`), `con_adaptiveThreshold`(정적 `dlt_threshold` : 미설정(기본 480) / 400 / 2000, `getValue()`), `con_groupbyZone`(`gdl_boolean`, `getText()`). `adaptive` 는 `@dynamic N` 이라 con 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용(별도 재생성 버튼 없음). 검증 버튼 3개 : `적응형 적용 상태 확인` / `행-열 전환 레이아웃 확인` / `visibleRowNum / 세로 스크롤 확인`.
- **Target 구성**: Header 1행 / Body 1행 / Footer 1행, 4컬럼(폭 160), `visibleRowNum="3"`, 데이터 6행. Footer 를 둔 이유는 @spec1 의 `w2grid_adaptive_crosstab_footer` 클래스 확인용(`addAdaptive` 가 `hasFooter` 일 때만 부여). `adaptiveThreshold=2000` 을 con 기본값으로 두어 브라우저 창을 줄이지 않아도 현재 뷰포트(1500px)에서 적응형 적용 상태를 관측할 수 있게 함.
- **생성 중 발견/수정**: 최초 `style="width:700px;"`(height 미지정) + `visibleRowNum="3"` 구성에서 **적응형 미적용 케이스만** Body 행이 하나도 렌더되지 않음(`render` 높이 0, `body_tbody` 비어 있음, `lastIndex=-1`). 적응형 적용 시에는 CSS `.w2grid_adaptive_crosstab{height:auto!important}` 덕분에 정상 렌더되어 문제가 가려짐. → 그리드 style 에 `height:150px;` 명시하여 해결.
- **MCP 런타임 실측 (playwright2, port 51679, viewport 1500x900): console error 0 / warning 0.**

| con 조합 | crosstab 클래스 | footer 클래스 | Header Cell 배치 | Body 행 배치 | visibleRowNum | 표시 행 수 | 세로 스크롤 |
|---|---|---|---|---|---|---|---|
| crosstab / th=2000 / gz=false | true | true | 세로 | 가로 | all | 6/6 | false |
| crosstab / th=400 / gz=false | false | false | 가로 | 세로 | 3 | 3/6 | true |
| crosstab / th=미설정(480) / gz=false | false | false | 가로 | 세로 | 3 | 3/6 | true |
| adaptive 미설정 / th=2000 | false | false | 가로 | 세로 | 3 | 3/6 | true |
| adaptive=list / th=2000 | false | false | 가로 | 세로 | 3 | 3/6 | true |
| crosstab / th=2000 / gz=true | false | false | 가로 | 세로 | 3 | 3/6 | true |
| crosstab / th=2000 / gz=미설정(false) | true | true | 세로 | 가로 | all | 6/6 | false |

- **창 리사이즈 실측**(adaptive=crosstab, adaptiveThreshold 미설정=480) : viewport 1500 → 클래스 없음 / 420 으로 축소 → `w2grid_adaptive_crosstab w2grid_adaptive_crosstab_footer` 부여 + `visibleRowNum="all"` / 다시 1500 → 클래스 제거 + `visibleRowNum="3"` 복원. `gridView.js:1144` 의 `window.onresize → setAdaptive` 리스너로 즉시 반영됨(@description "웹브라우저의 너비가 변경될 때").
- **실측 수단(추정 금지)**: 클래스는 `target1.render.className.split(/\s+/)` 로 토큰 비교(부분 문자열 매칭 시 `_footer` 오탐). 행-열 전환은 `th.gridHeaderTDDefault[0/1].getBoundingClientRect().top` 동일 여부(동일=가로 나열=기본), `tr.grid_body_row[0/1].left` 동일 여부(동일=세로 나열=기본) 로 판정. 세로 스크롤은 `div[id$='scrollY_div'].offsetHeight > 0` 으로 판정 — drawType=virtual 이라 `dataLayer.scrollHeight > clientHeight` 는 항상 false 여서 사용 불가(CSS 가 crosstab 시 `scrollY_div{display:none}` 처리).

---

### accessibility_2.xml

- **XML 구조**: `validate_xml` `isValid:true`, errors 0 / warnings 0.
- **구성**: con 옵션 2개 — `con_accessibility` / `con_senseReader` (둘 다 `gdl_boolean`, `getText()`). `accessibility` 는 `@dynamic N` 이라 con 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용. 검증 버튼 2개 : `연쇄 설정 확인`(출력 2줄) / `적용값 대비 확인`(출력 1줄) + `onsort` 이벤트 출력.
- **Target 구성**: Header 1행 / Body 1행, 4컬럼, 데이터 6행. 그리드 `sortable="true"` 고정 + `sortEvent` 미설정(엔진 기본 `ondblclick`). Header Column `hcol1`(sortable 미지정→그리드값 상속=정렬 가능) / `hcol2 sortable="false"` / `hcol3 sortable="true"` / `hcol4 sortable="false"` 로 정렬 가능·불가 Column 을 함께 배치. Body Column 4개 모두 `embeddedInput="false"`.
- **생성 중 발견/수정**
  1. `target1.getOptions()` 는 **존재하지 않는 메소드**(`TypeError: g.getOptions is not a function`). gridView 적용 옵션은 `target1.options` 로 읽어야 함 → 전 핸들러 `.options` 로 수정.
  2. Body Column 을 기본(embeddedInput 미지정) 으로 두면 `accessibility=true`(senseReader 강제) 가 `embeddedInput=true` 를 강제하고, 엔진(`drawController.js:4332`)은 **embeddedInput 셀의 td 에는 tabIndex 를 넣지 않으므로** `focusFlow="linear"` 인데도 td tabIndex 부여 개수가 0/24 로 측정됨(Tab 정지점은 셀 내부 `input.w2grid_embedded_input`). → focusFlow 판정을 tabIndex 로 하려면 Body Column 에 `embeddedInput="false"` 명시가 필요(`cellInfo.js:530` 이 `embeddedInput !== "false"` 일 때만 강제). 4개 Column 모두 `embeddedInput="false"` 로 고정하여 해결.
- **MCP 런타임 실측 (playwright2, port 56075): console error 0 / warning 0.**

| con 조합 | focusFlow | td tabIndex 부여 | sortHeaderDiv | showSortableImage | sortEvent | 정렬아이콘(이름/부서/점수/등급) |
|---|---|---|---|---|---|---|
| accessibility=true, senseReader 미설정 | linear | 24 / 24 | true | true | onclick | true / false / true / false |
| accessibility 미설정, senseReader=true | linear | 20 / 20 | false | false | ondblclick | false / false / false / false |
| accessibility=false, senseReader=true | linear | 20 / 20 | false | false | ondblclick | false / false / false / false |
| 둘 다 미설정 | default | 0 / 20 | false | false | ondblclick | false / false / false / false |

- **정렬 실행 실측(실제 클릭)**: `accessibility=true` → `hcol1` **1회 클릭**에 `onsort(headerId:hcol1, sortOrder:1)` 발생 + col1 값이 오름차순(강대현/김철수/박민수/이영희/정한나/최지우) 으로 재배치. `hcol2`(sortable=false) 클릭은 미발생. `senseReader` 단독 → 1회 클릭 미발생, **더블클릭**에서 `onsort` 발생(기본 `ondblclick` 유지).
- **Tab 이동 실측(accessibility=true, focusMove=false 강제)**: `cell_0_0` 클릭 후 Tab ×3 → activeElement `mf_target1_cell_0_0` → `G_mf_target1__col2` → `__col3` → `__col4`. Cell 단위로 이동하며 `keyMoveEditMode=true` 강제로 편집 입력요소로 포커스가 들어감. `@propval` 의 "focusMove=false 이지만 senseReader=true 로 인해 Tab 이동은 허용" 과 일치(가이드대로 동작).

### drawType_1.xml

- **XML 구조**: `validate_xml` `isValid:true`, errors 0 / warnings 3(`UNUSED_EVENT_HANDLER` — `createEList` 로 동적 부여하는 onscrollstart/onscrollend/onscrolly 이므로 정상).
- **구성**: con 옵션 3개 — `con_drawType`(dlt_drawType: 미설정/virtual/native, `getValue()`) / `con_accessibility`(gdl_boolean, `getText()`) / `con_adaptive`(dlt_adaptive: 미설정/crosstab, `getValue()`). drawType 은 `@dynamic N` 이라 con 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용. 검증 버튼 4개 : `렌더링 방식 확인`(5줄) / `무시되는 속성 확인`(3줄) / `스크롤 이벤트 발생 확인`(1줄, 출력 후 카운트 초기화) / `Row 높이 상이 시 렌더 확인`(1줄).
- **Target 구성**: Header 1행 / Body 1행, 4컬럼, 데이터 30행, `style="width:700px;height:200px;"`(native 전제인 높이 명시). `visibleRowNum=5` / `overflowY=scroll` / `fixedRow=2` / `wheelRows=5` / `wheelStop=true` / `adaptiveThreshold=1000` 을 고정 부여하여 native 에서 무시되는지 대조 가능하게 구성.
- **MCP 런타임 실측 (playwright, port 53671): console error 0 / warning 0.**

| con drawType | options.drawType | options.visibleRowNum | 렌더 Body Row | 세로 스크롤 주체 | 엔진 scrollY_div 사용 | 휠1회 이동 Row(휠 100px) | 최하단 스크롤 후 최상단 Row | 그리드 바깥 스크롤 | onscrollstart/end/y |
|---|---|---|---|---|---|---|---|---|---|
| 미설정 | virtual | 5 | 5 | 엔진 | true | 5 | col1_1 | 0px | - |
| virtual | virtual | 5 | 5 | 엔진 | true | 5 | col1_1 | 0px | 1 / 1 / 10 |
| native | native | all | 30 | 웹브라우저 | false | 3 (200px 휠은 5) | col1_25 | 30px | 0 / 1 / 8 |

- **accessibility 강제 실측**: con drawType=virtual + accessibility=true → `options.drawType='native'` 강제, `visibleRowNum='all'`, senseReader=true / focusMove='false' / keyMoveEditMode=true / scrollFocus=true / titleAuto=true / sortHeaderDiv=true 연쇄 적용, 렌더 Body Row 30. 가이드 일치.
- **adaptive 임시 적용/복원 실측**: con drawType=native + adaptive=crosstab + adaptiveThreshold=1000 → 브라우저 너비 784/900px(≤1000) 에서 `w2grid_adaptive_crosstab` 적용 + `options.drawType='virtual'`, 너비 1400px 로 넓히면 클래스 제거 + `options.drawType='native'` 로 복원. 가이드 일치.
- **Row 높이 상이 실측**: 홀수 Row 에 `setCellStyle(i,0,'height','60px')` 적용 → native 는 상위 4Row 높이 37,61,38,61 로 전 30Row 실제 렌더 / virtual 은 뷰포트 5Row 만 렌더(38,62,39,61).
- **결함/불일치**: 없음. 가이드의 @description/@propval/@spec 기술이 모두 실측과 일치.

### euroMask_1.xml

- **XML 구조**: `validate_xml` `isValid:true`, errors 0 / warnings 0.
- **구성**: con 옵션 1개 — `con_euroMask`(dlt_euroMask itemset, `getValue()`, 미설정 포함 전수 10개). euroMask 는 `@dynamic N` 이라 con 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용(별도 재생성 버튼 없음). 검증 버튼 3개 : `표시값 확인`(7줄) / `원본 데이터값 확인`(1줄) / `euroMask 반영값 확인`(2줄). 편집모드 검증은 사용자 더블클릭 관찰(자동 버튼 없음).
- **Target 구성**: Header 1행 / Body 1행, Body Column 6개(col1 euro / col2 euro+applyFormat=all+editFormat="" / col3 displayFormat="#.##0,000" / col4 displayFormatter / col5 dataType="number" / col6 inputType="textarea"), 데이터 5행 동일값 바인딩, `style="width:840px;height:210px;"`.
- **MCP 런타임 실측 (playwright, port 53671): console error 0 / warning 0.** target1 생성 확인, `tr.grid_body_row` 5개 렌더, 셀 td textContent 와 `getCellDisplayData` 값 일치.
- **Target 재생성 흐름**: con 을 `#.###,00` 으로 바꾸고 `btn_createTarget` 클릭 → `options.euroMask="#.###,00"`, row2 표시값 `1.234,50` 으로 갱신. 초기 `setSelectedIndex(1)` 로 `€#.###,#0[round]` 기본 적용 확인.

### numberMask_1.xml

- **XML 구조**: `validate_xml` `isValid:true`, errors 0 / warnings 0.
- **구성**: con 옵션 1개 — `con_numberMask`(dlt_numberMask itemset, `getValue()`, 미설정 포함 전수 10개). numberMask 는 `@dynamic N` 이라 con 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용(별도 재생성 버튼 없음). 검증 버튼 3개 : `표시값 확인`(9줄) / `원본 데이터값 확인`(1줄) / `numberMask 반영값 확인`(2줄). 편집모드 검증은 사용자 더블클릭 관찰(자동 버튼 없음).
- **Target 구성**: Header 1행 / Body 1행, Body Column 8개(col1 number / col2 float / col3 bigDecimal / col4 number+applyFormat=all+editFormat="" / col5 displayFormat="#,##0.000" / col6 displayFormatter / col7 dataType="text" / col8 inputType="textarea"), 데이터 5행(`-1234.5678`/`1234567.8912`/`1234.5`/`12`/`12AB34`) 동일값 바인딩, `style="width:1060px;height:210px;"`.
- **MCP 런타임 실측 (playwright): console error 0 / warning 0.** target1 생성 확인, `tr.grid_body_row` 5개 렌더, 셀 td textContent 와 `getCellDisplayData` 값 일치, 검증 버튼 3개 결과창 출력 정상(13줄), validation 19개 렌더.
- **Target 재생성 흐름**: con 을 `#,###.000` 으로 바꾸고 `btn_createTarget` 클릭 → `options.numberMask="#,###.000"`, row0 표시값 `-1,234.568` 로 갱신. 초기 `setSelectedIndex(1)` 로 `₩ #,###.#0[round] won`(@sample 마스크) 기본 적용 확인.
- **환경 메모**: 검증 시점에 Studio 내장 미리보기 서버(포트 53671)가 미기동이라 **standalone Tomcat9(포트 8084, webroot `C:\Program Files\Apache Software Foundation\Tomcat 9.0\webapps\unitTest`)** 를 기동해 검증. 해당 webroot 이 7/9 기준으로 stale 하여 `_wpack_/cm`, `_wpack_/frame`, `cm`, `frame` 및 본 샘플 xml/wpack js 를 소스에서 동기화함. wpack js 는 Studio 파일워처가 없어 `w-pack studio.js` 로 수동 생성.

### rupeeMask_1.xml

- **XML 구조**: `validate_xml` `isValid:true`, errors 0 / warnings 0.
- **구성**: con 옵션 1개 — `con_rupeeMask`(dlt_rupeeMask itemset, `getValue()`, 미설정 포함 전수 11개). rupeeMask 는 `@dynamic N` 이라 con 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용(별도 재생성 버튼 없음). 검증 버튼 3개 : `표시값 확인`(7줄) / `원본 데이터값 확인`(1줄) / `rupeeMask 반영값 확인`(2줄). 편집모드 검증은 사용자 더블클릭 관찰(자동 버튼 없음).
- **Target 구성**: Header 1행 / Body 1행, Body Column 6개(col1 rupee / col2 rupee+applyFormat=all+editFormat="" / col3 displayFormat="#,##0.000" / col4 displayFormatter / col5 dataType="number" / col6 inputType="textarea"), 데이터 5행(`-1234567.127`/`1234567890.8912`/`1234.5`/`12`/`12AB34`) 동일값 바인딩, `style="width:900px;height:210px;"`.
- **MCP 런타임 실측 (playwright, port 56746): console error 0 / warning 0.** target1 생성 확인, `tr.grid_body_row` 5개 렌더, 셀 td textContent 와 `getCellDisplayData` 값 완전 일치, 검증 버튼 3개 결과창 출력 정상(10줄), validation 19개 렌더.
- **Target 재생성 흐름**: con 을 `##,##,##,###.#0[round]` 로 바꾸고 `comp_init` 재실행 → `options.rupeeMask` 갱신, row1 표시값 `1,23,45,67,890.89` 로 갱신. 초기 `setSelectedIndex(1)` 로 `₹#,###.#0[round]`(@sample 마스크) 기본 적용 확인.
- **euro/number 와 다른 점**: 미설정 시 rupee 는 원본 문자열이 그대로 표시됨(`-1234567.127`, `12AB34` 유지). euro 처럼 `parseFloat` 절삭이 일어나지 않음.

### tengeMask_1.xml

- **XML 구조**: `validate_xml` `isValid:true`, errors 0 / warnings 0.
- **구성**: con 옵션 1개 — `con_tengeMask`(dlt_tengeMask itemset, `getValue()`, 미설정 포함 전수 11개. 마지막 항목 `# ###.#0[round]₸` 는 소수 구분자 `.` vs `,` 대조용). tengeMask 는 `@dynamic N` 이라 con 반영은 기존 `btn_createTarget`(→`comp_init`) 만 사용(별도 재생성 버튼 없음). 검증 버튼 3개 : `표시값 확인`(7줄) / `원본 데이터값 확인`(1줄) / `tengeMask 반영값 확인`(2줄). 편집모드 검증은 사용자 더블클릭 관찰(자동 버튼 없음).
- **Target 구성**: Header 1행 / Body 1행, Body Column 6개(col1 tenge / col2 tenge+applyFormat=all+editFormat="" / col3 displayFormat="# ##0,000" / col4 displayFormatter / col5 dataType="number" / col6 inputType="textarea"), 데이터 5행(`-1234,127`(@sample 입력값)/`1234567,8912`/`1234,5`/`12`/`12AB34`) 동일값 바인딩, `style="width:840px;height:210px;"`.
- **MCP 런타임 실측 (playwright2, port 56746): console error 0 / warning 0.** target1 생성 확인, 셀 td textContent 와 `getCellDisplayData` 값 완전 일치, 검증 버튼 3개 결과창 출력 정상(10줄).
- **@sample 재현**: `tengeMask="# ###,#0[floor]₸"` + 입력 `-1234,127` → `-1 234,12₸` 로 가이드 @sample 과 정확히 일치. 접미사 일반문자 `₸` 가 `[floor]` 오른쪽에 와도 정상 suffix 처리됨.
- **마스크 전수 실측** (col1 기준, 데이터 `-1234,127 / 1234567,8912 / 1234,5 / 12 / 12AB34`)
  | 마스크 | 표시값 |
  |---|---|
  | (미설정) | `-1234,127 \| 1234567,8912 \| 1234,5 \| 12 \| 12AB34` (포맷 미적용, 원본 유지) |
  | `# ###,#0[round]₸` | `-1 234,13₸ \| 1 234 567,89₸ \| 1 234,50₸ \| 12,00₸ \| 12NaN NaN34,00₸` |
  | `# ###,#0[floor]₸` | `-1 234,12₸ \| 1 234 567,89₸ \| 1 234,50₸ \| 12,00₸ \| 12NaN NaN34,00₸` |
  | `# ###,#0[ceil]₸` | `-1 234,13₸ \| 1 234 567,90₸ \| 1 234,50₸ \| 12,00₸ \| 12NaN NaN34,00₸` |
  | `# ###,##` | `-1 234,13 \| 1 234 567,89 \| 1 234,5 \| 12 \| 12NaN NaN34` (`#` 는 없으면 미표시) |
  | `# ###,000` | `-1 234,127 \| 1 234 567,891 \| 1 234,500 \| 12,000 \| ...` (`0` 는 없으면 0 채움 / 마스크 갯수만큼 3자리) |
  | `# ###` | `-1 234 \| 1 234 568 \| 1 235 \| 12 \| 12NaN NaN34` (소수 마스크 없으면 소수 미표시) |
  | `# ##0,00` | `-1 234,13 \| 1 234 567,89 \| 1 234,50 \| 12,00 \| ...` (정수 영역 `0` 자리채움은 일어나지 않음) |
  | `KZT# ###,00원` | `-KZT1 234,13원 \| KZT1 234 567,89원 \| ...` (좌/우 일반문자 유지, 부호는 prefix 앞) |
  | `# ###.#0[round]₸` | `-1 2340₸ \| 1 234 5680₸ \| 1 2350₸ \| 120₸ \| ...` (`.` 는 소수 구분자로 인식되지 않음) |
- **@spec4 편집모드 실측 (실제 더블클릭)**: 마스크 `# ###,#0[floor]₸` 에서 col1(applyFormat 기본) 편집 input value = `-1234,127`(원본), col2(applyFormat="all", editFormat="") 편집 input value = `-1 234,12₸`(포맷 적용). 가이드 일치. 편집 전후 DataList 원본값 5행 모두 불변.
- **공백 문자 실측**: 천단위 구분자는 **일반 스페이스 U+0020** (NBSP 아님). `1 234 567,89₸` 코드포인트 = `31,20,32,33,34,20,35,36,37,2C,38,39,20B8`. DOM textContent 와 `getCellDisplayData()` 값이 코드포인트까지 동일. XML 속성값 안의 공백도 그대로 유지되어 `options.tengeMask` 에 `# ###,#0[floor]₸` 로 반영됨.
- **가이드 불일치 1 (@spec5)**: 가이드는 "천단위 구분자는 ' '(Space), **소수점 구분자는 '.'**" 이라고 기술하나, 실제 소수 구분자는 **','** 이다. 엔진 근거 `cellInfo.js:759~764` (`dataType=="tenge"` → `defaultDelimiter=" "`, `defaultDecimalDelimiter=","`), `text.js:1035~1040` (`formatTengeCurrency` 기본값 동일). 같은 가이드의 @sample(`# ###,#0[floor]₸` → `-1 234,12₸`) 과 @propval 주 항목(마스크 문자 `,` 포함)은 `,` 쪽이 맞다. 즉 **@spec5 의 소수 구분자 표기만 오기**. validation 문구는 규칙에 따라 가이드 원문 유지.
- **가이드 불일치 2 (@propval 하위절)**: "`' '(Space), '.'` 마스크는 천단위 구분자와 소수점 위치로 표시됩니다" 의 `.` 도 동일한 오기. `.` 를 소수 마스크로 쓰면 소수부 인식 실패(precision 0) + 나머지 문자가 suffix 로 밀려 `-1 2340₸` 처럼 표시됨.
- **euro/number/rupee 와 다른 점**
  - 구분자 조합이 유일하게 `공백 + 쉼표` (euro `.`+`,` / number·rupee `,`+`.`).
  - 천단위 구분자가 공백이라 **마스크 왼쪽 일반문자에 공백을 넣으면 안 된다.** `KZT # ###,00원` 처럼 prefix 뒤에 공백을 두면 period 정규식 `[ ]+[#0]+` 이 `" #"` 를 먼저 잡아 그룹 자릿수가 1 로 깨진다(→ 샘플은 `KZT# ###,00원` 사용). euro/number/rupee 는 구분자가 공백이 아니라 이 함정이 없음.
  - 미설정 시 원본 문자열 그대로 표시(rupee 와 동일, euro 와 동일). 이 프로젝트 config.xml 에 tenge 노드가 없어 `config.xml tengeMask 설정값 : ''`.
  - NaN 표시는 euro/number/rupee 와 동일하게 전체 `NaN` 이 아니라 문자별 치환(`12AB34` → `12NaN NaN34,00₸`).

---

## gridView_P_disabledClass_1.xml (Body Column 단위 속성)

- **XML/스크립트 리뷰**: `grp_condition` 2단테이블 + `con_disabledClass` select1(빈값/`dc_class1`/`dc_class1 dc_class2`), `wf_body_sample` 재생성 버튼 재사용(별도 재생성 버튼 없음), 메소드 버튼 3개 + `클래스 확인` 버튼 1개. Column 별로 다른 속성을 부여해야 하므로 `createBasicGridView` 대신 `comp_init` 에서 gridView XML 문자열을 직접 조립해 `$p.dynamicCreate` 로 생성(noResultMessageClass 샘플과 동일 패턴).
- **Target 구성**: Header 1행 / Body 1행 4컬럼, `dlt_bind` 4행.
  - col1 : 렌더 시 `disabled="true"` + `disabledClass=<con>` (초기 렌더 적용 / 기본 클래스 병행 / 해제 검증 대상)
  - col2 : 렌더 시 `disabled="true"`, `disabledClass` 미설정 (기본 클래스 `w2grid_default_disabled` 검증)
  - col3 : 렌더 시 disabled 미설정 + `disabledClass=<con>` → `setColumnDisabled("col3", true)` 로 비활성화 (@spec1 대조)
  - col4 : 렌더 시 disabled 미설정 + `disabledClass=<con>` → `setCellDisabled(0, "col4", true)` 로 비활성화 (@spec1 대조, Cell 단위)
- **MCP 런타임 실측 (playwright, port 62734): console error 0 / warning 0.** `mf_target1` 생성 확인, 버튼 4개 실제 클릭 동작 정상, 결과창(`mf_wf_body_bottom_tbx_return`) 4줄 출력 정상.
- **con 전환 실측** (Cell(0,colN) `className` 중 disabled 관련 부분만 발췌)

  | con_disabledClass | col1 (렌더 disabled) | col2 (렌더 disabled, 미설정) | col3/col4 (비활성화 전) |
  |---|---|---|---|
  | (미설정, 빈값) | `w2grid_default_disabled` | `w2grid_default_disabled` | (없음) |
  | `dc_class1` | `dc_class1 w2grid_default_disabled` | `w2grid_default_disabled` | (없음) |
  | `dc_class1 dc_class2` | `dc_class1 dc_class2 w2grid_default_disabled` | `w2grid_default_disabled` | (없음) |

  복수 클래스 시 computed style 도 두 클래스가 모두 반영됨(`background-color: rgb(255,224,178)` = dc_class1, `font-style: italic` / `font-weight: 700` = dc_class2). `color` 는 엔진 disabled 폰트색(inline)이 우선해 dc_class2 의 `color` 는 가려짐.
- **메소드 실측** (con=`dc_class1` 기준)

  | 조작 | 결과 class | 가이드 |
  |---|---|---|
  | `setColumnDisabled("col3", true)` | col3 → `dc_class1 w2grid_default_disabled` | @spec1 "적용되지 않습니다" 와 **불일치** |
  | `setCellDisabled(0, "col4", true)` | col4 row0 → `dc_class1 w2grid_default_disabled` (row1 은 클래스 없음 = Cell 단위 정상) | @spec1 과 **불일치** |
  | `setRowDisabled(1, true)` (추가 실측) | row1 col3 → `dc_class1 w2grid_default_disabled` | @spec1 과 **불일치** |
  | `setColumnDisabled("col1", false)` | col1 → `dc_class1`, `w2grid_default_disabled` 모두 제거 | @spec3 일치 |
- **label1 staleness 수정**: `comp_init` 에서 `target1` 만 remove 하면 동일 cID 의 `label1` 이 살아있어 `$c.gcm.createLabel` 의 `dynamicCreate` 가 undefined 를 반환 → 라벨이 이전 con 값으로 고정됨. `if (typeof label1 !== 'undefined' && label1) label1.remove();` 추가로 해결(재생성 후 `[현재] disabledClass = ...` 갱신 확인, 라벨 DOM 중복 0).

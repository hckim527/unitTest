# calendar_M 보강 대조표

### calendar_M_setDisableAfterDate_1.xml

| API 가이드 항목 | 샘플 validation | 상태 |
|---------------|---------------|------|
| @param date<String:Y:-> 비활성화 기준 날짜를 "yyyyMMdd" 8자리 포맷의 문자열로 설정합니다. | (1) date(필수) 파라미터로 비활성화 기준 날짜를 'yyyyMMdd' 8자리 포맷의 문자열로 설정합니다. | OK |
| @param 비활성화된 날짜에는 "w2calendar_date_disable" CSS 클래스가 적용됩니다. | (2) 비활성화된 날짜에는 'w2calendar_date_disable' CSS 클래스가 적용됩니다. | OK |
| @spec setDisableAfterDate() 메소드로 설정된 값을 제거 하려면 enableAllDate() 메소드를 사용해야 합니다. | (3) setDisableAfterDate() 메소드로 설정된 값을 제거하려면 enableAllDate() 메소드를 사용해야 합니다. | OK |
| @spec calendarValueType=yearMonth 일때 setDisableAfterDate() 메소드로 제한 날짜를 설정할 수 있지만 컴포넌트에서 해당 년/월의 선택을 제한하지 않습니다. | (4) calendarValueType=yearMonth 일때 setDisableAfterDate() 메소드로 제한 날짜를 설정할 수 있지만 컴포넌트에서 해당 년/월의 선택을 제한하지 않습니다. | OK |
| @spec 컴포넌트에 설정된 기준 날짜 이후의 날짜에 취소선이 표시되며 해당 날짜는 선택할 수 없습니다. | (5) 컴포넌트에 설정된 기준 날짜 이후의 날짜에 취소선이 표시되며 해당 날짜는 선택할 수 없습니다. | OK |
| @spec 년/월 탐색은 제한하지 않습니다. | (6) 년/월 탐색은 제한하지 않습니다. | OK |
| @spec 날짜 선택 제한과 관련된 메소드는 중복으로 적용됩니다. | (7) 날짜 선택 제한과 관련된 메소드는 중복으로 적용됩니다. | OK |

## 결론
- API 가이드(@description, @param, @spec)의 모든 항목이 validation 7개에 1:1 매핑됨
- 필수여부 N param 없음 (date 는 필수 Y) → 기본값 동작 validation 불필요
- enum param 없음 → enum 분리 validation 불필요
- 누락/추가 없음, 분리 샘플 불필요 (_1 만)

### calendar_M_setSkin_1.xml

| API 가이드 항목 | 샘플 validation | 상태 |
|---------------|---------------|------|
| @description Calendar 컴포넌트의 calendarClass 속성을 설정합니다. | (description) [calendar > M > setSkin] Calendar 컴포넌트의 calendarClass 속성을 설정합니다. | OK |
| @param skin<String:Y:-> 컴포넌트의 요일에 적용될 스킨 클래스명을 설정합니다. | (1) skin(필수) 파라미터로 컴포넌트의 요일에 적용될 스킨 클래스명을 String 으로 설정합니다. | OK |
| @param "class1" : 팝업 달력의 요일이 세자리 영단어 대문자("SUN","MON")로 표시 | (2) 'class1' 설정 시 팝업 달력의 요일이 세자리 영단어 대문자('SUN','MON')로 표시됩니다. | OK |
| @param "class2" : 팝업 달력의 요일이 한자리 한글("일","월")로 표시 | (3) 'class2' 설정 시 팝업 달력의 요일이 한자리 한글('일','월')로 표시됩니다. | OK |
| @param "class3" : 팝업 달력의 요일이 두자리 영단어("Su","Mo")가 볼드체로 표시 | (4) 'class3' 설정 시 팝업 달력의 요일이 두자리 영단어('Su','Mo')가 볼드체로 표시됩니다. | OK |
| @param "class4" : 팝업 달력의 요일이 두자리 영단어("Su","Mo")로 표시 | (5) 'class4' 설정 시 팝업 달력의 요일이 두자리 영단어('Su','Mo')로 표시됩니다. | OK |
| @param "class5" : 팝업 달력의 요일이 배경색이 있는 한자리 한글("일","월")로 표시 | (6) 'class5' 설정 시 팝업 달력의 요일이 배경색이 있는 한자리 한글('일','월')로 표시됩니다. | OK |
| @param 위 5개 값 이외의 값을 설정하면 스킨이 변경되지 않습니다. | (7) 위 5개 값 이외의 값을 설정하면 스킨이 변경되지 않습니다. | OK |
| @spec calendarClass 속성값에 관계없이 토요일은 파란색, 일요일은 빨간색으로 표시됩니다. | (8) calendarClass 속성값에 관계없이 토요일은 파란색, 일요일은 빨간색으로 표시됩니다. | OK |
| @spec displayDayType=text 이면 컴포넌트의 요일이 한자리 한글("일","월") 텍스트로 표시됩니다. | (9) displayDayType=text 이면 컴포넌트의 요일이 한자리 한글('일','월') 텍스트로 표시됩니다. | OK |

## 결론 (setSkin)
- API 가이드(@description, @param, @spec)의 모든 항목이 validation 9개에 1:1 매핑됨
- 필수여부 N param 없음 (skin 은 필수 Y) → 기본값 동작 validation 불필요
- skin enum 값(class1~5 + invalid 값) 모두 validation 분리 및 UI 케이스(con_skinCase) 구성 완료
- @spec 2건(토/일 색상, displayDayType=text) 도 별도 validation + UI(con_displayDayType) 케이스 구성
- 누락/추가 없음, 분리 샘플 불필요 (_1 만)

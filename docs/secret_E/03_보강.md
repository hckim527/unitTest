
### secret_E_onblur_1.xml / secret_E_onblur_2.xml (2026-09-22)

분리 사유: 발생 조건 축(_1: con_disabled, con_readOnly)과 처리 순서 축(_2: validator/ref/onFocusClass 고정 전제, con 없음)은 서로 다른 검증 축. description 은 두 샘플 동일.
wrap 불가: common_E_onblur_1 은 con_tabIndex/con_tagname 만 있어 disabled/readOnly/validator/ref/onFocusClass 전제를 부모가 추가할 수 없음 -> 독립 샘플.

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트가 포커스를 잃을 때 발생합니다. | _1 (1) Secret 컴포넌트가 포커스를 잃을 때 onblur 이벤트가 발생합니다. | ✓ |
| @param e FocusEvent 객체 (e.* 개별 속성) | _1 (2) 이벤트 핸들러에 FocusEvent 객체 e 가 전달됩니다. (개별 속성은 규칙상 비대상) | ✓ |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, gridView_E_onkeydown_1 선례) | 제외 |
| @spec disabled=true 또는 readOnly=true 인 경우 onblur 미발생 | _1 (3) 원문 | ✓ |
| @spec 하위절 • validator 반환값 적용 | _2 (1) validator 속성의 사용자 정의 함수 반환값이 적용된 후 onblur 이벤트가 발생합니다. | ✓ |
| @spec 하위절 • ref DataCollection 에 입력값 적용 | _2 (2) ref 속성으로 바인딩한 DataCollection 에 입력값이 적용된 후 onblur 이벤트가 발생합니다. | ✓ |
| @spec 하위절 • onFocusClass 클래스 제거 | _2 (3) onFocusClass 속성에 의해 추가된 클래스가 제거된 후 onblur 이벤트가 발생합니다. | ✓ |

엔진 근거: `uiplugin/input/input.js:7148 handleBlurEventBasic` — readOnly && !readOnlyFocusEvent 면 return -> fireEvent("oneditend")(=`_setAction` 의 validateString(사용자 함수) + setValueNM(모델 반영)) -> removeClass(onFocusClass) -> onchange/onviewchange -> onblur. 가이드 순서와 일치.
가이드-엔진 참고: secret 가이드는 readOnly=true 시 무조건 미발생으로 기술하나 엔진은 `readOnlyFocusEvent`(secret 가이드 미기재 옵션, 기본 false) 가 true 면 발생시킴. 가이드 미기재 옵션이라 validation 미추가.

### secret_E_onchange_1.xml / _2.xml / _3.xml (2026-09-22)

분리 사유: 발생 조건+info(_1: par_value + setValue), ref 바인딩 데이터 선택 변경(_2: 고정 ref + 행 선택 버튼), 이벤트 발생 순서(_3: 사용자 조작만, 버튼 없음)는 각각 다른 검증 축. description 은 3개 샘플 동일.
wrap 불가: common_E_onchange_1/_2 는 validation 1건 구판이고 secret 가이드에 없는 con_enterBlur / oneditenter / oneditkeyup 을 포함 -> 독립 샘플.

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description 입력값이 변경된 후 포커스가 해제될때 발생 | _1 (1) | ✓ |
| @description 의 추가 문장(| 이어짐) setValue() 로 변경 시 포커스와 관계없이 발생 | _1 (2) 원문 | ✓ |
| @param info | _1 (3) | ✓ |
| @param info.oldValue 평문 문자열 | _1 (4) | ✓ |
| @param info.newValue 평문 문자열 | _1 (5) | ✓ |
| @spec 입력값에 변경이 없으면 미발생 | _1 (6) 원문 (사용자 조작 + 같은 값 setValue 로 확인) | ✓ |
| @spec 입력값이 변경된 상태로 Enter 입력 시 발생 | _1 (7) 원문 | ✓ |
| @spec ref DataCollection 바인딩 시 데이터 선택이 변경되면 발생 | _2 (1) 원문 | ✓ |
| @spec 의 추가 문장(| 이어짐) 이때, 변경된 데이터값이 같으면 미발생 | _2 (2) 원문 (_2 (1) 바로 다음에 두어 문맥 유지) | ✓ |
| @spec 사용자 입력에 의한 변경만 처리하려면 onviewchange 사용 | _1 (8) 원문 (onviewchange 는 대비 출력만) | ✓ |
| @spec 마우스 포커스 해제 순서 | _3 (1) | ✓ |
| @spec Tab 포커스 해제 순서 | _3 (2) | ✓ |
| @spec Enter 입력 순서 | _3 (3) | ✓ |

엔진 근거: `uiplugin/input/input.js` — blur 시 `handleBlurEventBasic`(7203 onchange -> 7212 onviewchange -> onblur), setValue 는 4398 `oldValue != _getValue()` 일 때만 onchange, Enter 는 keyup 처리(8300)에서 enterBlur 기본값 true 이면 `this.blur()` 호출 -> onkeyup 뒤에 onchange > onviewchange > onblur. secret 가이드에 enterBlur 가 없지만 기본값 true 로 Enter 시 onblur 까지 발생하며 가이드 순서와 실측이 일치함.

### secret_E_onclick_1.xml (2026-09-23)

분리 없음: 가이드 항목이 4건(발생 / e / 순서 / disabled·readOnly)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플. (secret_E_onblur_1 과 동일 구성)
wrap 불가: `common_E_onclick_1.xml` 은 target1(enabled) / target2(disabled) 고정 구조에 onclick·onmousedown·onmouseup 핸들러만 정의된 구판(validation 3건)이고, grp_condition 이 비어 있어 부모가 con 을 추가할 수 없음. 2.0 가이드의 순서 항목에 포함된 onfocus 와 readOnly=true 발생 축을 담을 수 없어 독립 샘플로 작성.

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트를 마우스 왼쪽 버튼으로 클릭할 때 발생합니다. | (1) Secret 컴포넌트를 마우스 왼쪽 버튼으로 클릭할 때 onclick 이벤트가 발생합니다. | ✓ |
| @param e PointerEvent 객체 | (2) 이벤트 핸들러에 PointerEvent 객체 e 가 전달됩니다. | ✓ |
| @param e.altKey / e.bubbles / e.button / e.buttons / e.cancelable / e.clientX / e.clientY / e.ctrlKey / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.metaKey / e.movementX / e.movementY / e.offsetX / e.offsetY / e.pageX / e.pageY / e.relatedTarget / e.screenX / e.screenY / e.shiftKey / e.target / e.timeStamp / e.type / e.view / e.which / e.x / e.y (31건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증) | 제외 |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 선례) | 제외 |
| @spec Secret 컴포넌트를 클릭하면 "onmousedown > onfocus > onmouseup > onclick" 순서로 이벤트가 발생합니다. | (3) 원문 (큰따옴표는 규칙상 작은따옴표로 대체) | ✓ |
| @spec disabled=true 인 경우 onclick 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onclick 이벤트가 발생합니다. | (4) 원문 | ✓ |
| @related disabled / readOnly / ondblclick / onmousedown / onmouseup | - (@related 자체 기능은 검증 대상 아님. onmousedown/onmouseup 은 @spec 순서 확인용 출력으로만 바인딩, ondblclick 은 미바인딩) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. 순서는 onmousedown/onfocus/onmouseup/onclick 각 핸들러가 발생 즉시 `▶ 이벤트명` 한 줄을 출력해 출력 순서 = 실제 발생 순서로 확인(자동 실행 버튼 없이 사용자 클릭). onclick 줄에 현재 disabled/readOnly 조건을 같이 실어 중복 줄을 없앴고, e 는 `e : [object PointerEvent]` 한 줄.

엔진 근거: secret 은 구현이 `uiplugin/input/input.js` (secret.js 는 주석 전용). 클릭 이벤트는 `input.js:3138 systemEvents` 에 onclick/onmousedown/onmouseup 이 등록되어 DOM 이벤트로 그대로 전달되고, disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 마우스 이벤트 자체를 발생시키지 않는다. readOnly 는 `readonly="readonly"` 만 붙어 마우스 이벤트는 정상 발생. 포커스 이벤트만 `input.js:7385 if (this.getReadOnly() && !this.options.readOnlyFocusEvent) return;` 로 차단된다(`readOnlyFocusEvent` 기본 false, `input.js:3189`).

가이드-엔진 불일치(참고):
- secret 가이드 @spec 은 순서를 `"onmousedown > onfocus > onmouseup > onclick"` 로 onfocus 를 무조건 발생하는 것처럼 기술하나, onfocus 는 **컴포넌트가 포커스를 갖고 있지 않을 때만** 발생한다(이미 포커스가 있는 상태의 재클릭 / readOnly=true 는 onfocus 없이 onmousedown > onmouseup > onclick). 동일 템플릿의 input 가이드(`uiplugin/input/input.js:245`)는 `"onmousedown > [onfocus >] onmouseup > onclick"` 으로 **대괄호 조건 표기**를 쓰고 있어 secret 가이드 쪽에 대괄호가 누락된 것으로 보인다. validation 문구는 원문 유지.

### secret_E_ondblclick_1.xml (2026-09-23)

분리 없음: 가이드 항목이 4건(발생 / e / 순서 / disabled·readOnly)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플. (secret_E_onclick_1 과 동일 기준)
wrap 불가: `common_E_ondblclick_1.xml` 은 description 이 `[common > E > onclick]` 으로 남아 있는 구판(validation 2건, disabled 항목은 주석 처리)이고 target1(enabled)/target2(disabled) 고정 구조에 grp_condition 이 비어 있어 부모가 con 을 추가할 수 없음. 2.0 가이드의 7단계 순서 항목과 readOnly=true 발생 축을 담을 수 없어 독립 샘플로 작성. (`input_E_ondblclick_1.xml` 도 이 구판을 wrap 하는 형태)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트를 마우스 왼쪽 버튼으로 더블 클릭할 때 발생합니다. | (1) Secret 컴포넌트를 마우스 왼쪽 버튼으로 더블 클릭할 때 ondblclick 이벤트가 발생합니다. | ✓ |
| @param e PointerEvent 객체 | (2) 이벤트 핸들러에 PointerEvent 객체 e 가 전달됩니다. | ✓ |
| @param e.altKey / e.bubbles / e.button / e.buttons / e.cancelable / e.clientX / e.clientY / e.ctrlKey / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.metaKey / e.movementX / e.movementY / e.offsetX / e.offsetY / e.pageX / e.pageY / e.relatedTarget / e.screenX / e.screenY / e.shiftKey / e.target / e.timeStamp / e.type / e.view / e.which / e.x / e.y (31건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증) | 제외 |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / secret_E_onclick_1 선례) | 제외 |
| @spec Secret 컴포넌트를 더블 클릭하면 "onmousedown > onmouseup > onclick > onmousedown > onmouseup > onclick > ondblclick" 순서로 이벤트가 발생합니다. | (3) 원문 (큰따옴표는 규칙상 작은따옴표로 대체) | ✓ |
| @spec disabled=true 인 경우 ondblclick 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 ondblclick 이벤트가 발생합니다. | (4) 원문 | ✓ |
| @related disabled / readOnly / onclick / onmousedown / onmouseup | - (@related 자체 기능은 검증 대상 아님. onmousedown/onmouseup/onclick/onfocus 는 @spec 순서 확인용 출력으로만 바인딩) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. 순서는 onmousedown/onfocus/onmouseup/onclick/ondblclick 각 핸들러가 발생 즉시 `▶ 이벤트명` 한 줄을 출력해 출력 순서 = 실제 발생 순서로 확인(자동 실행 버튼 없이 사용자 더블 클릭). ondblclick 줄에 현재 disabled/readOnly 조건을 같이 실어 중복 줄을 없앴고, e 는 `e : [object MouseEvent]` 한 줄. secret_E_onclick_1 과의 차이는 축(ondblclick)·핸들러 1개 추가(target1_ondblclick)·onclick 줄에서 조건 표기 제거뿐.

엔진 근거: secret 은 구현이 `uiplugin/input/input.js` (secret.js 는 주석 전용). ondblclick 은 `input.js` systemEvents 에 등록되어 DOM dblclick 이 그대로 전달되고, disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 마우스 이벤트 자체를 만들지 않는다. readOnly 는 `readonly="readonly"` 만 붙어 마우스 이벤트는 정상 발생하고, 포커스 이벤트만 `input.js:7385 if (this.getReadOnly() && !this.options.readOnlyFocusEvent) return;` 로 차단된다.

가이드-엔진 불일치(참고):
- **e 객체 타입**: 가이드 @param 은 "PointerEvent 객체가 전달됩니다" 라고 기술하나, 실측 dblclick 이벤트의 생성자는 **MouseEvent** 다(UI Events 스펙상 PointerEvent 로 올라오는 것은 click/auxclick/contextmenu 뿐). 같은 페이지의 click 은 PointerEvent 로 확인되어 브라우저 차이가 아니라 이벤트 종류 차이. 동일 템플릿의 input 가이드(`uiplugin/input/input.js:254`)도 동일 문구라 템플릿 차원 오기로 보인다. validation 문구는 원문 유지.
- **순서의 onfocus 누락**: 가이드 순서 문구는 7단계뿐이나, 컴포넌트가 포커스를 갖고 있지 않은 상태에서 더블 클릭하면 실제로는 첫 `onmousedown` 직후 `onfocus` 가 1회 발생한다(`onmousedown > onfocus > onmouseup > onclick > onmousedown > onmouseup > onclick > ondblclick`). onclick 가이드는 input 판에서 `[onfocus >]` 대괄호 조건 표기를 쓰는데(`input.js:245`), **ondblclick 가이드는 secret/input 양쪽 모두 대괄호 표기조차 없다**(`secret.js:209`, `input.js:292`). readOnly=true 또는 이미 포커스가 있는 상태에서는 onfocus 없이 7단계 그대로. validation 문구는 원문 유지.

#### secret_E_ondblclick_1.xml 보강 (2026-09-23, onfocus 제거)

ondblclick 가이드 @spec 의 순서 문구에는 onfocus 가 없다(7단계). 그런데 샘플이 secret_E_onclick_1(가이드 순서에 onfocus 가 포함된 이벤트)의 바인딩을 그대로 이어받아 onfocus 를 출력하고 있었다 -> validation (3) 원문 7단계 대비 결과창이 8줄이 되어 어긋남. onfocus 는 ondblclick 의 검증 대상이 아니므로(다른 기능의 유효성은 대상 샘플에서 제외) 바인딩·핸들러·라벨 문구에서 제거했다.

변경 내역:
- `createEList(["onmousedown", "onfocus", "onmouseup", "onclick", "ondblclick"])` -> `createEList(["onmousedown", "onmouseup", "onclick", "ondblclick"])`
- `scwin.target1_onfocus` 핸들러 삭제
- 상단 주석 `onmousedown, onfocus, onmouseup, onclick 은 발생 순서 확인용...` -> `onmousedown, onmouseup, onclick 은 발생 순서 확인용...`
- createLabel 정리: [절차] 에서 `바깥 영역을 클릭해 포커스를 해제한 뒤` 삭제(onfocus 미바인딩이라 포커스 상태가 출력 순서에 영향 없음), [확인] 의 onfocus 안내 문장 삭제 후 가이드 7단계 순서를 괄호로 명시 -> `[확인] 결과창에 출력된 이벤트 줄의 순서(onmousedown > onmouseup > onclick > onmousedown > onmouseup > onclick > ondblclick)와 ondblclick 줄의 e 객체 (각 이벤트는 발생 즉시 한 줄씩 출력됩니다)`
- 미변경: validation 4건(원문 유지), description, con_disabled / con_readOnly 구성, 그 외 구조

가이드-엔진 갭 기록 유지: 위 "순서의 onfocus 누락" 항목(포커스가 없던 상태의 실제 DOM 이벤트에는 첫 onmousedown 직후 onfocus 가 1회 발생하며, 가이드는 secret/input 양쪽 모두 `[onfocus >]` 대괄호 표기조차 없음)은 **가이드 기술 갭 기록으로 그대로 남긴다**. 다만 이는 onfocus 이벤트 샘플에서 다룰 축이라 ondblclick 샘플에서는 출력하지 않는다.

재실측(MCP, 포트 61613): 3종 조건 모두 가이드 7단계와 정확히 일치. 상세는 02_검증.md 의 "수정 후 재검증" 표 참고.

#### secret_E_onclick_1.xml / secret_E_ondblclick_1.xml 라벨 간소화 (2026-09-23)

사용자 지시: 단순 샘플(조작이 조건 선택 + 클릭 1종, 확인 포인트가 결과창 한 곳)에는 `[전제]/[절차]/[확인]` 머리표지가 과하다 -> 머리표지를 빼고 조작 1줄 + 확인 1줄로 압축. UT_01_생성.md §5-1 의 머리표지 구성은 권장 사항이므로 항목이 적은 샘플은 생략 가능. 두 샘플의 라벨 톤을 동일하게 맞춤(1줄: 조작 흐름, 2줄: 출력 방식 및 주의).

변경 내역 (createLabel 인자만 교체, 그 외 스크립트·validation·description·con 구성 무변경):
- secret_E_onclick_1: `[전제] target1 은 Secret 컴포넌트이며 … \n[절차] … \n[확인] …` -> `조건 선택 후 Target 재생성 -> 결과창 clear -> 바깥 영역을 클릭해 포커스를 해제한 뒤 target1 을 마우스 왼쪽 버튼으로 클릭합니다.\n각 이벤트는 발생 즉시 한 줄씩 출력되며, target1 이 이미 포커스를 가진 상태에서 다시 클릭하면 onfocus 줄은 출력되지 않습니다.`
- secret_E_ondblclick_1: `[전제] … \n[절차] … \n[확인] … 순서(onmousedown > … > ondblclick) …` -> `조건 선택 후 Target 재생성 -> 결과창 clear -> target1 을 마우스 왼쪽 버튼으로 더블 클릭합니다.\n각 이벤트는 발생 즉시 한 줄씩 출력됩니다.`
  - 기존 [확인] 에 있던 7단계 순서 나열은 validation (3) 원문과 중복이라 삭제(라벨은 validation 에 없는 운영 정보만).
- "target1 은 Secret 컴포넌트" 같은 자명한 전제와 "disabled, readOnly 는 위 조건에서 선택" 은 화면 구성만 봐도 드러나므로 삭제.
- onclick 의 onfocus 조건부 출력 안내는 가이드 @spec 순서(`onmousedown > onfocus > onmouseup > onclick`)와 실제 동작 차이를 사용자가 오인하지 않도록 **유지**(아래 "순서의 onfocus 누락" 갭 기록과 연결).

**onfocus 제거 지시는 철회됨**: 2026-09-23 중 secret_E_onclick_1 의 onfocus 바인딩 제거 지시가 있었으나 사용자가 번복(onclick 가이드 @spec 순서에는 onfocus 가 명시돼 있어 유지가 맞음). 착수 전 취소되어 파일 변경은 없고, `createEList(["onmousedown","onfocus","onmouseup","onclick"])` / `scwin.target1_onfocus` / 상단 주석은 그대로다. (secret_E_ondblclick_1 의 onfocus 제거는 가이드 순서에 onfocus 가 없는 별개 건으로 유효)

기존 가이드-엔진 갭 기록(순서의 onfocus 누락, e 객체 타입 불일치)은 그대로 유지.

재실측(MCP, 포트 61613): 라벨 렌더 정상(`\n`->`<br>`), onclick 1회 클릭 4줄+e / ondblclick 7줄+e 로 라벨 교체 전과 동일. 상세는 02_검증.md 참고.

---

## 테스트 정보 라벨 제거 (2026-09-23, secret_E_onclick_1 / secret_E_ondblclick_1)

사용자 지시: "단순 샘플은 라벨(테스트정보 기재된) 미생성 해도 될거같아". 직전 작업(머리표지 제거 -> 1~2줄 압축)의 연장선에서, 압축해도 여전히 validation·con_ 이름의 되풀이뿐인 단순 샘플은 라벨 자체를 만들지 않기로 확정.

생략 기준 충족 여부 (UT_01_생성.md "라벨 생성 여부" / memory `feedback_simple_sample_skip_info_label`):
- ① 조작이 자명함: 조건 선택 -> Target 재생성 -> target1 클릭(또는 더블클릭) 뿐. O
- ② 숨은 전제·고정 설정 없음: disabled/readOnly 는 화면 상단 조건 테이블에 그대로 노출. O
- ③ 라벨 내용이 추가 운영 정보 없이 validation 문구/조건명과 중복. O (onclick 의 "onfocus 줄 조건부 미출력" 안내도 validation (3) + 아래 가이드 갭 기록으로 이미 커버)

변경 내역 (두 파일 공통, 그 외 무변경):
- `comp_init` 에서 `$c.gcm.createLabel(...)` 호출 삭제.
- 더 이상 생성되지 않는 `label1` 의 `if (typeof label1 !== 'undefined') label1.remove();` 분기 삭제, 주석 "기존 컴포넌트 있으면 삭제" -> "기존 target 있으면 삭제" 로 정리 (라벨 없는 기존 샘플 secret_M_trigger_1 등과 동일한 형태).
- **유지**: validation 4건, description, con_disabled / con_readOnly, `createEList` 이벤트 목록, 전체 이벤트 핸들러. 특히 secret_E_onclick_1 의 `onfocus` 바인딩/핸들러는 onclick 가이드 @spec 순서(`onmousedown > onfocus > onmouseup > onclick`)에 포함되므로 그대로 둔다.

기존 가이드-엔진 갭 기록(onclick @spec 순서의 onfocus 관련 기술, ondblclick e 객체 타입 MouseEvent 불일치)은 모두 유지.

재실측(MCP, 포트 61613): `#mf_label1` 부재 확인, target1 재생성 정상, onclick 1회 클릭 4줄+e / ondblclick 1회 더블클릭 7줄+e 로 라벨 제거 전과 동일. console error 0. 상세는 02_검증.md 참고.

### secret_E_onfocus_1.xml (2026-09-23)

분리 없음: 가이드 항목이 4건(발생 / e / disabled·readOnly 미발생 / onFocusClass 추가 후 발생)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플. 짝인 onblur 는 처리 순서 축이 3건(validator / ref / onFocusClass)이라 `_2` 로 분리했으나, onfocus 의 순서 축은 onFocusClass 1건뿐이라 `_1` 안에 고정 전제로 흡수하는 편이 조작이 단순하다.
wrap 불가: `common_E_onfocus_1.xml` 은 objParent 위임 구판(validation 2건: Tab / 클릭)이고 grp_condition 에 con_tabIndex / con_tagname 만 있어 disabled / readOnly / onFocusClass 전제를 부모가 추가할 수 없음 -> 독립 샘플. (secret_E_onblur_1 / secret_E_onclick_1 과 동일 판단)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트가 포커스를 받을 때 발생합니다. | (1) Secret 컴포넌트가 포커스를 받을 때 onfocus 이벤트가 발생합니다. | ✓ |
| @param e FocusEvent 객체 | (2) 이벤트 핸들러에 FocusEvent 객체 e 가 전달됩니다. | ✓ |
| @param e.bubbles / e.cancelable / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.relatedTarget / e.target / e.timeStamp / e.type / e.view (12건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증) | 제외 |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / onclick_1 / ondblclick_1 선례) | 제외 |
| @spec disabled=true 또는 readOnly=true 인 경우 이벤트가 발생하지 않습니다. | (3) disabled=true 또는 readOnly=true 인 경우 onfocus 이벤트가 발생하지 않습니다. | ✓ |
| @spec onFocusClass 속성에 의해 클래스가 추가된 후 onfocus 이벤트가 발생합니다. | (4) 원문 | ✓ |
| @related disabled / onFocusClass / readOnly / onblur | - (@related 자체 기능은 검증 대상 아님. onFocusClass 는 (4) 의 전제로만 고정 설정하고 미설정 대비·기본 w2input_focus 동작은 secret_P_onFocusClass_1 에서 다룸) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `onFocusClass="onFocusClass"`(tc.css 배경 노랑) 고정. 출력은 `comp_init` 말미의 기준선 1줄(`재생성 직후 class : … / bg : …`) + onfocus 핸들러 2줄(조건·발생·class·bg / `e : …`). 기준선과 핸들러 시점 class 를 대비해야 "추가된 후" 판정이 서므로 기준선 줄을 남겼다. disabled/readOnly 케이스는 판정이 성립하는 줄이 없어 onfocus 줄 자체가 출력되지 않는 것으로 판정(secret_E_onblur_1 과 동일).

라벨: **생성**. 생략 기준(UT_01_생성.md "라벨 생성 여부") 중 ② 숨은 전제·고정 설정 없음 을 충족하지 못한다 — `onFocusClass` 가 화면 con 에 노출되지 않는 고정 전제이고, validation 4 의 확인 방법(재생성 직후 줄 ↔ onfocus 줄 class 대비)이 화면만 봐서는 드러나지 않는다. 머리표지 없이 2줄(고정 전제+확인 방법 / 조작 절차)로 압축. (onclick_1 / ondblclick_1 은 고정 전제가 없어 라벨을 제거한 것과 대비)

엔진 근거: secret 구현은 `uiplugin/input/input.js` (secret.js 는 주석 전용). `input.js:7380 handleFocusValueEventAdv` — `7385 if (this.getReadOnly() && !this.options.readOnlyFocusEvent) return;` (readOnly 차단) → `7394 fireEvent("onbeforeedit")` → `7427 this.addClass(this.render, this.options.onFocusClass)` → `7618 event.fireEvent(this, "onfocus", e)`. **클래스 추가가 onfocus fire 보다 앞** 이므로 가이드 @spec 과 일치. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 포커스 자체를 주지 않는다(실측 `document.activeElement.id` 빈값).

가이드-엔진 불일치(참고):
- **disabled 와 readOnly 의 미발생 기전이 다르다.** 가이드 @spec 은 두 조건을 한 문장으로 묶었으나, disabled=true 는 포커스 자체가 들어가지 않고(activeElement 가 target1 이 아님), readOnly=true 는 **포커스는 정상 진입하되 엔진이 `input.js:7385` 에서 return 하여 이벤트만 차단**한다(실측 activeElement = `mf_target1`). 결과(이벤트 미발생)는 같아 validation 문구는 원문 유지.
- **readOnlyFocusEvent 옵션 미기재**: 엔진은 `readOnlyFocusEvent`(기본 false, `input.js:3189`)가 true 면 readOnly=true 에서도 onfocus 를 발생시키나 secret 가이드에 해당 옵션 기재가 없다. 가이드 미기재 옵션이라 validation 미추가(secret_E_onblur_1 과 동일 판단).
- **readOnly=true 에서 onFocusClass 도 무시**: 실측 class 가 `w2input  w2input_readonly` 로 onFocusClass 가 붙지 않는다. onfocus @spec 에는 언급이 없고 `onFocusClass` 가이드 쪽(`secret_P_onFocusClass_1` (6))에 이미 있는 항목이라 본 샘플에는 추가하지 않는다.

---

## 테스트 정보 라벨 판정 (2026-09-23, secret_E 담당 에이전트)

기준: UT_01_생성.md '라벨 생성 여부' 절 + feedback_simple_sample_skip_info_label (①조작 자명 ②숨은 전제 없음 ③validation/con_ 중복뿐 — 셋 다 충족 시에만 삭제)
추가 제약: TSQA 스크립트가 `#mf_label1` 을 "바깥 영역 클릭" 타깃으로 사용하는 샘플은 삭제 금지.

| 파일 | 판정 | 사유 | TSQA(mf_label1) 참조 |
|---|---|---|---|
| secret_E_onblur_1.xml | 삭제 | 라벨 3줄이 con_disabled/con_readOnly 이름과 validation 문구 반복뿐, 절차(조건 선택→재생성→클릭→바깥 클릭)가 자명, 고정 속성 없음 | 없음 (outsideLocator = `#mf_grp_condition`) |
| secret_E_onblur_2.xml | 유지 | 화면 con_ 에 없는 고정 전제 validator=scwin.fn_validator(입력값+_v) / ref=data:gdm_ref.key1 / onFocusClass(배경 노랑) + 초기값 secret 이 마스킹돼 화면 확인 불가 | 있음 (ts_secret_E_onblur_2:53) |
| secret_E_onchange_1.xml | 유지 | 초기값 secret 마스킹 전제 + 4갈래 비자명 절차(값 변경 없음은 원래 값으로 되돌리기, setValue 는 포커스 미부여 상태에서·현재 값과 같은 value 도 시도) | 있음 (ts_secret_E_onchange_1:40) |
| secret_E_onchange_2.xml | 유지 | ref=data:dlt_ref.label 고정 + 픽스처 구성(행0 data_A / 행1·행2 data_B 동일값)과 재생성 시 미선택 초기화가 화면에 안 드러남 | 없음 |
| secret_E_onchange_3.xml | 유지 | 초기값 secret 마스킹 전제 + "값 입력→결과창 clear→마우스/Tab/Enter 중 하나로 해제" 라는 화면만으로 알 수 없는 절차 | 있음 (ts_secret_E_onchange_3:50) |
| secret_E_onfocus_1.xml | 유지 | onFocusClass=onFocusClass(tc.css 배경 노랑) 고정이 화면 con_ 에 없음 + 재생성 직후 class 줄과 onfocus 시점 class 줄 비교라는 확인 방법 | 없음 |

### MCP 확인 (포트 61613, 세션 1)
- secret_E_onblur_1 : 라벨 textbox 소멸 확인, Target 재생성 1회 정상, target1 클릭 → `#mf_grp_condition` 클릭으로 포커스 해제 시 결과창 `disabled 미설정 / readOnly 미설정 -> onblur 이벤트 발생 / e : [object FocusEvent]` 출력(기존 TSQA 기대값과 동일). console error 0 (password field not in form 안내 verbose 2건만).

### secret_E_onkeydown_1.xml (2026-09-23)

분리 없음: 가이드 항목이 7건이고 검증 축이 con_disabled / con_readOnly 2개뿐이며, 나머지(연속 발생 / onkeypress 순서 / 다국어 / getValue 시점)는 모두 같은 화면에서 키 입력 방식만 바꿔 확인되는 단일 축 -> 단일 샘플. (secret_E_onclick_1 과 동일 구성)
wrap 불가: `common_E_onkeydown_1.xml` 은 구판(validation 5건)으로 secret 2.0 가이드에 없는 항목(자식 -> 부모 버블링, dataType 입력 제한)을 담고 있고 getValue 시점 / disabled·readOnly 축이 없다. grp_condition 이 con_dataType 전용이라 부모가 con_disabled / con_readOnly 를 추가할 수 없어 독립 샘플로 작성.

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트 영역에서 키보드의 키를 누를때 발생합니다. | (1) Secret 컴포넌트 영역에서 키보드의 키를 누를때 onkeydown 이벤트가 발생합니다. | ✓ |
| @param e KeyboardEvent 객체 | (2) 이벤트 핸들러에 KeyboardEvent 객체 e 가 전달됩니다. | ✓ |
| @param e.altKey / e.bubbles / e.cancelable / e.code / e.composed / e.ctrlKey / e.defaultPrevented / e.detail / e.eventPhase / e.isComposing / e.isTrusted / e.key / e.location / e.metaKey / e.repeat / e.shiftKey / e.target / e.timeStamp / e.type / e.view (20건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증. 단 문자키/다국어 구분에 필요한 `e.key` 값만 출력에 동반 표기) | 제외 |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / secret_E_onclick_1 선례) | 제외 |
| @spec onkeydown 이벤트 발생 시점에는 눌린 키가 입력 영역에 반영되지 않은 상태이므로 이벤트에서 getValue() 메소드 사용 시 이전 입력값이 반환됩니다. | (3) 원문 | ✓ |
| @spec 키를 누르고 있으면 onkeydown 이벤트가 연속적으로 발생합니다. | (4) 원문 | ✓ |
| @spec 문자에 해당하는 키 입력시 onkeydown 이벤트가 발생한 후 onkeypress 이벤트가 발생합니다. | (5) 원문 | ✓ |
| @spec 하위절(\|) 웹브라우저에 따라 다국어 입력 시 onkeypress 이벤트가 발생하지 않을 수 있습니다. | (6) 원문 | ✓ |
| @spec disabled=true 인 경우 onkeydown 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onkeydown 이벤트가 발생합니다. | (7) 원문 | ✓ |
| @related getValue / onkeypress / onkeyup | - (@related 자체 기능은 검증 대상 아님. getValue 는 @spec 에 명시돼 있어 onkeydown 핸들러 안에서만 호출, onkeypress 는 @spec 순서·다국어 확인용 출력 전용, onkeyup 은 연속 발생 출력 카운터 초기화 전용으로 출력 없음) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onkeydown","onkeypress","onkeyup"])`. onkeydown 은 `▶ onkeydown (disabled x / readOnly y) key : K -> getValue() : V` + `e : [object KeyboardEvent]` 2줄, onkeypress 는 `▶ onkeypress key : K` 1줄. 연속 발생은 [[feedback_onmousemove_output_three_then_ellipsis]] 정본대로 각 이벤트별 카운터로 앞 3회만 출력하고 3회째에 `...` 를 덧붙이며, 카운터는 onkeyup 과 comp_init 에서 초기화(common_E_onkeydown_1 정본과 동일한 onkeyup 리셋 — 키를 떼면 다시 출력).

라벨 생성: **생성함**. 사유 — ① password 입력창이라 화면에서 값을 볼 수 없는데 초기값 `secret`(6자) 이 헬퍼로 설정되는 숨은 전제가 있고(validation (3) 의 이전값 대비에 필요) ② "같은 키 길게 누르기 / 한글 입력" 은 화면만 봐서 알 수 없는 다단계 조작이며 ③ 연속 발생 시 앞 3회만 출력하고 `...` 로 줄인다는 출력 규약은 validation 에 없는 운영 정보다. (UT_01 §5-1 "라벨 생성 여부" 의 생성 기준에 해당)

엔진 근거: secret 구현은 `uiplugin/input/input.js`(secret.js 는 주석 전용). 키 이벤트는 `input.js:3138` systemEvents 에 onkeydown/onkeypress/onkeyup 이 등록되어 DOM 이벤트가 그대로 전달된다. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 키 이벤트 자체를 발생시키지 않고, readOnly 는 `readonly="readonly"` 만 붙어 키 이벤트는 정상 발생하되 값만 변경되지 않는다.

가이드-엔진 불일치: 없음. 7건 모두 실측과 일치(02_검증.md 표 참조). 다국어 항목은 CDP IME 경로(key=Process, keyCode 229 + imeSetComposition)로 onkeypress 미발생을 실측 확인.

### secret_E_onviewchange_1.xml / _2.xml / _3.xml (2026-09-23)

분리: **3분할(짝 샘플 secret_E_onchange_1/_2/_3 와 동일 축)**. 축이 ① 발생 조건 + info ② ref 바인딩 DataCollection 의 데이터 선택 변경 ③ 이벤트 발생 순서 로 나뉘고, ②는 `ref="data:dlt_ref.label"` 고정 + dlt_ref 픽스처, ③은 6개 이벤트 핸들러라는 **서로 다른 컴포넌트 생성 전제**를 요구한다. ②를 ①에 합치면 ①의 나머지 7항목(포커스 해제 / setValue / 값 변경 없음 / Enter)이 전부 "ref 바인딩된 상태" 라는 전제 아래에서만 검증되어 전제가 달라진다 -> 합치지 않음. ②는 validation 1건뿐이지만 축·픽스처가 독립이고 TSQA 에서 onchange_2 와 1:1 대응되므로 단독 파일 유지.
wrap 불가: `common_E_onviewchange_1.xml`(+_2) 은 구판으로 validation 이 빈 문자열 1건이며 부모(input_E_onviewchange_1/_2) 가 tData 를 오버라이드하는 구조다. 공통판 grp_condition 은 con_ref / con_enterBlur 전용이고 par_value(setValue 값) / 행선택 3버튼 / 순서용 6이벤트를 부모가 추가할 수 없다. 또 secret 2.0 가이드의 info 평문 문구·순서 3종·미발생 3종이 공통판에 없어 독립 샘플로 작성(secret_E_onchange_* 선례와 동일).
description: 3개 파일 동일 — `[secret > E > onviewchange]<br/>Secret 컴포넌트에서 사용자의 조작에 의해 입력값이 변경된 후 포커스가 해제될때 발생합니다.` (@description 첫 문장만)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트에서 사용자의 조작에 의해 입력값이 변경된 후 포커스가 해제될때 발생합니다. | _1 (1) Secret 컴포넌트에서 사용자의 조작에 의해 입력값이 변경된 후 포커스가 해제될때 onviewchange 이벤트가 발생합니다. | ✓ |
| @description 하위절(\|) 스크립트에서 setValue() 메소드로 입력값을 변경한 경우 onviewchange 이벤트는 발생하지 않습니다. | _1 (2) 원문 | ✓ |
| @param info 변경된 입력값에 대한 정보가 객체로 전달됩니다. | _1 (3) info 파라미터로 변경된 입력값에 대한 정보가 객체로 전달됩니다. | ✓ |
| @param info.oldValue 변경되기 전 입력값을 마스킹되지 않은 평문 문자열로 갖습니다. | _1 (4) info.oldValue 속성으로 ~ 원문 | ✓ |
| @param info.newValue 변경된 후 입력값을 마스킹되지 않은 평문 문자열로 갖습니다. | _1 (5) info.newValue 속성으로 ~ 원문 | ✓ |
| @spec 입력값에 변경이 없으면 onviewchange 이벤트는 발생하지 않습니다. | _1 (6) 원문 | ✓ |
| @spec 입력값이 변경된 상태로 Enter 키 입력 시 onchange 이벤트가 발생합니다. | _1 (7) 원문 유지 | ✓ (가이드 문구 갭 — 아래 참조) |
| @spec ref 속성으로 DataCollection 을 바인딩 한 경우 데이터 선택이 변경되어도 onviewchange 이벤트는 발생하지 않습니다. | _2 (1) 원문 | ✓ |
| @spec 사용자의 입력뿐만 아니라 스크립트 등 다른 원인에 의해 값이 변경된 경우도 처리하려면 onchange 이벤트를 사용하여야 합니다. | _1 (8) 원문 | ✓ |
| @spec 마우스에 의한 포커스 해제 시 "onchange > onviewchange > onblur" 순서로 이벤트가 발생합니다. | _3 (1) 원문(큰따옴표 -> 작은따옴표) | ✓ |
| @spec Tab 키로 포커스 해제 시 "onkeydown > onchange > onviewchange > onblur" 순서로 이벤트가 발생합니다. | _3 (2) 원문 | ✓ |
| @spec Enter 키 입력 시 "onkeydown > onkeypress > onkeyup > onchange > onviewchange > onblur" 순서로 이벤트가 발생합니다. | _3 (3) 원문 | ✓ |
| @related setValue / onchange | - (@related 자체 기능은 검증 대상 아님. setValue 는 @description 하위절에 명시돼 있어 _1 의 미발생 확인 수단으로만 호출, onchange 는 _1 의 대비 출력 / _3 의 순서 출력 전용) | 제외 |

구성
- _1 : `createEList(["onviewchange","onchange"])`, par_value(xf:input, initValue=abc) + setValue 버튼. onviewchange 는 `▶ onviewchange 발생 / 화면 입력창 type : password` + `info.oldValue` + `info.newValue` 3줄(평문 vs 마스킹 대비), onchange 는 `▶ onchange 발생` 1줄(대비 전용). con_ 옵션 없음(가이드에 조건 속성 없음).
- _2 : `ref="data:dlt_ref.label"` 고정, dlt_ref 픽스처 행0 data_A / 행1 data_B / 행2 data_B(onchange_2 와 동일), 행0/행1/행2 선택 버튼 3개. 선택 줄 + `선택 후 target1 값` 줄(password 마스킹이라 화면으로 값 변경을 볼 수 없어 전제 확보용). onchange 는 배선하지 않음 — 행1->행2 가 같은 값이라 대비 출력이 들쑥날쑥해 오독을 부르기 때문. con_ 옵션 없음.
- _3 : `createEList(["onkeydown","onkeypress","onkeyup","onchange","onviewchange","onblur"])`, 각 핸들러 발생 즉시 `▶ 이벤트명` 1줄([[reference_event_order_deferred_flush]] 즉시-출력 정본). con_/par_ 옵션 없음.
- 3개 모두 `scwin.gVar.bReady` 로 헬퍼의 초기값 setValue 로 발생하는 onchange 를 억제.

라벨 생성: **3개 모두 생성함**. 사유 — ① password 입력창이라 화면에서 값을 볼 수 없는데 헬퍼가 초기값 `secret` 을 setValue 로 넣는 숨은 전제가 있고 ② _1 은 4가지 케이스(포커스 해제 / Enter / 값 변경 없음 / setValue), _3 은 3가지 해제 방식이라는 다단계 조작이며 ③ _2 는 ref 고정 + dlt_ref 픽스처 + 재생성 시 미선택 초기화라는 화면에 드러나지 않는 전제가 있다. (UT_01 §5-1 "라벨 생성 여부" 의 생성 기준 / [[feedback_simple_sample_skip_info_label]] 의 생성 기준에 해당) 부수 효과로 `#mf_label1` 이 마우스 포커스 해제용 바깥 클릭 타깃이 되어 _1/_3 의 TSQA 로케이터로 쓸 수 있다.

가이드-엔진 불일치
- **없음(동작 기준)**. 12항목 전부 MCP 실측 일치(02_검증.md 표 참조). 순서 3종은 가이드 @spec 과 정확히 동일.
- **가이드 문구 갭 1건**: onviewchange 의 `@spec` 중 "입력값이 변경된 상태로 Enter 키 입력 시 **onchange** 이벤트가 발생합니다." 는 onchange 가이드의 같은 문장을 그대로 복사한 템플릿 잔재로 보인다(`uiplugin/secret/secret.js` 의 onchange / onviewchange 두 블록에 동일 문장). 실측상 Enter 입력 시 onchange 와 onviewchange 가 모두 발생하므로 문장 자체는 참이지만 onviewchange 절에서는 주어가 어긋난다. [[feedback_validation_keep_guide_wording]] 에 따라 validation 문구는 원문 유지했고, 샘플은 onchange 를 함께 배선해 두 줄이 모두 출력되므로 원문 그대로 판정 가능하다.

### secret_E_onkeypress_1.xml (2026-09-23)

분리 없음: 가이드 항목이 8건이고 검증 축은 con_disabled / con_readOnly 2개뿐이며, 나머지(문자가 아닌 키 / 연속 발생 / onkeydown 순서 / 다국어 / getValue 시점)는 모두 같은 화면에서 키 입력 방식만 바꿔 확인되는 단일 축 -> 단일 샘플. (짝 샘플 secret_E_onkeydown_1 과 동일 기준)
wrap 불가: `common_E_onkeypress_1.xml` 은 구판(validation 4건)으로 secret 2.0 가이드에 없는 항목(자식 -> 부모 버블링, dataType 입력 제한)을 담고 있고 getValue 시점 / 연속 발생 / 다국어 / disabled·readOnly 축이 없다. grp_condition 이 con_dataType 전용이라 부모가 con_disabled / con_readOnly 를 추가할 수 없어 독립 샘플로 작성(`input_E_onkeypress_1.xml` 은 이 구판을 wrap 하는 형태).

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트 영역에서 키보드로 문자키를 누를때 발생합니다. | (1) Secret 컴포넌트 영역에서 키보드로 문자키를 누를때 onkeypress 이벤트가 발생합니다. | ✓ |
| @description 하위절(\|) 기능키(F1~F12), Shift 키와 같이 문자가 아닌 키는 발생하지 않습니다. | (2) 원문 (description 에서는 제외하고 별도 validation 으로 분리 — UT_01 §1) | ✓ |
| @param e KeyboardEvent 객체 | (3) 이벤트 핸들러에 KeyboardEvent 객체 e 가 전달됩니다. | ✓ |
| @param e.altKey / e.bubbles / e.cancelable / e.code / e.composed / e.ctrlKey / e.defaultPrevented / e.detail / e.eventPhase / e.isComposing / e.isTrusted / e.key / e.location / e.metaKey / e.repeat / e.shiftKey / e.target / e.timeStamp / e.type / e.view (20건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (3) 으로 전달 사실만 검증. 단 문자키/기능키/다국어 구분에 필요한 `e.key` 값만 출력에 동반 표기) | 제외 |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / secret_E_onclick_1 / secret_E_onkeydown_1 선례) | 제외 |
| @spec onkeypress 이벤트 발생 시점에는 눌린 키가 입력 영역에 반영되지 않은 상태이므로 이벤트에서 getValue() 메소드 사용 시 이전 입력값이 반환됩니다. | (4) 원문 | ✓ |
| @spec 키를 누르고 있으면 onkeypress 이벤트가 연속적으로 발생합니다. | (5) 원문 | ✓ |
| @spec 문자에 해당하는 키 입력시 onkeydown 이벤트가 발생한 후 onkeypress 이벤트가 발생합니다. | (6) 원문 | ✓ |
| @spec 하위절(\|) 웹브라우저에 따라 다국어 입력 시 onkeypress 이벤트가 발생하지 않을 수 있습니다. | (7) 원문 | ✓ |
| @spec disabled=true 인 경우 onkeypress 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onkeypress 이벤트가 발생합니다. | (8) 원문 | ✓ |
| @related getValue / onkeydown / onkeyup | - (@related 자체 기능은 검증 대상 아님. getValue 는 @spec 에 명시돼 있어 onkeypress 핸들러 안에서만 호출, onkeydown 은 @spec 순서·문자가 아닌 키·다국어 미발생을 보이는 대비 출력 전용, onkeyup 은 연속 발생 출력 카운터 초기화 전용으로 출력 없음) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onkeydown","onkeypress","onkeyup"])`. onkeypress 는 `▶ onkeypress (disabled x / readOnly y) key : K -> getValue() : V` + `e : [object KeyboardEvent]` 2줄, onkeydown 은 `▶ onkeydown key : K` 1줄. 연속 발생은 [[feedback_onmousemove_output_three_then_ellipsis]] 정본대로 각 이벤트별 카운터로 앞 3회만 출력하고 3회째에 `...` 를 덧붙이며, 카운터는 onkeyup 과 comp_init 에서 초기화.

secret_E_onkeydown_1 대비 diff: ① description / validation (1) 의 문구가 "키보드의 키" -> "키보드로 문자키" ② @description 하위절이 있어 validation (2) 기능키·Shift 미발생 1건 추가(총 7 -> 8건) ③ 주·보조 핸들러 역할 교체(주 = onkeypress 2줄 + 조건 표기, 보조 = onkeydown 1줄) ④ 라벨 [절차] 에 "Shift 나 기능키 입력" 단계 추가. 나머지(con 구성, gVar/카운터, onkeyup 리셋, body 마크업)는 동일.

라벨 생성: **생성함**. 사유 — ① password 입력창이라 화면에서 값을 볼 수 없는데 초기값 `secret`(6자) 이 헬퍼로 설정되는 숨은 전제가 있고((4) 의 이전값 대비에 필요) ② "같은 키 길게 누르기 / Shift·기능키 / 한글 입력" 은 화면만 봐서 알 수 없는 다단계 조작이며 ③ 연속 발생 시 앞 3회만 출력하고 `...` 로 줄인다는 출력 규약은 validation 에 없는 운영 정보다. (UT_01 §5-1 "라벨 생성 여부" 의 생성 기준 / [[feedback_simple_sample_skip_info_label]] 의 생성 기준에 해당 — secret_E_onkeydown_1 과 동일 판단)

엔진 근거: secret 구현은 `uiplugin/input/input.js`(secret.js 는 주석 전용). 키 이벤트는 `input.js:3138` systemEvents 에 onkeydown/onkeypress/onkeyup 이 등록되어 DOM 이벤트가 그대로 전달된다. onkeypress 는 브라우저가 문자 입력을 동반하는 키에서만 발생시키므로 기능키·조합키·IME(key=Process) 에서는 미발생한다. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 키 이벤트 자체를 발생시키지 않고, readOnly 는 `readonly="readonly"` 만 붙어 키 이벤트는 정상 발생하되 값만 변경되지 않는다.

가이드-엔진 불일치: 없음. 8건 모두 실측과 일치(02_검증.md 표 참조). 다국어 항목은 CDP IME 경로(key=Process, keyCode 229 + imeSetComposition)로 onkeypress 미발생을 실측 확인.

### secret_E_onkeyup_1.xml (2026-09-23)

분리 없음(1파일). 짝 샘플 `secret_E_onkeydown_1.xml` / `secret_E_onkeypress_1.xml` 와 동일 구성(con_disabled / con_readOnly)에서 onkeyup 축으로 교체.
wrap 불가: `common_E_onkeyup_1.xml` 은 구판으로 validation 이 자식 버블링 / dataType 입력 제한 축이고 grp_condition 이 con_dataType 전용이라, secret 2.0 가이드의 getValue() 완전 입력값 반환 / disabled·readOnly 발생 여부를 부모가 con_ 추가로 덮을 수 없다 -> 독립 샘플(secret_E_onkeydown_1 선례와 동일).
description: `[secret > E > onkeyup]<br/>Secret 컴포넌트가 포커스된 상태에서 눌렀던 키보드의 키를 떼면 발생합니다.` (@description 첫 문장만)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트가 포커스된 상태에서 눌렀던 키보드의 키를 떼면 발생합니다. | (1) Secret 컴포넌트가 포커스된 상태에서 눌렀던 키보드의 키를 떼면 onkeyup 이벤트가 발생합니다. | ✓ |
| @param e (KeyboardEvent) + e.altKey ~ e.view 20개 속성 | (2) 이벤트 핸들러에 KeyboardEvent 객체 e 가 전달됩니다. | ✓ (표준 DOM 이벤트 객체 e 의 개별 속성은 [[feedback_event_e_property_validation]] 에 따라 개별 validation 미생성. 키 구분에 필요한 e.key 만 출력에 포함) |
| @spec Event Object 의 설명은 표준 속성만을 다루고 있으며 웹브라우저에 따라 일부 차이가 있을 수 있습니다. | - | 제외 (브라우저 구현 차이 안내 문구로 확인 수단 없음. secret_E_onblur_1 / onkeydown_1 / onkeypress_1 선례) |
| @spec onkeyup 이벤트 발생 시점에는 눌린 키가 반영된 상태이므로 onkeydown, onkeypress 이벤트와 다르게 getValue() 메소드 사용 시 완전한 입력값이 반환됩니다. | (3) 원문 | ✓ |
| @spec disabled=true 인 경우 onkeyup 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onkeyup 이벤트가 발생합니다. | (4) 원문 | ✓ |
| @related onkeydown / onkeypress | - (@related 자체 기능은 검증 대상 아님. 두 이벤트는 @spec 에 "onkeydown, onkeypress 이벤트와 다르게" 로 명시돼 있어 getValue() 반환값 대비 출력 전용으로만 배선) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onkeydown","onkeypress","onkeyup"])`. onkeyup 은 `▶ onkeyup (disabled x / readOnly y) key : K -> getValue() : V` + `e : [object KeyboardEvent]` 2줄, 대비용 onkeydown / onkeypress 는 `▶ 이벤트명 key : K -> getValue() : V` 1줄씩. onkeyup 은 키를 뗄 때 1회만 발생하므로 카운터를 두지 않고, 키를 누르고 있으면 연속 발생하는 대비용 onkeydown / onkeypress 에만 [[feedback_onmousemove_output_three_then_ellipsis]] 정본대로 앞 3회 + `...` 카운터를 두었다(카운터 리셋은 onkeyup 과 comp_init).

validation 미추가 항목: 가이드에 onkeyup 의 연속 발생 관련 @spec 이 없으므로 "키를 누르고 있으면 뗄 때 1회만 발생" 은 validation 으로 만들지 않았다([[feedback_validation_no_arbitrary_addition]]). 3회+`...` 규약은 대비용 출력의 홍수 방지 목적이며 라벨 [확인] 에만 안내한다.

라벨 생성: **생성함**. 사유 — ① password 입력창이라 화면에서 값을 볼 수 없는데 초기값 `secret`(6자) 이 헬퍼로 설정되는 숨은 전제가 있고(validation (3) 의 이전값/갱신값 대비 판정에 필요) ② onkeydown / onkeypress 줄이 "대비용" 이라는 점과 연속 발생 시 앞 3회만 출력하고 `...` 로 줄인다는 출력 규약은 validation 에 없는 운영 정보다. (UT_01 §5-1 "라벨 생성 여부" 의 생성 기준 / [[feedback_simple_sample_skip_info_label]] 의 생성 기준에 해당)

엔진 근거: secret 구현은 `uiplugin/input/input.js`(secret.js 는 주석 전용). 키 이벤트는 input.js systemEvents 에 onkeydown/onkeypress/onkeyup 이 등록되어 DOM 이벤트가 그대로 전달된다. disabled 는 `<input disabled="disabled">` 로 렌더되어 포커스 자체가 불가해 키 이벤트가 발생하지 않고, readOnly 는 `readonly="readonly"` 만 붙어 키 이벤트는 정상 발생하되 값만 변경되지 않는다.

가이드-엔진 불일치: 없음. 4건 모두 실측 일치(02_검증.md 표 참조).

### secret_E_onchange_3.xml 라벨 절차 교정 (2026-09-23)

라벨 `createLabel` 문구만 교정. validation 3건 / description / 핸들러 / createEList 구성 무변경.

변경 전 [절차]: `target1 클릭 -> 값 입력 -> 결과창 clear -> 아래 중 하나로 포커스 해제`
변경 후 [절차]: `결과창 clear -> target1 클릭 -> 값 입력 -> 아래 중 하나로 포커스 해제 (clear 버튼 클릭 자체가 포커스 해제이므로 반드시 입력 전에 clear 합니다)`
추가 안내 1줄: `값 입력 중 발생한 onkeydown, onkeypress, onkeyup 줄이 먼저 나오고 그 뒤에 포커스 해제 시점의 순서가 이어집니다`

사유: 결과창 clear 버튼 클릭이 포커스를 해제하므로, 기존 순서대로 조작하면 clear 시점에 이벤트가 발생·출력된 뒤 지워지고 이어지는 해제에서는 onblur 만 남는다. 근거 규칙 — UT_01_생성.md "포커스 해제 계열 샘플의 조작 순서", [[reference_clear_button_click_releases_focus]], [[feedback_createlabel_newline_br]](`\n` 줄바꿈, `<br/>` 직접 사용 금지).

톤 정합: 같은 폴더 `secret_E_onviewchange_3.xml`(처음부터 올바른 순서로 작성됨) 의 라벨 문구와 표현·구성을 동일하게 맞췄다. 두 샘플은 동일한 이벤트 순서 축을 다루므로 라벨이 갈리면 안 된다.

적용 범위: 기존 샘플 전수 수정은 하지 않는다(secret 폴더만 교정). 신규 생성 샘플부터 이 순서를 적용.

### secret_E_onmousedown_1.xml (2026-09-23)

분리 없음: 가이드 항목이 3건(발생 / e / disabled·readOnly)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플. (secret_E_onclick_1 / secret_E_ondblclick_1 과 동일 기준)
wrap 불가: `common_E_onmousedown_1.xml` 은 구판으로 validation 3건(왼쪽/오른쪽/휠 버튼)이 전부 `@description` 의 "마우스 버튼" 을 임의로 3분할한 형태이고, target1(enabled)/target2(disabled) 고정 구조에 `grp_condition` 이 비어 있어 부모가 con 을 추가할 수 없다. secret 2.0 가이드의 readOnly=true 발생 축을 담을 수 없어 독립 샘플로 작성(`input_E_onmousedown_1.xml` 은 이 구판을 wrap 하는 형태).
description: `[secret > E > onmousedown]<br/>Secret 컴포넌트 영역에서 마우스 버튼을 누르는 순간 발생합니다.` (@description 첫 문장만)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트 영역에서 마우스 버튼을 누르는 순간 발생합니다. | (1) Secret 컴포넌트 영역에서 마우스 버튼을 누르는 순간 onmousedown 이벤트가 발생합니다. | ✓ |
| @param e MouseEvent 객체 | (2) 이벤트 핸들러에 MouseEvent 객체 e 가 전달됩니다. | ✓ |
| @param e.altKey / e.bubbles / e.button / e.buttons / e.cancelable / e.clientX / e.clientY / e.ctrlKey / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.metaKey / e.movementX / e.movementY / e.offsetX / e.offsetY / e.pageX / e.pageY / e.relatedTarget / e.screenX / e.screenY / e.shiftKey / e.target / e.timeStamp / e.type / e.view / e.which / e.x / e.y (31건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증. 단 @description 의 "마우스 버튼" 이 좌/중/우 어느 버튼인지 구분하는 데 필요한 `e.button` 값만 출력에 동반 표기 — onkeydown_1 의 `e.key` 선례) | 제외 |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / onclick_1 / ondblclick_1 / onkeydown_1 선례) | 제외 |
| @spec disabled=true 인 경우 onmousedown 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onmousedown 이벤트가 발생합니다. | (3) 원문 | ✓ |
| @related disabled / readOnly / onclick / onmouseup | - (@related 자체 기능은 검증 대상 아님. **onmousedown 가이드에는 이벤트 발생 순서 @spec 이 없으므로** onclick / onmouseup 은 순서 확인용으로도 바인딩하지 않았다 — 근거 없는 출력 줄을 만들지 않는 ondblclick_1 의 onfocus 제거 선례와 같은 기준) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onmousedown"])` 단일 바인딩. 출력은 `▶ onmousedown (disabled x / readOnly y) button : n (한글표기)` + `e : [object MouseEvent]` 2줄. 버튼 표기는 `scwin.fn_button` 으로 가이드 @param e.button 의 매핑(0=왼쪽, 1=가운데, 2=오른쪽)을 그대로 적용하고, 그 외 값은 숫자만 출력한다. 사용자 조작 대행 버튼은 두지 않고 사용자가 직접 누른다([[feedback_spec_verify_by_user_action_not_auto_button]]).

secret_E_onclick_1 대비 diff: ① 축(onmousedown) ② `createEList` 가 4개 -> 1개(순서 @spec 이 없어 onfocus/onmouseup/onclick 미바인딩) ③ validation 4건 -> 3건(순서 항목 없음) ④ 출력에 `button : n` 표기 추가 ⑤ `fn_button` 추가. 나머지(con 구성, gVar, fn_disp, body 마크업)는 동일.

라벨 생성: **생성하지 않음**. 사유 — ① 조작이 `조건 선택 -> Target 재생성 -> target1 위에서 마우스 버튼 누르기` 로 자명하고 ② 숨은 전제·고정 설정이 없으며(초기값 `secret` 은 이 샘플의 판정에 쓰이지 않는다 — 값 비교 없이 이벤트 발생 여부만 본다) ③ 라벨에 쓸 내용이 validation 문구와 con_ 이름의 되풀이뿐이다. UT_01_생성.md §5-1 "라벨 생성 여부" 의 생략 3조건을 모두 만족하며, 같은 기준으로 라벨을 제거한 secret_E_onclick_1 / secret_E_ondblclick_1 과 동일 판단([[feedback_simple_sample_skip_info_label]]).

엔진 근거: secret 은 구현이 `uiplugin/input/input.js` (secret.js 는 주석 전용). onmousedown 은 `input.js:3138 systemEvents` 에 등록되어 DOM mousedown 이 그대로 전달된다. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 마우스 이벤트 자체를 발생시키지 않고, readOnly 는 `readonly="readonly"` 만 붙어 마우스 이벤트는 정상 발생한다(포커스 이벤트만 `input.js:7385 if (this.getReadOnly() && !this.options.readOnlyFocusEvent) return;` 로 차단).

가이드-엔진 불일치: **없음**. 3항목 모두 실측 일치(02_검증.md 표 참조). `e` 생성자도 가이드 @param 대로 MouseEvent 로 확인되어 ondblclick 에서 발견된 PointerEvent 오기 같은 갭은 없다.

가이드 판 차이(참고): 같은 템플릿의 `input` 가이드(`uiplugin/input/input.js` onmousedown 블록)는 `@spec` 이 "컴포넌트가 비활성화(disabled)상태이면 이벤트가 발생하지 않습니다." 한 줄이고 `@related` 에 disabled / readOnly 가 없는 구판이다. secret 판이 2.0 으로 갱신된 쪽이라 secret 가이드를 기준으로 작성했다.

### secret_E_onmouseup_1.xml (2026-09-23)

분리 없음: 가이드 항목이 3건(발생 / e / disabled·readOnly)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플. (secret_E_onclick_1 / secret_E_ondblclick_1 / secret_E_onmousedown_1 과 동일 기준)
wrap 불가: `common_E_onmouseup_1.xml` 은 구판으로 validation 3건이 전부 `@description` 의 "마우스 버튼" 을 왼쪽/오른쪽/휠로 임의 3분할한 형태이고(disabled 항목은 주석 처리된 채 남아 있음), target1(enabled)/target2(disabled) 고정 구조에 `grp_condition` 이 비어 있어 부모가 con 을 추가할 수 없다. secret 2.0 가이드의 readOnly=true 발생 축을 담을 수 없어 독립 샘플로 작성(`input_E_onmouseup_1.xml` 은 이 구판을 wrap 하는 형태).
description: `[secret > E > onmouseup]<br/>Secret 컴포넌트 영역에서 마우스 버튼을 떼는 순간 발생합니다.` (@description 첫 문장만)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트 영역에서 마우스 버튼을 떼는 순간 발생합니다. | (1) Secret 컴포넌트 영역에서 마우스 버튼을 떼는 순간 onmouseup 이벤트가 발생합니다. | ✓ |
| @param e MouseEvent 객체 | (2) 이벤트 핸들러에 MouseEvent 객체 e 가 전달됩니다. | ✓ |
| @param e.altKey / e.bubbles / e.button / e.buttons / e.cancelable / e.clientX / e.clientY / e.ctrlKey / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.metaKey / e.movementX / e.movementY / e.offsetX / e.offsetY / e.pageX / e.pageY / e.relatedTarget / e.screenX / e.screenY / e.shiftKey / e.target / e.timeStamp / e.type / e.view / e.which / e.x / e.y (31건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증. 단 @description 의 "마우스 버튼" 이 좌/중/우 어느 버튼인지 구분하는 데 필요한 `e.button` 값만 출력에 동반 표기 — onkeydown_1 의 `e.key`, onmousedown_1 의 `e.button` 선례) | 제외 |
| @spec Event Object 설명은 표준 속성만… 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / onclick_1 / ondblclick_1 / onkeydown_1 / onmousedown_1 선례) | 제외 |
| @spec disabled=true 인 경우 onmouseup 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onmouseup 이벤트가 발생합니다. | (3) 원문 | ✓ |
| @related disabled / readOnly / onclick / onmousedown | - (@related 자체 기능은 검증 대상 아님. **onmouseup 가이드에는 이벤트 발생 순서 @spec 이 없으므로** onclick / onmousedown 은 순서 확인용으로도 바인딩하지 않았다 — ondblclick_1 의 onfocus 제거 / onmousedown_1 과 같은 기준) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onmouseup"])` 단일 바인딩. 출력은 `▶ onmouseup (disabled x / readOnly y) button : n (한글표기)` + `e : [object MouseEvent]` 2줄. 버튼 표기는 `scwin.fn_button` 으로 가이드 @param e.button 의 매핑(0=왼쪽, 1=가운데, 2=오른쪽)을 그대로 적용하고, 그 외 값은 숫자만 출력한다. 사용자 조작 대행 버튼은 두지 않고 사용자가 직접 뗀다([[feedback_spec_verify_by_user_action_not_auto_button]]).

secret_E_onclick_1 대비 diff: ① 축(onmouseup) ② `createEList` 가 4개 -> 1개(순서 @spec 이 없어 onmousedown/onfocus/onclick 미바인딩) ③ validation 4건 -> 3건(순서 항목 없음) ④ 출력에 `button : n` 표기 추가 ⑤ `fn_button` 추가. 나머지(con 구성, gVar, fn_disp, body 마크업)는 동일. secret_E_onmousedown_1 과는 이벤트명만 다른 대칭 구성으로 맞췄다(같은 시간대 병렬 작성 — 톤·출력 형식 일치 확인).

라벨 생성: **생성하지 않음**. 사유 — ① 조작이 `조건 선택 -> Target 재생성 -> target1 위에서 마우스 버튼 떼기` 로 자명하고 ② 숨은 전제·고정 설정이 없으며(초기값 `secret` 은 이 샘플의 판정에 쓰이지 않는다 — 값 비교 없이 이벤트 발생 여부만 본다) ③ 라벨에 쓸 내용이 validation 문구와 con_ 이름의 되풀이뿐이다. UT_01_생성.md §5-1 "라벨 생성 여부" 의 생략 3조건을 모두 만족하며, 같은 기준으로 라벨을 두지 않은 secret_E_onclick_1 / secret_E_ondblclick_1 / secret_E_onmousedown_1 과 동일 판단([[feedback_simple_sample_skip_info_label]]).

포커스 해제 절차 규칙 적용 여부: onmouseup 은 포커스 해제로 발생하는 이벤트가 아니므로 UT_01 "포커스 해제 계열 샘플의 조작 순서" 의 대상은 아니다. 다만 결과창 clear 클릭이 곧 포커스 해제라는 점([[reference_clear_button_click_releases_focus]])을 살려 검증 절차를 `clear -> 마우스 조작` 순으로 고정했고, 이 순서라면 clear 자체로 생기는 잔여 이벤트가 결과창에 섞이지 않는다(clear 는 결과창 버튼 위에서 눌렸다 떼지므로 target1 의 onmouseup 을 만들지 않음 — 실측 확인).

엔진 근거: secret 은 구현이 `uiplugin/input/input.js` (secret.js 는 주석 전용). onmouseup 은 `input.js:3138 systemEvents` 에 등록되어 DOM mouseup 이 그대로 전달된다. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 마우스 이벤트 자체를 발생시키지 않고, readOnly 는 `readonly="readonly"` 만 붙어 마우스 이벤트는 정상 발생한다(포커스 이벤트만 `input.js:7385 if (this.getReadOnly() && !this.options.readOnlyFocusEvent) return;` 로 차단).

가이드-엔진 불일치: **없음**. 3항목 모두 실측 일치(02_검증.md 표 참조). `e` 생성자도 가이드 @param 대로 MouseEvent 로 확인.

가이드 판 차이(참고): 같은 템플릿의 `input` 가이드(`input.js:458~503` onmouseup 블록)는 `@spec` 이 "컴포넌트가 비활성화(disabled)상태이면 이벤트가 발생하지 않습니다." 한 줄이고 `@related` 도 onclick / onmousedown 둘뿐인 구판이다(readOnly 발생 조항 없음). secret 판이 2.0 으로 갱신된 쪽이라 secret 가이드를 기준으로 작성했다 — onmousedown_1 과 동일한 상황.


### secret_E_onmousemove_1.xml (2026-09-23)

분리 없음: 가이드 항목이 4건(발생 / e / 연속 발생 / disabled·readOnly)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플.
wrap 불가: `common_E_onmousemove_1.xml` 은 구판으로 validation 이 1건(`컴포넌트 영역에서 마우스 포인터가 이동할 때 발생하여야 합니다.`)뿐이고, target1(enabled)/target2(disabled) 고정 구조에 `grp_condition` 이 비어 있어 부모가 con 을 추가할 수 없다. secret 2.0 가이드의 readOnly=true 발생 축과 연속 발생 @spec 을 담을 수 없어 독립 샘플로 작성(`input_E_onmousemove_1.xml` 은 이 구판을 wrap 하는 형태).
description: `[secret > E > onmousemove]<br/>Secret 컴포넌트 영역에서 마우스 포인터가 이동할 때 발생합니다.` (@description 첫 문장만)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 컴포넌트 영역에서 마우스 포인터가 이동할 때 발생합니다. | (1) Secret 컴포넌트 영역에서 마우스 포인터가 이동할 때 onmousemove 이벤트가 발생합니다. | O |
| @param e MouseEvent 객체 | (2) 이벤트 핸들러에 MouseEvent 객체 e 가 전달됩니다. | O |
| @param e.altKey / e.bubbles / e.button / e.buttons / e.cancelable / e.clientX / e.clientY / e.ctrlKey / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.metaKey / e.movementX / e.movementY / e.offsetX / e.offsetY / e.pageX / e.pageY / e.relatedTarget / e.screenX / e.screenY / e.shiftKey / e.target / e.timeStamp / e.type / e.view / e.which / e.x / e.y (31건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증. onmousedown_1 의 `e.button` 같은 동반 출력도 두지 않았다 — 이동 이벤트는 어떤 키/버튼인지 구분할 필요가 없어 출력에 실을 근거가 없다) | 제외 |
| @spec Event Object 설명은 표준 속성만... 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / onclick_1 / ondblclick_1 / onkeydown_1 / onmousedown_1 선례) | 제외 |
| @spec 마우스 이동에 따라 이벤트가 연속적으로 발생합니다. | (3) 원문 | O |
| @spec disabled=true 인 경우 onmousemove 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onmousemove 이벤트가 발생합니다. | (4) 원문 | O |
| @related disabled / readOnly / onmouseout / onmouseover | - (@related 자체 기능은 검증 대상 아님. **onmousemove 가이드에는 이벤트 발생 순서 @spec 이 없으므로** onmouseover / onmouseout 은 카운터 초기화용으로도 바인딩하지 않았다 — [[feedback_onmousemove_output_three_then_ellipsis]] 의 trigger 판(comp_init 리셋) 적용) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onmousemove"])` 단일 바인딩. 출력은 `▶ onmousemove (disabled x / readOnly y)` + `e : [object MouseEvent]` 2줄이며, **연속 발생이므로 앞 3회만 출력하고 3회째 뒤에 `...` 를 덧붙인다**([[feedback_onmousemove_output_three_then_ellipsis]] 정본 패턴). 카운터(`gVar.nCount_1`)는 `comp_init` 에서만 초기화한다(trigger 판). 사용자 조작 대행 버튼은 두지 않고 사용자가 직접 마우스를 움직인다([[feedback_spec_verify_by_user_action_not_auto_button]]).

라벨 생성: **생성함**(`label1`, 3줄). 사유 — UT_01_생성.md 5-1 "라벨 생성 여부" 의 생략 3조건 중 세 번째("라벨에 쓸 내용이 validation 문구나 con_ 이름과 중복될 뿐")를 만족하지 못한다. ① 결과창 출력 규약(앞 3회 + `...`)은 validation 에 없는 **운영 정보**이고, 이를 모르면 "이동은 계속했는데 3줄만 나온다" 를 결함으로 오인한다. ② 카운터가 `comp_init` 에서만 초기화되어 **결과창 clear 만으로는 재출력되지 않는다**는 점은 화면만 봐서는 알 수 없는 확인 포인트다([[feedback_onmousemove_output_three_then_ellipsis]] 가 trigger 판 사용 시 라벨 [확인] 에 명시하라고 규정). 같은 기준으로 라벨을 만든 `secret_E_onkeydown_1`(연속 발생 + 3회 규약) 선례와 동일 판단이며, 라벨을 생략한 `secret_E_onclick_1` / `onmousedown_1`(단발 이벤트라 출력 규약이 없음) 과는 이 지점에서 갈린다. validation 문구(연속 발생 사실, 미발생 조건)는 라벨에 재기재하지 않았다([[feedback_label_no_validation_dup]]).

엔진 근거: secret 은 구현이 `uiplugin/input/input.js` (secret.js 는 주석 전용). onmousemove 는 `input.js:3138 systemEvents` 에 등록되어 DOM mousemove 가 그대로 전달된다. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 마우스 이벤트 자체를 발생시키지 않고, readOnly 는 `readonly="readonly"` 만 붙어 마우스 이벤트는 정상 발생한다.

가이드-엔진 불일치: **없음**. 4항목 모두 실측 일치(02_검증.md 표 참조).

가이드 판 차이(참고): 같은 템플릿의 `input` 가이드(`uiplugin/input/input.js:505` onmousemove 블록)는 @spec 3번째가 "컴포넌트가 비활성화(disabled)상태이면 이벤트가 발생하지 않습니다." 한 줄이고 @related 에 disabled / readOnly 가 없는 구판이다. secret 판이 2.0 으로 갱신된 쪽이라 secret 가이드를 기준으로 작성했다.


### secret_E_onmouseover_1.xml (2026-09-23)

분리 없음: 가이드 항목이 3건(발생 / e / disabled·readOnly)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플.
wrap 불가: `common_E_onmouseover_1.xml` 은 구판으로 validation 이 1건(`컴포넌트 영역 내로 마우스 포인터가 들어 왔을 때 발생합니다.`)뿐이고, target1(enabled)/target2(disabled) 고정 구조에 `grp_condition` 이 비어 있어 부모가 con 을 추가할 수 없다. 게다가 자식판이 `onmouseenter` 핸들러까지 들고 있어 secret 2.0 가이드에 없는 축이 섞인다. readOnly=true 발생 축을 담을 수 없어 독립 샘플로 작성(`input_E_onmouseover_1.xml` 은 이 구판을 wrap 하는 형태).
description: `[secret > E > onmouseover]<br/>Secret 영역 내로 마우스 포인터가 들어 왔을 때 발생합니다.` (@description 원문 그대로 — 자매 샘플들과 달리 가이드가 "Secret 컴포넌트 영역" 이 아니라 "Secret 영역" 으로 표기하므로 원문 유지)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 영역 내로 마우스 포인터가 들어 왔을 때 발생합니다. | (1) Secret 영역 내로 마우스 포인터가 들어 왔을 때 onmouseover 이벤트가 발생합니다. | O |
| @param e MouseEvent 객체 | (2) 이벤트 핸들러에 MouseEvent 객체 e 가 전달됩니다. | O |
| @param e.altKey / e.bubbles / e.button / e.buttons / e.cancelable / e.clientX / e.clientY / e.ctrlKey / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.metaKey / e.movementX / e.movementY / e.offsetX / e.offsetY / e.pageX / e.pageY / e.relatedTarget / e.screenX / e.screenY / e.shiftKey / e.target / e.timeStamp / e.type / e.view / e.which / e.x / e.y (31건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증. onmousedown_1 / onmouseup_1 의 `e.button` 같은 동반 출력도 두지 않았다 — 진입 이벤트는 눌린 버튼을 구분할 필요가 없어 출력에 실을 근거가 없다) | 제외 |
| @spec Event Object 설명은 표준 속성만... 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / onclick_1 / ondblclick_1 / onkeydown_1 / onmousedown_1 / onmouseup_1 / onmousemove_1 선례) | 제외 |
| @spec 컴포넌트가 비활성화(disabled)상태이면 이벤트가 발생하지 않습니다. | (3) 에 통합 | **중복 통합** |
| @spec disabled=true 인 경우 onmouseover 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onmouseover 이벤트가 발생합니다. | (3) 원문 | O |
| @related disabled / readOnly / onmousemove / onmouseout | - (@related 자체 기능은 검증 대상 아님. onmouseover 가이드에는 이벤트 발생 순서 @spec 이 없으므로 onmousemove / onmouseout 은 바인딩하지 않았다) | 제외 |

**중복 @spec 처리 판단**: secret 의 onmouseover 가이드에만 `컴포넌트가 비활성화(disabled)상태이면 이벤트가 발생하지 않습니다.` 와 `disabled=true 인 경우 ... 발생하지 않지만 readOnly=true 인 경우엔 ... 발생합니다.` 두 줄이 함께 있다. 앞줄이 뒷줄의 전반부에 **완전히 포함**되고 검증 수단(disabled=true 로 재생성 후 진입 -> 미발생)도 동일하므로, UT_01_생성.md 2절 "API 가이드 문서에서 의미가 중복되는 내용은 1개의 validation 항목으로 통합하여 작성할 것" 에 따라 **뒷줄 원문 하나로 통합**했다(원문 문구는 손대지 않음 — [[feedback_validation_keep_guide_wording]]). 앞줄을 별도 항목으로 남기면 같은 조작·같은 기대결과를 두 번 판정하게 되어 [[feedback_validation_case_count]] 의 "validation 수 = 케이스 수" 원칙과도 어긋난다. 통합 사유는 tData 위 코드 주석에도 남겼다.

**중복이 생긴 원인(가이드 갭)**: 자매 이벤트(onmousedown / onmouseup / onmouseout)의 secret 가이드는 @spec 이 2줄(표준 속성 안내 + disabled/readOnly)뿐이다. 반면 구판인 `uiplugin/input/input.js:415` onmouseover 블록은 @spec 이 `표준 속성 안내` + `컴포넌트가 비활성화(disabled)상태이면 이벤트가 발생하지 않습니다.` 2줄이고 @related 에 disabled / readOnly 가 없다. 즉 secret 판 onmouseover 는 2.0 갱신 시 **구판 문장을 지우지 않고 새 문장을 덧붙여** 3줄이 된 템플릿 잔재로 보인다. 엔진 동작상의 모순은 아니므로 가이드-엔진 불일치가 아니라 **가이드 내부 중복**으로 분류한다.

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onmouseover"])` 단일 바인딩. 출력은 `▶ onmouseover (disabled x / readOnly y)` + `e : [object MouseEvent]` 2줄. 사용자 조작 대행 버튼은 두지 않고 사용자가 직접 마우스를 올린다([[feedback_spec_verify_by_user_action_not_auto_button]]).

연속 발생 출력 규약 미적용: [[feedback_onmousemove_output_three_then_ellipsis]] 의 앞 3회 + `...` 패턴은 **연속 발생 이벤트** 전용이다. onmouseover 는 영역 진입 시 1회만 발생하며(실측: 영역 안에서 2회 추가 이동해도 출력 불변 — 02_검증.md 표), 가이드에도 연속 발생 @spec 이 없다. 따라서 카운터/`...` 를 두지 않고 onmousedown_1 / onmouseup_1 과 같은 단발 2줄 출력으로 작성했다.

라벨 생성: **생성하지 않음**. 사유 — ① 조작이 `조건 선택 -> Target 재생성 -> target1 위로 마우스 포인터 이동` 으로 자명하다(버튼을 누를 필요도 없어 onmousedown/onmouseup 보다 단순) ② 숨은 전제·고정 설정이 없다(초기값 `secret` 은 판정에 쓰이지 않는다 — 값 비교 없이 발생 여부만 본다) ③ 라벨에 쓸 내용이 validation 문구와 con_ 이름의 되풀이뿐이다. onmousemove_1 이 라벨을 만든 이유(출력 3회 제한 + 카운터가 comp_init 에서만 초기화라는 운영 정보)는 여기에 해당하지 않는다 — 진입 1회 발생이라 출력 규약 자체가 없고, 결과창 clear 후 다시 진입하면 그대로 재출력된다. UT_01_생성.md 5-1 "라벨 생성 여부" 의 생략 3조건을 모두 만족하므로 secret_E_onclick_1 / ondblclick_1 / onmousedown_1 / onmouseup_1 과 동일 판단([[feedback_simple_sample_skip_info_label]]).

엔진 근거: secret 은 구현이 `uiplugin/input/input.js` (secret.js 는 주석 전용). onmouseover 는 `input.js:3138 systemEvents` 에 등록되어 DOM mouseover 가 그대로 전달된다. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 마우스 이벤트 자체를 발생시키지 않고, readOnly 는 `readonly="readonly"` 만 붙어 마우스 이벤트는 정상 발생한다.

가이드-엔진 불일치: **없음**. 3항목 모두 실측 일치(02_검증.md 표 참조). `e` 생성자도 가이드 @param 대로 MouseEvent.

### secret_E_onmouseout_1.xml (2026-09-23)

분리 없음: 가이드 항목이 3건(발생 / e / disabled·readOnly)이고 con 은 2개(con_disabled, con_readOnly)뿐이라 한 화면에서 모두 조작 가능 -> 단일 샘플. (secret_E_onmousedown_1 / onmouseup_1 / onmouseover_1 과 동일 기준)
wrap 불가: `common_E_onmouseout_1.xml` 은 구판으로 validation 이 2건(`컴포넌트 영역에서 마우스 포인터가 나갔을 때 이벤트가 발생하여야 합니다.` / `컴포넌트가 disabled 속성을 지원하는 경우 Disabled 상태이면 이벤트가 발생하지 않습니다.`)이고, target1(enabled)/target2(disabled) 고정 구조에 `grp_condition` 이 비어 있어 부모가 con 을 추가할 수 없다. 게다가 공통판은 onmouseout 과 함께 `target1_onmouseleave` 까지 출력하는 구성이라 secret 2.0 가이드에 없는 축이 섞인다. readOnly=true 발생 축을 담을 수 없어 독립 샘플로 작성(`input_E_onmouseout_1.xml` 은 이 구판을 wrap 하는 형태).
description: `[secret > E > onmouseout]<br/>Secret 영역에서 마우스 포인터가 나갔을 때 발생합니다.` (@description 원문 — onmousedown/onmouseup/onmousemove 판의 "Secret 컴포넌트 영역" 과 달리 onmouseout/onmouseover 판 원문은 "Secret 영역" 이라 원문 그대로 유지)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| @description Secret 영역에서 마우스 포인터가 나갔을 때 발생합니다. | (1) Secret 영역에서 마우스 포인터가 나갔을 때 onmouseout 이벤트가 발생합니다. | O |
| @param e MouseEvent 객체 | (2) 이벤트 핸들러에 MouseEvent 객체 e 가 전달됩니다. | O |
| @param e.altKey / e.bubbles / e.button / e.buttons / e.cancelable / e.clientX / e.clientY / e.ctrlKey / e.currentTarget / e.defaultPrevented / e.detail / e.eventPhase / e.isTrusted / e.metaKey / e.movementX / e.movementY / e.offsetX / e.offsetY / e.pageX / e.pageY / e.relatedTarget / e.screenX / e.screenY / e.shiftKey / e.target / e.timeStamp / e.type / e.view / e.which / e.x / e.y (31건) | - (표준 DOM 이벤트 객체 e 의 개별 속성은 규칙상 비대상, (2) 로 전달 사실만 검증. onmousedown_1 / onmouseup_1 의 `e.button` 같은 동반 출력도 두지 않았다 — 가이드가 e.button 을 "0 값을 갖습니다" 로 못박아 좌/중/우 구분 정보가 아니고, 이탈 이벤트는 어떤 버튼인지 구분할 필요가 없다. onmousemove_1 / onmouseover_1 과 같은 판단) | 제외 |
| @spec Event Object 설명은 표준 속성만... 브라우저 차이 | - (안내 문구, 확인 수단 없음 -> 제외, secret_E_onblur_1 / onclick_1 / ondblclick_1 / onkeydown_1 / onmousedown_1 / onmouseup_1 / onmousemove_1 선례) | 제외 |
| @spec disabled=true 인 경우 onmouseout 이벤트가 발생하지 않지만 readOnly=true 인 경우엔 onmouseout 이벤트가 발생합니다. | (3) 원문 | O |
| @related disabled / readOnly / onmousemove / onmouseover | - (@related 자체 기능은 검증 대상 아님. **onmouseout 가이드에는 이벤트 발생 순서 @spec 이 없으므로** onmousemove / onmouseover 는 순서 확인용으로도 바인딩하지 않았다 — onmousemove_1 / onmouseup_1 과 같은 기준) | 제외 |

구성: con_disabled / con_readOnly(gdl_boolean, 기본 미설정) + Target 재생성. `createEList(["onmouseout"])` 단일 바인딩. 출력은 `▶ onmouseout (disabled x / readOnly y)` + `e : [object MouseEvent]` 2줄. 사용자 조작 대행 버튼은 두지 않고 사용자가 직접 포인터를 넣었다 뺀다([[feedback_spec_verify_by_user_action_not_auto_button]]). 검증 절차는 `조건 선택 -> Target 재생성 -> clear -> target1 위로 진입 -> 바깥으로 이탈` 로 고정했다(clear 클릭이 target1 의 onmouseout 을 만들지 않으므로 잔여 출력이 섞이지 않는다).

**연속 발생 규약 미적용**: onmousemove_1 은 [[feedback_onmousemove_output_three_then_ellipsis]] 의 앞 3회 + `...` 규약을 쓰지만, onmouseout 은 영역 이탈 시 1회만 발생하는 단발 이벤트라 카운터(`gVar.nCount_1`)를 두지 않았다. 실측에서도 이탈 1회당 출력이 정확히 2줄이었다 — secret 은 래퍼 div 없이 `<input>` 단일 요소로 렌더되어([[feedback_secret_reference_input_samples]]) 자식 요소 경유 중복 mouseout 이 발생하지 않는다.

secret_E_onmouseup_1 대비 diff: ① 축(onmouseout) ② 출력에서 `button : n` 표기와 `fn_button` 제거(가이드가 e.button 을 0 고정으로 규정) ③ description/validation (1) 문구가 가이드 원문 차이로 "Secret 영역" 표기. 나머지(con 구성, gVar, fn_disp, body 마크업, 2줄 출력)는 동일. 짝인 secret_E_onmouseover_1(같은 시간대 병렬 작성)과 톤·출력 형식을 맞춰 진입/이탈 대칭 구성으로 두었다.

라벨 생성: **생성하지 않음**. 사유 — UT_01_생성.md 5-1 "라벨 생성 여부" 의 생략 3조건을 모두 만족한다. ① 조작이 `조건 선택 -> Target 재생성 -> target1 위로 마우스 이동 -> 바깥으로 이동` 으로 자명하고 ② 숨은 전제·고정 설정이 없으며(초기값 `secret` 은 판정에 쓰이지 않는다 — 값 비교 없이 이벤트 발생 여부만 본다) ③ 라벨에 쓸 내용이 validation 문구와 con_ 이름의 되풀이뿐이다. 라벨을 만든 onmousemove_1 과 갈리는 지점은 **출력 규약의 유무**다 — onmousemove_1 은 앞 3회 + `...` 규약과 "clear 만으로는 재출력 안 됨" 이라는 화면만으로 알 수 없는 확인 포인트가 있어 생성했으나, onmouseout 은 단발 이벤트라 그런 규약이 없다. 라벨을 생략한 secret_E_onclick_1 / ondblclick_1 / onmousedown_1 / onmouseup_1 과 동일 판단([[feedback_simple_sample_skip_info_label]]).

엔진 근거: secret 은 구현이 `uiplugin/input/input.js` (secret.js 는 주석 전용). onmouseout 은 `input.js:3138 systemEvents: [... "onmouseover", "onmouseout", "onmousemove"]` 에 등록되어 DOM mouseout 이 그대로 전달된다. disabled 는 `<input disabled="disabled">` 로 렌더되어 브라우저가 마우스 이벤트 자체를 발생시키지 않고(실측 class `w2input w2input_disabled`), readOnly 는 `readonly="readonly"` 만 붙어 마우스 이벤트는 정상 발생한다(실측 class `w2input  w2input_readonly`). 포커스 이벤트만 `input.js:7385 if (this.getReadOnly() && !this.options.readOnlyFocusEvent) return;` 로 차단된다.

가이드-엔진 불일치: **없음**. 3항목 모두 실측 일치(02_검증.md 표 참조). `e` 생성자도 가이드 @param 대로 MouseEvent 로 확인.

가이드 문구 관찰(참고, validation 문구는 원문 유지):
- **@param e.relatedTarget 설명이 mouseout 기준과 어긋난다.** 가이드는 "이벤트가 발생하기 전 요소가 전달됩니다" 로 적었으나 DOM 표준에서 mouseout 의 `relatedTarget` 은 **포인터가 새로 들어간 요소**다(빠져나온 요소는 `target`). 마우스 이벤트 5종에 공통으로 쓰인 템플릿 문구라 onmouseover 판(들어오기 전 요소 = 맞음)에 맞춰 쓰인 것으로 보인다. 표준 DOM 속성이라 validation 비대상이므로 문구만 기록.
- **@description 의 대상 표기 흔들림**: 같은 secret 마우스 이벤트인데 onmousedown/onmouseup/onmousemove 는 "Secret 컴포넌트 영역", onmouseout/onmouseover 는 "Secret 영역" 이다(`secret.js:246,293,340,388,435`). 동일 대상이나 표기가 통일되어 있지 않다. 원문 유지 규칙에 따라 각 샘플은 자기 가이드 문구를 그대로 썼다.
- **가이드 판 차이**: 같은 템플릿의 `input` 가이드 onmouseout 블록은 @spec 이 "컴포넌트가 비활성화(disabled)상태이면 이벤트가 발생하지 않습니다." 한 줄인 구판으로 readOnly 발생 조항이 없다. secret 판이 2.0 으로 갱신된 쪽이라 secret 가이드를 기준으로 작성했다 — onmousedown_1 / onmouseup_1 / onmousemove_1 과 동일 상황. 짝인 onmouseover 판에는 disabled 한 줄 @spec 이 **남아 있어** 중복 통합 판단이 필요했으나(해당 절 참조), onmouseout 판에는 그 줄이 아예 없어 통합 대상이 없었다.

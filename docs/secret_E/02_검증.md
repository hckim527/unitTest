
## secret_E_onblur_1.xml / secret_E_onblur_2.xml (2026-09-22)

- validate_xml: websquare-mcp 서버 연결 실패(fetch failed) -> python minidom well-formed 확인으로 대체(2건 OK)
- wpack: 신규 XML 자동 컴파일 확인(`_wpack_/sample/secret/secret_E_onblur_{1,2}.js`, HTTP 200). 수동 변환 생략
- MCP(playwright3, 포트 61613) 실측 — console error 0 (두 샘플)

### _1 (실제 click -> 바깥 영역 click)
| 조건 | activeElement | onblur 출력 |
|---|---|---|
| disabled 미설정 / readOnly 미설정 | mf_target1 | `disabled 미설정 / readOnly 미설정 -> onblur 이벤트 발생` + `e : [object FocusEvent]` |
| disabled=true | (포커스 안 됨) | 없음 |
| readOnly=true | mf_target1 (포커스는 들어감) | 없음 |
| disabled=false / readOnly=false | mf_target1 | 발생 + `e : [object FocusEvent]` |

### _2 (고정: validator=scwin.fn_validator, ref=data:gdm_ref.key1, onFocusClass=onFocusClass)
- 재생성 직후: 값 secret / gdm_ref.key1 secret / class `w2input `
- click 후 class `w2input onFocusClass`, 타이핑 중 gdm_ref.key1 = secret(blur 전 미반영)
- `abc` 입력 후 바깥 click:
  - `사용자 정의 함수 실행 시점 -> 파라미터 val : abc (3자) / 반환값 : abc_v (5자) / 바인딩 데이터 값 : secret (6자) / class : w2input onFocusClass / bg : #ffff00`
  - `onblur 핸들러 시점 -> 입력값 : abc_v (5자) / 바인딩 데이터 값 : abc_v (5자) / class : w2input / bg : #ffffff`
- 값 변경 없이 재 blur 시에도 사용자 정의 함수가 다시 실행되어 `abc_v_v` 로 누적(엔진 validateString 은 blur 마다 commandAdapter 실행)
- 결과: PASS

### secret_E_onchange_1.xml / _2.xml / _3.xml (2026-09-22)

MCP(playwright, 포트 61613) 실측 — 3개 샘플 모두 로딩·target1 생성 정상, pageerror/console error 0.
- _1 : 초기 로딩 시 출력 없음(헬퍼 setValue 는 bReady 로 차단). 입력 후 바깥 클릭 -> onchange(info.oldValue : secret (string) / info.newValue : secret1 (string)) + onviewchange. 입력 후 Enter -> onchange + onviewchange, 포커스 해제됨(activeElement 없음). 입력 없이 해제 / 입력 후 원래 값으로 되돌리고 해제 -> 출력 없음. setValue(abc) -> onchange(secret12 -> abc), onviewchange 없음. 같은 값 setValue(abc) 재실행 -> onchange 없음. 입력창 type=password (화면 마스킹), info 는 평문.
- _2 : ref=data:dlt_ref.label (행0 data_A / 행1 data_B / 행2 data_B). 행0 선택 -> onchange('' -> data_A), 행1 선택 -> onchange(data_A -> data_B), 행2 선택(같은 값) -> onchange 없음. Target 재생성 후 dlt_ref 데이터 보존, rowPosition -1, target1 값 빈값(헬퍼 초기값이 데이터를 덮지 않음).
- _3 : 마우스 -> onchange > onviewchange > onblur / Tab -> onkeydown > onchange > onviewchange > onblur / Enter -> onkeydown > onkeypress > onkeyup > onchange > onviewchange > onblur. 3개 모두 가이드와 일치. 수정 이력: 초기 로딩 시 헬퍼 setValue 의 onchange 1줄이 찍혀 bReady 가드 추가 후 재검증.

### secret_E_onclick_1.xml (2026-09-23)

MCP(playwright, 포트 61613) 실측 — 페이지 로딩·target1/label1/con_disabled/con_readOnly 생성 정상, console error 0 (password 필드 관련 VERBOSE 안내 메시지만 3건).
렌더: `<input id="mf_target1" class="w2input " type="password" style="width:200px; height:40px;">`

| 조건 (Target 재생성 후 실제 locator.click()) | 결과창 출력 |
|---|---|
| disabled 미설정 / readOnly 미설정 (포커스 없는 상태에서 클릭) | `▶ onmousedown` / `▶ onfocus` / `▶ onmouseup` / `▶ onclick (disabled 미설정 / readOnly 미설정)` / `e : [object PointerEvent]` |
| disabled 미설정 / readOnly 미설정 (이미 포커스를 가진 상태에서 재클릭) | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick (...)` / `e : [object PointerEvent]` (onfocus 없음) |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 (onmousedown/onfocus/onmouseup/onclick 모두 미발생) |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick (disabled 미설정 / readOnly true)` / `e : [object PointerEvent]` (onfocus 없음) |

- 발생 순서는 가이드 @spec 과 일치 (onmousedown > onfocus > onmouseup > onclick).
- disabled=true 미발생 / readOnly=true 발생 = 가이드 @spec 과 일치.
- 차단 판정은 `dispatchEvent` 가 아니라 실제 `locator.click()` 으로 측정함.
- 결과: PASS

### secret_E_ondblclick_1.xml (2026-09-23)

MCP(playwright, 포트 61613) 실측 — 페이지 로딩·target1/label1/con_disabled/con_readOnly 생성 정상, console error 0 (password 필드 VERBOSE 안내만).
렌더: `<input id="mf_target1" class="w2input " type="password" style="width:200px; height:40px;">`
validate_xml: websquare-mcp 서버 연결 실패(fetch failed) -> python minidom well-formed 확인으로 대체. wpack: `_wpack_/sample/secret/secret_E_ondblclick_1.js` 자동 생성 확인.

| 조건 (Target 재생성 후 실제 locator.dblclick()) | 결과창 출력 |
|---|---|
| disabled 미설정 / readOnly 미설정 (포커스 없는 상태) | `▶ onmousedown` / `▶ onfocus` / `▶ onmouseup` / `▶ onclick` / `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ ondblclick (disabled 미설정 / readOnly 미설정)` / `e : [object MouseEvent]` |
| disabled 미설정 / readOnly 미설정 (이미 포커스를 가진 상태에서 재더블클릭) | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ ondblclick (...)` / `e : [object MouseEvent]` (onfocus 없음) |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 (force dblclick 에도 전 이벤트 미발생) |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ ondblclick (disabled 미설정 / readOnly true)` / `e : [object MouseEvent]` (onfocus 없음) |
| disabled=false / readOnly=false | 포커스 없는 상태와 동일 (onfocus 포함 8줄) |

- 발생 순서는 가이드 @spec 과 일치 (onmousedown > onmouseup > onclick > onmousedown > onmouseup > onclick > ondblclick). 단 포커스가 없던 상태에서는 **첫 onmousedown 직후 onfocus 가 1회 끼어든다**(가이드 순서 문구에 미표기).
- disabled=true 미발생 / readOnly=true 발생 = 가이드 @spec 과 일치. 차단 판정은 `dispatchEvent` 가 아니라 실제 `locator.dblclick()`(force 포함) 으로 측정.
- e 객체 실측 (네이티브 리스너로 동일 이벤트 캡처, isTrusted=true):
  | 이벤트 | 생성자 | detail |
  |---|---|---|
  | mousedown(1st/2nd) | MouseEvent | 1 / 2 |
  | mouseup(1st/2nd) | MouseEvent | 1 / 2 |
  | click(1st/2nd) | **PointerEvent** | 1 / 2 |
  | **dblclick** | **MouseEvent** | **2** |
  - 가이드의 `e.detail` = 2, `e.bubbles` = true, `e.relatedTarget` = null, `e.movementX/Y` = 0, `e.button` = 0 모두 실측 일치.
  - 다만 dblclick 의 이벤트 생성자는 **MouseEvent** 로 가이드의 "PointerEvent 객체가 전달됩니다" 와 다름(아래 03_보강.md 불일치 항목 참고).
- 결과: PASS

#### secret_E_ondblclick_1.xml 수정 후 재검증 (2026-09-23, onfocus 제거)

수정 사유: ondblclick 가이드 @spec 의 순서는 `onmousedown > onmouseup > onclick > onmousedown > onmouseup > onclick > ondblclick` 7단계이고 **onfocus 가 없다**. 직전 작성한 secret_E_onclick_1(가이드 순서에 onfocus 포함)에서 바인딩을 그대로 가져와 onfocus 를 출력하는 바람에 validation (3) 원문 7단계와 결과창 8줄이 어긋났다. onfocus 는 이 이벤트의 검증 대상이 아니므로 바인딩/핸들러/라벨 문구에서 제거. (위 onfocus 실측 기록 및 03_보강.md 의 가이드 갭 기록은 그대로 유지)

변경: `createEList(["onmousedown","onmouseup","onclick","ondblclick"])` (onfocus 제거), `scwin.target1_onfocus` 핸들러 삭제, 상단 주석 이벤트 나열에서 onfocus 제거, createLabel 문구 정리(포커스 해제 절차/onfocus 안내 삭제, [확인] 에 가이드 7단계 순서 명시). validation 4건 / con 구성 / description 변경 없음.

MCP(playwright, 포트 61613) 재실측 — console error 0 (password 필드 VERBOSE 안내만).

| 조건 (Target 재생성 -> clear -> 바깥 클릭으로 포커스 해제 -> 실제 locator.dblclick()) | 결과창 출력 |
|---|---|
| disabled 미설정 / readOnly 미설정 (activeElement 없음 = 포커스 없는 상태) | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ ondblclick (disabled 미설정 / readOnly 미설정)` / `e : [object MouseEvent]` |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 (force dblclick 에도 미발생) |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ ondblclick (disabled 미설정 / readOnly true)` / `e : [object MouseEvent]` |

- **포커스가 없던 상태에서도 onfocus 줄이 사라져** 3종 조건 모두 가이드 @spec 7단계와 정확히 일치(이벤트 7줄 + e 1줄).
- 차단 판정은 dispatchEvent 가 아니라 실제 `locator.dblclick()`(disabled 는 force 포함) 으로 측정.
- 결과: PASS

#### secret_E_onclick_1.xml / secret_E_ondblclick_1.xml 라벨 정리 후 재검증 (2026-09-23)

수정 사유(사용자 지시): "비교적 간단한 샘플은 [전제], [절차], [확인] 이 불필요하지 않을까?" -> 항목이 적은 단순 샘플은 머리표지 없이 1~2줄로 압축(UT_01_생성.md §5-1 의 머리표지는 권장 구성). 두 샘플 톤 통일. **스크립트 로직(바인딩/핸들러/validation/description/con 구성)은 변경 없음, createLabel 문구만 교체.**
- 참고: secret_E_onclick_1 의 onfocus 제거 지시는 사용자 번복으로 **철회**되었다. onclick 가이드 @spec 순서에 onfocus 가 명시돼 있어 `createEList(["onmousedown","onfocus","onmouseup","onclick"])` 와 `scwin.target1_onfocus` 핸들러, 상단 주석은 **수정 전 그대로 유지**한다(제거 작업은 착수 전 취소되어 실제 변경 이력 없음).

MCP(playwright, 포트 61613) 재실측 — XML 수정 후 새 라벨이 그대로 렌더되어 wpack 번들 반영 확인. console error 0 (password 필드 VERBOSE 안내만).

| 샘플 | 라벨 렌더(innerHTML) |
|---|---|
| secret_E_onclick_1 | `조건 선택 후 Target 재생성 -> 결과창 clear -> 바깥 영역을 클릭해 포커스를 해제한 뒤 target1 을 마우스 왼쪽 버튼으로 클릭합니다.<br>각 이벤트는 발생 즉시 한 줄씩 출력되며, target1 이 이미 포커스를 가진 상태에서 다시 클릭하면 onfocus 줄은 출력되지 않습니다.` |
| secret_E_ondblclick_1 | `조건 선택 후 Target 재생성 -> 결과창 clear -> target1 을 마우스 왼쪽 버튼으로 더블 클릭합니다.<br>각 이벤트는 발생 즉시 한 줄씩 출력됩니다.` |

- `\n` -> `<br>` 변환 및 `->` escape 정상. 머리표지 제거 후에도 조작/확인 포인트가 모두 남아 있음.

| 샘플 / 조건 | 실제 조작 | 결과창 출력 |
|---|---|---|
| secret_E_onclick_1 (disabled 미설정 / readOnly 미설정, 포커스 해제 상태) | `locator('#mf_target1').click()` | `▶ onmousedown` / `▶ onfocus` / `▶ onmouseup` / `▶ onclick (disabled 미설정 / readOnly 미설정)` / `e : [object PointerEvent]` |
| secret_E_ondblclick_1 (disabled 미설정 / readOnly 미설정) | `locator('#mf_target1').dblclick()` | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ ondblclick (disabled 미설정 / readOnly 미설정)` / `e : [object MouseEvent]` |

- 두 샘플 모두 라벨 교체 전 실측 결과와 동일(onclick 은 onfocus 포함 4줄 + e, ondblclick 은 7줄 + e). dispatchEvent 미사용.
- 결과: PASS

#### secret_E_onclick_1.xml / secret_E_ondblclick_1.xml 라벨 제거 후 재검증 (2026-09-23)

수정 사유(사용자 지시): "단순 샘플은 라벨(테스트정보 기재된) 미생성 해도 될거같아" -> 두 샘플 모두 UT_01_생성.md "라벨 생성 여부" 의 생략 기준 3개를 모두 충족(① 조작이 자명: 조건 선택 -> Target 재생성 -> 클릭/더블클릭 ② 숨은 전제·고정 설정 없음 ③ 라벨 내용이 validation 문구/con_ 이름과 중복뿐)하여 `$c.gcm.createLabel(...)` 호출을 **삭제**.

변경: `comp_init` 의 createLabel 호출과 `label1` 삭제 분기 제거(주석 "기존 컴포넌트 있으면 삭제" -> "기존 target 있으면 삭제"). **validation 4건 / description / con_disabled / con_readOnly / 이벤트 바인딩(createEList) / 핸들러는 무변경**, onclick 의 onfocus 바인딩·핸들러도 가이드 @spec 순서에 포함되므로 유지.

MCP(playwright, 포트 61613) 재실측 — XML 수정 후 라벨이 사라진 화면이 렌더되어 wpack 번들 반영 확인(수동 wpack 변환 없음). console error 0 (password 필드 VERBOSE 안내만).

| 샘플 | 라벨 DOM(`#mf_label1`) | target1 | 화면 구성 |
|---|---|---|---|
| secret_E_onclick_1 | 없음 | 정상 생성 (`#mf_target1`) | ■ 테스트 설명/유효성 - ■ Target 재생성 조건(disabled/readOnly) - Target 재생성 + ■ 테스트 샘플 - ■ 리턴 값 확인 |
| secret_E_ondblclick_1 | 없음 | 정상 생성 (`#mf_target1`) | 동일 |

| 샘플 / 조건 | 실제 조작 | 결과창 출력 |
|---|---|---|
| secret_E_onclick_1 (disabled 미설정 / readOnly 미설정) | Target 재생성 -> clear -> `locator('#mf_target1').click()` | `▶ onmousedown` / `▶ onfocus` / `▶ onmouseup` / `▶ onclick (disabled 미설정 / readOnly 미설정)` / `e : [object PointerEvent]` |
| secret_E_ondblclick_1 (disabled 미설정 / readOnly 미설정) | Target 재생성 -> clear -> `locator('#mf_target1').dblclick()` | `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ onmousedown` / `▶ onmouseup` / `▶ onclick` / `▶ ondblclick (disabled 미설정 / readOnly 미설정)` / `e : [object MouseEvent]` |

- 결과창 출력은 라벨 제거 전과 **완전히 동일**(onclick 4줄 + e, ondblclick 7줄 + e). Target 재생성 버튼 동작도 정상(라벨이 먼저 생성되던 자리가 비어도 target1 재생성 순서 이상 없음).
- dispatchEvent 미사용, mcp-chrome 일괄 종료 없음.
- 결과: PASS

## secret_E_onfocus_1.xml (2026-09-23)

| 항목 | 결과 |
|---|---|
| XML well-formed | PASS ([xml] 파서 통과. websquare-mcp validate_xml 은 서버(192.168.100.214:15748) 연결 실패로 미사용) |
| 페이지 로딩 / target1 생성 | PASS — `<input id="mf_target1" class="w2input " type="password" style="width:200px; height:40px;">`, `options.onFocusClass = "onFocusClass"` |
| createLabel 렌더 | PASS — `#mf_label1` 2줄, `\n` -> `<br>` 정상 변환 |
| console error | 0 (Chrome 의 `[DOM] Password field is not contained in a form` VERBOSE 안내만, 다른 secret 샘플과 동일) |

MCP 실측 (포트 61613, playwright2, 실제 사용자 조작)

| 조건 | 조작 | 결과창 출력 | 판정 |
|---|---|---|---|
| disabled 미설정 / readOnly 미설정 | target1 클릭 | `재생성 직후 class : w2input  / bg : #ffffff` → `disabled 미설정 / readOnly 미설정 -> onfocus 이벤트 발생 / class : w2input onFocusClass / bg : #ffff00` → `e : [object FocusEvent]` | validation 1,2,4 PASS |
| disabled 미설정 / readOnly 미설정 | Tab 키로 포커스 이동 | 클릭과 동일 3줄 | validation 1 PASS (클릭·Tab 모두 발생) |
| disabled=true | target1 클릭(force) | 재생성 직후 줄만(`class : w2input w2input_disabled / bg : #eaeef1`), onfocus 줄 없음. `document.activeElement.id` 는 빈값 = 포커스 자체가 들어가지 않음 | validation 3 PASS |
| readOnly=true | target1 클릭 | onfocus 줄 없음. `document.activeElement.id = "mf_target1"` (포커스는 진입하나 이벤트만 미발생), class = `w2input  w2input_readonly` (onFocusClass 미부착) | validation 3 PASS |

수정 이력
- 최초 createLabel 2번째 줄이 `조건 선택 후 Target 재생성 -> 결과창 clear -> ...` 순서였는데, 실측 결과 clear 가 `comp_init` 이 출력한 `재생성 직후 class` 기준선 줄까지 지워 validation 4 의 대조값이 사라짐 -> `결과창 clear -> 조건 선택 후 Target 재생성 -> ...` 로 순서 교정. 교체 후 재실측 정상.

### secret_E_onkeydown_1.xml (2026-09-23)

- XML well-formed 확인(python minidom). `validate_xml`: websquare-mcp 서버 연결 실패(fetch failed) -> minidom 확인으로 대체(secret_E_ondblclick_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onkeydown_1.js` 자동 생성 확인(HTTP 200). 수동 변환 생략.
- MCP(playwright 세션1, 포트 61613) 렌더 확인: `#mf_target1` 정상 생성(`<input class="w2input " type="password">`), 초기값 `secret`, `#mf_label1` 라벨 3줄 정상 렌더, console error 0.
- 실제 키 입력(page.keyboard / CDP Input)만 사용. dispatchEvent 미사용.

| 조건 / 조작 | 결과창 출력 |
|---|---|
| disabled 미설정 / readOnly 미설정, `a` -> `b` 순차 입력 | `▶ onkeydown (disabled 미설정 / readOnly 미설정) key : a -> getValue() : secret` / `e : [object KeyboardEvent]` / `▶ onkeypress key : a` / `▶ onkeydown (...) key : b -> getValue() : secreta` / `e : [object KeyboardEvent]` / `▶ onkeypress key : b` (최종값 secretab) |
| 같은 키(`c`) 를 누르고 있는 상태(autoRepeat down 6회 후 up) | onkeydown 3블록 출력 후 `...`, onkeypress 3회 출력 후 `...` (실제 입력은 c 6자). 키를 떼고 `d` 입력 시 출력 재개 |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 (onkeydown / onkeypress 모두 미발생) |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onkeydown (disabled 미설정 / readOnly true) key : y -> getValue() : secret` / `e : [object KeyboardEvent]` / `▶ onkeypress key : y` (값은 secret 유지) |
| 한글 IME 입력 (CDP `Input.dispatchKeyEvent` key=Process/keyCode 229 + `Input.imeSetComposition`) | `▶ onkeydown (...) key : Process -> getValue() : secret` / `e : [object KeyboardEvent]` 만 출력, **onkeypress 줄 없음** (값은 secretㄱ) |

판정 요약: 가이드 @spec 5건 모두 실측 일치. onkeydown 시점 getValue() 는 직전 입력값을 반환(`a` 입력 시 secret, `b` 입력 시 secreta)하여 "이전 입력값 반환" 이 두 줄 대비로 그대로 드러난다.

### secret_E_onviewchange_1.xml / _2.xml / _3.xml (2026-09-23)

- XML well-formed 확인(python minidom). `validate_xml`: websquare-mcp 서버 연결 실패(`http://192.168.100.214:15748/... fetch failed`) -> minidom 확인으로 대체(secret_E_onkeydown_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onviewchange_{1,2,3}.js` 자동 생성 확인(HTTP 200). 수동 변환 생략.
- MCP(playwright 세션3, 포트 61613). 공유 탭 레이스 방지를 위해 `page.context().newPage()` 전용 탭 사용. 실제 조작(click / keyboard.type / press Tab / press Enter)만 사용, dispatchEvent 미사용. pageerror 0건.

#### _1 (발생 조건 + info)

| 조작 | 결과창 출력 | 판정 |
|---|---|---|
| target1 클릭 -> `X` 입력 -> label1(바깥) 클릭 | `▶ onchange 발생` / `▶ onviewchange 발생 / 화면 입력창 type : password` / `info.oldValue : secret (string)` / `info.newValue : secretX (string)` | validation 1,3,4,5 PASS (화면은 password 마스킹인데 info 는 평문) |
| target1 클릭 -> 입력 없이 바깥 클릭 | 출력 없음 | validation 6 PASS |
| target1 클릭 -> `Y` 입력 -> Enter | `▶ onchange 발생` / `▶ onviewchange 발생 / ... password` / `info.oldValue : secretX` / `info.newValue : secretXY` | validation 7 PASS |
| 포커스 미진입 상태에서 setValue 클릭(par_value=abc) | `setValue(abc) 호출 -> 호출 전 값 : secretXY` / `▶ onchange 발생` (onviewchange 줄 없음) | validation 2,8 PASS |

#### _2 (ref 바인딩 DataList 선택 변경)

| 조작 | 결과창 출력 | 판정 |
|---|---|---|
| 행0 선택 -> 행1 선택 -> 행2 선택 | `DataList 행0 선택 (label : data_A)` / `선택 후 target1 값 : data_A` / `DataList 행1 선택 (label : data_B)` / `선택 후 target1 값 : data_B` / `DataList 행2 선택 (label : data_B)` / `선택 후 target1 값 : data_B` — onviewchange 줄 없음 | validation 1 PASS |
| (대조) target1 클릭 -> `Q` 입력 -> 바깥 클릭 | `▶ onviewchange 발생` / `info.oldValue : data_B` / `info.newValue : data_BQ` | 핸들러 배선 정상 = 위 미출력이 미발생임을 보장 |

secret 은 password 마스킹이라 화면으로 값 변경을 볼 수 없으므로 `선택 후 target1 값` 줄로 "데이터 선택이 실제 반영되었다" 는 전제를 남겼다(onchange_2 대비 추가된 줄).

#### _3 (이벤트 발생 순서)

| 조작 | 결과창 출력 순서 | 판정 |
|---|---|---|
| 마우스 : clear -> 클릭 -> `X` 입력 -> 바깥 클릭 | onkeydown, onkeypress, onkeyup(입력) / **onchange > onviewchange > onblur** | validation 1 PASS |
| Tab : clear -> 클릭 -> `Y` 입력 -> Tab | 입력 3줄 / **onkeydown > onchange > onviewchange > onblur** | validation 2 PASS |
| Enter : clear -> 클릭 -> `Z` 입력 -> Enter | 입력 3줄 / **onkeydown > onkeypress > onkeyup > onchange > onviewchange > onblur** | validation 3 PASS |

수정 이력
- _3 의 createLabel 절차가 처음에는 secret_E_onchange_3 문구를 그대로 가져와 `target1 클릭 -> 값 입력 -> 결과창 clear -> 포커스 해제` 였는데, **clear 버튼 클릭 자체가 target1 의 포커스 해제라서** 그 시점에 onchange/onviewchange/onblur 가 발생해 출력된 뒤 곧바로 clear 로 지워지고, 이어지는 "포커스 해제" 에서는 값이 이미 커밋돼 onblur 만 남는다. -> `결과창 clear -> target1 클릭 -> 값 입력 -> 포커스 해제` 로 순서 교정하고, 입력 중 발생한 onkeydown/onkeypress/onkeyup 줄이 앞에 붙는다는 안내를 [확인] 에 추가. 교정 후 재실측 정상(위 표). **secret_E_onchange_3.xml 도 같은 절차 문구를 갖고 있어 동일 교정이 필요하다(본 작업 범위 밖이라 미수정).**

### secret_E_onkeypress_1.xml (2026-09-23)

- XML well-formed 확인(python minidom). `validate_xml`: websquare-mcp 서버 연결 실패 -> minidom 확인으로 대체(secret_E_onkeydown_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onkeypress_1.js` 자동 생성 확인(HTTP 200). 수동 변환 생략.
- MCP(playwright 세션1, 포트 61613) 렌더 확인: `#mf_target1` 정상 생성(`<input class="w2input " type="password" style="width:200px; height:40px;">`), 초기값 `secret`, `#mf_label1` 라벨 3줄 정상 렌더, 유효성 8건 정상 출력, console error 0(VERBOSE `Password field is not contained in a form` 3건은 크로미움 안내 로그).
- 실제 키 입력(page.keyboard / CDP Input)만 사용. dispatchEvent 미사용.

| 조건 / 조작 | 결과창 출력 |
|---|---|
| disabled 미설정 / readOnly 미설정, `a` -> `b` 순차 입력 | `▶ onkeydown key : a` / `▶ onkeypress (disabled 미설정 / readOnly 미설정) key : a -> getValue() : secret` / `e : [object KeyboardEvent]` / `▶ onkeydown key : b` / `▶ onkeypress (...) key : b -> getValue() : secreta` / `e : [object KeyboardEvent]` (최종값 secretab) |
| `Shift` / `F2` / `ArrowLeft` 입력 | `▶ onkeydown key : Shift` / `▶ onkeydown key : F2` / `▶ onkeydown key : ArrowLeft` 만 출력, **onkeypress 줄 없음** |
| 같은 키(`c`) 를 누르고 있는 상태(autoRepeat down 6회 후 up) | onkeydown 3줄 + `...`, onkeypress 3블록 + `...` (실제 입력은 c 6자). 키를 떼고 `d` 입력 시 출력 재개 |
| 한글 IME 입력 (CDP `Input.dispatchKeyEvent` key=Process/keyCode 229 + `Input.imeSetComposition`) | `▶ onkeydown key : Process` 만 출력, **onkeypress 줄 없음** (값은 …ㄱ 으로 입력됨) |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 (onkeydown / onkeypress 모두 미발생) |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onkeydown key : y` / `▶ onkeypress (disabled 미설정 / readOnly true) key : y -> getValue() : secret` / `e : [object KeyboardEvent]` (값은 secret 유지) |

판정 요약: 가이드 8항목 모두 실측 일치. onkeypress 시점 getValue() 는 직전 입력값을 반환(`a` 입력 시 secret, `b` 입력 시 secreta)하여 "이전 입력값 반환" 이 두 줄 대비로 드러난다. 문자가 아닌 키(Shift/F2/ArrowLeft) 와 한글 IME 는 onkeydown 만 출력되고 onkeypress 가 비어 "미발생" 이 대비로 입증된다.

### secret_E_onkeyup_1.xml (2026-09-23)

- XML well-formed 확인(python minidom). `validate_xml`: websquare-mcp 서버 연결 실패(192.168.100.214:15748) -> minidom 확인으로 대체(secret_E_onkeydown_1 / onkeypress_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onkeyup_1.js` 자동 생성 확인(HTTP 200). 수동 변환 생략.
- MCP(playwright 세션3, 포트 61613) 렌더 확인: `#mf_target1` 정상 생성(password 입력창, 초기값 `secret`), `#mf_label1` 라벨 3줄 정상 렌더, 유효성 4건 정상 출력, console error 0(VERBOSE `Password field is not contained in a form` 은 크로미움 안내 로그).
- 실제 키 입력(page.keyboard.press)만 사용. dispatchEvent 미사용.

| 조건 / 조작 | 결과창 출력 | 판정 |
|---|---|---|
| disabled 미설정 / readOnly 미설정, 클릭 후 `a` 입력 | `▶ onkeydown key : a -> getValue() : secret` / `▶ onkeypress key : a -> getValue() : secret` / `▶ onkeyup (disabled 미설정 / readOnly 미설정) key : a -> getValue() : secreta` / `e : [object KeyboardEvent]` | validation 1,2,3 PASS |
| `x` -> `y` -> `z` -> `1` 순차 입력 | 4블록 모두 출력(각 블록에서 onkeydown/onkeypress 는 직전 값, onkeyup 은 갱신된 값 — secret/secretx, secretx/secretxy, secretxy/secretxyz, secretxyz/secretxyz1) | validation 3 PASS + onkeyup 리셋으로 카운터 정상 복구 |
| readOnly=true, 클릭 후 `b` 입력 | `▶ onkeydown key : b -> getValue() : secret` / `▶ onkeypress key : b -> getValue() : secret` / `▶ onkeyup (disabled 미설정 / readOnly true) key : b -> getValue() : secret` / `e : [object KeyboardEvent]` (값은 secret 유지) | validation 4 후단 PASS |
| disabled=true, 입력창 클릭(force) 후 `c` 입력 | 출력 없음 (activeElement 도 비어 포커스 자체가 들어가지 않음) | validation 4 전단 PASS |

판정 요약: 가이드 4항목 모두 실측 일치. @spec 의 "onkeydown, onkeypress 이벤트와 다르게 완전한 입력값 반환" 은 한 액션에서 세 줄이 나란히 찍혀 `secret`(down/press) vs `secreta`(up) 대비로 그대로 드러난다.

### secret_E_onchange_3.xml 라벨 절차 교정 재검증 (2026-09-23)

`secret_E_onviewchange_3.xml` 실측 중 발견된 함정으로 `secret_E_onchange_3.xml` 의 라벨 [절차] 문구만 교정했다(validation 3건 / description / 핸들러 / createEList 구성은 무변경).

수정 사유: 기존 절차 `target1 클릭 -> 값 입력 -> 결과창 clear -> 포커스 해제` 는, **결과창 clear 버튼 클릭 자체가 입력 컴포넌트의 포커스를 해제**하기 때문에 clear 시점에 onchange / onviewchange / onblur 가 이미 발생·출력된 뒤 곧바로 지워진다. 이어지는 해제 조작에서는 값이 이미 커밋된 상태라 onblur 만 남아 사용자가 "이벤트가 안 뜬다" 로 오인한다. (UT_01_생성.md "포커스 해제 계열 샘플의 조작 순서" / [[reference_clear_button_click_releases_focus]])

MCP 재확인(playwright 세션1, 포트 61613, 실제 키/마우스 조작만 사용 — dispatchEvent 미사용):

| 조작 | 결과창 출력 | 판정 |
|---|---|---|
| 결과창 clear -> `#mf_target1` 클릭 -> `End` -> `a` 입력 -> `#mf_label1`(바깥 영역) 마우스 클릭 | `▶ onkeydown` / `▶ onkeyup` / `▶ onkeydown` / `▶ onkeypress` / `▶ onkeyup` / `▶ onchange` / `▶ onviewchange` / `▶ onblur` (최종값 `secreta`) | validation 1 PASS — 입력 중 키 이벤트 5줄 뒤에 `onchange > onviewchange > onblur` 순서가 그대로 남음 |

라벨 렌더 확인: `#mf_label1` innerText 6줄 정상(`\n` -> `<br/>` 변환). wpack `_wpack_/sample/secret/secret_E_onchange_3.js` 는 Studio 가 자동 갱신해 새 문구가 반영되어 있어 수동 변환 불요.

TSQA 영향: `TSQA/src/sample/secret/ts_secret_E_onchange_3.xml` 은 이미 `clear -> click -> 입력 -> 해제` 순으로 작성되어 있고(`inputLines` 상수로 키 이벤트 5줄을 기대값에 반영), 포커스 해제 타깃으로 `#mf_label1` 을 클릭한다. 이번 변경은 라벨 **문구**만 바꾸고 라벨 객체·ID 는 유지되므로 스크립트 영향 없음(수정하지 않음).

### secret_E_onmousedown_1.xml (2026-09-23)

- XML well-formed 확인(python minidom). `validate_xml`: websquare-mcp 서버 연결 실패(`http://192.168.100.214:15748/... fetch failed`) -> minidom 확인으로 대체(secret_E_onkeydown_1 / onkeypress_1 / onkeyup_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onmousedown_1.js` 자동 생성 확인(6,274 bytes, `target1_onmousedown` / `fn_button` 포함). 수동 변환 생략.
- MCP(playwright 세션1, 포트 61613) 렌더 확인: `#mf_target1` 정상 생성(`<input id="mf_target1" class="w2input " type="password" style="width: 200px; height: 40px;">`), 초기값 `secret`, 유효성 3건 정상 출력, **테스트 정보 라벨 없음(`#mf_label1` 미생성)** = 의도대로. console error 0(VERBOSE `Password field is not contained in a form` 9건만 — 크로미움 안내 로그).
- 실제 마우스 조작만 사용(`locator.click({button})` / `page.mouse.down()+up()`). dispatchEvent 미사용([[reference_dispatchevent_click_skips_engine_toggle]]).
- 절차는 [[reference_clear_button_click_releases_focus]] 에 따라 `결과창 clear -> target1 클릭` 순으로 수행.

| 조건 / 조작 | 결과창 출력 | 판정 |
|---|---|---|
| disabled 미설정 / readOnly 미설정, 마우스 **왼쪽** 버튼 누름 | `▶ onmousedown (disabled 미설정 / readOnly 미설정) button : 0 (왼쪽)` / `e : [object MouseEvent]` | validation 1, 2 PASS |
| disabled 미설정 / readOnly 미설정, 마우스 **가운데(휠)** 버튼 누름 | `▶ onmousedown (disabled 미설정 / readOnly 미설정) button : 1 (가운데)` / `e : [object MouseEvent]` | validation 1 PASS (@description "마우스 버튼" = 좌/중/우 전부) |
| disabled 미설정 / readOnly 미설정, 마우스 **오른쪽** 버튼 누름 | `▶ onmousedown (disabled 미설정 / readOnly 미설정) button : 2 (오른쪽)` / `e : [object MouseEvent]` | validation 1 PASS |
| disabled=false / readOnly=false, 왼쪽 버튼 누름 | `▶ onmousedown (disabled false / readOnly false) button : 0 (왼쪽)` / `e : [object MouseEvent]` | validation 1 PASS |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onmousedown (disabled 미설정 / readOnly true) button : 0 (왼쪽)` / `e : [object MouseEvent]` | validation 3 후단 PASS |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 | validation 3 전단 PASS |
| disabled=true + readOnly=true | 출력 없음 | validation 3 전단 PASS (disabled 우선) |

판정 요약: 가이드 3항목 모두 실측 일치. `e` 는 실측 생성자도 **MouseEvent** 로 가이드 `@param` 문구와 일치한다(ondblclick 의 PointerEvent 오기와 달리 갭 없음).

MCP 함정 기록: `#mf_target1` 이 내부 스크롤 컨테이너 안에 있어 `getBoundingClientRect().y` 가 음수(-126)로 나오는 상태에서 `page.mouse.move(box.x, box.y)` 를 쓰면 좌표가 뷰포트 밖이라 **이벤트가 아예 발생하지 않는다**(출력 공백 = "미발생" 으로 오판하기 쉬움). `locator.click()` 은 자동 스크롤하므로 정상. disabled 상태처럼 `locator.click()` 의 enabled 체크에 걸리는 케이스에서 `page.mouse` 를 쓸 때는 **`scrollIntoViewIfNeeded()` 후 boundingBox 를 다시 읽을 것**. 본 검증은 뷰포트를 1280x900 으로 키우고 스크롤 보정한 뒤 재측정했다.

### secret_E_onmouseup_1.xml (2026-09-23)

- XML well-formed 확인(python minidom). `validate_xml`: websquare-mcp 서버 연결 실패(`http://192.168.100.214:15748/... fetch failed`) -> minidom 확인으로 대체(secret_E_onkeydown_1 / onkeypress_1 / onkeyup_1 / onmousedown_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onmouseup_1.js` 자동 생성/갱신 확인(6,127 bytes, `target1_onmouseup` / `fn_button` 포함). 수동 변환 생략.
- MCP(playwright 세션3, 포트 61613) 렌더 확인: `#mf_target1` 정상 생성(`<input id="mf_target1" class="w2input " type="password" style="width: 200px; height: 40px;">`), 유효성 3건 정상 출력, **테스트 정보 라벨 없음(`#mf_label1` count=0)** = 의도대로. console error 0.
- 실제 마우스 조작만 사용(`page.mouse.down({button})` + `page.mouse.up({button})`). dispatchEvent 미사용([[reference_dispatchevent_click_skips_engine_toggle]]).
- 절차는 [[reference_clear_button_click_releases_focus]] 에 따라 `결과창 clear -> target1 위에서 마우스 down/up` 순으로 수행.

| 조건 / 조작 | 결과창 출력 | 판정 |
|---|---|---|
| disabled 미설정 / readOnly 미설정, 마우스 **왼쪽** 버튼 떼기 | `▶ onmouseup (disabled 미설정 / readOnly 미설정) button : 0 (왼쪽)` / `e : [object MouseEvent]` | validation 1, 2 PASS |
| disabled 미설정 / readOnly 미설정, 마우스 **가운데(휠)** 버튼 떼기 | `▶ onmouseup (disabled 미설정 / readOnly 미설정) button : 1 (가운데)` / `e : [object MouseEvent]` | validation 1 PASS (@description "마우스 버튼" = 좌/중/우 전부) |
| disabled 미설정 / readOnly 미설정, 마우스 **오른쪽** 버튼 떼기 | `▶ onmouseup (disabled 미설정 / readOnly 미설정) button : 2 (오른쪽)` / `e : [object MouseEvent]` | validation 1 PASS |
| disabled=false / readOnly=false, 왼쪽 버튼 떼기 | `▶ onmouseup (disabled false / readOnly false) button : 0 (왼쪽)` / `e : [object MouseEvent]` | validation 1 PASS |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onmouseup (disabled 미설정 / readOnly true) button : 0 (왼쪽)` / `e : [object MouseEvent]` | validation 3 후단 PASS |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 | validation 3 전단 PASS |
| disabled=true + readOnly=true | 출력 없음 | validation 3 전단 PASS (disabled 우선) |

판정 요약: 가이드 3항목 모두 실측 일치. `e` 의 실측 생성자도 **MouseEvent** 로 가이드 `@param` 문구와 일치한다(ondblclick 의 PointerEvent 오기 같은 갭 없음 — onmousedown_1 과 동일 결론).

MCP 함정 재확인(onmousedown_1 기록과 동일 현상): 기본 뷰포트에서는 `#mf_target1` 이 화면 밖이라 `page.mouse.move(box.x, box.y)` 좌표가 뷰포트를 벗어나 **이벤트가 아예 발생하지 않고 결과창이 공백**이 된다 — "미발생" 으로 오판하기 쉽다. 본 검증은 뷰포트를 1400x1000 으로 키우고 매 조작 전 `scrollIntoViewIfNeeded()` 후 boundingBox 를 다시 읽어 재측정했다(첫 패스는 이 함정으로 전 케이스 공백이 나왔고, 네이티브 `addEventListener('mouseup')` 프로브로 원인을 특정함).

포트 관련 기록: `wmic process where "name='websquare.exe'"` 가 이 세션에서 PID 를 반환하지 않아(Studio 는 `websquare_26.0911` 로 기동 중) 기존 포트 자동감지 스크립트가 **AnySign4PC 의 10530 포트를 오탐**했다(`websquare.html` 200 + `_wpack_/....js` 200 이지만 본문은 `bad json format` JSON). `Get-NetTCPConnection` 으로 websquare.exe(PID 24480) 가 잡은 61610~61614 중 `_wpack_` 200 인 **61613** 이 실제 미리보기 포트였다(지시받은 포트와 동일).


### secret_E_onmousemove_1.xml (2026-09-23)

- XML well-formed 확인(python minidom). `validate_xml`: websquare-mcp 서버 연결 실패가 이어져 minidom 확인으로 대체(secret_E_onmousedown_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onmousemove_1.js` 자동 생성 확인(6,798 bytes). 수동 변환 생략.
- MCP(playwright 세션1, 포트 61613, 뷰포트 1280x900) 실측: `#mf_target1` 정상 생성(`<input id="mf_target1" class="w2input " type="password" style="width: 200px; height: 40px;">`), 초기값 `secret`, 유효성 4건 정상 출력, `#mf_label1` 3줄 렌더 정상(`
` -> `<br/>`), console error 0(VERBOSE `Password field is not contained in a form` 6건만).
- 실제 마우스 이동만 사용(`page.mouse.move` 10~11 스텝). dispatchEvent 미사용([[reference_dispatchevent_click_skips_engine_toggle]]).
- **MCP 함정 재확인**: `#mf_target1` 은 내부 스크롤 컨테이너 안이라 `scrollIntoViewIfNeeded()` 후 `boundingBox()` 를 다시 읽어야 한다. 본 검증은 스크롤 보정 후 box=`{x:415, y:274, w:200, h:40}` 로 안정 측정되어 전 케이스에서 동일 좌표를 사용했다(onmousedown_1 에서 기록된 y 음수 오판 재현 없음).

| 조건 / 조작 | 결과창 출력 | 판정 |
|---|---|---|
| disabled 미설정 / readOnly 미설정, 좌->우 30여 회 이동 | `▶ onmousemove (disabled 미설정 / readOnly 미설정)` / `e : [object MouseEvent]` x3 + `...` | validation 1, 2, 3 PASS (이동 횟수가 훨씬 많은데 3회까지만 출력되고 `...` 로 줄어듦 = 연속 발생) |
| 결과창 clear 후 재이동(Target 재생성 없음) | 출력 없음 | 카운터가 `comp_init` 에서만 초기화됨 — 라벨 [확인] 문구와 일치 |
| Target 재생성 후 재이동 | 3회 + `...` 재출력 | 카운터 초기화 정상 |
| disabled=false / readOnly=false | `▶ onmousemove (disabled false / readOnly false)` x3 + `...` | validation 1, 3 PASS |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onmousemove (disabled 미설정 / readOnly true)` x3 + `...` | validation 4 후단 PASS |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 | validation 4 전단 PASS |
| disabled=true + readOnly=true | 출력 없음 | validation 4 전단 PASS (disabled 우선) |

판정 요약: 가이드 4항목 모두 실측 일치. `e` 직렬화가 `[object MouseEvent]` 로 가이드 `@param` 문구(MouseEvent)와 일치한다.


### secret_E_onmouseover_1.xml (2026-09-23)

- XML well-formed 확인. `validate_xml`: websquare-mcp 서버(`192.168.100.214:15748`) 연결 실패가 이어져 생략(secret_E_onmousedown_1 / onmousemove_1 선례와 동일).
- wpack: `_wpack_/sample/secret/secret_E_onmouseover_1.js` 자동 생성 확인(6,413 bytes, 09:44). 수동 변환 생략.
- 포트: `Get-NetTCPConnection` 으로 websquare.exe(PID 24480) 가 61610~61614 를 모두 listen 중임을 확인하고, 지시받은 기본값 **61613** 으로 접속해 정상 동작. AnySign4PC 오탐(10530) 재현 없음(자동감지 스크립트 미사용).
- MCP(playwright 세션3, 포트 61613, **뷰포트 1400x1000**) 실측: `#mf_target1` 정상 생성(`<input id="mf_target1" class="w2input " type="password" style="width: 200px; height: 40px;">`), 유효성 3건 정상 렌더(`mf_wf_body_left_validation0~2`), console error 0(VERBOSE `Password field is not contained in a form` 5건만).
- 실제 마우스 이동만 사용(`page.mouse.move` 5~12 스텝). 매 조작 전 `scrollIntoViewIfNeeded()` 후 `boundingBox()` 재취득 -> box=`{x:451, y:204, w:200, h:40}` 로 안정. dispatchEvent 미사용([[reference_dispatchevent_click_skips_engine_toggle]]).
- 진입 절차: 매 케이스마다 ① 포인터를 (1200,900) 으로 대피 -> ② 결과창 clear -> ③ 컴포넌트 상단 80px 지점으로 이동 -> ④ 중앙으로 진입. 대피 단계를 넣어야 직전 케이스의 포인터 위치 때문에 진입이 성립하지 않는 상황을 막을 수 있다.

| 조건 / 조작 | 결과창 출력 | 판정 |
|---|---|---|
| disabled 미설정 / readOnly 미설정, 영역 밖 -> 안으로 진입 | `▶ onmouseover (disabled 미설정 / readOnly 미설정)` / `e : [object MouseEvent]` | validation 1, 2 PASS |
| 위 상태에서 영역 **안에서만** 추가로 2회 이동 | 추가 출력 없음(2줄 그대로) | 진입 시 1회만 발생 = 연속 발생 이벤트 아님 -> 앞 3회 + `...` 출력 규약 미적용 근거 |
| disabled=false / readOnly=false | `▶ onmouseover (disabled false / readOnly false)` / `e : [object MouseEvent]` | validation 1, 3 PASS |
| readOnly=true (`class="w2input  w2input_readonly" readonly="readonly"`) | `▶ onmouseover (disabled 미설정 / readOnly true)` / `e : [object MouseEvent]` | validation 3 후단 PASS |
| disabled=true (`class="w2input w2input_disabled" disabled="disabled"`) | 출력 없음 | validation 3 전단 PASS |
| disabled=true + readOnly=true | 출력 없음 | validation 3 전단 PASS (disabled 우선) |

판정 요약: 가이드 3항목 모두 실측 일치. `e` 직렬화가 `[object MouseEvent]` 로 가이드 `@param` 문구와 일치한다.

### secret_E_onmouseout_1.xml (2026-09-23)

| 항목 | 결과 |
|---|---|
| XML 파싱 | OK (xml.dom.minidom) |
| 네임스페이스/head/body 구조 | OK (secret_E_onmouseup_1 / onmousedown_1 / onmousemove_1 과 body 마크업 동일) |
| tData description / validation 3건 렌더 | OK (좌측 패널 실측: (1)(2)(3) 그대로 표시) |
| con_ 구성 | con_disabled / con_readOnly (gdl_boolean, 기본 코드 "1" = 미설정) |
| 이벤트 바인딩 | `$c.gcm.createEList(["onmouseout"])` 단일 |
| 핸들러 출력 | `▶ onmouseout (disabled x / readOnly y)` + `e : [object MouseEvent]` 2줄 |
| console error | 0 (password field not in form VERBOSE 안내만) |

MCP 실측 (포트 61613, 세션 1, 뷰포트 1400x1000, 실제 `page.mouse.move` 진입->이탈. dispatchEvent 미사용)

| con_disabled | con_readOnly | 렌더 | 이탈 시 결과창 | 판정 |
|---|---|---|---|---|
| 미설정 | 미설정 | `<input id="mf_target1" class="w2input " type="password">` | `▶ onmouseout (disabled 미설정 / readOnly 미설정)` + `e : [object MouseEvent]` | (1)(2) O |
| true | 미설정 | `class="w2input w2input_disabled" disabled="disabled"` | (출력 없음) | (3) 앞절 O |
| 미설정 | true | `class="w2input  w2input_readonly" readonly="readonly"` | `▶ onmouseout (disabled 미설정 / readOnly true)` + `e : [object MouseEvent]` | (3) 뒤절 O |
| false | false | `class="w2input "` | `▶ onmouseout (disabled false / readOnly false)` + `e : [object MouseEvent]` | 대조 O |

- 이탈 1회당 출력 정확히 2줄(= 이벤트 1회) — secret 은 `<input>` 단일 요소라 자식 요소 경유 중복 mouseout 이 없다.
- 좌표 함정 회피: 매 조작 전 `scrollIntoViewIfNeeded()` 후 `boundingBox()` 재취득, 이탈 좌표는 박스 하단 +150~180px(뷰포트 1000px 내).

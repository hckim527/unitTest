# footer_P — 02 검증

---

### footer_P_class_1.xml

| 항목 | 결과 |
|---|---|
| XML 구조 (네임스페이스 / head·body / description·validation) | PASS |
| 스크립트 로직 (버튼 onclick ↔ 핸들러 정의 일치, setReturnValue 출력 2회 = validation 2건) | PASS |
| UI 구성 규칙 (UT_01 §7 정적 배치 — wf_body_sample 삭제, grp_sample > grp_comp 직접 배치, comp_init/Target 재생성 버튼 없음) | PASS |
| MCP 런타임 검증 | **PASS** |

#### MCP 실측 (engine R1641, port 49920, mcp__playwright3)

- 화면 동일성 확인: 본문에 `footer > P > class` 노출 확인 후 결과 판독 (병렬 에이전트 탭 공유 오판 방지). 버튼 클릭 + 결과창 읽기를 단일 `browser_run_code` 호출로 수집.
- `document.querySelectorAll("footer")` → 2개
  - `[0] id=target1 class="oddClass evenClass"` → tagName `FOOTER`, computed border `1px solid rgb(255, 0, 0)` / border-radius `15px` / display `block`
  - `[1] id=target2 class=""` (class 미설정) → border `0px none rgb(33, 43, 54)` / border-radius `0px`
- 렌더된 class 에 `w2footer` / `w2group` 등 **웹스퀘어 기본 클래스 미추가** 확인
- `<footer>` 는 raw HTML pass-through 로 Scope 접두사 없이 `target1`/`target2` 그대로 (비교: WebSquare 버튼은 `mf_btn_getClassName`)
- 버튼(`class 확인`) 출력
  ```
  target1 적용 스타일 - border : 1px solid rgb(255, 0, 0) , border-radius : 15px
  렌더된 class 속성 - target1 : 'oddClass evenClass' , target2 : ''
  ```
- console error / pageerror **0건**
- body_left description / validation 2건 정상 렌더

**MCP 검증: PASS / 발견 이슈 없음 (수정 이력 없음)**

---

### footer_P_id_1.xml

- **XML 구조**: WebSquare 네임스페이스/head-body 정상. `description` 1건 + `validation` 5건 정의. UT_01 §7 정적 배치 규칙 적용(`wf_body_sample` 미사용, `grp_sample > grp_comp` 직접 배치, `comp_init`/`Target 재생성` 버튼 없음).
- **구성**: `grp_comp` 내 정적 `<footer>` 3개 — ① `id="target1"` ② id 미설정 ③ `id="style"`(예약어). 버튼 3개(`btn_getId` / `btn_getScriptAccess` / `btn_getReservedId`), `setReturnValue` 5회 = validation 5건 1:1.
- **스크립트**: `document.querySelector` 사용(§5), 핸들러 3개 ↔ `ev:onclick` 3개 일치. `window.addEventListener("error", ...)` 로 예약어 id 의 JS 오류 건수 카운트.
- **MCP 검증 (port 49920, `websquare/websquare.html?w2xPath=/sample/footer/footer_P_id_1.xml`)**: 화면이 본 샘플(`[footer > P > id]`)임을 confirm 후 버튼 3개 클릭 + 결과창 읽기를 1회 호출로 수행.

실측 결과창 5행:
```
id 설정 - 렌더된 id : target1 , '#target1' 조회 개수 : 1
id 미설정 - 자동 부여된 id : wq_uuid_11
Scope 적용 여부 - footer 의 id : target1 , 비교용 WebSquare button(btn_getId) 의 렌더 id : mf_btn_getId
스크립트 접근 - id 설정(target1) : 조회 성공 (내용 변경 반영) , id 미설정 Footer 를 지정 id(target2) 로 조회 : 조회 실패
예약어(style) id 설정 - 렌더된 id : style , 페이지 로드 후 발생한 JS 오류 건수 : 0
```
렌더된 DOM 실측: `<footer id="target1">` / `<footer id="wq_uuid_11">` / `<footer id="style">`.

- **console error 0** → **MCP 검증: PASS**

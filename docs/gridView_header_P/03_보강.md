### header_P_id_1.xml

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| `@description` GridView Header 의 ID 를 설정합니다. | (1) GridView Header 의 ID 를 설정합니다. | ✓ |
| `@propval <String>` GridView Header 의 ID 를 설정합니다. | (1) 과 동일 의미 → 통합 | ✓ |
| `@propval |` 속성값은 참고용으로 사용되며 GridView 렌더링 시 엔진에서 부여한 ID 값으로 대체됩니다. | (2) 동일 문구 | ✓ |
| `@spec` Header 영역과 Header Column 영역은 별도로 정의됩니다. | (3) 동일 문구 | ✓ |
| `@sample` / `@related` (내용 없음) | - | 해당 없음 |

- validation 3건 (가이드 원문 유지, 임의 추가/누락 없음). `necessary Y` / `dynamic N` 이므로 con_id 변경은 `Target 재생성` 으로만 반영.

#### MCP 실측 raw (엔진 R: websquare_25.0822)
- 선언 `w2:header id="header1"` → 렌더 DOM 에 `header1` / `mf_header1` **0건**. Header 영역 실제 id 는 항상 `{gridView id}_head_table` (= `mf_target1_head_table`), 엔진 하드코딩 `layoutController.js:1018 attr.id = this.id + "_head_table"`.
- Header 영역 요소는 **`<table>` 이 아니라 `mf_target1_body_table` 내부의 `<thead>`** (footer 의 `_foot_table`, body 의 `_body_table` 은 별도 table 인 것과 다름). → 조회 시 `table[id$=...]` 로 잡으면 미검출.
- Header **Column** 은 선언 id 가 유지되어 `th#mf_target1_header_col1` ~ `_col4` 로 렌더 (footer Column 과 동일 동작, gBody Column 이 `mf_target1_cell_{row}_{col}` 로 대체되는 것과 대비) → `@spec` 의 "Header 영역과 Header Column 영역은 별도 정의" 를 실측으로 입증.
- 선언값은 `target1.struct.head.attr = {"id":"header1"}` 에만 보존됨.
- `w2:header` 는 구조 엘리먼트라 별도 컴포넌트 객체 없음 (`WebSquare.util.getComponentById('header1'/'mf_header1')` → null).

---

### header_P_class_1.xml

대상 API : `WebSquare.uiplugin.gridView.header.class` (gridView 하위 요소 `<w2:header>` 의 속성, `@tagversion 2.0`, `@dynamic N`, `@necessary N`, `@type styleclass`)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| `@description` GridView Header 에 적용하려는 클래스의 이름을 설정합니다. | description 필드 | ✓ (한 문장이며 `@propval` 본문과 동일 → 중복 validation 미생성, UT_01 §1) |
| `@propval <String>` GridView Header 에 적용하려는 클래스의 이름을 설정합니다. | (1) 동일 | ✓ |
| `@propval \|` 속성값은 참고용으로 사용되며 GridView 렌더링 시 엔진에서 부여한 기본 클래스만 적용됩니다. | (2) 동일 | ✓ |
| `@spec` Header 영역과 Header Column 영역은 별도로 정의됩니다. | (3) 동일 | ✓ |
| `@related` `header.columnClass` | - | 제외(UT_01 §2 related 제외 규칙). columnClass 는 미설정으로 두고 동작 검증 버튼도 만들지 않음 |
| `@default` (빈값) / `@necessary N` | con_class '미설정' 옵션 + 재생성으로 커버 | ✓ (`@propval` 에 default 표기가 없어 별도 "미설정 시" 항목은 생성하지 않고 옵션 케이스로만 확인) |

누락 0건 / validation 3건 ↔ `setReturnValue` 3행.

**보강 체크리스트**

- [x] `@description` 내용이 validation 에 반영됨
- [x] `@propval` 항목(본문 + `|` 하위절) 전부 validation 존재 — enum 타입 아님(String)
- [x] param 없음(Property) → 필수여부 N param 기본값 validation 해당 없음
- [x] param 통합 없음(해당 없음)

**구성 방식**

- `@dynamic N` 이므로 런타임 변경 없이 **렌더 시점 설정**으로만 검증. `con_class` selectbox(미설정 / `oddClass` / `oddClass evenClass`) 변경 후 `body_sample.xml` 기본 제공 `Target 재생성` 버튼(→ `comp_init`) 으로 반영. 중복 재생성 버튼 미생성.
- `createBasicGridView` 는 header 에 `class` 를 넣을 수 없으므로 `comp_init` 에서 `<w2:header ... class="...">` 를 포함한 gridView XML 을 직접 구성해 `$p.dynamicCreate` 로 생성 (common.xml 무수정).
- footer 판(`footer_P_class_1.xml`) / gBody 판(`gBody_P_class_1.xml`) 과 동일 구조를 유지하되 `w2:header` 에만 class 를 설정.

**실측 raw 값 (MCP playwright 세션1, console error 0)**

- 선언 마크업 : `<w2:header style="" id="header1" class="oddClass evenClass"> ... </w2:header>`
- `target1.struct.head.attr` = `{"id":"header1","className":"oddClass evenClass"}`
- Header 영역 요소 : **`thead#mf_target1_head_table`**, `className` = **`gridHeaderTableDefault`** (설정한 `oddClass evenClass` **미적용**)
- Header Column 영역 요소 : `th#mf_target1_column1`, `className` = `gridHeaderTDDefault gridHeaderTDDefault_data gridHeaderTDDefault_background  mf_target1_header__column0`
- Header row 요소 : `tr` (id 없음), `className` = `gridHeaderStyle_0`
- `target1.options` 실측 : `headerTableClass="gridHeaderTableDefault"`, `headerTrClass="gridHeaderTRDefault"`, `headerTdClass="gridHeaderTDDefault"`, `bodyTableClass="gridBodyTableDefault"`
- `con_class` = `oddClass`(단일) / `(미설정)` 으로 재생성해도 동일하게 `gridHeaderTableDefault`
- 참고 : Body table(`table#mf_target1_body_table`) 의 className 도 `gridHeaderTableDefault` (`layoutController.js:720` 에서 `headerTableClass` 재사용)

→ **가이드(`@propval |`) 와 실측 일치. 결함 아님.** `headerTableClass` 는 footer/gBody 와 달리 엔진이 실제 사용하는 값이지만, 그 값 자체가 항상 기본값으로만 채워지므로 선언한 class 는 렌더 결과에 반영되지 않는다.

**원인 (엔진 소스 대조, 참고)**

- `uiplugin/gridView/layoutController.js:287` : `this.options.headerTableClass = this.struct.head.attr["class"] || "gridHeaderTableDefault";`
- 파서가 `class` 를 **`className` 키로 정규화**해 저장(`struct.head.attr.className` 실측 확인) → `attr["class"]` 는 항상 `undefined` → 기본값 폴백.
- 즉 footer(`:301`) / gBody(`:290`) 와 **원인 동일**. 다만 footer/gBody 는 계산값이 미사용(gBody) 이거나 별도 폴백 경로였던 반면, header 는 `headerTableClass` 가 `:719`(header filler table) / `:720`(body table) / `:1019`(getHeaderHtmlInfo) 에서 실제 사용되는데도 값이 기본값 폴백이라 결과가 같다.

**@spec 근거 (Header 영역 / Header Column 영역 분리)**

- Header 영역 = `thead#mf_target1_head_table` (header 의 `class` 대상, 기본 클래스 `gridHeaderTableDefault`)
- Header Column 영역 = `th#mf_target1_column1` (header 의 `columnClass` 대상, 기본 클래스 `gridHeaderTDDefault` 계열)
- 두 영역이 별개 요소·별개 기본 클래스로 렌더됨을 출력해 "별도 정의"를 입증. columnClass 속성 자체의 동작은 검증하지 않음.

**주의 (다음 작업 참고)**

- 기본 drawType(browserScroll)에서 GridView Header 영역은 `table` 이 아니라 **`thead#{id}_head_table`** 로 렌더된다. `drawTypeX="hybrid"` 일 때만 별도 요소 구조가 달라진다. 따라서 Header 영역 조회는 `table[id$='_head_table']` 이 아니라 `[id$='_head_table']` 로 해야 한다. (footer 는 `table[id$='_foot_table']` 로 조회 가능 — 비대칭)

**w-pack 변환**

- `_wpack_/sample/gridView_header/header_P_class_1.js` 생성 + touch 완료.

---

### header_P_columnClass_1.xml

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| `@description` GridView Header 의 각 Cell(th) 영역에 적용할 클래스의 이름을 설정합니다. | description 필드 (첫 문장 1개) | ✓ |
| `@propval <String>` CSS 파일이나 style 블럭에 정의된 클래스 이름을 설정합니다. | (1) CSS 파일이나 style 블럭에 정의된 클래스 이름을 설정합니다. | ✓ |
| `@propval \|` 하나의 클래스 이름을 설정할 수 있으며 공백으로 두 개 이상의 클래스를 설정할 수 없습니다. | (2) 동일 문구 | ✓ |
| `@propval \|` 속성값은 Header Cell(th) 영역의 class 속성에 추가되는 것이 아니라 기존값을 치환합니다. | (3) 동일 문구 | ✓ |
| `@propval \|` 속성값을 설정하지 않으면 기본 클래스인 "gridHeaderTDDefault" 가 설정됩니다. | (4) 동일 문구 (큰따옴표 → 작은따옴표) | ✓ |
| `@spec` columnClass 속성값을 기준으로 파생 클래스 이름이 자동으로 생성되며 다음과 같이 처리됩니다. | (5) columnClass 속성값을 기준으로 파생 클래스 이름이 자동으로 생성됩니다. | ✓ |
| `@spec \|` • 일반 Header Cell 에는 "{columnClass} {columnClass}_data {columnClass}_background" 등의 클래스가 설정됩니다. | (6) 동일 문구 | ✓ |
| `@spec \|` • RowNum Column 의 Header Cell 에는 "{columnClass} {columnClass}_rowNumber {columnClass}_background" 등의 클래스가 설정됩니다. | (7) 동일 문구 | ✓ |
| `@spec \|` • RowStatus Column 의 Header Cell 에는 "{columnClass} {columnClass}_rowStatus {columnClass}_background" 등의 클래스가 설정됩니다. | (8) 동일 문구 | ✓ |
| `@related` `WebSquare.uiplugin.gridView.header.class` | - | 별개 API → 제외 (UT_01 §2) |
| `@sample` (내용 없음) | - | 해당 없음 |

- validation 총 8건. 누락/임의추가 없음.
- gBody 판에 있는 두 번째 `@spec`("접두사 체계 CSS 를 직접 모두 정의해야 합니다")은 **header 가이드에 없으므로 validation 에 추가하지 않음.** 다만 화면에서 차이가 드러나야 하므로 접두사 체계 CSS 는 샘플 `<style>` 블럭에 정의(운영 정보로 label1 에 안내).

**검증 수단**

- `con_columnClass` (미설정 / `tcHeaderClass` / `tcHeaderClass tcEtcClass`) → `Target 재생성`(기본 `btn_createTarget`) 으로만 반영 (`@dynamic N`).
- `클래스 확인` 버튼 → th className 전문 + computedStyle 실측값 10줄 출력.
- `<style>` 블럭에 `.tcHeaderClass` / `_data` / `_rowNumber` / `_rowStatus` / `_background` / `.tcEtcClass` 정의(!important) → 화면 색·테두리로 파생 클래스 적용이 육안 확인 가능.

**엔진 근거 (실측 일치 확인)**

- `uiplugin/gridView/layoutController.js:289` : `this.options.headerTdClass = this.struct.head.attr.columnClass || "gridHeaderTDDefault";` — 치환/기본값 폴백.
- `:846` : `cell["class"] = className + " " + className + "_data";` — `_data` 파생.
- `:1384~1386` : `headerTdCCss = tdClass + "_background ..."` — **`_background` 는 head(htmlInfo.head) 경로에서만 부여** → footer/gBody 에는 없고 header 에만 존재.
- `:1391~1424` : RowNum th `tdClass + " " + tdClass + "_rowNumber " + headerTdCCss`, RowStatus th `tdClass + " " + tdClass + "_rowStatus " + headerTdCCss`.

**w-pack 변환**

- `_wpack_/sample/gridView_header/header_P_columnClass_1.js` 생성 + touch 완료.

---

### header_P_width_1.xml

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| `@description` GridView Header 영역의 너비를 설정합니다. | (description) `[header > P > width]<br/>GridView Header 영역의 너비를 설정합니다.` | ✓ |
| `@propval` `<String>` GridView Header 영역의 너비를 설정합니다. | (1) GridView Header 영역의 너비를 설정합니다. | ✓ |
| `@propval` `|` 속성값은 참고용으로 사용되며 GridView 렌더링 시 Header 영역의 너비는 Header Column 으로 결정됩니다. | (2) 속성값은 참고용으로 사용되며 GridView 렌더링 시 Header 영역의 너비는 Header Column 으로 결정됩니다. | ✓ |
| `@spec` Header 영역과 Header Column 영역은 별도로 정의됩니다. | (3) Header 영역과 Header Column 영역은 별도로 정의됩니다. | ✓ |
| `@spec` GridView Header 영역은 별도의 컴포넌트 객체가 생성되지 않는 구조 엘리먼트이므로 스크립트에서 컴포넌트로 조회할 수 없으며, width 값을 조회하거나 변경할 대상 객체 자체가 존재하지 않습니다. | (4) 동일 문구 | ✓ |
| `@default` (없음) / `@option` (없음) / `@related` (없음) | - | 대상 없음 |

- `@type string` 은 enum 이 아니므로 값별 validation 분해 대상 아님. `@propval` 값이 `<String>` 단일이라 default 값 2분할(`{값} 설정 시` + `미설정 시`) 대상도 아니며, gBody 판(`gBody_P_width_1.xml`)과 동일하게 가이드 원문 4건을 그대로 유지했다.
- validation 총 4건 — 임의 추가/누락 없음.

**validation ↔ 검증 수단 매핑 (모든 항목에 트리거 존재)**

| validation | 검증 수단 |
|---|---|
| (1) 너비 설정 | `con_width` 옵션(미설정 / 300px / 1200px) + Target 재생성 → `너비 확인` 버튼 `[선언]` 라인 |
| (2) 참고용 · Header Column 으로 결정 | `너비 확인` `[렌더]`(선언 width 로 결정되었는지 false) + `[합계]`(Header Column 폭 합 = Header 영역 너비, 결정되었는지 true) / `con_columnWidth`(80·140·200) 변경 후 재생성 시 실제 너비가 따라 변함 |
| (3) 영역과 Column 영역 별도 정의 | `w2:header` 자체 width 와 `w2:header > w2:column` width 를 각각 옵션으로 분리 선언 + `너비 확인` `[영역]` 라인(Header 영역 요소 id ↔ Header Column 영역 요소 id) |
| (4) 컴포넌트 객체 미생성 · 조회 대상 없음 | `Header 컴포넌트 조회` 버튼 3줄 (선언 id 조회 / 렌더 id 조회 / 조회·변경 대상 객체 존재 여부) |

**보강 체크리스트**

- [x] `@description` 반영
- [x] `@propval` (본문 + `|` 하위절) 반영
- [x] `@spec` 2건 각각 반영 및 각각 실측 트리거 존재
- [x] enum param 없음 / 필수여부 N param 없음 (Property API)
- [x] MCP 재검증 PASS (console error 0)
- [x] w-pack 변환 완료 (`_wpack_/sample/gridView_header/header_P_width_1.js`)

**보강 결과** : 누락 항목 없음. 추가 validation 없이 Phase 1 종료.

**MEMORY 승격 대상**

- gridView 컬럼 폭은 `w2:header > w2:column` 의 width 로만 결정되고 `w2:header` 태그 자체의 width 및 gBody/footer 하위 column width 는 무시된다 (실측 3케이스 + `layoutController.initializeTableInfo()` 선착순 `colWidthList` 기록 근거).

---

### header_P_tooltip_1.xml

대상 API : `WebSquare.uiplugin.gridView.header.tooltip` (gridView 하위 요소 `<w2:header>` 의 속성, `@tagversion 2.0`, `@dynamic N`, `@necessary N`, `@type string`)

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| `@description` GridView Header 영역으로 마우스가 들어왔을때 표시할 툴팁을 설정합니다. | (1) 동일 문구 (description 필드에도 동일 문장) | ✓ |
| `@propval <String>` 툴팁으로 표시할 텍스트를 설정합니다. | (2) 동일 문구 | ✓ |
| `@propval \|` 속성값은 참고용으로 사용되며 실제 툴팁은 Header Cell 값과 GridView 의 관련 속성을 설정해야 합니다. | (3) 동일 문구 | ✓ |
| `@spec` (내용 없음) | - | 해당 없음 (임의 항목 미생성) |
| `@sample` (내용 없음) | - | 해당 없음 |
| `@related` `header.tooltipLocaleRef` / `tooltipHeader` / `tooltipHeaderShowAlways` | - | 별개 API → validation·전용 버튼 미생성 (UT_01 §2). 단 `@propval` 하위절이 요구하는 **전제**이므로 `tooltipHeader="true"`, `tooltipHeaderShowAlways="true"` 는 샘플 구성에 **고정 설정**으로만 반영 |
| `@default` (빈값) / `@necessary N` | con_tooltip '미설정' 옵션 + `Target 재생성` 으로 커버 | ✓ (`@propval` 에 default 표기가 없어 "미설정 시" 별도 항목은 생성하지 않음) |

- validation 총 3건. 누락/임의추가 없음.

**보강 체크리스트**

- [x] `@description` 내용이 validation 에 반영됨
- [x] `@propval` 항목(본문 + `|` 하위절) 전부 validation 존재 — enum 타입 아님(String)
- [x] param 없음(Property) → 필수여부 N param 기본값 validation 해당 없음
- [x] param 통합 없음(해당 없음)

**검증 수단**

- `con_tooltip` (미설정 / `Header 영역 툴팁` / `GridView Header Tooltip`) → `@dynamic N` 이므로 `body_sample.xml` 기본 제공 `Target 재생성`(`btn_createTarget` → `comp_init`) 으로만 반영. 중복 재생성 버튼 미생성.
- `tooltip 반영 확인` 버튼(`btn_checkTooltip`) : 선언값 / `struct.head.attr.tooltip` / 렌더 DOM 에서 선언값이 붙은 요소 개수 / Header 영역·Header Cell 의 `data-tooltip` 을 3행 출력.
- **실제 마우스 hover 로 툴팁 표시** 후 그 상태에서 Header Cell 클릭 → grid render 루트의 **capture 단계 click 리스너**(`scwin.attachTooltipClick`)가 `$c.gcm.getTooltipInfo(wf_body_bottom)` 호출 → 화면에 실제 표시된 툴팁 요소의 textContent/display 를 출력. (bubble 단계/`oncellclick` 은 엔진이 이미 툴팁을 닫은 뒤라 빈값)

**MCP 실측 raw (엔진 R: websquare_25.0822, playwright3, console error 0)**

- 선언 마크업 : `<w2:header style="" id="header1" tooltip="Header 영역 툴팁">`
- `target1.struct.head.attr` = `{"id":"header1","tooltip":"Header 영역 툴팁"}` → **선언값 보존**
- 렌더된 Header 영역 : `<thead id="mf_target1_head_table" data-tooltip="Header 영역 툴팁" style="width: 559px;" class="gridHeaderTableDefault">`
  → 선언값이 **`data-tooltip` 으로 thead 에 그대로 출력**되지만 그 값을 소비하는 코드가 없어 툴팁으로는 표시되지 않음.
- Header Cell(th) : `<th ... id="mf_target1_header_col1" data-value="name1" data-colindex="0" ...><nobr>name1</nobr></th>` — `data-tooltip` / `title` / `aria-label` **없음**
- 선언값이 붙은 요소 개수(tooltip/data-tooltip/title/aria-label 기준) : **1건(thead 뿐)**
- Header Cell hover 시 실제 표시되는 툴팁 : `div#mf_target1_tooltip` (`display: block`), textContent = **`name1`** (Header Cell 값). 선언한 `Header 영역 툴팁` 은 표시되지 않음. `.w2balloonTip` 은 미생성.
- 미설정 케이스 : `struct.head.attr` 에 tooltip 키 없음, thead `data-tooltip` 없음, 툴팁은 동일하게 Header Cell 값(`name2`) 표시.

→ **가이드(`@propval |` 하위절) 와 실측 일치. 결함 아님.** 선언값은 참고용으로만 남고, 실제 Header 툴팁은 Header Cell 값 + GridView 의 `tooltipHeader`(+`tooltipHeaderShowAlways`) 설정으로 만들어진다.

**원인 (엔진 소스 대조)**

- `uiplugin/gridView/layoutController.js:6058` : `this.struct = { head: this.xml2JSON(this.headerXml), ... }` → `w2:header` 속성 전체가 `struct.head.attr` 에 파싱 저장.
- `:834 getTableInfo` : `var attr = _wg.extend({}, tableJSON.attr);` 로 attr 를 그대로 복사하고 `id`/`style` 만 덮어씀 → `tooltip` 키가 살아남아 thead 마크업에 `data-tooltip` 으로 출력됨(엔진이 비표준 속성에 `data-` 접두사를 붙임).
- `uiplugin/gridView/headerController.js:177~182` : 툴팁 등록은 **`th`(headTdArr) 를 순회하며** `td.getAttribute("data-tooltip")` 을 읽어 `_wg.getBody().setToolTip(td, ...)` 를 호출한다. **thead 는 순회 대상이 아니므로** header 영역의 `data-tooltip` 은 소비되지 않는다. (또한 이 경로는 `this.options.tooltipHeader === false` 일 때만 동작)
- `uiplugin/gridView/eventController.js:5024` : `if (this.options.tooltipHeaderShowAlways && (cellInfo.options.tooltipLocaleRef || cellInfo.options.tooltip))` — 여기서 `cellInfo = this.getHeaderInfo(col_id)` 즉 **Header Column(th) 단위 정보**이며 Header 영역(`w2:header`) 속성이 아니다. 본 샘플은 Header Column 에 tooltip 을 주지 않았으므로 `label` 은 `:5100~` 경로로 **Header Cell 값**(`name1`)이 사용된다.

**주의 (다음 작업 참고)**

- `w2:header` 의 `tooltip` 선언값은 "어디에도 안 남는" 것이 아니라 **thead 의 `data-tooltip` 속성으로는 출력된다**. 반영 여부를 `getAttribute("tooltip")` 으로만 확인하면 잘못된 결론(전혀 미출력)에 도달하므로 `data-tooltip` 까지 확인해야 한다.
- Header 툴팁 텍스트 검증은 `title` 속성이 아니라 별도 레이어 `div#{grid id}_tooltip`(class `w2grid_tooltip`) 의 textContent 로 실측해야 한다. 헤드리스에서도 `hover()` → `waitForTimeout(1200)` 로 정상 캡처됨(본 샘플 실측).

**w-pack 변환**

- `_wpack_/sample/gridView_header/header_P_tooltip_1.js` 생성 + touch 완료.

---

### header_P_tooltipLocaleRef_1.xml

| API 가이드 항목 | 샘플 validation | 상태 |
|---|---|---|
| `@description` 클라이언트 다국어가 적용되었을 때 tooltip 속성의 툴팁에 표시할 텍스트의 Key 값을 설정합니다. | description 필드 (첫 문장 1개) | ✓ |
| `@propval <String>` 툴팁에 표시할 다국어 텍스트의 Key 값을 설정합니다. | (1) 동일 문구 | ✓ |
| `@propval \|` 속성값은 참고용으로 사용되며 실제 툴팁은 Header Cell 값과 GridView 의 관련 속성을 설정해야 합니다. | (2) 동일 문구 | ✓ |
| `@spec` (비어 있음) | - | 해당 없음 |
| `@related` header.tooltip / tooltipHeader / tooltipHeaderShowAlways | - | 제외(별개 API). 전제 구성에만 반영 |

- `@propval` 값이 `<String>` 타입 표기라 "{값} 설정 시" 사전조건 접두는 붙이지 않음(같은 폴더의 `header_P_id_1` / `header_P_class_1` 과 동일 처리).
- 추가 validation 없음. 총 2건.

**"미지원" 오판 방지 — 검출 방식 유효성 선확인**

가이드상 이 속성은 전제가 두 겹(① 클라이언트 다국어 적용 ② Header Cell 값 + GridView 관련 속성)이라, 툴팁이 다국어 텍스트로 안 바뀌는 것을 곧바로 "미지원"으로 결론내면 오판이 된다. 결론 전에 **positive control** 을 먼저 실행했다.

- 동일 페이지·동일 hover 검출 경로에서 Header **Column** 단위 옵션에 값을 주입 →
  `mf_target1.getHeaderInfo("column1").options.tooltipLocaleRef = "websquare_name"` 후 재hover
  → 툴팁 textContent 가 `name1` → **`웹스퀘어`** 로 변경 확인.
- 즉 hover + `div#{gridId}_tooltip` textContent 실측 방식은 **다국어 툴팁을 검출할 능력이 있다**. 그럼에도 `w2:header` 선언 시에는 계속 `name1` 이 표시되므로, 이는 검출 실패가 아니라 **설계상 참고용 속성**이라는 결론이 성립.

**MCP 실측 raw (엔진 R: websquare_25.0822, playwright 세션1, console error 0)**

- 선언 마크업 : `<w2:header style="" id="header1" tooltip="헤더 영역 툴팁 원문" tooltipLocaleRef="websquare_name">`
- `target1.struct.head.attr` = `{"id":"header1","tooltip":"헤더 영역 툴팁 원문","tooltipLocaleRef":"websquare_name"}` → **선언값 보존**
- 렌더된 Header 영역 : `<thead id="mf_target1_head_table" data-tooltip="헤더 영역 툴팁 원문" data-tooltiplocaleref="websquare_name" style="width: 559px;" class="gridHeaderTableDefault">`
  → 선언값이 **`data-tooltiplocaleref`(소문자화된 `data-` 접두 속성) 로 thead 에 출력**되지만 소비하는 코드가 없음
- Header Cell(th) : `mf_target1_column1 ~ column4` (`name1`~`name4`), `data-tooltiplocaleref` 없음
- Header Cell hover 시 실제 툴팁 : `div#mf_target1_tooltip` (`display: block`), textContent = **`name1`** (Header Cell 값). `웹스퀘어` 아님
- `target1.getLocaleValue("websquare_name")` = `웹스퀘어`, `("c_label_4_1")` = ` 툴팁 예시`, `("invalid_key")` = `global_undefined` → 다국어 팩 자체는 정상 로드/조회됨(전제 ① 충족 확인)
- `target1.options` : `useLocale=true`, `tooltipHeader=true`, `tooltipHeaderShowAlways=true` (전제 ② 충족 확인)

→ **가이드(`@propval |` 하위절)와 실측 일치. 결함 아님.** 두 전제를 모두 충족시켜도 툴팁 텍스트는 Header Cell 값이며, `w2:header` 의 `tooltipLocaleRef` 는 선언값이 struct/DOM 에 보존되는 참고용 속성이다.

**원인 (엔진 소스 대조)**

- `uiplugin/gridView/layoutController.js:834 getTableInfo` : `var attr = _wg.extend({}, tableJSON.attr);` 로 `w2:header` 속성 전체를 복사하고 `id`/`class`/`style` 만 덮어씀 → `tooltipLocaleRef` 키가 살아남아 thead 마크업에 `data-tooltiplocaleref` 로 출력.
- `uiplugin/gridView/eventController.js:2791~2796` : `srcElem.tagName == "TH"` 일 때만 `if (this.options.tooltipHeader || this.options.headerTooltipDisplay) this.headerOver(e, srcElem);` → **thead 는 hover 대상이 아님**.
- `uiplugin/gridView/eventController.js:5024~5031` : `var cellInfo = this.getHeaderInfo(col_id);` 즉 **Header Column(th) 단위 cellInfo** 를 읽어 `cellInfo.options.tooltipLocaleRef` 로 `getLocaleValue()` 를 수행한다. Header 영역(`w2:header`) 의 속성은 이 경로에 들어오지 않는다.
- 따라서 `tooltipString` 이 비어 `:5102~` 의 fallback 으로 `label = tempSrcElem.getElementsByTagName(dataTag)[0].innerHTML` → **Header Cell 값**이 툴팁으로 표시된다.
- `uiplugin/gridView/headerController.js:177~182` 의 `td.getAttribute("data-tooltiplocaleref")` → `setToolTip()` 경로도 **th 순회 전용**이며 `this.options.tooltipHeader === false` 일 때만 동작하므로 본 샘플 전제(true)에서는 미동작.

**주의 (다음 작업 참고)**

- `w2:header` 의 비표준 속성(`tooltip`, `tooltipLocaleRef` 등) 선언값은 "DOM 에 안 남는" 것이 아니라 **thead 의 `data-{소문자속성명}` 으로 출력된다**. 반영 여부를 `getAttribute("tooltipLocaleRef")` 로만 보면 잘못된 결론에 도달한다.
- Header 툴팁 텍스트는 `title` 속성이 아니라 별도 레이어 `div#{gridId}_tooltip`(class `w2grid_tooltip`) 의 textContent 로 실측해야 하며, `$c.gcm.getTooltipInfo` 의 `[class*="_tooltip"]` 분기가 이를 커버한다(`.w2balloonTip` 은 미생성).
- 다국어 전제는 프로젝트 config 에서 이미 충족(`languagePack.@useLanguagePack=true`, `language=ko`, `/lang/ko.js`) 이므로 샘플에서 `WebSquare.util.loadLocale` 호출이 필요 없다.

**w-pack 변환**

- `_wpack_/sample/gridView_header/header_P_tooltipLocaleRef_1.js` 생성 + touch 완료.

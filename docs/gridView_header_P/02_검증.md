### header_P_id_1.xml

- 렌더링: PASS (`mf_target1` 생성, description "[header > P > id]" 표시 확인)
- console error: 0, alert/dialog 없음
- 버튼 `ID 확인`(btn_checkId) 동작 PASS

| 케이스 (con_id) | 결과창 출력 |
|---|---|
| header1 | `[1] header1` / `[2] mf_target1_head_table, 선언 id DOM 개수 0` / `[3] Header 영역 mf_target1_head_table, Header Column 영역 mf_target1_header_col1` |
| myHeaderArea | `[1] myHeaderArea` / `[2] mf_target1_head_table, 선언 id DOM 개수 0` / `[3] 동일` |
| 미설정 | `[1] (미설정)` / `[2] mf_target1_head_table` / `[3] 동일` |

- 수정 이력: 최초 작성 시 Header 영역을 `table[id$='_head_table']` 로 조회했으나 미검출(`(Header 영역 없음)`).
  엔진은 Header 영역을 **별도 table 이 아닌 `mf_target1_body_table` 내부의 `<thead id="mf_target1_head_table">`** 로 렌더하므로
  선택자를 `[id$='_head_table']` 로, Header Column 조회를 `tr > th` 로 변경 후 재검증 PASS.

---

### header_P_class_1.xml

- 출력 경로 : `unitTest/src/main/webapp/sample/gridView_header/header_P_class_1.xml`
- 미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_header/header_P_class_1.xml`

| 검증 항목 | 결과 |
|---|---|
| XML 구조(네임스페이스/head·body/description·validation) | PASS |
| 스크립트 로직(핸들러 ↔ 버튼 onclick 일치, setReturnValue 출력) | PASS |
| UI 구성(con_class 2단테이블, 재생성은 기본 `btn_createTarget` 재사용, 중복 버튼 없음) | PASS |
| MCP playwright 런타임 검증 | PASS (console error 0) |

**MCP 실측**

- `window.mf_target1` : object (gridView 생성 확인)
- `클래스 확인` 버튼 결과창 출력 (con_class = `oddClass evenClass`)
  ```
  [선언] header 에 설정한 class : oddClass evenClass
  [렌더] Header 영역의 실제 className : gridHeaderTableDefault
  [영역] Header 영역 요소 : mf_target1_head_table / Header Column 영역 요소 : mf_target1_column1
  ```
- `Target 재생성` 후 con_class = `(미설정)` / `oddClass` 케이스도 `[렌더] ... gridHeaderTableDefault` 로 동일
- `label1` 재생성 시 `[현재] header class = ...` 갱신 확인
- console error / warning : 0

**수정 이력**

1. 최초 작성 시 Header 영역 조회를 `table[id$='_head_table']` 로 작성 → 기본 drawType(browserScroll)에서 Header 영역이 `table` 이 아니라 **`thead#mf_target1_head_table`** 으로 렌더되어 null 반환.
   `[id$='_head_table']` (태그 무관 선택자)로 수정 후 정상. (`layoutController.js:796` — drawTypeX 가 hybrid 일 때만 별도 `<thead id=..._head_table>`, 그 외에는 `getTableHtml(head,"thead",...)` 로 thead 렌더)

---

### header_P_columnClass_1.xml

- 출력 경로 : `unitTest/src/main/webapp/sample/gridView_header/header_P_columnClass_1.xml`
- 미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_header/header_P_columnClass_1.xml`

| 검증 항목 | 결과 |
|---|---|
| XML 구조(네임스페이스/head·body/description·validation 8건) | PASS |
| 스크립트 로직(핸들러 ↔ 버튼 onclick 일치, setReturnValue 10건 출력) | PASS |
| UI 구성(con_columnClass 2단테이블, 재생성은 기본 `btn_createTarget` 재사용, 중복 버튼 없음) | PASS |
| MCP playwright 런타임 검증 (playwright2 세션) | PASS (console error / warning 0) |

**MCP 실측 — th className 전문 (3 케이스)**

| con_columnClass | 일반 Header Cell(th) className | RowNum th className | RowStatus th className |
|---|---|---|---|
| `tcHeaderClass` | `tcHeaderClass tcHeaderClass_data tcHeaderClass_background  mf_target1_header__column2` | `tcHeaderClass tcHeaderClass_rowNumber tcHeaderClass_background  mf_target1_header__column0` | `tcHeaderClass tcHeaderClass_rowStatus tcHeaderClass_background  mf_target1_header__column1` |
| (미설정) | `gridHeaderTDDefault gridHeaderTDDefault_data gridHeaderTDDefault_background  mf_target1_header__column2` | `gridHeaderTDDefault gridHeaderTDDefault_rowNumber gridHeaderTDDefault_background  ...` | `gridHeaderTDDefault gridHeaderTDDefault_rowStatus gridHeaderTDDefault_background  ...` |
| `tcHeaderClass tcEtcClass` (공백 2개) | `tcHeaderClass tcEtcClass tcHeaderClass tcEtcClass_data tcHeaderClass tcEtcClass_background  mf_target1_header__column2` | `tcHeaderClass tcEtcClass tcHeaderClass tcEtcClass_rowNumber tcHeaderClass tcEtcClass_background  ...` | `tcHeaderClass tcEtcClass tcHeaderClass tcEtcClass_rowStatus tcHeaderClass tcEtcClass_background  ...` |

- 치환 확정 : `tcHeaderClass` 설정 시 기본 클래스 `gridHeaderTDDefault` 완전 소멸(`[3] false`). 미설정 시에만 `gridHeaderTDDefault` 계열 적용(`[3] true`).
- `_background` 파생 클래스는 **Header 전용**으로 3종 Cell 모두에 추가 부여됨 (gBody/footer 판에는 없음). CSS 실측 : 일반 Cell border-bottom `4px double rgb(112, 48, 160)` 적용 확인.
- 파생 CSS 실측(`tcHeaderClass`) : 일반 Cell background `rgb(255, 242, 204)` + border `2px solid rgb(192, 0, 0)`, RowNum `rgb(189, 215, 238)`, RowStatus `rgb(198, 224, 180)`.
- 공백 2개 케이스 : 엔진이 문자열 전체를 class 에 붙인 뒤 **마지막 토큰에만** 접미사를 붙여 `tcHeaderClass` 가 중복 부여되고 접두사 체계가 깨짐(`[9] false`). gBody 판과 동일 거동.

**수정 이력**

1. 최초 작성 시 Header th 조회를 `table[id$='_head_table']` 로 작성 → null. 기본 drawType 에서 Header 는 별도 table 이 아니라 `mf_target1_body_table` 내부 `thead#mf_target1_head_table` 로 렌더됨.
   → `target1.render.querySelector("th").parentNode.querySelectorAll("th")` (첫 header row 기준) 로 변경 후 정상.
2. `[9]` 출력이 `_data` 토큰에서 역산한 baseToken 기준이라 공백 2개 케이스에서 `true` 로 오도될 수 있어, **설정값 전체를 접두사로 갖는지** 비교하도록 변경 → 공백 2개 케이스 `false` 로 실동작이 드러남.

---

### header_P_width_1.xml

- 출력 경로 : `unitTest/src/main/webapp/sample/gridView_header/header_P_width_1.xml`
- 미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_header/header_P_width_1.xml`

| 검증 항목 | 결과 |
|---|---|
| XML 구조(네임스페이스/head·body/description·validation 4건) | PASS |
| 스크립트 로직(핸들러 ↔ 버튼 onclick 일치, setReturnValue 7건 출력) | PASS |
| UI 구성(con_width + con_columnWidth 2단테이블 1행, 재생성은 기본 `btn_createTarget` 재사용, 중복 버튼 없음) | PASS |
| MCP playwright 런타임 검증 (playwright2 세션) | PASS (console error / warning 0) |

**MCP 실측 — 선언 header width vs 렌더 Header 영역 실제 너비 vs Header Column 폭 합**

| con_width (w2:header@width) | con_columnWidth (header>column@width) | 렌더 Header 영역 실측 너비 | Header Column 폭 합 | 선언 width 로 결정? | Header Column 으로 결정? |
|---|---|---|---|---|---|
| `300px` | `140` | 559px | 559px (139+140+140+140) | false | true (차이 0px) |
| `1200px` | `200` | 799px | 799px (199+200+200+200) | false | true (차이 0px) |
| (미설정) | `80` | 319px | 319px (79+80+80+80) | (대비 없음) | true (차이 0px) |

- Grid 자체 style 은 `width:600px` 고정인데도 Header 영역 실측 너비가 319 / 559 / 799px 로 **Header Column width 합을 그대로 따라감** → 선언 header width(300px / 1200px)는 어느 케이스에서도 반영되지 않음.
- 렌더된 Header 영역 요소(`thead#mf_target1_head_table`)에 `width` 속성이 **남아있지 않음**(`getAttribute("width") === null`) → 참고용 속성으로만 소비되고 DOM 에 전달되지 않음.
- 첫 컬럼만 1px 작게(139/199/79) 측정되는 것은 테이블 좌측 테두리 분배분이며 합계는 Header 영역 너비와 정확히 일치(diff 0px).
- 근거(소스) : `uiplugin/gridView/layoutController.js` `initializeTableInfo()` 가 head→body→foot 순으로 `_getColInfo()` 를 호출하며 `this.colWidthList[x] == null` 일 때만 선착순 기록하고 `this.colWidthList[x] = parseInt(tdWidth || 0)` 로 미선언도 0 을 기록 → 컬럼 폭은 header column 에서만 결정됨. header 태그 자체의 width 는 컬럼 폭 계산에 참여하지 않음.

**MCP 실측 — 구조 엘리먼트 컴포넌트 조회 시도 결과**

| 조회 대상 | `WebSquare.util.getComponentById` 결과 |
|---|---|
| 선언한 header id (`header1` / `mf_header1`) | `undefined (조회 결과 없음)` |
| 렌더된 Header 영역 id (`mf_target1_head_table`) | `THEAD DOM 엘리먼트 (컴포넌트 객체 아님)` |
| width 를 조회/변경할 Header 컴포넌트 객체 존재 여부 | `false` |

- `getComponentById` 는 `idCache[id] || window[id]` 구조라 컴포넌트가 없으면 DOM 엘리먼트가 fallback 으로 반환되므로, `typeof obj.getPluginName === "function"` 으로 컴포넌트 여부를 판별했다. THEAD 는 `getPluginName` 이 없어 컴포넌트가 아님이 확정됨(단정이 아닌 조회 결과 출력으로 근거 기록).

**구성 방식 / 주의**

- Header 영역은 기본 drawType 에서 별도 `table` 이 아니라 body table 내부 `thead#{gridId}_head_table` 로 렌더되므로 조회는 `[id$='_head_table']` (태그 무관 선택자) 사용. `table[id$='_head_table']` 로 찾으면 null.
- 너비 판정은 `getBoundingClientRect().width` 실측 기준(`scwin.getRealWidth`). body/head table 의 `style.width` 는 엔진 내부 계산값이라 실제 렌더 폭과 불일치하여 오판 소지가 있음.
- `dynamic N` 이므로 con 변경은 `wf_body_sample` 기본 제공 `btn_createTarget`(Target 재생성)으로만 반영. 별도 재생성 버튼 없음.

**수정 이력**

- 없음 (최초 작성분으로 MCP 검증 1회 PASS).

---

### header_P_tooltip_1.xml

- 출력 경로 : `unitTest/src/main/webapp/sample/gridView_header/header_P_tooltip_1.xml`
- 미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_header/header_P_tooltip_1.xml`

| 검증 항목 | 결과 |
|---|---|
| XML 구조(네임스페이스/head·body/description·validation 3건) | PASS |
| 스크립트 로직(핸들러 ↔ 버튼 onclick 일치, setReturnValue 3행 + 툴팁 출력) | PASS |
| UI 구성(con_tooltip 2단테이블, 재생성은 기본 `btn_createTarget` 재사용, 중복 버튼 없음) | PASS |
| MCP playwright 런타임 검증 (playwright3 세션, 전용 탭 goto→검증→close) | PASS (console error 0, pageerror 0, alert 없음) |

**MCP 실측 — 케이스별 결과창 출력**

| con_tooltip | `tooltip 반영 확인` 버튼 출력 | Header Cell hover 후 클릭 시 출력 |
|---|---|---|
| `Header 영역 툴팁` | `[1] Header 영역 툴팁 / struct.head.attr.tooltip : Header 영역 툴팁`<br/>`[2] 선언값이 적용된 요소 개수 : 1`<br/>`[3] Header 영역 요소 data-tooltip : Header 영역 툴팁 / Header Cell(th) data-tooltip : (없음)` | `tooltipDisplay textContent : name1`<br/>`tooltipDisplay display : block` |
| (미설정) | `[1] (미설정) / struct.head.attr.tooltip : (없음)`<br/>`[2] -`<br/>`[3] (없음) / (없음)` | `tooltipDisplay textContent : name2`<br/>`tooltipDisplay display : block` |

- 실제 표시된 툴팁 DOM : `div#mf_target1_tooltip` (class `w2grid_tooltip`), `display: block`, textContent = **Header Cell 값**(`name1`/`name2`). `.w2balloonTip` 은 생성되지 않음(null).
- `Target 재생성` 후 label1 의 `[현재] header tooltip = 미설정` 갱신 확인, 리스너 재부착 정상.

**수정 이력**

1. 최초 작성 시 `[3]` 을 `getAttribute("tooltip")` 으로 조회 → 두 케이스 모두 `(없음)` 이라 `[2] 1건` 과 모순되는 출력.
   실측상 엔진은 `w2:header` 의 tooltip 을 **`data-tooltip`** 으로 thead 에 그대로 내보내므로 `getAttribute("data-tooltip")` 조회로 변경 후 재검증 PASS.

---

### header_P_tooltipLocaleRef_1.xml

- 출력 경로 : `unitTest/src/main/webapp/sample/gridView_header/header_P_tooltipLocaleRef_1.xml`
- 미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_header/header_P_tooltipLocaleRef_1.xml`

| 검증 항목 | 결과 |
|---|---|
| XML 구조(네임스페이스/head·body/description·validation 2건) | PASS |
| 스크립트 로직(`btn_checkLocaleRef_onclick` ↔ 버튼 onclick 일치, `target1_onheadermouseover` ↔ `ev:onheadermouseover` 일치) | PASS |
| UI 구성(con_tooltipLocaleRef 2단테이블 1행, 재생성은 기본 `btn_createTarget` 재사용, 중복 버튼 없음) | PASS |
| MCP playwright 런타임 검증 (playwright 세션 1) | PASS (console error 0, alert 없음) |

**다국어 전제 구성**

- 프로젝트 전역 설정이 이미 클라이언트 다국어 적용 상태 : `websquare/config.js` 의 `languagePack.@useLanguagePack = "true"`, `language.@value = "ko"`, `url[ko] = /lang/ko.js`. 샘플에서 별도 로케일 로드 코드 불필요.
- `/lang/ko.js` 의 `WebSquare.WebSquareLang` 키 중 `websquare_name`(=웹스퀘어), `c_label_4_1`(= 툴팁 예시) 를 con 옵션값으로 사용하고, 없는 키 케이스로 `invalid_key` 를 추가.
- 툴팁 표시 전제(가이드 `|` 하위절)는 `comp_init` 에서 고정 : GridView `useLocale="true" tooltipHeader="true" tooltipHeaderShowAlways="true"`, Header Cell 값 `name1~name4`, `w2:header` 의 `tooltip="헤더 영역 툴팁 원문"`.
- `@related` 3종(`header.tooltip` / `tooltipHeader` / `tooltipHeaderShowAlways`)은 전제 구성에만 반영하고 validation 항목·전용 버튼은 만들지 않음.

**MCP 실측 — 케이스별 결과창 출력** (Header Cell `name1` 실제 마우스 hover → `선언값 확인` 버튼 클릭)

| con_tooltipLocaleRef | hover 시 출력 | `선언값 확인` 출력 |
|---|---|---|
| `websquare_name` | `[hover] Header Cell 값 : name1`<br/>`tooltipDisplay textContent : name1`<br/>`tooltipDisplay display : block` | `[선언] header tooltipLocaleRef : websquare_name / 해당 Key 의 다국어 텍스트 : 웹스퀘어`<br/>`[반영] struct.head.attr.tooltipLocaleRef : websquare_name / Header 영역 요소 속성 : websquare_name` |
| `c_label_4_1` | `... textContent : name1` | `... 다국어 텍스트 :  툴팁 예시` / `... c_label_4_1 / c_label_4_1` |
| `invalid_key` | `... textContent : name1` | `... 다국어 텍스트 : global_undefined` / `... invalid_key / invalid_key` |
| (미설정) | `... textContent : name1` | `[선언] header tooltipLocaleRef : (미설정) / 해당 Key 의 다국어 텍스트 : (없음)`<br/>`[반영] struct.head.attr.tooltipLocaleRef : (없음) / Header 영역 요소 속성 : (없음)` |

- 어떤 케이스에서도 실제 표시된 툴팁은 **Header Cell 값**(`name1`)이며 다국어 텍스트(`웹스퀘어`)로 바뀌지 않음 → 가이드 `|` 하위절과 일치.
- 툴팁 DOM : `div#mf_target1_tooltip` (class `w2output w2grid_tooltip`), `display: block`. `.w2balloonTip` 은 미생성이므로 `$c.gcm.getTooltipInfo` 의 `[class*="_tooltip"]` 분기로 출력됨.
- `Target 재생성` 후 `label1` 의 `[현재] header tooltipLocaleRef = ...` 갱신 및 `ev:onheadermouseover` 재부착 정상.

**수정 이력**

1. 최초 작성 시 `[반영]` 의 Header 영역 요소 속성을 `getAttribute("tooltipLocaleRef")` 로 조회 → 항상 `(없음)` 이라 `struct.head.attr` 에는 값이 있는데 DOM 에는 전혀 없다는 잘못된 결론이 나올 수 있었음. 엔진이 비표준 속성에 `data-` 접두사를 붙여 thead 에 출력(`data-tooltiplocaleref`)하므로 `getAttribute("data-tooltipLocaleRef")` 로 변경 후 재검증 PASS.

# gridView.gBody P 샘플 — 검증 결과

### gBody_P_class_1.xml

경로 : `unitTest/src/main/webapp/sample/gridView_gBody/gBody_P_class_1.xml`
미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_gBody/gBody_P_class_1.xml`

## 1. 코드 리뷰

| 항목 | 결과 |
|---|---|
| WebSquare XML 구조(네임스페이스, head/body, xf:model) | OK — `footer_P_class_1.xml` 구조 그대로 |
| `scwin.tData.description` / `validation` 정의 | OK (description 1문장, validation 3건) |
| `con_class` selectbox 를 `grp_condition` 2단테이블에 배치, width/height 미지정 | OK |
| '미설정' 옵션 label/value 모두 빈값 | OK |
| 정적 select1 기본값 `con_class.setSelectedIndex(2)` 를 `onpageload` 에서 명시 | OK |
| `@dynamic N` → 별도 재생성 버튼 신규 생성 없음, `body_sample.xml` 기본 `Target 재생성` 사용 | OK |
| 버튼 onclick(`scwin.btn_checkClass_onclick`) ↔ 함수 정의 일치 | OK |
| `setReturnValue` 호출 3행 ↔ validation 3건 | OK |
| DOM 조회에 `querySelector` 사용 | OK |
| `@related` `gBody.columnClass` 검증 버튼/항목 미생성 | OK (UT_01 §2 related 제외) |

## 2. w-pack 변환

- `studio.js` 실행 성공 : `/sample/gridView_gBody/gBody_P_class_1.xml [success] 8579 bytes => 8088 bytes`
- 생성물 : `_wpack_/sample/gridView_gBody/gBody_P_class_1.js` → `touch` 완료
- 최초 접속 시 "W2X 파일을 읽어들이는데 실패했습니다" alert 미발생

## 3. MCP(playwright2) 런타임 검증 — PASS

| 확인 | 결과 |
|---|---|
| 페이지 로딩 / `window.mf_target1` 생성 | OK (`typeof === "object"`) |
| validation 3건 좌측 패널 렌더 | OK ((1)~(3) 표시) |
| createLabel 라벨 4줄 줄바꿈 렌더 + 현재 con 값 반영 | OK |
| `클래스 확인` 버튼 → 결과창 3행 출력 | OK |
| `Target 재생성` 버튼 → con 변경 반영(라벨 `[현재] gBody class = 미설정` 갱신) | OK |
| console error | **0건** (warning 0) |

### 케이스별 실측

| con_class | `struct.body.attr` | Body 영역 요소 | Body 영역 className |
|---|---|---|---|
| `oddClass evenClass` | `{"id":"gBody1","className":"oddClass evenClass"}` | `table#mf_target1_body_table` | `gridHeaderTableDefault` |
| `oddClass` | `{"id":"gBody1","className":"oddClass"}` | `table#mf_target1_body_table` | `gridHeaderTableDefault` |
| (미설정) | `{"id":"gBody1"}` | `table#mf_target1_body_table` | `gridHeaderTableDefault` |

Body Column 영역 : `td#mf_target1_cell_0_0` / className `w2grid_input_table gridBodyDefault gridBodyDefault_data mf_target1_columnstyle_0_`

결과창 실제 출력(미설정 케이스) :

```
[선언] gBody 에 설정한 class : (미설정)
[렌더] Body 영역의 실제 className : gridHeaderTableDefault
[영역] Body 영역 요소 : mf_target1_body_table / Body Column 영역 요소 : mf_target1_cell_0_0
```

## 4. 발견 이슈

- 없음. 수정 이력 없음(최초 생성분 그대로 PASS).
- 참고: 설정한 class 미적용은 가이드 `@propval |` 문구와 일치하므로 결함 아님. 상세는 `03_보강.md` 참조.


---

### gBody_P_id_1.xml

경로 : `unitTest/src/main/webapp/sample/gridView_gBody/gBody_P_id_1.xml`
미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_gBody/gBody_P_id_1.xml`

## 1. XML 구조 검증

- WebSquare XML 네임스페이스 / head-body 구조 정상. `description` 1건, `validation` 3건 정의됨.
- `grp_condition` 은 snippet "5_02 2단테이블" 구조로 `con_id` selectbox 1개 배치(미설정 / `gBody1` / `myBodyArea`), 미설정 항목은 label·value 모두 빈값.
- `con_` 컨트롤에 `width`/`height` 미설정(`style=""`).
- `@dynamic N` 이므로 재생성은 `wf_body_sample` 기본 제공 `Target 재생성`(`btn_createTarget` → `comp_init`) 만 사용, 중복 재생성 버튼 없음.
- 참조 샘플 `gridView_footer/footer_P_id_1.xml` 과 구조 동일(영역명만 Footer → Body).

## 2. 스크립트 로직 검증

- `scwin.btn_checkId_onclick` ↔ `w2:button ev:onclick="scwin.btn_checkId_onclick"` 일치.
- `setReturnValue` 3행 ↔ validation 3건 (UT_01 §4-1 준수).
- DOM 조회는 `document.querySelector` / `render.querySelector` 사용 (`getElementById` 미사용).
- `comp_init` 순서 : 기존 target·label 제거 → `createLabel` → `createDynamicDataList` → `dynamicCreate`.

## 3. MCP 검증 (playwright3, 전용 탭 goto → 검증 → close)

w-pack 변환 후 접속. **PASS**. console error 0건 / dialog(alert) 0건.

| 케이스 | 조작 | 결과창 출력 |
|---|---|---|
| con_id = `gBody1` (기본) | 페이지 로드 → `ID 확인` | `[1] gBody 에 설정한 id : gBody1` / `[2] 렌더된 Body 영역의 실제 id : mf_target1_body_table / 설정한 id 로 조회된 DOM 개수 : 0` / `[3] Body 영역 요소 id : mf_target1_body_table / Body Column 영역 요소 id : mf_target1_cell_0_0` |
| con_id = `myBodyArea` | `Target 재생성` → `ID 확인` | `[1] ... : myBodyArea` / `[2] ... mf_target1_body_table / ... 0` / `[3] ... mf_target1_body_table / ... mf_target1_cell_0_0` |
| con_id = 미설정 | `Target 재생성` → `ID 확인` | `[1] ... : (미설정)` / `[2] ... mf_target1_body_table / ... -` / `[3] ... mf_target1_body_table / ... mf_target1_cell_0_0` |

- 페이지 정체 확인 : `#mf_wf_body_left` 텍스트에 `[gBody > P > id]` 존재 확인 후 값 신뢰.
- `window.mf_target1` 생성 확인, `target1.render` 및 `wf_body_bottom` 참조 정상.
- createLabel 라벨 4줄(`[전제]`/`[현재]`/`[절차]`/`[확인]`) 정상 렌더, `Target 재생성` 후 `[현재] gBody id = 미설정` 으로 갱신됨.

## 4. 발견 이슈

- 없음. 수정 이력 없음(최초 생성분 그대로 PASS).
- 참고: 선언한 id 미적용은 가이드 `@propval |` 문구와 일치하므로 결함 아님. 상세는 `03_보강.md` 참조.

---

### gBody_P_columnClass_1.xml

경로 : `unitTest/src/main/webapp/sample/gridView_gBody/gBody_P_columnClass_1.xml`
미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_gBody/gBody_P_columnClass_1.xml`

## 1. 코드 리뷰

| 항목 | 결과 |
|---|---|
| WebSquare XML 구조(네임스페이스, head/body, xf:model) | OK — `footer_P_columnClass_1.xml` 구조 그대로 |
| `scwin.tData.description` / `validation` 정의 | OK (description 1문장, validation 10건) |
| `con_columnClass` selectbox 를 `grp_condition` 2단테이블에 배치, width/height 미지정 | OK |
| '미설정' 옵션 label/value 모두 빈값 | OK |
| 정적 select1 기본값 `con_columnClass.setValue("tcBodyClass")` 를 `onpageload` 에서 명시 | OK |
| `@dynamic N` → 별도 재생성 버튼 신규 생성 없음, `body_sample.xml` 기본 `Target 재생성` 사용 | OK |
| 버튼 onclick(`scwin.btn_checkColumnClass_onclick`) ↔ 함수 정의 일치 | OK |
| `setReturnValue` 호출 10행 ↔ validation 10건 | OK |
| DOM 조회에 `querySelector` 사용 | OK |
| RowNum / RowStatus Column 포함(`rowNumVisible="true" rowStatusVisible="true"`) | OK (파생 클래스 3종 실측 전제) |
| 접두사 체계 CSS 를 `<style type="text/css">` 블럭에 직접 정의 | OK (`.tcBodyClass` / `_data` / `_rowNumber` / `_rowStatus` / `.tcEtcClass`) |
| `@related` `gBody.class` 검증 버튼/항목 미생성 | OK (UT_01 §2 related 제외) |

## 2. w-pack 변환

- `studio.js` 실행 성공 : `/sample/gridView_gBody/gBody_P_columnClass_1.xml [success] 13127 bytes => 12100 bytes`
- 생성물 : `_wpack_/sample/gridView_gBody/gBody_P_columnClass_1.js` → `touch` 완료
- 최초 접속 시 "W2X 파일을 읽어들이는데 실패했습니다" alert 미발생

## 3. MCP(playwright 1번 세션) 런타임 검증 — PASS

| 확인 | 결과 |
|---|---|
| 페이지 로딩 / `window.mf_target1` 생성 | OK |
| validation 10건 좌측 패널 렌더 | OK ((1)~(10) 표시) |
| createLabel 라벨 4줄 줄바꿈 렌더 + 현재 con 값 반영 | OK |
| `클래스 확인` 버튼 → 결과창 10행 출력 | OK |
| `Target 재생성` 버튼 → con 3종 모두 반영 | OK |
| console error | **0건** (warning 0) |

### 케이스별 실측 (Body 첫 행 td className 전문)

| con_columnClass | RowNum Cell | RowStatus Cell | 일반 Cell |
|---|---|---|---|
| (미설정) | `gridBodyDefault gridBodyDefault_rowNumber` | `gridBodyDefault gridBodyDefault_rowStatus w2grid_rowStatus_R` | `w2grid_input_table gridBodyDefault gridBodyDefault_data mf_target1_columnstyle_0_` |
| `tcBodyClass` | `tcBodyClass tcBodyClass_rowNumber` | `tcBodyClass tcBodyClass_rowStatus w2grid_rowStatus_R` | `w2grid_input_table tcBodyClass tcBodyClass_data mf_target1_columnstyle_0_` |
| `tcBodyClass tcEtcClass` (공백 2개) | `tcBodyClass tcEtcClass tcBodyClass tcEtcClass_rowNumber` | `tcBodyClass tcEtcClass tcBodyClass tcEtcClass_rowStatus w2grid_rowStatus_R` | `w2grid_input_table tcBodyClass tcEtcClass tcBodyClass tcEtcClass_data mf_target1_columnstyle_0_` |

computed style (일반 Cell background-color / border-top) :

| con_columnClass | background-color | border-top |
|---|---|---|
| (미설정) | `rgb(255, 255, 255)` | `1px dashed rgb(234, 238, 241)` (엔진 기본 테마) |
| `tcBodyClass` | `rgb(255, 242, 204)` | `2px solid rgb(192, 0, 0)` (샘플 style 블럭 정의값) |
| `tcBodyClass tcEtcClass` | `rgb(255, 242, 204)` | `0px none rgb(192, 0, 0)` (`_data` 미매칭 → 테두리 소실) |

RowNum / RowStatus background-color 실측 :

| con_columnClass | RowNum | RowStatus |
|---|---|---|
| (미설정) | `rgb(229, 239, 247)` | `rgb(255, 255, 255)` |
| `tcBodyClass` | `rgb(189, 215, 238)` | `rgb(198, 224, 180)` |
| `tcBodyClass tcEtcClass` | `rgb(255, 242, 204)` | `rgb(255, 242, 204)` (파생 CSS 미매칭, 기준 클래스 색만 적용) |

결과창 실제 출력(`tcBodyClass` 케이스) :

```
[1] 설정한 columnClass : tcBodyClass / 일반 Body Cell 의 css 적용값(background-color, border) : rgb(255, 242, 204), 2px solid rgb(192, 0, 0)
[2] 공백 포함 설정 여부 : false / 일반 Body Cell 의 class 토큰 : w2grid_input_table | tcBodyClass | tcBodyClass_data | mf_target1_columnstyle_0_
[3] 기본 클래스(gridBodyDefault) 잔존 여부 : false
[4] 미설정 여부 : false / 렌더에 적용된 기준 클래스 : tcBodyClass
[5] 자동 생성된 파생 클래스 : tcBodyClass_data, tcBodyClass_rowNumber, tcBodyClass_rowStatus
[6] 일반 Body Cell className : w2grid_input_table tcBodyClass tcBodyClass_data mf_target1_columnstyle_0_
[7] RowNum Body Cell className : tcBodyClass tcBodyClass_rowNumber 
[8] RowStatus Body Cell className : tcBodyClass tcBodyClass_rowStatus w2grid_rowStatus_R
[9] 기준 클래스와 파생 클래스 접두사 일치 여부 : true
[10] 파생 클래스 CSS 적용값(RowNum background-color, RowStatus background-color) : rgb(189, 215, 238), rgb(198, 224, 180)
```

## 4. 발견 이슈

- 엔진 결함 없음. 가이드 기술과 실측이 모두 일치.
- 공백 2개 설정 시 동작(가이드는 "설정할 수 없다"고만 서술)을 단정하지 않고 실측 : 엔진은 문자열 전체를 그대로 class 에 붙인 뒤 **마지막 토큰에만** `_data`/`_rowNumber`/`_rowStatus` 접미사를 붙인다. 결과적으로 첫 토큰(`tcBodyClass`)이 중복 부여되고 파생 클래스는 `tcEtcClass_data` 로 생성되어 접두사 체계가 깨진다 → 가이드의 "공백으로 두 개 이상 설정할 수 없다"가 실측으로 확인됨.

---

### gBody_P_width_1.xml

경로 : `unitTest/src/main/webapp/sample/gridView_gBody/gBody_P_width_1.xml`
미리보기 : `http://127.0.0.1:65388/websquare/websquare.html?w2xPath=/sample/gridView_gBody/gBody_P_width_1.xml`

## 1. 코드 리뷰

| 항목 | 결과 |
|---|---|
| WebSquare XML 구조(네임스페이스, head/body, xf:model) | OK — 동일 API 의 `footer_P_width_1.xml` 구조 그대로 |
| `scwin.tData.description` / `validation` 정의 | OK (description 1문장, validation 4건) |
| `con_width` selectbox 를 `grp_condition` 2단테이블에 배치, width/height 미지정 | OK |
| '미설정' 옵션 label/value 모두 빈값 | OK |
| 정적 select1 기본값 `con_width.setValue("300px")` 를 `onpageload` 에서 명시 | OK |
| `@dynamic N` → 재생성 버튼 신규 생성 없음, `body_sample.xml` 기본 `Target 재생성` 사용 | OK |
| 버튼 onclick(`scwin.btn_checkWidth_onclick` / `scwin.btn_checkBodyObject_onclick`) ↔ 함수 정의 일치 | OK |
| `setReturnValue` 호출 7행 ↔ validation 4건 (너비 4행 + 컴포넌트 조회 3행) | OK |
| DOM 조회에 `querySelector` 사용 | OK |
| createLabel `\n` 줄바꿈 / 작은따옴표·HTML 태그 미사용 | OK |

## 2. w-pack 변환

`studio.js` + `touch` 로 `_wpack_/sample/gridView_gBody/gBody_P_width_1.js` 생성 — `[success]` (경고 0, 에러 0).

## 3. MCP(playwright2) 런타임 검증 — PASS

| 확인 | 결과 |
|---|---|
| 페이지 로딩 / `window.mf_target1` 생성 | OK |
| `label1` 라벨 렌더 + Target 재생성 시 `[현재] gBody width` 갱신 | OK (300px → 1200px → 미설정 반영 확인) |
| `너비 확인` 버튼 4행 출력 | OK |
| `Body 컴포넌트 조회` 버튼 3행 출력 | OK |
| 좌측 유효성 4건 렌더 | OK |
| console error | **0건** |

### 케이스별 실측 (grid style width:600px, header/gBody column width 모두 140)

| gBody width | 렌더된 Body 영역 요소(width 속성) | Body 영역 실제 너비(offsetWidth) | Body Column 폭 합(실측) |
|---|---|---|---|
| 300px | (없음) | **560px** | 559px (139 + 140 + 140 + 140) |
| 1200px | (없음) | **560px** | 559px (139 + 140 + 140 + 140) |
| 미설정 | (없음) | **560px** | 559px (139 + 140 + 140 + 140) |

- 선언한 gBody width 가 300px / 1200px / 미설정 중 무엇이든 Body 영역 실제 너비는 **560px 로 동일** → 선언값 미반영(참고용), Body Column 으로 결정됨이 실측으로 입증.
- Body Column 폭 합 559px 와 Body 영역 560px 의 1px 차이는 테이블 테두리(첫 셀 rect 139 = 140 - 좌우 0.5px border). `getBoundingClientRect` 실측: Body table 260.19~820.19(560), Column 영역 260.69~819.69(559).
- 참고 : `body_table` 의 `style.width` 는 554px 로 남으나 실제 렌더 폭(560px)과 다르며, colgroup 은 `139px/140px/140px/140px` 로 Body Column 값을 따른다.

### 컴포넌트 조회 실측 (@spec 2)

- 선언한 gBody id(`gBody1`) 로 조회 : `undefined (조회 결과 없음)`
- 렌더된 Body 영역 id(`mf_target1_body_table`) 로 조회 : `TABLE DOM 엘리먼트 (컴포넌트 객체 아님)` — `WebSquare.util.getComponentById` 가 `idCache[...] || window[id]` 이므로 DOM 엘리먼트가 fallback 반환됨. `typeof obj.getPluginName === "function"` 으로 컴포넌트 아님을 판별.
- width 를 조회/변경할 Body 컴포넌트 객체 존재 여부 : `false`

## 4. 발견 이슈

- 엔진 결함 없음. 가이드 기술과 실측이 모두 일치.
- 엔진 근거 : `uiplugin/gridView/layoutController.js` 에서 `this.struct.body.attr` 로 읽는 값은 `class`/`rowClass`/`columnClass` 뿐이며(290~292행) `width` 는 어디서도 참조하지 않는다. 컬럼 폭은 `initializeTableInfo()` 가 head→body→foot 순으로 `_getColInfo()` 를 호출하며 `this.colWidthList[x] == null` 선착순 기록 + `parseInt(tdWidth || 0)` 로 미선언도 0 을 기록하므로 header 패스에서 확정되고 body/foot 패스는 성립하지 않는다.

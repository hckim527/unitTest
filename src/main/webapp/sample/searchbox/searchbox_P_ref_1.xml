<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:ev="http://www.w3.org/2001/xml-events" xmlns:w2="http://www.inswave.com/websquare"
	xmlns:xf="http://www.w3.org/2002/xforms">
	<head meta_screenName="[searchbox] ref" meta_author="InswaveSystems" meta_type="메인">
		<w2:historyInfo>
			<w2:history meta_no="01" meta_desc="최초작성" meta_date="20260514" meta_user="InswaveSystems"></w2:history>
		</w2:historyInfo>
		<w2:type>COMPONENT</w2:type>
		<w2:buildDate />
		<w2:MSA />
		<xf:model>
			<w2:dataCollection baseNode="map">
			</w2:dataCollection>
			<w2:workflowCollection />
		</xf:model>
		<w2:layoutInfo />
		<w2:publicInfo method="" />
		<script lazy="false" type="text/javascript"><![CDATA[scwin.gVar = {
    "cRef" : "",
}



// 테스트 설명, 유효성 값 설정
scwin.tData = {
    "description" : "[searchbox > P > ref]<br/>SearchBox 컴포넌트의 입력값에 바인딩할 DataCollection 정보를 설정합니다.",
    "validation" : [
        "접근 가능한 DataCollection 의 컬럼을 'data:[DataCollection ID].[Key 또는 Column ID]' 형식으로 설정합니다.",
        "유효하지 않은 값으로 설정 시 바인딩 처리되지 않고 입력창에 빈값이 표시됩니다.",
        "DataList 를 바인딩 한 경우 DataList 의 데이터를 선택하지 않으면 입력창에 빈값이 표시됩니다.",
        "데이터를 선택하지 않은 상태에서 입력된 값은 데이터 선택 시 사라집니다.",
    ]
}



scwin.wf_sample_onwframeload = function () {
    let wf_body_left = wf_sample.getObj("wf_body_left");
    // 테스트 설명, 유효성 생성
    wf_body_left.getObj("scwin").createValidation(scwin.tData);

    // wframe 의 btn_getText_onclick 을 searchbox 용으로 override
    // (wframe 기본 핸들러는 target1.render.tagName 으로 input/textarea 분기 → searchbox 는 wrapper DOM 이라 INPUT 이 아니어서 textContent 빈값 반환됨)
    wf_sample.getObj("scwin").btn_getText_onclick = function (e) {
        wf_sample.getObj("wf_body_bottom").getObj("scwin").setReturnValue("text : " + target1.getValue());
    };
};



scwin.onpageload = function () {
    $c.gcm.createCommonDatalist();

    // comp 초기 생성
    scwin.comp_init();
};



// comp 생성
scwin.comp_init = async function () {
    // 기존 target 있으면 삭제
    if (typeof target1 !== 'undefined') {
        target1.remove();
    }

    // 조건 설정
    let properties = {
        ref: scwin.gVar["cRef"],
        style: 'width:200px;height:30px',
    };

    let cList = $c.gcm.createCList(properties);

    await $c.gcm.createEmptySearchBox(cList);
}]]></script>
	</head>
	<body ev:onpageload="scwin.onpageload">
		<w2:wframe style='' id="wf_sample" src="/sample/common_P/common_P_ref_1.xml" ev:onwframeload="scwin.wf_sample_onwframeload"></w2:wframe>
	</body>
</html>

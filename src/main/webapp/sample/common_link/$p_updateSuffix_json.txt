{
    "suffix": [
        {
            "path": "/langpack",
            "defaults": "lang_${year}_${month}"
        },
        {
            "extension": {
                "js": "js_${day}",
                "xml": "xml_${week}"
            },
            "defaults": "etc_${random}"
        }
    ]
}

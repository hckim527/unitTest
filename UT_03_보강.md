# unitTest 샘플 — 보강 규칙

> **Phase 1 워크플로우: 01_생성 → 02_검증 → 03_보강**
> - `unitTest/UT_02_검증.md` 규칙에 따라 검증이 완료된 샘플을 대상으로 본 보강을 수행한다.
> - 보강이 완료되면 Phase 1이 종료되고, `TSQA/TS_01_생성.md`부터 Phase 2를 수행한다.
>
> **진행 현황 보고:** 각 단계 진행 중 아래 형식으로 현황을 보고한다.
> | 단계 | 총 샘플 | 완료 | 남은 | 진행률 |
> |------|--------|------|------|--------|
> | 생성 | N | n | N-n | n/N% |
> | 검증 | N | n | N-n | n/N% |
> | 보강 | N | n | N-n | n/N% |

---

## 1. API 가이드 ↔ validation 1:1 대조

1. 샘플 생성 완료 후, API 가이드 문서의 내용과 샘플의 validation 항목을 1:1 대조하여 누락이 없는지 반드시 확인한다.
   - API 가이드 문서의 `@description`, `@propval`, `@param`, `@spec` 내용이 모두 validation에 반영되었는지 확인
   - param의 설정값이 enum 타입인 경우 각 enum 값에 대한 validation 항목이 있는지 확인
   - 필수여부가 N인 param은 생략 시 기본값 동작 validation이 있는지 확인

---

## 2. 대조표 작성

2. 대조 결과를 `unitTest/docs/{컴포넌트}_{타입}/03_보강.md` 파일에 기록한다.
   - 형식:
     ```
     ### {API명}_1.xml

     | API 가이드 항목 | 샘플 validation | 상태 |
     |---------------|---------------|------|
     | {가이드 내용 1} | (1) {샘플 validation} | ✓ |
     | {가이드 내용 2} | - | ❌ 누락 → 추가 |
     ```

---

## 2-1. docs 기록 ↔ MEMORY 승격 경계 (중복·누수 방지)

`docs/{컴포넌트}_{타입}/03_보강.md` 와 사용자 메모리(`MEMORY.md` + memory 파일)는 **역할이 다르므로 내용을 구분**한다.

- **docs(03_보강.md) = 그 샘플의 이력/감사 기록.** 다음만 넣는다:
  - API 가이드 ↔ validation 1:1 대조표
  - 그 샘플에 한정된 MCP 실측 raw값, 제외 판단 근거, 구성 방식
  - → **자동 로드되지 않으므로**, 다른 작업에 자동 반영되어야 할 일반 지식은 여기에만 두지 말 것.
- **MEMORY = 모든 작업에 자동 반영되어야 할 일반화 지식.** 다음은 docs 에 적는 동시에 **반드시 MEMORY 에도 한 줄 승격**한다:
  - 재현 가능한 **엔진 결함**(컴포넌트·API·증상·우회법)
  - 다른 샘플에서도 부딪힐 **API 사실/제약/패턴**(reference)
  - 사용자가 준 **작업 방식 지침**(feedback)
- **판단 기준(한 줄):** *"이 사실을 다음 작업에서도 자동으로 알고 있어야 하나?"* → 예면 MEMORY 승격, 아니면 docs 에만.
- **중복 금지:** 동일 내용을 docs·MEMORY 양쪽에 장문으로 적지 말 것. MEMORY 는 한 줄 요약(인덱스)으로, 상세는 memory 파일/ docs 에 둔다. MEMORY.md 인덱스 한 줄은 ~200자 이내로 유지(파일 크기 한도 초과 방지).

---

## 3. 누락 항목 보강

3. 누락된 validation 항목이 발견되면:
   - 샘플 XML의 `scwin.tData.validation` 배열에 항목을 추가한다.
   - 필요 시 해당 validation을 검증할 수 있는 버튼/옵션/로직도 함께 추가한다.
   - 보강 후 `unitTest/UT_02_검증.md` 절차에 따라 재검증한다.

---

## 4. 보강 후 재검증

4. 보강으로 추가/수정된 validation 항목 및 로직은 반드시 `unitTest/UT_02_검증.md` 절차에 따라 재검증한다.
   - 샘플 페이지가 정상 렌더링되는지 확인
   - 추가된 버튼/옵션이 정상 동작하는지 확인
   - 보강된 validation 출력값이 기대대로 나오는지 확인
   - 전체 PASS 될 때까지 반복

---

## 5. 보강 체크리스트

5. 보강 완료 후 다음을 확인한다:
   - [ ] API 가이드 문서의 모든 `@description` 내용이 validation에 반영됨
   - [ ] 모든 `@propval` / `@param` enum 값에 대한 validation이 존재함
   - [ ] 필수여부 N인 param의 기본값 동작 validation이 존재함
   - [ ] param 별로 개별 validation 항목이 존재함 (여러 param을 1개로 통합하지 않음)

---

## 6. w-pack 변환 (Phase 2 진입 전 필수)

6. 보강이 완료된 샘플 XML을 w-pack으로 JS 변환한다.
   - TSQA 자동화 스크립트 생성(Phase 2) 전에 반드시 수행해야 한다.
   - WebSquare Studio 내장 w-pack을 사용하며, `--config` 옵션으로 config.js를 지정해야 Studio와 동일한 결과를 얻을 수 있다.
   - 실행 명령어 (`studio.js` + `touch`):
     ```bash
     cd "C:/WebSquare_Studio/ai_x64/websquare_25.0822/plugins/com.inswave.node.win32.win32.x86_64_20.18.8/node/node_modules/w-pack" && \
     node studio.js '{encoded JSON}' && \
     touch "{생성된 JS 파일 경로}"
     ```
   - `studio.js`에 전달하는 JSON (URL 인코딩하여 전달):
     ```json
     {
       "--js": "0", "--css": "1", "--noDebug": "false", "--babel": "false",
       "--parseMeta": "true", "--context": "/",
       "--baseDir": "C:/WebSquare_Studio/ai_x64/websquare_25.0822/workspace/unitTest/src/main/webapp",
       "--dest": "C:/WebSquare_Studio/ai_x64/websquare_25.0822/workspace/unitTest/src/main/webapp/_wpack_",
       "--ignore": "C:/WebSquare_Studio/ai_x64/websquare_25.0822/workspace/unitTest/src/main/webapp/websquare",
       "--noFixBaseDir": "true", "--encrypt": "false",
       "--globalPath": "/cm/gcc/data.xml,/cm/gcc/date.xml,/cm/gcc/ext.xml,/cm/gcc/win.xml,/cm/gcc/util.xml,/cm/gcc/str.xml,/cm/gcc/sbm.xml,/cm/gcc/num.xml,/cm/gcc/hkey.xml,/cm/gcc/common.xml",
       "--globalCommon": "scwin", "--commonPath": "NA",
       "--config": "C:/WebSquare_Studio/ai_x64/websquare_25.0822/workspace/unitTest/src/main/webapp/websquare/config.js",
       "--excludePrefix": "__",
       "--src": "{변환할 XML 파일 경로}"
     }
     ```
   - w-pack 변환 후 생성된 JS 파일을 반드시 `touch`해야 한다. touch가 서버에 파일 변경을 알려주는 트리거 역할을 한다.
   - MCP(Playwright)에서 alert가 발생하면 `browser_close`로 세션을 닫고 새 세션으로 재접속한다.
   - `C:\wpack`의 standalone w-pack, `index.js`는 사용하지 않는다. (버전/출력 형식 차이로 오류 발생)


---

## 보강 완료 → Phase 1 종료 → Phase 2

> **보강이 완료되면 Phase 1(unitTest 샘플 생성→검증→보강)이 종료된다.**
> **`TSQA/TS_01_생성.md` 규칙을 적용하여 Phase 2(TSQA 자동화 스크립트 생성→검증→보강)를 수행한다.**

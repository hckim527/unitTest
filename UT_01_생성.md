# unitTest 샘플 — 생성 규칙

> **Phase 1 워크플로우: 01_생성 → 02_검증 → 03_보강**
> - 본 문서의 규칙에 따라 샘플 생성이 완료되면, `unitTest/UT_02_검증.md` 규칙을 적용하여 검증을 수행한다.
> - Phase 1 완료 후 → `TSQA/TS_01_생성.md`부터 Phase 2를 수행한다.
>
> **진행 현황 보고:** 각 단계 진행 중 아래 형식으로 현황을 보고한다.
> | 단계 | 총 샘플 | 완료 | 남은 | 진행률 |
> |------|--------|------|------|--------|
> | 생성 | N | n | N-n | n/N% |
> | 검증 | N | n | N-n | n/N% |
> | 보강 | N | n | N-n | n/N% |

---

## 1. 샘플 파일 구조

- _2, _3.xml 등 번호가 붙은 샘플을 만들 때, `description` 필드는 반드시 _1.xml의 description과 동일하게 유지할 것
  - 예: `"description" : "[gridView > P > readOnlyBackgroundColor]<br/>읽기 전용(readonly) 상태인 Cell 의 배경색으로 적용될 색상을 설정합니다."`
- `description` 필드에는 `@description` 의 **첫 문장(첫 마침표까지) 1개만** 넣을 것. `@description` 에 `|` 로 이어지는 추가 문장(예: "inputType 이 'checkbox','radio','custom' 인 경우에만 동작합니다.")이 있으면 description 에서는 제외하고, 그 추가 문장은 **별도 validation 항목으로 생성**할 것 (description 과 validation 에 같은 문장을 중복하지 말 것)
- `con_`/`par_` selectbox 의 **'미설정'(속성 미적용/기본값) 옵션 아이템은 label 과 value 를 모두 빈값('')으로** 작성할 것 (공통 `gdl_boolean` 의 빈 항목처럼). 즉 `<xf:label>(미설정)</xf:label>` 같은 텍스트 라벨 대신 `<xf:label></xf:label><xf:value></xf:value>` (빈 라벨 + 빈 value) 사용.
- propval 또는 param 에 "인덱스 또는 ID 를 설정" 문구가 포함되면 인덱스 사용 샘플과 ID 사용 샘플 2개로 분리하여 생성할 것
- API 가이드 문서의 param 설명에 "숨겨진 Column 도 포함하여" 등 숨겨진 Column 관련 문구가 있으면, 인덱스 기반 샘플(_1.xml)에서 `comp_init` 내 GridView 생성 후 `target1.setColumnVisible(1, false)` 로 두번째 컬럼을 숨겨서 생성할 것

---

## 2. Validation 생성 규칙

- API 가이드 문서의 내용을 가능하면 수정 없이 validation 에 생성할 것
- **`@propval` 기반 validation 은 해당 propval 의 값을 사전조건으로 문구 앞에 명시할 것.** `@propval` 원문은 결과만 기술하는 경우가 많아 그대로 넣으면 "어떤 값일 때의 동작"인지 빠진다. 원문 결과 문구는 유지하되 앞에 propval 값 조건을 붙인다.
  - 일반 값(default 아님): `"{값} 설정 시, ~"` — 예: `<true>` "허용합니다." → `"true 설정 시, 허용합니다."`
  - **default 값**(`<false (default)>` 등 기본값)은 **2개 항목으로 분리** 생성: `"{값} 설정 시, ~"` 와 `"미설정 시, ~"`. 명시적으로 그 값을 줄 때와 속성을 미설정해 기본값으로 동작할 때를 각각 검증한다.
    - 예: `<false (default)>` "허용하지 않습니다." → `"false 설정 시, 허용하지 않습니다."` + `"미설정 시, 허용하지 않습니다."`
  - **`@propval` 의 `|` 연속(추가) 문구도 해당 값 조건을 앞에 붙일 것.** `|` 로만 구분돼 값 맥락 없이 결과 문구만 있으면 "어떤 값일 때의 동작"인지 이해가 어렵다. 그 `|` 문구가 속한 `@propval` 값의 조건을 붙인다.
    - 예: `<true>` 의 `|` "사용자가 변경한 너비가 유지됩니다." → `"true 설정 시, 사용자가 변경한 너비가 유지됩니다."`
    - 단, `|` 문구에 **이미 자체 사전조건**(예: "useShiftKey=false 인 경우 ~", "값이 작을수록 ~")이 있으면 그대로 둔다.
    - `|` 연속 문구는 주 항목과 달리 **미설정 변형까지 중복 생성하지 않는다**(주 항목만 `{값} 설정 시`+`미설정 시` 2개로 분리, 부가 문구는 `{값} 설정 시` 1개만).
- API 가이드 문서에서 의미가 중복되는 내용은 1개의 validation 항목으로 통합하여 작성할 것
- `@related` 에 나열된 다른 Property/Method 는 **대상 API 의 validation 근거(`@description`/`@propval`/`@param`/`@spec`/`@return`)에 관련 내용이 명시되지 않으면 샘플에 포함하지 말 것** — validation 항목은 물론 해당 연관 API 를 호출하는 검증 버튼/핸들러도 생성 금지. `@related` 는 참고용 연관 API 목록일 뿐 검증 대상이 아니며, 그 API 는 각자의 샘플에서 다룬다.
  - 예: getAllVisibleData 샘플에서 "원본값(Display 포맷 미적용)" 검증은 getAllVisibleData 반환값과 그리드 표시값의 대비로 입증하면 충분하며, `@related` 인 getAllDisplayData 를 호출하는 비교 버튼은 만들지 않는다.
- `@spec` 등에 기재된 내용 중 **다른 기능(다른 Property/Method/이벤트)에서 검증해야 하는 유효성은 해당 API 샘플에서 제외**할 것 — validation 항목은 물론 그 기능을 동작시키는 검증 버튼/핸들러도 생성 금지. 대상 API 자체의 동작이 아니라 연동되는 다른 기능의 동작을 입증하는 내용은 그 기능의 샘플에서 다룬다.
  - 예: 어떤 Property 의 `@spec` 에 "필터/정렬이 적용되면 ~ 재처리된다" 처럼 다른 기능(필터/정렬)의 동작이 트리거 조건으로 언급되어도, 그 다른 기능 자체의 동작 보장은 해당 기능 샘플에서 검증하고 대상 샘플에서는 제외한다.
- param 의 설정값이 enum 타입인 경우 각 enum 값에 대한 validation 항목이 있어야 함
- param 여러개를 1개의 validation 으로 생성하지 말고 각각 생성할 것
- param 정의 형식은 `<타입:필수여부:기본값>` 임 (예: `<Boolean:N:true>` → 타입: Boolean, 필수: N, 기본값: true)
- 필수여부가 N 인 param 은 해당 param 생략 시 기본값으로 동작하는지 확인하는 validation 항목을 추가할 것
- 필수여부가 N 인 param 은 메소드 실행 시 해당 파라미터를 생략하는 로직이 추가되어야 함
- param 관련 validation 작성 시 파라미터명 뒤에 필수/선택을 표시할 것 (예: `"element(필수) 파라미터로..."`, `"number(선택) 파라미터로..."`)
- validation 문자열 내에 큰따옴표(`"`)가 필요한 경우 작은따옴표(`'`)로 대체할 것 (CDATA 내부 JSON 파싱 에러 방지)
- 이벤트(@event) API 가이드에서 이벤트 객체 `e` 의 개별 속성(`e.bubbles`, `e.cancelable`, `e.target`, `e.relatedTarget`, `e.type`, `e.timeStamp`, `e.view` 등)에 대해서는 별도 validation 항목으로 생성하지 말 것
  - 이벤트 핸들러에서 `e` 객체를 전달받는다는 점만 1개의 validation 으로 표현하면 충분 (예: `"이벤트 객체 e (FocusEvent) 가 핸들러에 전달되어야 합니다."`)
  - 이벤트 발화 시점/조건/관련 동작은 별도 validation 으로 작성
- 이벤트 핸들러에서 검증 출력 작성 시:
  - 표준 DOM 이벤트 객체 `e` (FocusEvent / KeyboardEvent / MouseEvent / PointerEvent): 단순 한 줄 `wf_body_bottom.getObj("scwin").setReturnValue("e : " + e);` 으로 전달 여부만 확인
  - **`e` 객체 이외의 파라미터(`info` 객체 등)는 속성값을 개별 출력하여 검증해야 함** (예: `info.oldValue`, `info.newValue`, `info.keyCode`, `info.value` 등 API 가이드에 명시된 속성 모두 출력)
- `nextTabID` 속성으로 다른 컴포넌트를 가리킬 때, **대상 컴포넌트(예: `input2`)는 target1 생성 전에 먼저 동적 생성**되어야 nextTabID 매칭이 적용됨. 생성 순서: `nextTabID 대상 → target1`

---

## 3. ID/명명 규칙

- 컴포넌트 동적 생성 시 적용할 Property 값 선택 콤보 또는 입력창의 id 는 `con_프로퍼티명` 형태로 설정
- 메소드의 파라미터 선택 콤보 또는 입력창의 id 는 `par_파라미터명` 형태로 설정
- 버튼의 label에 파라미터 정보를 포함하지 않음
  - O: `label="setFooterStyle"`
  - X: `label="setFooterStyle(cellInfo, styleName, styleValue)"`
- 단, 메소드를 실행하는 버튼이 2개 이상이고 각 메소드의 파라미터가 다른 경우 버튼의 라벨값에 파라미터를 포함
  - 예: `label="setFixedColumn(fixedColNum)"`, `label="getFixedColumnLastIndex(includeDefaultColumn)"`

---

## 4. UI 구성 규칙

- rowIndex, colIndex 등의 selectbox 옵션 값은 실제 행/컬럼 개수만큼 설정할 것
  - 예: 4개 컬럼이면 0,1,2,3 / 4개 행이면 0,1,2,3
- `createBasicDataList`은 기본 4행, `createBasicGridView`는 기본 4컬럼
- 옵션 항목(selectbox 등)은 한 줄(tr)에 2개까지만 생성할 것
- 옵션 1줄 생성 시 tr 태그 하위에 th, td, th, td 는 반드시 쌍으로 생성할 것
- **옵션/파라미터 컨트롤(con_, par_ 의 selectbox·input 등)은 `width`/`height` 를 설정하지 말 것**
  - `style=""` 로 두어 부모 th/td 영역에 autofit 되도록 한다 (고정 `width:200px;` 등 금지)
  - 단 grp_comp 의 **테스트 대상 컴포넌트(target)** 는 이 규칙 대상이 아니며, 동적 생성 시 적절한 크기를 지정한다(예: `createEmptyInput` 은 style 미포함이라 `style:'width:200px; height:30px;'` 명시 — [[reference_createbasicinput_initvalue_input_hardcoded]])
- true/false 옵션을 갖는 selectbox는 `data:gdl_boolean` 데이터리스트를 바인딩
  ```xml
  <xf:select1 ... id="par_visible" label="label" nodeSet="data:gdl_boolean" value="value">
      <xf:choices>
          <xf:itemset nodeset="data:gdl_boolean">
              <xf:label ref="label"></xf:label>
              <xf:value ref="value"></xf:value>
          </xf:itemset>
      </xf:choices>
  </xf:select1>
  ```
- **con_ 옵션 셀렉트박스는 `grp_condition` 안에 tblbox 2단테이블(snippet "5_02 2단테이블") 구조로 배치**할 것 (th=속성명 textbox, td=selectbox, th/td 쌍). 기존 gridView P 샘플의 `grp_condition` 마크업을 그대로 참고.
- **`gdl_boolean` 바인딩 셀렉트박스 값은 `getText()` 로 읽을 것 (`getValue()` 아님).** gdl_boolean 은 value 코드가 미설정=`'1'`/true=`'2'`/false=`'3'`, label 이 `''`/`'true'`/`'false'` 이므로, `comp_init` 에서 라벨(`''`/`'true'`/`'false'`)로 분기하려면 `getText()` 를 써야 한다. (con_ 이 자체 값 selectbox(gdl_boolean 아님)면 `getValue()` 사용)
- **정적 select1(gdl_boolean 등)은 초기 `selectedIndex` 가 적용되지 않으므로, `onpageload` 에서 기본값을 명시 설정**할 것: `con_xxx.setValue("2")`(=true) 또는 `con_xxx.setSelectedIndex(n)`. gdl_boolean 에 `setValue` 는 **코드값("1"/"2"/"3")** 을 넘겨야 하며 `setValue("true")` 는 매칭되지 않음.
- **con_ 는 렌더 시 결정되는 속성이므로 con 변경 후 자동 재생성하지 말 것 → "Target 재생성" 버튼으로만 반영.** Target 재생성 버튼은 `body_sample.xml` wframe 이 제공하는 **기존 `btn_createTarget`(→`comp_init`) 을 그대로 활용**하고, 별도/중복 재생성 버튼을 새로 만들지 말 것.

---

## 4-1. 출력값 작성 규칙

- **검증을 위한 출력값은 반드시 필요한 값만 출력하여 불필요한 검증을 최소화한다.**
- API 가이드의 핵심 동작 검증에 직접 필요한 값만 `setReturnValue` 로 출력
- 디버깅용 부가 정보(컴포넌트 ID, 옵션 전체 dump, 중간 상태 등)는 출력하지 않음
- 동일 케이스에서 한 줄로 충분한 정보를 여러 줄로 분할하지 않음
- 사용자가 검증할 항목 수 ≈ `setReturnValue` 호출 횟수 가 일치하도록 작성

---

## 5. 스크립트 작성 규칙

- DOM 요소를 찾을 때 `document.getElementById` 대신 `document.querySelector`를 사용할 것
  - 예: `document.querySelector("td[id*='target1_cell_0_0']")`
- createLabel(테스트 참고 라벨) 관련 규칙은 **§5-1 createLabel 규칙** 에 통합되어 있으니 그곳을 따를 것
- set 형태의 메소드 샘플에는 정상적으로 설정되었는지 확인하는 로직이 반드시 포함되어야 함
  - 쌍이 되는 get 메소드가 있으면 get 메소드를 호출하여 설정값을 확인
  - 쌍이 되는 get 메소드가 없으면 DOM 에서 해당 값을 확인할 수 있는 영역을 찾아 확인
- tooltip 관련 샘플에서 툴팁 텍스트를 확인할 때는 `document.querySelector('.w2balloonTip_label')` 직접 접근 대신 공통 함수 `$c.gcm.getTooltipInfo(wf_body_bottom)`를 사용할 것
  - `document.querySelector`로 직접 접근하면 이전 케이스에서 생성된 숨겨진 풍선 DOM이 잔존하여 잘못된 값을 읽을 수 있음
- 오늘 날짜 검증이 필요한 샘플(gotoToday, setToday 등)은 `WebSquare.date.getCurrentServerDate()` 사용
  - 기본 호출 시 `yyyyMMdd` 형식 반환 (예: `"20251225"`)
  - 인자로 포맷 지정 가능: `WebSquare.date.getCurrentServerDate("yyyyMMddHHmmssSSS")` → `"20251225112433707"`
  - `new Date()` 기반 직접 생성 금지 (WebSquare 표준 API 사용)
  - 출력: 비교 결과만 한 줄로 출력 (예: `"[오늘 날짜 일치 성공 여부] " + (afterValue === todayValue)`)

---

## 5-1. createLabel — 테스트 참고 라벨 규칙 (통합)

> createLabel 관련 규칙은 분산하지 말고 본 절에만 둔다.

### 생성 시점·순서
- `comp_init` 에서 target 생성 후 메소드/조건으로 상태가 변경된 경우, `$c.gcm.createLabel` 로 관련 내용을 textbox 라벨로 표시할 것
- `comp_init` 의 createLabel 라벨은 **순서상 가장 먼저 생성**(기존 target 삭제 후, 조건 설정 전)
  - 예: `$c.gcm.createLabel('label="Row 0 은 readOnly 설정, Row 2 는 readOnly 해제"', '', 'label1');`

### 라벨 내용 구성 (테스트 참고용)
- **필수 검증 내용만 간결하게 작성할 것 (간소화 우선).** 검증에 직접 필요한 핵심(전제/대상 동작/확인 방법)만 담고, 중복 설명·장황한 배경·부가 묘사는 넣지 않는다.
  - 포함: 검증에 필요한 ⓐ 현재 con 적용값(또는 고정 전제) ⓑ 검증 대상 동작 ⓒ 확인 방법(버튼/관찰 포인트)
  - 제외: 동일 의미 반복, 컴포넌트 내부 구조의 불필요한 상세, 자명한 설명
  - **validation 과 중복 금지**: validation 에 이미 있는 내용(@description/@param/@return/@spec 문구 — 예: 미발생 조건, 반환값 true/false 의미)을 라벨에 다시 적지 말 것. 라벨은 validation 에 없는 **운영 정보**(전제 설정/조작 절차/확인 방법) 위주로 작성.
  - **내부 구현 세부 금지**: 라벨에 엔진 내부 동작/소스 경로/내부 메서드·컨트롤러 이름(예: keyEventController, handleCopyEvent, "키다운 경로 …") 같은 구현 디테일을 [참고]로 넣지 말 것. 사용자가 보는 동작 수준의 차이(예: "이벤트는 발생하고 데이터만 제외") 정도만 허용하고, 그 원인(내부 로직)은 적지 않는다.
- 숨겨진 컬럼·고정 전제 등 **검증에 영향을 주는 상태 변경만 명시**할 것 (예: 숨겨진 컬럼, useShiftKey=true 고정 등)
- **줄바꿈으로 보기 좋게 구성할 것**: 테스트 정보가 여러 항목(전제 / 절차 / 확인 포인트 등)이면 `\n` 으로 줄을 나눠 가독성을 높인다. 한 줄에 ` / ` 로 욱여넣지 말 것.
  - createLabel 은 라벨 텍스트 안의 `\n` 을 줄바꿈으로 렌더한다(common.xml 에서 `\n`→`<br/>` 변환 + escape 처리, 특수문자는 그대로 안전 표시).
  - 예: `$c.gcm.createLabel('label="[전제] col1 은 preventCopyColumn 으로 복사 차단 (헤더 name1 (복사차단) 표시)\n[절차] 셀 선택 후 Ctrl+C 복사 -> oncustomcopy 발생\n[확인] 핸들러 반환값(true/false/setData) 별 클립보드 저장값을 붙여넣기 영역에서 확인"', '', 'label1');`
  - 권장 구성: `[전제] ...\n[절차] ...\n[확인] ...` 처럼 머리표지(`[전제]`/`[절차]`/`[확인]`)로 줄을 구분.

### 문자 제약
createLabel 은 cList 가 **`label="..."` 단독 형식**이면 텍스트를 `setLabel` + `escape` 로 주입(XML 속성 파싱 우회, common.xml). 이를 전제로:
- **그대로 사용 가능(안전)**: 부등호 `<` `>`, `&`, 라벨 내부 큰따옴표 `"`, 화살표 `->` `=>` `<-` (유니코드 `→` 강제 불필요)
- **금지/주의** (createLabel escape 와 무관 — JS 문자열 리터럴 레벨이라 회피 필수):
  - **작은따옴표 `'`** : createLabel 인자가 작은따옴표 wrapper(`'label="..."'`)라 텍스트 안 `'` 가 조기 종료 → `SyntaxError`. 단어 강조는 괄호 등 다른 구분자로.
  - **백슬래시+따옴표 시퀀스(`\'` `\"`)** : JS 변환 시 `SyntaxError` 로 페이지 미로드 → 한글 설명으로 대체(`\'`→백슬래시, `\"`→큰따옴표 등). `setReturnValue("...")` / `w2:textbox label="..."` 등 JS 문자열로 변환되는 곳에도 동일 적용.
  - **HTML 태그(`<br/>` 등)** : `escape` 처리로 **리터럴 표시**되므로 직접 쓰지 말 것 → **줄바꿈은 `\n` 사용**(createLabel 이 `\n`→`<br/>` 자동 변환). 한 줄로 욱여넣는 ` / ` 보다 `\n` 줄바꿈을 우선.
- **단서**: cList 에 label 외 다른 속성을 함께 넘기면 escape 우회가 적용되지 않아(기존 동작) `<` `>` `"` 제약이 다시 살아남.

---

## 6. 컴포넌트별 패턴

### gridView 병합 셀 (mergeCell)
- 병합 셀이 필요한 샘플에서는 `comp_init` 함수 내 gridView 동적 생성 후 `mergeCell` API 호출
- 사용법: `target1.mergeCell({"rowIndex" : 0, "colIndex" : 2, "colSpan" : 2, "rowSpan" : 2});`
- 파라미터: `rowIndex`/`colIndex` (시작 위치), `colSpan`/`rowSpan` (병합 범위)
- 참고 파일: `gridView_M_setFocusedMultiCell_1.xml` (94번 라인)

---

## 7. 정적 컴포넌트 배치 규칙

- `grp_comp` 하위에 정적으로 컴포넌트를 배치해야 하면, `wf_body_sample` wframe을 삭제하고 `body_sample.xml`의 `grp_sample > grp_comp` 구조를 직접 배치할 것
  ```xml
  <!-- wf_body_sample 삭제 후 아래 구조로 대체 -->
  <xf:group style="" id="grp_sample">
      <w2:textbox style="" id="" label="■ 테스트 샘플" class="tc_tbx_label_1" escape="false"></w2:textbox>
      <xf:group style="" id="grp_comp" class="tc_group_basic">
          <!-- 정적 컴포넌트 배치 -->
      </xf:group>
  </xf:group>
  ```
- 이 경우 `Target 재생성` 버튼과 `comp_init` 함수는 불필요

---

## 8. 공통 메소드 wrap 샘플 패턴 (image, fliptoggle, generator, roundRectangle 등)

`{컴포넌트}_M_{공통메소드}_1.xml` 형태 샘플은 기본적으로 다음 패턴을 따른다.

```xml
<script>
scwin.onpageload = function () { scwin.comp_init(); };
scwin.comp_init = async function () {
    if (typeof target1 !== 'undefined') target1.remove();
    $c.gcm.createBasicXxx();
}
</script>
<body ev:onpageload="scwin.onpageload">
    <w2:wframe style='height: 100%' id="wf_sample" src="/sample/common_M/common_M_{메소드}_1.xml"></w2:wframe>
</body>
```

아래는 동기/비동기 plugin 차이로 발생하는 예외 케이스 모음.

### 8.1 비동기 plugin 구분
- **동기 plugin**: `button`, `image` 등 — `dynamicCreate` 시 즉시 활성화
- **비동기 plugin**: `fliptoggle`, `generator`, `roundRectangle` 등 — 별도 모듈/SVG 로드 단계가 있음
- 비동기 plugin은 빈 `id` 또는 외부 컨테이너 의존성 문제로 추가 처리가 필요한 경우가 있음

### 8.2 getID / getOriginalID 샘플
- 동기 plugin: 빈 cID 로 `dynamicCreate` 호출 시 자동 UUID(`mf_wq_uuid_XX`) 부여
- 비동기 plugin: 빈 cID 로 호출 시 `Cannot read properties of undefined (reading 'appendTo')` 에러로 컴포넌트 미생성 → `scwin.gVar.cID` **초기값을 `"target1"` 등 유효 ID로 명시**

### 8.3 getGenerator 샘플 (searchbox 패턴)
비동기 plugin을 `<w2:generator>` template 안에 자식으로 직접 두고 `dynamicCreate` 하면 `_cloneRecursive` 에러 발생 (generator의 `_compHash` 가 비동기 plugin을 포함 못함).

→ **빈 generator + 자식 별도 dynamicCreate** 패턴 사용 (searchbox 패턴):
```javascript
// 1. 빈 generator 생성
let strGenerator = '<w2:generator ' + strNameSpace + ' id="generator1"></w2:generator>';
scwin.generator1 = await $p.dynamicCreate("generator1", "generator", strGenerator, grp_comp);

// 2. 자식을 별도 dynamicCreate (parent 인자에 generator1 객체 전달)
let strChild = '<w2:fliptoggle ' + strNameSpace + ' id="generator1_child" ... ev:onclick="scwin.generator1_child_onclick">...</w2:fliptoggle>';
scwin.generator1_child = await $p.dynamicCreate("generator1_child", "fliptoggle", strChild, generator1);

// 3. generator 자식 행 인스턴스화
scwin.generator1.insertChild();

// 4. 일반 위치(generator 자식 아님)에 target1 생성
$c.gcm.createBasicXxx('', 'ev:onclick="scwin.target1_onclick"');
```

generator 컴포넌트 자체가 wrap 대상일 때 (`generator_M_*`) `generator1_child` 의 내부 generator 도 마찬가지. 또한 dynamicCreate 후 `insertChild()` 를 추가로 호출해야 자식 행이 표시됨 (createBasicGenerator 는 기본 `insertChild()` 3회).

### 8.4 focus 샘플 — 기본 포커스 가능 여부에 따른 분기
컴포넌트의 `render` 가 IMG/DIV 인 경우 `tabIndex` 기본 -1로 포커스 불가. 동적 생성 시 `tabIndex` 또는 `tagname` 설정 필요.

| 컴포넌트 | 기본 포커스 | con_tabIndex / con_tagname 처리 |
|---|---|---|
| button (BUTTON) | 가능 | `wf_sample.getObj("con_tabIndex").setDisabled(true)` + `con_tagname` 동일 |
| image / fliptoggle / generator / roundRectangle | 불가 | setDisabled 호출 안 함 (사용자가 con 에서 값 선택 후 포커스 검증) |

샘플 구조 (기본 포커스 불가 컴포넌트):
```javascript
scwin.gVar = { "cTabIndex": "", "cTagName": "" }
let properties = { tabIndex: scwin.gVar["cTabIndex"], tagname: scwin.gVar["cTagName"] };
let cList = $c.gcm.createCList(properties);
$c.gcm.createBasicXxx(cList, 'ev:onfocus="scwin.target1_onfocus"');
```

### 8.5 setReadOnly / setDisabled 샘플 — tabIndex 동적 설정
기본 포커스 불가 컴포넌트는 이벤트 검증을 위해 cList 에 `tabIndex: "0"` 명시 (button 은 불필요):
```javascript
let properties = { tabIndex: "0" };
let cList = $c.gcm.createCList(properties);
let eList = $c.gcm.createEList(["onclick", "onfocus", "onkeydown"]);  // setReadOnly
// let eList = $c.gcm.createEList(["onclick", "onfocus"]);             // setDisabled
$c.gcm.createBasicXxx(cList, eList);
```

### 8.6 setPosition / getPosition 샘플 — `position:relative` 필수
컴포넌트 자체 또는 내부 자식(SVG, `w2fliptoggle_mask` 등)이 `position: absolute` 로 떠 있어 부모 기준 positioning이 필요. cStyle 에 `position:relative` 누락 시 자식이 **viewport 전체로 확장되어 다른 버튼/UI 위를 덮어 클릭 차단** 됨.

```javascript
cStyle += "width:80px; height:30px; position:relative;"   // fliptoggle
cStyle += "width:100px; height:100px; position:relative; background-color: silver;"  // roundRectangle
```

또한 `getPosition` 은 `"left"` 또는 `"top"` 인자 필수. 인자 없이 호출 시 undefined.

### 8.7 roundRectangle 동적 parent 안 SVG renderer 콘솔 에러
`roundRectangle` 을 동적 생성된 parent group 안에 자식으로 `dynamicCreate` 시 `Cannot read properties of null (reading 'createRoundRect')` 콘솔 에러 + SVG 도형 미렌더링 발생. 단, 컴포넌트 자체와 메소드(setPosition/getPosition 등) 동작은 정상. 콘솔 에러는 무시 가능.

### 8.8 createBasic 함수명 대소문자
공통 함수 명명에서 컴포넌트명이 camelCase 로 들어가므로 호출 시 주의:
- `createBasicFlipToggle` (F, T 대문자)
- `createBasicRoundRectangle` (R, R 대문자)
- `createBasicImage`, `createBasicGenerator`, `createBasicButton` 등

### 8.9 wpack 자동 재생성
XML 파일 저장 시 WebSquare 가 `_wpack_/sample/.../...js` 파일을 자동 재컴파일. wpack 수동 작성/삭제 금지 (`_wpack_` 파일 삭제 시 404 발생).

---

## 생성 완료 → 다음 단계

> **생성이 완료되면 `unitTest/UT_02_검증.md` 규칙을 적용하여 검증 단계를 수행한다.**

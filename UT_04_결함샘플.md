# unitTest — 엔진 결함 재현 샘플 생성 규칙

> **목적**: Jira 이슈 등록을 위해 엔진 결함을 독립적으로 재현할 수 있는 최소 샘플을 생성한다.
> - UT_01/02/03 규칙(validation 중심)과 분리되어 있으며, **재현 절차 + 기대/실제 비교**가 핵심이다.
> - 사용자가 "Jira 등록용 샘플 생성" 또는 "결함 확인 샘플 생성" 요청 시 본 문서 규칙을 적용한다.

---

## 1. 파일 위치 및 명명 규칙

- **경로**: `unitTest/src/main/webapp/jira/<YYYYMM>/` — **현재 년월(정기 테스트 월) 폴더 하위**에 생성한다.
  - `<YYYYMM>` 는 **생성 시점의 현재 년월**(예: 2026년 7월 → `202607`). 폴더가 없으면 생성 후 그 하위에 둔다.
  - 예: `unitTest/src/main/webapp/jira/202607/fail_{컴포넌트}_{API/속성}_{결함요약}.xml`
  - (과거 평면 배치분은 그대로 두고, **신규 생성분만** 월 폴더에 둔다.)
- **파일명 접두어**: `fail_` 로 시작
- **파일명 네이밍**:
  - `fail_{컴포넌트}_{API/속성}_{결함요약}.xml` 형식
  - 예: `fail_wframe_setReadOnly_childPropagation.xml`, `fail_inputCalendar_renderType_oldValue.xml`
- **link 파일(보조 WFrame 로딩용)**: 같은 `jira/<YYYYMM>/` 폴더에 `_link.xml` 접미어로 생성
  - 예: `fail_wframe_setReadOnly_childPropagation_link.xml`
  - 메인 샘플에서 `src="/jira/<YYYYMM>/fail_xxx_link.xml"` 형식으로 참조
- **MCP 재현/미리보기 경로**: 월 폴더를 포함한 `w2xPath=/jira/<YYYYMM>/fail_xxx.xml` 로 접근한다.

---

## 2. 독립 실행 원칙

**반드시 하나의 XML 파일만으로 결함을 재현할 수 있어야 한다.**

- ❌ `<w2:wframe src="/frame/page/body_left.xml">`, `<w2:wframe src="/frame/page/body_top.xml">`, `<w2:wframe src="/frame/page/body_bottom.xml">` 등 공통 프레임 WFrame 사용 금지
- ❌ `$c.gcm.createCommonDatalist()`, `$c.gcm.createBasicXxx()` 등 unitTest 공통 함수 의존 금지
- ❌ `createValidation(scwin.tData)` 호출 금지 (validation 영역 불필요)
- ✅ 리턴 값 출력 영역은 `/frame/page/body_bottom.xml` 의 소스를 **inline 복사**하여 포함

### 2.1. 리턴 값 출력 영역 inline 템플릿 (body_bottom.xml 복사)

**script 영역에 추가:**
```javascript
// 리턴 값 초기화
scwin.btn_clear_onclick = function (e) {
    tbx_return.setValue("");
};

// 리턴 값 설정
scwin.setReturnValue = function (strValue) {
    tbx_return.setValue(tbx_return.getValue() + strValue);
    tbx_return.setValue(tbx_return.getValue() + "\n");
};
```

**body 영역에 추가 (기존 wf_body_bottom wframe 대신):**
```xml
<xf:group class="" id="grp_bottom" style="">
    <w2:textbox class="tc_tbx_label_1" escape="false" id="" label="■ 리턴 값 확인" style=""></w2:textbox>
    <xf:group id="" style="display: flex;justify-content: flex-end">
        <w2:button class="tc_btn_basic" ev:onclick="scwin.btn_clear_onclick" id="btn_clear" label="clear" style=""></w2:button>
    </xf:group>
    <xf:textarea style="border: 1px solid var(--ws-gray-400);border-radius: 8px;width: 100%;height: 120px;" id="tbx_return" class="tc_tbx_result_1"></xf:textarea>
</xf:group>
```

**호출부**: 기존 `wf_body_bottom.getObj("scwin").setReturnValue(...)` → `scwin.setReturnValue(...)` 로 치환

---

## 1.1. ★5.0(SP5) 결함 샘플은 별도 경로 (2026-09-11 사용자 지시)

**5.0 엔진 결함 재현 샘플은 5.0 Studio 프로젝트의 `jira` 폴더에 만든다.**
```
C:\Users\user\Desktop\websquare_5_0_5_20260910_1547.win32.win32.x86_64_B\websquare\workspace\GA_202609\WebContent\jira\
```
- 6.0 결함 샘플은 기존대로 `unitTest/src/main/webapp/jira/<YYYYMM>/`.
- 5.0 쪽은 **월 폴더를 두지 않고 `jira/` 직하**에 둔다(현재 구조 기준).
- 5.0 프로젝트 구조: `WebContent/{_wpack_, cm, jira, websquare}` — 6.0 unitTest 와 공통 프레임 구조가 다르므로
  **독립 실행 원칙(§2)이 더 중요하다**. 공통 wframe·공통 함수에 의존하면 5.0 쪽에서 열리지 않는다.
- 미리보기 경로: `w2xPath=/jira/fail_xxx.xml` (월 폴더 없음).

---

## 1.2. ★5.0 결함 샘플의 서술 방식 — "6.0 과 동작이 다르다" (2026-09-11 사용자 지시)

**5.0 결함 샘플의 핵심 키포인트는 "6.0 과 동작이 달라 문제가 발생할 수 있다" 이다.**

- ❌ **API 가이드 문구를 인용하지 말 것.** 5.0 가이드는 6.0 과 내용이 다를 수 있다.
  (실사례: `show`/`hide` 의 `aria-hidden` 규정이 **5.0 가이드에는 아예 없다** — 6.0 가이드 문구를 근거로 쓰면 5.0 기준에서 성립하지 않는다.
   5.0 가이드 위치: `<5.0 Studio>\websquare\plugins\com.inswave.websquare.help_*\html\websquare\` — 필요하면 여기서 확인한다.)
- ❌ `[기대]` / `[실제]` 표기도 쓰지 않는다("무엇이 맞는지"를 단정하게 되므로).
- ✅ 라벨 구성은 이렇게:
  | 라벨 | 내용 |
  |---|---|
  | 요약(굵게) | `hide() / show() 호출 시 ○○ 동작이 6.0 과 다르다. 5.0 은 A, 6.0 은 B 여서 …` |
  | `[전제]` | 재현 조건(배치·설정) |
  | `[확인 방법]` | 무엇을 클릭/입력하면 되는지 한 줄 |
  | `[6.0 동작]` | 6.0 실측값 |
  | `[5.0 동작]` | 5.0 실측값 |
  | `[영향]` | 그 차이가 **왜 문제인지** + 차이가 어디에 한정되는지 |

→ 결함 원인(엔진 내부 로직·소스 경로·함수명)은 §3.1 대로 **여전히 기재하지 않는다**. 적는 것은 **두 버전의 실측 동작 차이와 그 영향**뿐이다.

---

## 2.2. ★class 금지 — inline style 로 작성 (2026-09-11 사용자 지시)

**결함 샘플은 `class` 를 쓰지 말고 `style` 로 작성한다.** `tc_*` 클래스를 쓰면 Jira 등록 시 `css/tc.css` 를 함께 첨부해야 해서 번거롭다.
샘플 한 파일만 열어도 의도한 모양이 나와야 한다. CSS 변수(`var(--ws-*)`)도 **리터럴 값으로** 바꿔 쓴다(디자인킷 CSS 의존 제거).

| 기존 class | 대체 inline style |
|---|---|
| `class="tc_tbx_label_1"` | `style="font-size:16px;font-weight:bold;margin-top:16px;margin-bottom:16px;"` |
| `class="tc_tbx_label_2"` | `style="background-color:#EAEEF1;font-size:16px;font-weight:bold;padding-left:5px;"` |
| `class="tc_btn_basic"` | `style="background-color:#005AB5;color:#FFFFFF;border-radius:4px;padding:5px;height:35px;"` |
| `class="tc_tbx_result_1"` | **제거** (tc.css 에 정의 없음 — 마커 클래스) |
| `var(--ws-gray-400)` | `#C4CDD5` |
| `var(--ws-primary-600)` | `#005AB5` |
| `var(--ws-gray-0)` | `#FFFFFF` |
| `var(--ws-form-radius)` | `4px` |

→ §2.1 의 리턴 값 출력 영역 템플릿도 class 대신 위 style 로 바꿔 복사한다.

**★`rem` 단위 금지 — 반드시 `px` 로 쓸 것.** `rem` 은 루트 폰트 크기에 의존하는데, 환경마다 다르다.
- 6.0 unitTest 환경: `cm/css/contents.css` 의 `html{font-size:10px}` 때문에 `1.6rem` = **16px**
- 그 CSS 가 없는 환경(예: 5.0 배포본에 복사): `1.6rem` ≈ **25.6px** → 글자가 과도하게 커짐
결함 샘플은 다른 환경에 복사해 여는 것이 전제이므로 **절대값(px)** 으로 고정한다.

---

## 3. 샘플 구조

### 3.1. description
- 결함 한 줄 요약
- 예: `"[FAIL] inputCalendar onyearselect.info.oldValue 가 renderType=native 일 때 setValue() 로 설정한 연도가 아닌 시스템 현재 연도 기준으로 발생하는 결함 재현"`
- **원인(cause)은 기재하지 않는다** — 엔진 내부 로직/소스 경로(예: `layoutController.js`)/내부 함수명(예: `_getResizerColumnInfo`)/변경 유형(break→continue, 재구현 등) 같은 **결함 원인은 샘플(라벨/description/출력)에 적지 말 것**. 결함 샘플에는 **현상만** — 즉 기대 동작(정상) vs 실제 동작(결함) 과 재현 조건만 기술한다. (원인 규명은 별도 보고에만 남긴다)

### 3.2. validation (선택)
- 결함을 설명하는 2개 항목 (정상 기대 동작 + 실제 결함 동작)
- 예:
  ```
  "renderType=component (기본값) : setValue('20251111') 후 ... oldValue 가 2025 로 발생해야 합니다.",
  "renderType=native : setValue('20251111') 후 ... oldValue 가 2025 로 발생해야 합니다 (실제 : 시스템 현재 연도로 발생)."
  ```

### 3.3. onpageload
- 결함 재현에 필요한 **사전 조건 설정** (setValue, 초기 데이터 등)
- 시스템 환경 정보 출력 (예: `시스템 현재 연도 : 2026`) → 결함 재현 시 환경 영향 확인용
- **[재현 절차] 안내는 setReturnValue 로 출력하지 말 것** (결과창에 절차 텍스트를 찍지 않는다). 재현 절차/기대·실제는 상단 textbox 라벨(결함 요약)로 표기하고(원인은 기재하지 않음 — §3.1 참조), 결과창에는 재현 버튼 클릭 시의 실제 동작 결과값만 출력한다.

### 3.4. 비교 구성 (정상 vs 결함)
- 가능하면 **정상 동작 컴포넌트**와 **결함 발생 컴포넌트**를 **동일 조건으로 2개 배치**하여 비교 확인
- 예: renderType=component / renderType=native 두 InputCalendar 를 나란히 배치

### 3.5. 재현 트리거 — **사용자 조작 우선, 자동 버튼 금지**
- **사용자 조작(클릭·Tab/키입력·hover·타이핑·드래그 등)으로 재현 가능한 결함은 그 조작을 대행하는 버튼을 만들지 않는다.** 사용자가 직접 조작하고 결과창 출력을 관찰하도록 구성한다. ([[feedback_no_user_action_button]], [[feedback_spec_verify_by_user_action_not_auto_button]])
  - ❌ `btn.focus()` 를 대신 호출하는 "포커스 확인" 버튼, `element.click()` 대행 버튼, 키이벤트 dispatch 버튼
  - ✅ 조작 시작점이 필요하면 **동작 없는 앵커 버튼**(예: Tab 시작용 `포커스 시작`) 정도만 두고, 나머지는 사용자가 직접 조작
- **버튼으로 만들어도 되는 것**: JS 로만 트리거 가능한 API 호출(`setReadOnly(true)` 등), 사용자 UI 로 수행 불가능한 상태 설정.
- **상단 라벨에 `[확인 방법]` 을 명시**한다 — 무엇을 클릭하고 어떤 키를 누르면 되는지 한 줄. 이어서 `[기대]` / `[실제]` 를 각각 한 줄로.

### 3.6. 출력값 간소화
- 결과창에는 **판정에 필요한 최소 정보만** 한 줄씩 출력한다. 장황한 설명 문구·DOM 속성 나열·원인 해설을 넣지 않는다.
  - ✅ `[A] col 0 → col 1  (readonly Cell)` / `1. 그리드 시작 영역` / `정렬 발생 : sortOrder=1`
  - ❌ `[A: senseReader=true / ignoreNonEditableCell 미설정] 이동 셀 (row: 0, col: 1), 이전 셀 (row: 0, col: 0)  <-- readonly Cell 인데 포커스가 이동함`
- **초기화/사전조건 단계에서 발생하는 이벤트는 출력하지 않는다**(예: `setFocusedCell` 최초 지정의 `col null → col 0`). 가드로 걸러 사용자가 조작한 결과만 남긴다.
- 조건·설정값 설명은 결과창이 아니라 **컴포넌트 옆 라벨**로 표기한다(A/B 비교 구성이면 각 그리드 위 라벨).

---

## 4. 참조 예시

### 4.1. `fail_wframe_setReadOnly_childPropagation.xml`
**이슈 요약**: `wframe.setReadOnly(true)` 호출 시 wframe 자체에만 readonly 속성이 적용되고 내부 자식 input/textarea/selectbox/inputCalendar 컴포넌트에는 전파되지 않음.

**구성**:
- 독립 실행 구조 (wf_body_bottom wframe 제거, inline textarea + clear 버튼)
- 재현 버튼: `setReadOnly(true)`, `setReadOnly(false)`, `DOM 상태 확인`
- link XML (`fail_wframe_setReadOnly_childPropagation_link.xml`) 에 자식 컴포넌트 4종 정적 배치
- `DOM 상태 확인` 버튼 클릭 시 각 자식의 `getAttribute('readonly')` / `el.readOnly` 속성을 출력하여 미전파 증거 제시

**경로**: `unitTest/src/main/webapp/jira/fail_wframe_setReadOnly_childPropagation.xml`

### 4.2. `fail_inputCalendar_renderType_oldValue.xml`
**이슈 요약**: `renderType=native` 일 때 `setValue('20251111')` 호출 후 팝업 달력의 '이전연월' 버튼 클릭 시 `onyearselect.info.oldValue` 가 setValue 한 연도(2025)가 아닌 시스템 현재 연도(2026)로 발생.

**구성**:
- 정상 케이스(`renderType=component`) 와 결함 케이스(`renderType=native`) 두 InputCalendar 를 동일 조건으로 배치
- onpageload 에서 두 컴포넌트 모두 `setValue('20251111')` 호출 + 시스템 현재 연도 출력
- 각 컴포넌트의 `ev:onyearselect` 핸들러가 oldValue/newValue 를 출력
- 사용자가 각 InputCalendar 의 달력 아이콘 → '이전연월'(<<) 버튼 클릭 → 두 컴포넌트의 oldValue 비교로 결함 확인

**경로**: `unitTest/src/main/webapp/jira/fail_inputCalendar_renderType_oldValue.xml`

---

## 5. Jira 등록 시 첨부 정보

샘플 생성 후 사용자가 Jira 이슈 등록 시 다음을 함께 전달하면 유용:

1. **결함 재현 샘플 XML 경로** (본 규칙으로 생성된 파일)
2. **재현 절차** (샘플 onpageload 출력에 포함된 내용)
3. **기대 결과** vs **실제 결과** (샘플 validation 항목)
4. **엔진 버전** / **테스트 환경** (시스템 날짜 등 환경 의존 정보)
5. **관련 API 가이드 문서 위치** (엔진 소스 주석 `@method` ~ `@deprecated` 발췌)

---

## 6. 에이전트에 요청할 때

사용자가 "Jira 등록용 샘플 생성" 또는 "결함 확인 샘플 생성" 요청 시:
1. 본 `UT_04_결함샘플.md` 규칙을 적용한다.
2. 엔진 결함의 재현 조건을 사용자로부터 확인한다.
3. 참조 예시(위 4.1/4.2)를 참고하여 독립 실행 가능한 `fail_*.xml` 샘플을 생성한다.
4. w-pack 변환 생략 (Studio 자동 수행).
